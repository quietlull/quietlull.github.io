{
 "component": "code-block",
 "candidates": [
  {
   "site": "Maxime Heckel — blog.maximeheckel.com (react-three-fiber / GLSL shader writing)",
   "url": "https://blog.maximeheckel.com/posts/the-study-of-shaders-with-react-three-fiber/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* Read from the live page's runtime-injected <style> tags. Structure:\n   div.c-kreyRl (card) > div.c-cuGhLv (header bar) > p[data-testid=\"codesnippet-title\"].c-jSxxNn + button[aria-label=\"Copy code to clipboard\"].c-dkLMcm\n                       > pre.c-dGhWkF.prism-code > div.c-QIkcn (token-line) > div.c-eotEYs (lineno) + span.c-hJePes */\n\n.c-kreyRl{position:relative;background:var(--card-bg, var(--card-background));-webkit-backdrop-filter:var(--card-blur, none);backdrop-filter:var(--card-blur, none);border-radius:var(--border-radius-2);box-shadow:var(--card-shadow);overflow:hidden;--border:1px solid var(--border-color);border:var(--border)}\n\n.c-cuGhLv{display:flex;justify-content:space-between;align-items:center;border-top-left-radius:var(--border-radius-1);border-top-right-radius:var(--border-radius-1);padding:12px 12px;color:var(--text-tertiary);font-weight:500;font-size:var(--font-size-1);border-bottom:var(--border)}\n\n.c-jSxxNn{margin-block-start:0px;font-size:var(--font-size-1);margin-bottom:0px;color:var(--text-primary);font-weight:500}\n\n.c-dGhWkF{--shadow-size:70px;--shadow-color:oklch(from var(--gray-000) l c h / 0.75);margin-top:0;margin-bottom:0;text-align:left;padding:var(--space-2) 0px;border-bottom-left-radius:var(--border-radius-2);border-bottom-right-radius:var(--border-radius-2);background-color:var(--code-snippet-background);font-family:var(--font-mono-code);font-size:var(--font-size-1);line-height:24px;overflow-x:auto}\n\n.c-QIkcn{display:table;border-collapse:collapse;padding:0px 14px;border-left:3px solid transparent}\n.c-eotEYs{width:45px;padding:0 12px;user-select:none;opacity:1;color:var(--text-tertiary)}\n.c-hJePes{display:table-cell;width:100%}\n\n/* tokens read from the same page */\n--code-snippet-background: var(--gray-200);\n--border-radius-2: 12px;\n--space-2: 8px;\n--font-size-1: 14px;\n--font-mono-code: 'FiraCode', 'FiraCode Fallback';",
   "howItWorks": "A bordered, overflow-hidden card. Inside it a flex header bar (justify-content:space-between) holds the filename on the left and the copy button on the right, separated from the code by border-bottom:var(--border) — so the 'tab' is a full-width header strip, not a protruding tab. NO traffic lights anywhere. The <pre> below carries only the two BOTTOM corner radii, letting the card's overflow:hidden clip the top; that is what makes header and code read as one welded object. Line numbering is pure CSS table layout: each line is display:table, the gutter is a fixed 45px cell, the content cell is display:table-cell;width:100% — so long lines scroll horizontally on the <pre> without the gutter sliding away. The copy button is a single inline SVG holding all three paths at once (two clipboard paths + a checkmark path); state is animated by flipping opacity 1->0 / 0->1 plus filter:blur(0px)->blur(2px) and a stroke-dashoffset/pathLength draw-on for the check, rather than by swapping icons.",
   "transfers": "The whole geometry transfers directly and it is plain CSS, no build step. Specifically: the flex space-between header strip with a filename left / copy right; border-bottom as the only separator; bottom-only radii on the <pre> under a card with overflow:hidden; the display:table line-number gutter (a genuinely good trick for shader code with long lines); the one-SVG-three-paths copy button with opacity+blur crossfade to a checkmark. The 2000ms copied-state reset is a conventional, safe default. Header padding 12px, radius 12px, mono 14px/24px are sane starting numbers.",
   "doesNotTransfer": "Colour: his palette is a cool oklch gray ramp with a blue/indigo UI accent — exactly the cool UI accent Rod's palette law forbids. Take --code-snippet-background as a slot and fill it with Rod's night-ground, not var(--gray-200). Also: the class names are styled-components hashes (c-kreyRl etc.) so rename them; oklch(from ...) relative colour syntax is used for shadows and may need a fallback; FiraCode is an extra font dependency (OFL, free, but still a font to ship); the backdrop-filter card blur is glassmorphism, which conflicts with the paper-morphism call.",
   "confidence": "read-from-source"
  },
  {
   "site": "Astro Starlight docs — the Expressive Code code block (the most widely shipped plain-CSS implementation of this exact component)",
   "url": "https://starlight.astro.build/guides/authoring-content/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* Read from https://starlight.astro.build/_astro/ec.v4551.css (18,222 bytes) */\n\n/* --- the copy button --- */\n.copy{display:flex;gap:0.25rem;flex-direction:row;position:absolute;inset-block-start:calc(var(--ec-brdWd) + var(--button-spacing));inset-inline-end:calc(var(--ec-brdWd) + var(--ec-uiPadInl) / 2)}\n\n.copy button{position:relative;align-self:flex-end;margin:0;padding:0;border:none;border-radius:0.2rem;z-index:1;cursor:pointer;transition-property:opacity, background, border-color;transition-duration:0.2s;transition-timing-function:cubic-bezier(0.25, 0.46, 0.45, 0.94);width:2.5rem;height:2.5rem;background:var(--code-background);opacity:0.75}\n\n/* --- the filename TAB variant (this is the one Rod wants) --- */\n.expressive-code .frame.has-title:not(.is-terminal){--button-spacing:calc(1.9rem + 2 * (var(--ec-uiPadBlk) + var(--ec-frm-edActTabIndHt)))}\n\n.expressive-code .frame.has-title:not(.is-terminal) .title{position:relative;color:var(--ec-frm-edActTabFg);background:var(--ec-frm-edActTabBg);background-clip:padding-box;margin-block-start:var(--ec-frm-edTabsMargBlkStart);padding:calc(var(--ec-uiPadBlk) + var(--ec-frm-edActTabIndHt)) var(--ec-uiPadInl);border:var(--ec-brdWd) solid var(--ec-frm-edActTabBrdCol);border-radius:var(--tab-border-radius) var(--tab-border-radius) 0 0;border-bottom:none;overflow:hidden}\n\n.expressive-code .frame.has-title:not(.is-terminal) .title::after{content:'';position:absolute;pointer-events:none;inset:0;border-top:var(--ec-frm-edActTabIndHt) solid var(--ec-frm-edActTabIndTopCol);border-bottom:var(--ec-frm-edActTabIndHt) solid var(--ec-frm-edActTabIndBtmCol)}\n\n.expressive-code .frame.has-title pre,.expressive-code .frame.has-title code,.expressive-code .frame.is-terminal pre,.expressive-code .frame.is-terminal code{border-top:none;border-top-left-radius:0;border-top-right-radius:0}\n\n/* --- the TERMINAL variant: this one DOES draw the three dots. AVOID. --- */\n.expressive-code .frame.is-terminal .header::before{content:'';position:absolute;pointer-events:none;left:var(--ec-uiPadInl);width:2.1rem;height:0.56rem;line-height:0;background-color:var(--ec-frm-trmTtbDotsFg);opacity:var(--ec-frm-trmTtbDotsOpa);-webkit-mask-image:var(--ec-frm-trmIcon);mask-image:var(--ec-frm-trmIcon);mask-repeat:no-repeat}\n\n--ec-frm-trmTtbDotsFg: color-mix(in srgb, var(--sl-color-gray-5), transparent 25%);\n--ec-frm-trmTtbDotsOpa: 0.75;\n--ec-frm-trmIcon: url(\"data:image/svg+xml,<svg viewBox='0 0 60 16'><circle cx='8' cy='8' r='8'/><circle cx='30' cy='8' r='8'/><circle cx='52' cy='8' r='8'/></svg>\");\n\n/* markup, read from the page */\n<pre>...</pre><div class=\"copy\"><div aria-live=\"polite\"></div><button title=\"Copy to clipboard\" data-copied=\"Copied!\" data-code=\"---\\x7ftitle: My page title\\x7f---\"><div></div></button></div>",
   "howItWorks": "Two frame variants off one system, and the difference is exactly Rod's fork in the road. The TAB variant (.has-title:not(.is-terminal)) renders the filename as a real protruding tab: border-radius on the top two corners only, border-bottom:none, and the <pre> below it zeroes its own border-top and top radii so the tab and the code body fuse into a folder-tab silhouette. A ::after with only border-top and border-bottom paints a coloured indicator line across the top of the tab without an extra element. The TERMINAL variant instead centres a title and paints the three macOS dots as a single ::before — one element, a mask-image of an SVG with three circles, tinted by one background-color, so the dots are MONOCHROME, not red/yellow/green. The copy button is absolutely positioned into the frame's top-right, offset by a --button-spacing that each variant redefines so the button clears whichever chrome that variant has. Cleverest bit: the button carries the code in a data-code attribute (newlines encoded as \\x7f) and the copied label in data-copied, so the JS never has to walk the highlighted DOM to reconstruct plain text, and an adjacent div[aria-live=\"polite\"] announces the copy to screen readers.",
   "transfers": "Plain CSS, transcribes as-is. Take: the tab geometry (top-two-corner radius + border-bottom:none + <pre> zeroing its top border/radii) — that is the whole 'filename tab' device in three declarations. Take the --button-spacing indirection so the copy button positions itself correctly whether or not a title is present. Take the data-code attribute trick; it is strictly more robust than reading textContent out of syntax-highlighted spans, which is a real bug source for shader code. Take the aria-live region and the title/data-copied label pattern — that is the accessibility half most implementations skip, and it is free. The 2.5rem hit target already clears WCAG 2.5.8.",
   "doesNotTransfer": "The three-dot ::before must be deleted outright — that is the traffic-light chrome Rod rejected. Note it is only on .is-terminal, so adopting the tab variant sidesteps it cleanly rather than requiring surgery. Colour: Starlight's --sl-color-* ramp is a neutral/blue docs palette with a purple-ish accent; none of it transfers, only the token slots do. Also the --ec-* variable names are terse to the point of hostile (edActTabIndHt = editor active tab indicator height) — rename on adoption or the CSS becomes unreadable in six months, which fails the maintainability bar. This is a docs-system source, not a hand-made personal site, so it is good engineering evidence and weak taste evidence.",
   "confidence": "read-from-source"
  },
  {
   "site": "Josh Comeau — joshwcomeau.com (personal dev blog, warm palette, hand-made feel)",
   "url": "https://www.joshwcomeau.com/animation/keyframe-animations/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* Read from the concatenated /_next/static/css/*.css bundles (127,542 bytes total) */\n\n.c1qvfy1x{--copy-button-size:44px;--copy-button-margin:8px;position:relative;container-type:inline-size;isolation:isolate}\n\n.wqxmqd2{position:sticky;z-index:2;display:flex;justify-content:center;align-items:center;width:var(--copy-button-size);height:var(--copy-button-size);top:calc(var(--copy-button-sticky-top, 0px) + var(--copy-button-margin));transform:translateX(calc(var(--copy-button-margin) * -1));margin-block:var(--copy-button-margin);margin-left:auto;margin-right:0;border:1px solid;color:var(--color-gray-800);border-radius:5px;opacity:0;transition:opacity .25s;transition-delay:1s}\n\n.ae97emi:hover .wqxmqd2,.wqxmqd2:focus-visible{opacity:1;transition-delay:0s}\n\nhtml[data-color-mode=dark] .wqxmqd2:not([data-locked-to-color-mode=light]){background:var(--color-background);border-color:var(--color-gray-200)}\n\n/* markup */\n<div class=\"coaxrjn c1qvfy1x ae97emi\" style=\"--max-height:auto\"><button class=\"wqxmqd2\">…<span class=\"haso6lc wlgbkjj\">Copy to clipboard</span></button><div><pre class=\"shiki shiki-themes\">…</pre></div></div>",
   "howItWorks": "No filename bar at all — this is the copy button device in isolation, and its one real idea is position:sticky. The button sticks to the top of a long, scrollable code block so it stays reachable when you have scrolled halfway down a 200-line shader, instead of scrolling out of view like an absolutely-positioned button would. margin-left:auto pins it right within the flow; a negative translateX pulls it back off the edge by the same 8px as its margin, so one --copy-button-margin variable controls both offsets. The reveal is deliberately lazy: opacity:0 with transition-delay:1s means it fades OUT a full second after the pointer leaves (avoiding flicker when the mouse crosses the block) but appears instantly on hover or keyboard focus because the hover rule resets transition-delay to 0s. The button also has a visually-hidden 'Copy to clipboard' text label rather than relying on the icon alone, and an animated lucide loader/check icon pair driven by inline transform styles.",
   "transfers": "Two things worth stealing and both are small: (1) position:sticky + top offset on the copy button, which is the correct answer for the long code listings a shader blog actually produces — most implementations get this wrong; (2) the asymmetric transition-delay reveal (1s out, 0s in), which is a genuinely considered piece of interaction polish and costs two declarations. Also note the 44px hit target — that is the WCAG 2.5.5 enhanced target size, not the 24px minimum, and it is worth matching. The single --copy-button-margin driving both margin and the negative translateX is a tidy pattern.",
   "doesNotTransfer": "It has NO filename tab, so it does not satisfy the full brief on its own — pair it with the tab geometry from Starlight or Maxime. Colour does not transfer: his light-mode button uses var(--color-info-300)/--color-info-100, i.e. a blue info accent, which is a cool UI accent and violates the palette law. The class names are compiled CSS-in-JS hashes. The opacity:0 default means the button is invisible until hover — on touch, where there is no hover, that depends on :focus-visible alone, which is a degradation risk worth checking before adopting. container-type:inline-size is only there for his other container queries and is not needed for the button.",
   "confidence": "read-from-source"
  },
  {
   "site": "Cyanilux (Unity/URP shader tutorials) — CHECKED AS REQUESTED, DOES NOT HAVE THE COMPONENT",
   "url": "https://www.cyanilux.com/tutorials/urp-shader-code/",
   "confirmedHasComponent": false,
   "verbatimCss": "/* Read from https://www.cyanilux.com/css/style.css?v=12 (13,301 bytes). This is the site's ENTIRE code-block styling — there is no copy button and no filename bar to quote. */\n\n/* Code Snippets */\n.highlight {\n\tline-height: 120%;\n}\n\n/* {{< highlight c \"linenos=table,linenostart=1\" >}} */\n.highlight div {\n\toverflow : auto;\n\tpadding-right : 20px;\n\tbackground-color: #181818 !important;\n}\n.highlight pre {\n\tbackground-color: #181818 !important;\n}\n\n/* {{< highlight c >}} */\n.highlight > pre {\n\toverflow : auto;\n\tpadding : 20px;\n}\n\n/* inline code, for contrast */\n\tline-height: 100%;\n\tcolor: #00aabb;\n\tbackground-color: #303030;\n\tpadding-top : 3px; padding-bottom : 3px;\n\tpadding-left : 7px; padding-right : 7px;\n\tmargin : 0px;\n\tborder: 2px solid #161616;\n\tborder-radius : 10px;\n\n/* markup actually emitted (Hugo Chroma, 71 blocks on the page) */\n<div class=\"highlight\"><pre tabindex=\"0\" style=\"color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;\"><code class=\"language-c\" data-lang=\"c\"><span style=\"display:flex;\"><span>…",
   "howItWorks": "I verified this properly because the brief flagged it, and the honest answer is that it is NOT a source for this component. The page has 71 code blocks and ZERO copy buttons: grep for 'clipboard' returns 0, and the only occurrences of 'copy' on the page are the word 'copy' in prose. There is no filename bar and no frame chrome of any kind. The mechanism is bare Hugo Chroma: a div.highlight wrapping a <pre> that carries Monokai colours as INLINE styles, with the theme's background then stomped back to #181818 by two !important rules — i.e. the author fought his own generator rather than configuring it. Note the two sibling rules exist because Chroma emits a different DOM for line-numbered blocks (an inner div) than for plain ones, so each needs its own padding/overflow rule.",
   "transfers": "Almost nothing structural, because the device is not here. What it IS worth citing for is a negative/contextual finding: Cyanilux is the closest subject-matter neighbour Rod has (URP/HLSL tutorials, heavy code), and it ships no copy button at all and readers manage fine — so the copy button is a nicety, not table stakes, for this genre. The only reusable scrap is the padding:20px / overflow:auto pairing on the <pre> and the observation that generator-emitted inline styles will fight any theme, which is directly relevant since Rod is on Jekyll+Rouge and will hit the same class of problem.",
   "doesNotTransfer": "The component itself does not exist here, so do not cite this as provenance for the copy button or the filename tab. Colour: the inline-code accent is #00aabb, a cyan/teal — precisely the cool accent the palette law bans, and the site is a flat cold near-black (#181818/#272822/#303030) with no warmth at all. The !important pair is an anti-pattern to avoid, not copy. Also this is Hugo, not Jekyll, so the wrapper class names do not line up with Rouge output.",
   "confidence": "read-from-source"
  },
  {
   "site": "shadcn.io — the block already named as the PICK in the ledger",
   "url": "https://www.shadcn.io/blocks/cta-card-inset-code-snippet-copy",
   "confirmedHasComponent": true,
   "verbatimCss": "not found — the source is paywalled. Details of what I actually got:\n\n1. The page returns HTTP 200 (3,888,691 bytes) but the payload is almost entirely the Fumadocs navigation manifest (a JSON page-tree of every block on the site). The block's own TSX/JSX source is NOT in the HTML.\n2. The registry endpoint https://www.shadcn.io/r/cta-card-inset-code-snippet-copy.json returns HTTP 401:\n   {\"error\":\"Unauthorized\",\"message\":\"Token required for all downloads. Get your token at https://www.shadcn.io/dashboard/account\"}\n3. What the page DOES confirm, verbatim, is prose only:\n   <meta name=\"description\" content=\"A card-inset dark CTA section for React and Next.js with a light outer wrapper, dark inner card, prominent copy-to-clipboard code block with toggle feedback, and inverted primary ShadcnioButton built with shadcn/ui and Tailwind CSS\">\n   FAQ text: \"The handleCopy function calls navigator.clipboard.writeText with the snippet joined by newlines, then sets a local copied state to true and resets it to false after 2000ms via setTimeout.\"\n   FAQ text: \"the dark inner card will keep its rounded corners, border, and padding regardless of the snippet shape.\"\n4. Only a rasterised preview exists publicly: https://www.shadcn.io/preview/cta/cta-card-inset-code-snippet-copy.webp (1440x900).\nZero CSS declarations are readable. I am not going to invent them.",
   "howItWorks": "Reconstructed from the page's own prose ONLY, not from source — flagging that clearly. It is a CTA section, not a standalone code block: a light outer wrapper containing an inset dark card ('card-inset'), and inside that a code snippet with a copy-to-clipboard button that toggles to a confirmed state and resets after 2000ms. The snippet is held in JS as an array of lines and joined with newlines on copy. The description mentions no filename tab and no traffic lights; the ledger note 'filename tab + copy, NO traffic lights' therefore appears to be Rod's own annotation of the intended direction rather than a description of what this block ships.",
   "transfers": "Honestly: only the concept, plus one implementation detail that is confirmed in prose and worth keeping — hold the snippet as a lines array and join('\\n') on copy, rather than scraping innerText out of the highlighted DOM. The light-outer / dark-inset-card relationship is a real and useful idea for Rod's night-ground palette (a warm paper surround with the code well recessed into night). But none of the geometry is measurable from what is publicly readable.",
   "doesNotTransfer": "Everything measurable. This is shadcn/ui + Tailwind utilities, so even with the source in hand it would be utility soup requiring translation to plain CSS, against the stated preference. The source is token-gated behind an account, so it cannot be cited as provenance under the provenance law — there is no readable snippet to tier as True or Remixed. It is also a generic design-system marketing block, which is close to the 'default corporate stuff' Rod has already dismissed as worthless evidence. Recommend demoting or replacing this pick.",
   "confidence": "partial"
  }
 ],
 "recommendation": "Adopt Maxime Heckel as the primary source, with Starlight/Expressive Code as the technical second, and drop the shadcn pick.\n\nWhy the shadcn pick should be demoted: it fails the provenance law on a technicality that matters. Its registry returns HTTP 401 (\\\"Token required for all downloads\\\") and the page HTML carries only the Fumadocs nav manifest plus FAQ prose — there is no readable snippet, so it cannot be tiered True or Remixed at all. Every number about it would be invented. It is also Tailwind + shadcn/ui (translation work) and a generic marketing block, which is near the \\\"default corporate stuff\\\" Rod already dismissed. The ledger note attached to it (\\\"filename tab + copy, NO traffic lights\\\") does not actually describe that block — its own description mentions neither — so that note reads as Rod's intent, not an extraction. The intent is good; the source is the wrong carrier for it.\n\nWhy Maxime Heckel is the right primary: he is the only candidate that is simultaneously (a) the same subject matter as Rod — GLSL and react-three-fiber shader writing, (b) plain readable CSS I transcribed in full, and (c) already exactly the shape Rod specified: filename left, copy right, no traffic lights. Three devices are worth taking wholesale: the flex space-between header strip separated only by a border-bottom; the bottom-only corner radii on the <pre> under an overflow:hidden card, which is what welds header to body; and the display:table line-number gutter, which keeps the gutter fixed while long HLSL lines scroll — a problem Rod will actually hit.\n\nTake two things from Starlight rather than its look: the data-code attribute (the code is stored on the button with newlines encoded, so the JS never reconstructs plain text from highlighted spans — a real bug class avoided for shader code) and the adjacent div[aria-live=\\\"polite\\\"], which is the accessibility half most implementations skip and costs nothing. Its tab geometry is also the cleanest three-declaration statement of the filename-tab device if Rod prefers a protruding tab over Maxime's full-width strip.\n\nOne finding worth flagging as directly relevant to Rod's rejection: Expressive Code draws its three dots only on .frame.is-terminal, as a single ::before using a mask-image of an SVG with three circles tinted by one background-color — so they are monochrome, not red/yellow/green. The tab variant has none. Adopting the tab variant sidesteps the chrome entirely rather than requiring deletion. Relatedly, Rod's own vendored Chirpy already ships traffic lights at _sass/base/_syntax.scss:137-149, drawn as one dot plus two box-shadow copies against var(--code-header-muted-color) — also monochrome. That is almost certainly the thing he was rejecting, and it is worth knowing his objection is to the macOS window metaphor itself, not to the colour.\n\nJosh Comeau is a supporting source, not a primary: no filename bar at all. Take one idea — position:sticky on the copy button so it stays reachable down a 200-line listing, with the asymmetric reveal (fades out after 1s, appears instantly on hover or :focus-visible). Check the touch degradation before adopting: it defaults to opacity:0, so on touch it depends on :focus-visible alone.\n\nCyanilux I checked because it was flagged, and the honest answer is that it is not a source for this component: 71 code blocks, zero copy buttons, grep for \\\"clipboard\\\" returns 0, no filename bar. It is still a useful data point — the closest subject-matter neighbour Rod has ships no copy button and readers cope, so this is a nicety rather than table stakes.\n\nPalette law across all four: every one of them fails it, and in the same direction. Maxime uses a blue/indigo accent, Starlight a neutral-plus-purple docs ramp, Josh a blue --color-info-300 button, Cyanilux a #00aabb cyan for inline code. Take all of these as token slots and fill them with Rod's warm night-ground; no accent colour transfers from any of them. Two further cautions on Maxime specifically: his card uses backdrop-filter blur, which is glassmorphism and conflicts with the paper-morphism call, so drop it; and his oklch(from ...) relative colour syntax may need a fallback."
}