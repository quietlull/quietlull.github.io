{
 "component": "prev-next",
 "candidates": [
  {
   "site": "TheRealMJP (Matt Pettineo) - graphics/shader programming blog",
   "url": "https://therealmjp.github.io/posts/shader-permutations-part1/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* https://therealmjp.github.io/css/style.min.4bc523c643bd50ebce05154df32e255bc3b0c9bae8e5b0be991a7f5163fae5af.css */\n.post-nav{display:flex;justify-content:space-between;margin-top:1.5em;margin-bottom:2.5em;font-size:1.2em}\n.post-nav a{flex-basis:50%;flex-grow:1}\n.post-nav .next-post{text-align:left;padding-right:5px}\n.post-nav .prev-post{text-align:right;padding-left:5px}\n.post-nav .post-nav-label{font-size:.8em;opacity:.8;text-transform:uppercase}\n\n/* markup, read from the page source */\n<div class=\"post-nav thin\">\n  <a class=\"next-post\" href=\"...\">\n    <span class=\"post-nav-label\"><svg class=\"feather feather-arrow-left\">...</svg>&nbsp;Newer</span><br><span>GPU Memory Pools in D3D12</span>\n  </a>\n  <a class=\"prev-post\" href=\"...\">\n    <span class=\"post-nav-label\">Older&nbsp;<svg class=\"feather feather-arrow-right\">...</svg></span><br><span>The Shader Permutation Problem - Part 2: How Do We Fix It?</span>\n  </a>\n</div>",
   "howItWorks": "Split halves with NO box at all. A flex row with justify-content:space-between; each child is a bare anchor at flex-basis:50%, flex-grow:1, so the two links tile the full measure and each one's whole 50% column is the hit target. Text aligns outward (left half left, right half right) and the 5px padding on the inner edge is the only gutter. Inside each half is a two-line stack made with a plain <br>: a small uppercase kicker at .8em/opacity .8 carrying an inline feather arrow SVG and the word Newer or Older, then the full post title on line two. The whole block is bumped to font-size:1.2em, sits 1.5em below the article and 2.5em above the comments. Note the semantics: it labels by TIME (Newer left, Older right), not by Prev/Next, and the class names are inverted relative to the visual order (.next-post is the left, newer one).",
   "transfers": "Everything structural: flex space-between + flex-basis:50%/flex-grow:1 to get two equal halves whose entire column is clickable; outward text-align; the kicker-over-title stack (small uppercase, slightly dimmed label, then the real post title at full size); the deliberate absence of a card/border/background so the nav sits directly on the page ground; the 1.2em size bump; the asymmetric 1.5em/2.5em vertical margins. Also transferable is the naming choice - Newer/Older reads more human than Prev/Next and suits a chronological dev log. The rules contain zero colour, so it drops onto a night ground unchanged.",
   "doesNotTransfer": "The feather icon set (swap for hand-drawn or typed chevrons). The site's dark Hugo skin and its .thin width container. The inverted class names are a trap to not copy. The <br>-based two-line stack is fragile with long titles - a flex column with a gap is the same geometry, better behaved. No colour or licensed font is involved, so nothing palette-bound is at risk.",
   "confidence": "read-from-source"
  },
  {
   "site": "The Book of Shaders (Patricio Gonzalez Vivo)",
   "url": "https://thebookofshaders.com/03/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* https://thebookofshaders.com/css/style.css */\n.navigationBar {\n    text-align: center;\n    cursor: pointer;\n    cursor: hand;\n}\n\nli.navigationBar {\n    list-style-type: none;\n    display: inline;\n    margin: 20px;\n    font-size: 24px;\n    font-style: italic;\n}\n\n/* markup, read from the page source */\n<hr>\n<ul class=\"navigationBar\" >\n\t<li class=\"navigationBar\" onclick=\"previusPage()\">&lt; &lt; Previous</li>\n\t<li class=\"navigationBar\" onclick=\"homePage()\"> Home </li>\n\t<li class=\"navigationBar\" onclick=\"nextPage()\">Next &gt; &gt;</li>\n</ul>",
   "howItWorks": "A centred inline triptych, not a split. Three list items are set display:inline inside a text-align:center list, so they flow as one centred line of type with 20px of margin on each side of each item as the only separator. There is no box, no border, no background, no grid - the entire device is 24px italic text plus an <hr> above it. The chevrons are LITERAL TYPED CHARACTERS with a space between them (\"< < Previous\", \"Next > >\"), not icons and not entities-as-glyphs, which is what gives it the typewritten feel. The middle slot is Home, so the row reads back-up-forward rather than just back-forward. No post titles are shown, only the three words.",
   "transfers": "The three-slot centred rhythm with an escape hatch (Home / index / back to all posts) in the middle - that is the actual idea worth stealing, and it fits an open-not-cluttered footer better than two heavy cards. The typed-chevron treatment: spaced ASCII < < and > > instead of icon SVGs reads hand-made and costs nothing. The oversized italic type as the ONLY styling. The <hr> as the separator above the block. The 20px inline margins as the sole spacing mechanic.",
   "doesNotTransfer": "The implementation is broken and must not be copied: the items are <li onclick=\"...\"> with no href, so they are not links, not keyboard focusable, not middle-clickable, and invisible to a screen reader as navigation. Rebuild as anchors. cursor:hand is dead IE-era syntax. Also this is chapter navigation in a linear book, not chronological post nav, so the Prev/Next direction is authored order, not date. No colour is involved, so nothing palette-bound.",
   "confidence": "read-from-source"
  },
  {
   "site": "Jendrik Illner - Graphics Programming Weekly",
   "url": "https://www.jendrikillner.com/post/graphics-programming-weekly-issue-300/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* https://www.jendrikillner.com/css/article.css */\n.pager {\n    list-style: none;\n    padding: 0;\n    display: flex;\n    justify-content: space-between;\n    gap: 10px;\n}\n\n.pager li {\n    display: inline-block;\n}\n\n.pager a {\n    color: var(--primary-color);\n    font-family: var(--font-family-text);\n    font-size: 16px;\n    text-decoration: none;\n    padding: 10px 20px;\n    border: 1px solid var(--primary-color);\n    border-radius: 30px;\n    transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;\n}\n\n.pager a:hover {\n    background-color: var(--primary-color);\n    color: var(--secondary-color);\n}\n\n/* :root, same file */\n:root {\n    --primary-color: #202124;\n    --secondary-color: white;\n    --font-family-text: \"Inter\", sans-serif;\n}\n\n/* markup, read from the page source */\n<nav>\n  <ul class=\"pager\">\n    <li class=\"previous\"><a href=\"...issue-299/\">&laquo; Previous </a></li>\n    <li class=\"next\"><a href=\"...issue-301/\">Next &raquo;</a></li>\n  </ul>\n</nav>",
   "howItWorks": "Two pill buttons pushed to opposite ends. The whole split is done by justify-content:space-between on a two-child flex list - .previous and .next carry NO CSS whatsoever, they are dead classes inherited from Bootstrap 3 (the site now loads Bootstrap 5, which dropped .pager entirely, so every visible rule here is the author's own). Each pill is 1px solid, border-radius:30px, padding:10px 20px, 16px text, no underline. Hover is a full fill inversion: the border colour becomes the background and the text flips to the page colour, over a 0.3s ease-in-out on background-color and color only. Labels only - guillemets and the word, no post title, no date, no arrow icons.",
   "transfers": "The pill geometry and its exact numbers (1px border, 30px radius, 10px/20px padding) - it is the lightest-weight way to make the two ends read as targets without building cards. The fill-inversion hover, which is a one-property-pair transition and stays legible on a dark ground if you invert the tokens. The lesson that the left/right split needs no per-side CSS at all when the container is space-between with exactly two children. The guillemet-and-word label (&laquo; Previous / Next &raquo;).",
   "doesNotTransfer": "The palette is bare monochrome #202124 on white - not hostile to the twilight scheme, but it contributes nothing to it, and a fill-inversion hover has to be re-derived against a night ground so the filled state does not blow out. Inter and Spline Sans are the site's fonts. The bigger cost is informational: labels only means the reader never learns what the next post IS, which is worse for a technical archive than MJP's title-bearing version.",
   "confidence": "read-from-source"
  },
  {
   "site": "Hugo PaperMod (official demo site) - the default post nav on a large share of indie dev blogs",
   "url": "https://adityatelange.github.io/hugo-PaperMod/posts/markdown-syntax/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* https://adityatelange.github.io/hugo-PaperMod/assets/css/stylesheet.8402523d366be05a2a71a1e346aadc28a50ae3e87baa9354b032bd5db2654f9e.css */\n.post-tags a,.share-buttons,.paginav{border-radius:var(--radius);background:var(--code-bg);border:1px solid var(--border)}\n.post-tags a:hover,.paginav a:hover{background:var(--border)}\n.paginav{display:flex;line-height:1.2}\n.paginav a{width:50%;display:flex;flex-direction:column;gap:.5rem;padding:.8rem;border-radius:var(--radius)}\n.paginav .title{letter-spacing:1px;text-transform:uppercase;font-size:.8rem;color:var(--secondary)}\n.paginav span:hover:not(.title){text-underline-offset:.2rem;text-decoration:underline}\n.paginav .next{margin-inline-start:auto;text-align:right}\n[dir=rtl] .paginav .next{text-align:left}\n\n/* tokens the rules resolve to, from :root in the same file */\n:root{--radius:8px;--secondary:rgb(108,108,108);--code-bg:rgb(245,245,245);--border:rgb(238,238,238)}\n\n/* markup, read from the page source */\n<nav class=\"paginav\">\n  <a class=\"prev\" href=\"...\">\n    <span class=\"title\">« Prev</span>\n    <span>PaperMod Installation Guide</span>\n  </a>\n  <a class=\"next\" href=\"...\">\n    <span class=\"title\">Next »</span>\n    <span>Code Syntax Guide</span>\n  </a>\n</nav>",
   "howItWorks": "ONE bordered container split in two, rather than two separate cards. The <nav> takes the border, background and 8px radius; the two anchors inside are each width:50% flex columns with .5rem gap and .8rem padding, so the seam between them is invisible and the hover tint on a half is what reveals the split. Each half stacks an uppercase, 1px-letter-spaced, .8rem grey kicker (« Prev / Next », guillemets typed into the label rather than drawn) over the real post title. The right half is pushed out with margin-inline-start:auto and text-align:right so it still sits flush when only one neighbour exists. Underline-on-hover is scoped to the title span only via span:hover:not(.title), so the kicker stays quiet.",
   "transfers": "The single-container-split-in-two idea (cheaper and calmer than two cards). The kicker treatment - uppercase, letter-spacing:1px, .8rem, secondary colour - which is the same move MJP makes and is clearly the convention for this component. margin-inline-start:auto as the graceful-degradation trick for a missing neighbour. Scoping the hover underline to the title span and not the label. Guillemets typed into the text.",
   "doesNotTransfer": "The skin is shared wholesale with .post-tags a and .share-buttons in a single grouped selector - that pale grey chip (background rgb(245,245,245), border rgb(238,238,238)) is a theme-wide look, so lifting the box imports a recognisable default-Hugo-theme tell along with it. Take the geometry, leave the chip. Provenance is also the weakest of the four: this is theme boilerplate on a theme demo site, not an author's own craft, so it should be cited as convention evidence rather than as a design to remix.",
   "confidence": "read-from-source"
  },
  {
   "site": "VitePress (Vue's docs framework) - the card-pair variant, read in full for comparison",
   "url": "https://vitepress.dev/guide/markdown",
   "confirmedHasComponent": true,
   "verbatimCss": "/* https://vitepress.dev/assets/style.D8VrFQ0P.css - scope hash [data-v-76cbe665] stripped for readability, values verbatim */\n.prev-next{border-top:1px solid var(--vp-c-divider);grid-row-gap:.5rem;padding-top:1.5rem;display:grid}\n.prev-next{grid-column-gap:1rem;grid-template-columns:repeat(2,1fr)}\n.pager-link{border:1px solid var(--vp-c-divider);border-radius:.5rem;width:100%;height:100%;padding:.6875rem 1rem .8125rem;transition:border-color .25s;display:block}\n.pager-link:hover{border-color:var(--vp-c-brand-1)}\n:where([dir=ltr]) .pager-link.next{text-align:right;margin-left:auto}\n.desc{color:var(--vp-c-text-2);font-size:.75rem;font-weight:500;line-height:1.66667;display:block}\n.title{color:var(--vp-c-brand-1);font-size:.875rem;font-weight:500;line-height:1.42857;transition:color .25s;display:block}\n.VPDocFooter{margin-top:4rem}\n\n/* tokens */\n--vp-c-divider:#e2e2e3;            /* light */\n--vp-c-divider:#2e2e32;            /* dark */\n--vp-c-brand-1:var(--vp-c-indigo-1);\n\n/* markup class names, read from the page source */\n<div class=\"prev-next\"><div class=\"pager\"><a class=\"VPLink link pager-link prev\">...</a></div><div class=\"pager\"><a class=\"VPLink link pager-link next\">...</a></div></div>",
   "howItWorks": "Two genuinely separate bordered cards in a 2-column grid (grid-template-columns:repeat(2,1fr), 1rem column gap), with a 1px border-top rule across the container and 1.5rem of padding beneath it acting as the visual divider from the article. Each card is full width and height with a .5rem radius and slightly optically-corrected vertical padding (.6875rem top / .8125rem bottom). Inside: a tiny .75rem grey .desc kicker over a .875rem brand-coloured .title. The next card is right-aligned and margin-left:auto so it holds the right edge when it is the only one. Hover only changes border-color over .25s - the card never fills.",
   "transfers": "The border-top-plus-padding-top divider, which is a lighter separation than an <hr> and works on a dark ground. The asymmetric top/bottom padding for optical centring. The grid-with-two-1fr-columns approach, which handles a missing neighbour better than flex if you place the surviving card by column. Hover as a border-colour change only - the quietest of the four hovers.",
   "doesNotTransfer": "The accent is indigo (--vp-c-brand-1: var(--vp-c-indigo-1)) used for the title text and the hover border - a cool UI accent, which is exactly what PALETTE LAW forbids. The geometry transfers, that colour does not. It is also docs-framework chrome, not post nav: the kicker says Previous/Next page and the pair is authored order, not chronology. Provenance is generic - cite it for the card geometry, not as taste evidence.",
   "confidence": "read-from-source"
  }
 ],
 "recommendation": "Adopt TheRealMJP's geometry as the base, and steal the middle slot from The Book of Shaders.\n\nMJP is the best fit on every axis. It is a graphics/shader programmer's own blog, not a theme demo, so the provenance is real. Its rule set is five declarations and contains no colour at all, so it drops onto a night ground untouched. Most importantly it is the only one of the four with NO BOX: two bare 50% halves sitting straight on the page, each a small uppercase kicker over the actual post title. Every boxed variant (PaperMod's grey chip, VitePress's bordered card pair, Starlight's shadowed cards) imports a rectangle that will fight a paper/lantern ground and read as default-theme output. Concretely: `display:flex; justify-content:space-between` on the container, `flex-basis:50%; flex-grow:1` on each anchor, outward `text-align`, kicker at `.8em / text-transform:uppercase / opacity:.8`, `font-size:1.2em` on the block, `margin:1.5em 0 2.5em`. Replace MJP's `<br>` stack with a flex column and a gap - same geometry, survives long titles.\n\nTwo grafts worth making. (1) From Book of Shaders, the third slot: its row is Previous / Home / Next, so the reader always has an escape to the index rather than only sideways moves. That suits open-not-cluttered better than two heavy halves, and its chevrons are literal spaced ASCII (`< <`, `> >`) rather than icons, which is a free hand-made tell. Do NOT copy its implementation - those are `<li onclick=\"previusPage()\">` with no href, so they are not links, not focusable, and not middle-clickable. (2) From MJP, the labelling: Newer/Older instead of Prev/Next reads human on a chronological dev log. Note that PaperMod and MJP independently land on the same kicker recipe (small, uppercase, letter-spaced or dimmed, secondary colour, sitting above the title) - that is the convention for this component, so following it is invisible rather than derivative.\n\nColour: nothing needs importing. MJP's rules are colourless, Jendrik's are monochrome `#202124`. Only VitePress carries an accent, indigo via `--vp-c-brand-1`, and that is a cool UI accent - geometry only, drop the colour.\n\nJendrik Illner is the alternate if the footer wants something lighter than a two-title block: pill buttons, `1px solid` / `border-radius:30px` / `padding:10px 20px`, pushed apart by `space-between`, hover inverts the fill. Cheapest device of the four, but labels only - the reader never learns what the next post is, which is a real loss on a technical archive.\n\nHonest negative evidence, checked from source and worth knowing: several of the highest-craft technical blogs ship NO prev/next at all. blog.frost.kiwi (WebGL/graphics, Eleventy) ends articles with a `More Articles / Top` pair instead. catlikecoding.com ends its shader tutorials with a plain sentence, `<p>The next tutorial is <a href=\"...\">Mathematical Surfaces</a>.</p>`, with zero dedicated CSS. lisyarus.github.io, tonsky.me, overreacted.io, joshwcomeau.com, taniarascia.com and piccalil.li were all fetched and grepped and have nothing. So a prose-sentence \"next up\" link is a legitimate, well-precedented option if the footer is getting crowded - and if a full component is built, it should earn its space with the post title, which is the one thing the bare-link versions cannot give.\n\nStarlight (starlight.astro.build) was also read in full and is near-identical to VitePress - `grid-template-columns:repeat(auto-fit,minmax(min(18rem,100%),1fr))` on `.pagination-links`, cards with `flex-basis:calc(50% - .5rem)` and `box-shadow:var(--sl-shadow-md)`, `[rel=next]` flipped with `flex-direction:row-reverse`. Its one idea not present elsewhere is the auto-fit grid, which collapses the pair to a single column on narrow screens with no media query. Not listed as a separate candidate because it is the same card device from the same docs-framework family."
}