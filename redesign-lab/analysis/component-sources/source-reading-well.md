{
 "component": "reading-well (prose measure, heading scale, paragraph rhythm, and the prose-vs-code measure relationship in long technical posts)",
 "candidates": [
  {
   "site": "Red Blob Games (Amit Patel) - A* pathfinding introduction",
   "url": "https://www.redblobgames.com/pathfinding/a-star/introduction.html",
   "confirmedHasComponent": true,
   "verbatimCss": ":root{--body-max: 41.25rem;--body-at: 62.5rem;--body-minmargin: 2.25rem;--body-slope: .33;--body-linelen: 66}\nbody{--serif: \"Iowan Old Style\", Cambria, Georgia, serif;--sans-serif: \"Avenir Next\", Candara, Ubuntu, \"Fira Sans\", system-ui, \"Segoe UI\", sans-serif;--monospace: \"Roboto Mono\", \"Cascadia Mono\", Consolas, monospace;font-family:var(--sans-serif);font-size:var(--font-size);font-size-adjust:ex-height .48;--body-flex: calc(var(--body-max) + var(--body-slope) * min(0px, 100vw - var(--body-at)));--body-width: round(down, min(var(--body-flex), 100vw - var(--body-minmargin)), 1px);--font-size: calc(var(--body-flex) / (var(--body-linelen) / 2));--line-height: calc(1.5 * var(--font-size));line-height:var(--line-height)}\nmain{font-family:var(--serif)}\nsection>*{width:var(--body-width);max-width:100%}\nh1{font-size:2em;margin:0;line-height:calc(1.5 * var(--line-height))}\nsection>h2{--h2-margin: 3em;margin:var(--line-height) auto;padding:calc(var(--line-height) / 2 - 1px) 0;font-size:1.25em;width:calc(2 * var(--h2-margin) + var(--body-width));max-width:100%}\nh3{--h2-margin: 3em;margin:var(--line-height) auto;font-size:1.125em}\np,figure,ul,ol{margin:var(--line-height) 0}\ntt,code,kbd,samp,pre{font-family:var(--monospace);font-size:.9em}\npre :is(tt,code,kbd,samp){font-size:unset}\npre{width:var(--body-width);max-width:100%;line-height:1.4;overflow:auto;border-block:1px solid #ccc;border-inline-width:0}\npre.simple,.comments pre{overflow:auto;width:calc(var(--body-width) * 1.2);border:none;background:transparent;box-shadow:none}\n.w-auto,section>.w-auto{width:auto}\n.w-full,section>.w-full{width:100%}\n.w-150b,section>.w-150b{width:calc(1.5 * var(--body-width))}\naside,p.note,div.note>p{width:calc(var(--body-width) - 2em);padding:1em;background:#eee;border:1px solid #ddd;box-shadow:0 8px 6px -6px #0006}\n@media(width>=34.375em){section p,section li{hanging-punctuation:first last;hyphens:auto;hyphenate-limit-chars:7}code,kbd,pre{hyphens:none}}\nsection figcaption{font-size:.75em;line-height:1.1;text-align:center}",
   "howItWorks": "The measure is the CONSTANT and the font size is the VARIABLE - the inverse of how everyone else does it. `--body-linelen: 66` declares the target line length in characters; `--font-size: calc(var(--body-flex) / (66/2))` derives type size from it (assuming avg char = 0.5em, so 66 chars = 33em). At full width --body-max 41.25rem = 660px gives font-size 20px, line-height 30px. Below --body-at (1000px viewport) the column shrinks at --body-slope .33 per px, and `round(down, ..., 1px)` snaps to whole pixels. `font-size-adjust: ex-height .48` normalises x-height so the 66-char measure survives font fallback. Vertical rhythm is a single unit: p/figure/ul/ol all get `margin: var(--line-height) 0`. Heading scale is deliberately tiny: h1 2em, h2 1.25em, h3 1.125em - h2 gets its emphasis from a full-bleed coloured band (`width: calc(2*3em + var(--body-width))`) rather than from size. CODE: `pre { width: var(--body-width); overflow: auto }` - identical measure to prose, plus `font-size: .9em` on all mono, so code is 18px against 20px prose, giving ~61 monospace columns in the same 660px. Longest code line on the page is 87 chars, so it scrolls. There IS a wider-code opt-in (`pre.simple { width: calc(var(--body-width) * 1.2) }` = 792px, ~73 cols) but I found it unused on three pages I checked. The bleed utilities (`.w-150b` = 1.5x body-width, `.w-full`) are used for FIGURES - on this page `.w-150b` appears exactly once, on a side-by-side diagram pair, never on a pre.",
   "transfers": "The whole measure-drives-font-size formula, verbatim - it is pure custom properties and calc, no framework. The character-count-as-a-named-variable idea (--body-linelen: 66). The single --line-height rhythm unit shared by p/figure/list. The compressed heading scale where h2 earns hierarchy through a band, not point size. `hyphens: auto` on prose with `hyphens: none` on code. `font-size-adjust: ex-height .48`. The `.w-150b` / `.w-full` opt-in width ladder as figure classes.",
   "doesNotTransfer": "Iowan Old Style / Avenir Next are system-licensed Apple faces (Cambria/Georgia fallbacks are the real render on Windows). The hsl(var(--hue)) tinting system and the blob-texture header bands are his artwork. `round()` in calc needs a modern-browser floor. The serif body type is a different voice from Rod's.",
   "confidence": "read-from-source"
  },
  {
   "site": "Josh W. Comeau - An Interactive Guide to CSS Grid / full-bleed article",
   "url": "https://www.joshwcomeau.com/css/full-bleed/",
   "confirmedHasComponent": true,
   "verbatimCss": "body{--font-family:\"Wotfard\",\"Wotfard-fallback\",sans-serif;--font-family-mono:\"Cartograph CF\",monospace;--viewport-padding:32px}\n@media (max-width:35.1875rem){body{--viewport-padding:16px}}\n/* article shell; inline style on the element supplies --max-article-width:42.875rem */\n.a1perbvc{display:grid;grid-template-columns:1fr min(var(--max-article-width),calc(100% - var(--viewport-padding) * 2)) 1fr;grid-column-gap:var(--viewport-padding);font-size:1.125rem}\n.a1perbvc>*{grid-column:2}\n.a1perbvc>.full-bleed{grid-column:1/4}\n.a1perbvc .fegvmv9{position:relative;grid-column:1/-1;padding-inline:var(--horizontal-padding,var(--viewport-padding));margin-inline:auto;width:100%;max-width:var(--max-width,68.75rem)}\n/* code card */\n.c1qvfy1x{--copy-button-size:44px;--copy-button-margin:8px;position:relative;container-type:inline-size;isolation:isolate}\n.coaxrjn.c1qvfy1x{--copy-button-sticky-top:5rem;max-height:var(--max-height);margin-inline:calc(var(--viewport-padding) * -1);margin-bottom:64px;background:var(--syntax-bg);border-radius:6px}\n.w1ey322x{--default-block-padding:var(--viewport-padding);--block-padding:var(--block-padding-override,var(--default-block-padding));padding-block:var(--block-padding);height:var(--code-snippet-height);max-height:100%;overflow:auto;line-height:1.6;font-size:var(--code-font-size,1rem);letter-spacing:0}\n@container (min-width: 42.875rem){.w1ey322x pre{white-space:pre-wrap!important}}\n.w1ey322x code{font-family:var(--font-family-mono)}\n.b1btlj5u .w1ey322x.w1ey322x{font-size:.9375rem}\n.b1btlj5u .coaxrjn.c1qvfy1x{border-radius:0;margin-left:calc(var(--viewport-padding) * -1);margin-right:calc(var(--viewport-padding) * -1);--inline-padding-override:calc(var(--viewport-padding) - var(--aside-border-width))}",
   "howItWorks": "THE ONLY SITE OF THE SEVEN THAT ACTUALLY GIVES CODE A WIDER BOX THAN PROSE - and it is a much smaller effect than the hypothesis predicts. The article is a 3-column grid: `1fr | min(42.875rem, 100% - 2*32px) | 1fr`. Everything defaults to `grid-column: 2` (the 686px prose column at 1.125rem = 18px, so roughly 38em, ~70-76 chars). Two escape hatches exist: `.full-bleed { grid-column: 1/4 }` for edge-to-edge media, and a mid-tier wrapper capped at `max-width: 68.75rem` (1100px) for wide figures. The code card uses NEITHER. It stays in column 2 and simply pulls out by exactly one gutter: `margin-inline: calc(var(--viewport-padding) * -1)` = -32px per side, so the card is 750px against the 686px prose column, then re-adds 32px as internal padding so the CODE GLYPHS LAND FLUSH WITH THE PROSE GLYPHS. The extra width is pure card chrome, zero extra columns of code. And decisively for the hypothesis: `@container (min-width: 42.875rem) { pre { white-space: pre-wrap !important } }` - once the code container is at least as wide as the prose column, HIS CODE SOFT-WRAPS. Long lines are handled by wrapping, not by widening. Code font is 1rem (16px) against 18px prose, i.e. code is smaller. Inside asides it drops again to .9375rem.",
   "transfers": "The `1fr min(measure, 100% - 2*gutter) 1fr` grid with `> * { grid-column: 2 }` and a single `.full-bleed { grid-column: 1/4 }` escape - that is four lines of plain CSS and is the cleanest bleed mechanism of any candidate. The negative-gutter code card trick (bleed by exactly the padding you re-add, so text stays optically aligned). Container-query-gated `pre-wrap` as an alternative to a horizontal scrollbar. Code font one step smaller than prose.",
   "doesNotTransfer": "Class names are Next.js/styled-components hashes (.a1perbvc, .coaxrjn) so it must be retyped as semantic CSS rather than copied. Wotfard and Cartograph CF are licensed retail fonts. His accent palette is pink/purple/blue - a COOL UI accent, which is exactly what palette law forbids; take the geometry only. The copy-to-clipboard button and Sandpack live editors are app surface, not typography.",
   "confidence": "read-from-source"
  },
  {
   "site": "lisyarus blog (Nikita Lisitsa, graphics programmer) - Exponential smoothing",
   "url": "https://lisyarus.github.io/blog/posts/exponential-smoothing.html",
   "confirmedHasComponent": true,
   "verbatimCss": "#page-content{max-width:800px;margin-top:60px;margin-bottom:60px;margin-left:auto;margin-right:auto;padding:20px;background-color:#eee;border-radius:20px;box-shadow:0px 0px 40px 20px #0007;font-family:sans-serif;color:#444;text-align:justify;line-height:24px}\nbody{margin:0px;background-image:url(\"/blog/assets/background.png\");background-attachment:fixed;overflow:visible}\nh1{font-size:24px;font-family:monospace;text-align:center;margin-top:48px}\n.page-title{font-size:24px;font-family:monospace;font-weight:700}\ncode{color:#cc5e91}\na code{color:#c60}\ncode.block{border-radius:20px;width:640px;margin-left:auto;margin-right:auto;margin-top:32px;margin-bottom:32px;box-shadow:0px 0px 10px 0px #0007;line-height:16px}\n.image-embed{max-width:640px;border-radius:20px;margin-top:16px;margin-bottom:16px;box-shadow:0px 0px 10px 0px #0007}\n.youtube-embed{width:640px;height:360px;border-radius:20px;margin-top:16px;margin-bottom:16px;box-shadow:0px 0px 10px 0px #0007}\nblockquote{max-width:640px;border-radius:20px;background-color:#fff;padding-top:12px;padding-bottom:12px;padding-left:16px;padding-right:16px;box-shadow:0px 0px 10px 0px #0007}\n.image-banner{max-width:calc(100% + 40px);margin-top:16px;margin-bottom:16px;margin-left:-20px;margin-right:-20px}\n@media (width <= 768px){code.block{max-width:100%;box-sizing:border-box}.image-embed{max-width:100%}}\n/* panda-syntax-dark.css */\npre code.hljs{display:block;overflow-x:auto;padding:1em}\ncode.hljs{padding:3px 5px}",
   "howItWorks": "DIRECT COUNTER-EVIDENCE: code is NARROWER than prose here. The reading column is `#page-content { max-width: 800px; padding: 20px }` = 760px of content at the UA-default 16px sans with `line-height: 24px` (1.5) and `text-align: justify` - that is 47.5em, roughly 95 characters, wide. Code is `code.block { width: 640px; margin-inline: auto }` - a centred card INSET 60px from each side of the prose column. And 640px is not arbitrary: `code.block` declares no font-size, so it renders at the UA monospace default (~13.33px, 0.6em advance = 8px per glyph), and 640/8 = EXACTLY 80 COLUMNS. The longest code line in the post is 78 characters. The author writes to 80 columns and sizes the box to 80 columns; `pre code.hljs { overflow-x: auto }` is only the safety net. 640px is a shared media constant - image embeds, video embeds and blockquotes all use it, so code is treated as a FIGURE, not as text. The only bleed on the site is `.image-banner { max-width: calc(100% + 40px); margin-inline: -20px }`, which escapes by exactly the container padding, and it is for banner images only.",
   "transfers": "The 80-column code card as a fixed pixel width derived from the monospace advance, inset inside a wider prose column. Sharing one media constant (640px) across code, images, video and blockquote so the page has a single secondary rhythm. The `calc(100% + 2*padding)` / negative-margin bleed for banners. The mobile fallback `code.block { max-width:100%; box-sizing:border-box }`. The paper-card-on-a-textured-ground structure (#eee card, 20px radius, `0 0 40px 20px #0007` shadow, fixed tiled background) is close to the paper-morphism direction already logged for this project.",
   "doesNotTransfer": "`text-align: justify` with no hyphenation produces rivers at 95 characters - do not copy that. The monospace headings (h1 at 24px monospace) are his voice. The inline-code pink #cc5e91 is a cool-leaning accent; his link orange #c60 is warm and is the one colour that would survive palette law. The 800px measure itself is too wide and is the thing to reject.",
   "confidence": "read-from-source"
  },
  {
   "site": "Bartosz Ciechanowski - Mesh Transforms",
   "url": "https://ciechanow.ski/mesh-transforms/",
   "confirmedHasComponent": true,
   "verbatimCss": ".article>*,#archive,.padding_wrapper{max-width:44rem;margin-left:auto;margin-right:auto;padding-left:3rem;padding-right:3rem}\nbody{font-family:\"IBM Plex Sans\",\"Helvetica Neue\",Arial,sans-serif !important;background:#F8F8F8;color:#444}\n.article li,.article p,.article .img_container{font-size:1.2em;line-height:1.6em}\np{margin:0;padding:0.7em 0}\n.article p{overflow-x:hidden;overflow-y:hidden}\nh1,h2,h3,h4,h5,h6{color:#535353;text-decoration:none;font-weight:600;font-optical-sizing:auto;font-family:\"Inter\",\"Helvetica Neue\",Arial,sans-serif;margin:0;padding:0.75em 0 0.25em 0}\nh1{font-size:1.8em}\nh2{font-size:1.5em}\nh3{font-size:1.3em}\nh4{font-size:1em}\nh5{font-size:.9em}\nh6{font-size:.8em}\n.article ul{padding:0.6em 0 0.6em 3em;margin:0 auto}\n.article ul li,.article ol li{padding:0.3em 0;margin-left:2em}\n.highlight,pre{margin:0 auto;font-size:1.05em}\n.highlight{margin:1em auto;padding:0em;background-color:#fff;border-radius:8px;border:#eaeaea 2px solid}\n.highlight>pre.chroma,.highlight>div.chroma{padding:0.5em;overflow-y:hidden;overflow-x:scroll;-webkit-overflow-scrolling:touch}\np code,li code{display:inline-block;white-space:no-wrap;background:#eaeaea;font-size:.8em;font-weight:bold;line-height:1.5em;color:#555;border-radius:0.4em;padding:0 .4em;margin:0em 0.13em;font-family:Menlo,Monaco,\"Andale Mono\",\"lucida console\",\"Courier New\",monospace;vertical-align:baseline}\nblockquote{margin-left:2.5em;padding-left:1em;font-style:italic;border-left:#eaeaea 4px solid}\n.full_width{max-width:100%}\ntable{font-size:1.2em;line-height:1.6em;margin:1em auto}\n@media only screen and (max-width:450px){.article>*,#archive{padding-left:1.2rem;padding-right:1.2rem}.article ul{padding-left:2em}}",
   "howItWorks": "The narrowest well of the seven, and the tightest heading scale. `.article > *` (a child combinator, so EVERY block-level child - paragraphs, headings, lists, code blocks alike) gets `max-width: 44rem` = 704px with 3rem padding each side, leaving a 608px content strip. Prose is `font-size: 1.2em` = 19.2px on a 16px root, `line-height: 1.6em`, so the measure is 31.7em - roughly 60-65 characters. Paragraph rhythm is `padding: 0.7em 0` with `margin: 0`, so gaps collapse predictably against the headings' `padding: 0.75em 0 0.25em 0` - note the asymmetry, three times more space above a heading than below it, which is what binds a heading to its paragraph. The heading scale is measured against the 16px ROOT while the body copy is 19.2px, so h3 (1.3em = 20.8px) is only 8% larger than a paragraph and h4 (1em = 16px) is SMALLER than body text: the hierarchy is carried by weight 600 plus the Inter/IBM Plex family switch, not by size. CODE: `.highlight` is a direct child of `.article`, so it inherits the identical 704/608px box - SAME MEASURE AS PROSE, no bleed. Code is set at 1.05em (16.8px) against 19.2px prose, so it is smaller and fits ~58 columns; the longest line in the article is 125 characters, and the author simply accepts `overflow-x: scroll`. Inline code is a separate object entirely: .8em, bold, grey pill, `display: inline-block`. `.full_width { max-width: 100% }` is the bleed class, and it is reserved for his interactive `.drawer_container` canvases - it appears zero times in this article's markup.",
   "transfers": "The `.article > * { max-width: X; margin-inline: auto; padding-inline: Y }` one-rule column - the simplest correct measure mechanism there is, and it automatically brings code blocks into the same column with no extra rules. The ~32em / 60-65 character measure. The asymmetric heading padding (0.75em above, 0.25em below). The compressed heading scale where hierarchy comes from weight and family rather than size. The inline-code pill recipe. The pattern of one named bleed class reserved for interactive figures.",
   "doesNotTransfer": "IBM Plex Sans + Inter via Google Fonts, and #F8F8F8 / #444 is a bright daylight paper scheme - the exact opposite of a night ground. Link blue #0181eb is a cool accent. His WebGL drawer canvases are the artwork. `white-space: no-wrap` in the inline-code rule is a typo (should be `nowrap`) and is inert - do not copy it forward.",
   "confidence": "read-from-source"
  },
  {
   "site": "Catlike Coding (Jasper Flick) - Rendering 2: Shader Fundamentals",
   "url": "https://catlikecoding.com/unity/tutorials/rendering/part-2/",
   "confirmedHasComponent": true,
   "verbatimCss": "body{background-color:#eee;color:#222;font:16px\"Lucida Grande\",Arial,sans-serif;line-height:1.5em;margin:0;padding:0;counter-reset:h2}\narticle{background-color:#fafafa;box-sizing:border-box;padding:1em;margin:1em auto;max-width:768px}\narticle header h1{display:inline-block;font-size:32px;margin:0}\narticle section h2{font-size:20px;margin:2em 0 1em}\narticle section h2:before{counter-increment:h2;content:counter(h2)\" \"}\nsection>h3{font-size:18px;margin:1.5em 0 1em}\nsection>h3:before{counter-increment:h3;content:counter(h2)\".\"counter(h3)\" \"}\nh2:before,h3:before{color:#888;font-size:14px}\narticle p{margin:1em 0}\npre{border-width:1px;border-style:solid;border-color:#999;font:13px Courier,monospace;padding:1em;tab-size:4;overflow:none;-moz-tab-size:4;-webkit-text-size-adjust:100%}\ncode{font:13px Courier,monospace}\nb,code,i{white-space:nowrap}\ndel,ins{background-color:#fafa99;color:inherit;font-style:inherit;font-weight:inherit;text-decoration:none}\ndel{text-decoration:line-through}\naside{background-color:#ddd;border:2px solid #ccc;font-size:14px;margin:1em;padding:0 1em}\nfigure{text-align:center;display:block;margin:2em auto;padding:0}\nfigure img{max-width:100%;height:auto}\nfigcaption{font-size:14px;font-style:italic}\n.formula{padding:0 0 0 1em}\nbody.dark{background-color:#111;color:#aaa}\n.dark #theme-switcher,.dark #toc,.dark article{background-color:#333}",
   "howItWorks": "The purest test of the hypothesis, because the whole 6267-byte stylesheet has exactly one width in it. `article { max-width: 768px; padding: 1em; box-sizing: border-box }` gives 736px of content for BOTH prose and code - one column, no bleed, no exceptions. Prose is 16px Lucida Grande / Arial at line-height 1.5em, so 736px is 46em, roughly 90-92 characters: too wide by any typographic standard. Code is `font: 13px Courier` - 0.8125x the prose size - inside `padding: 1em` and a 1px border, so the code strip is 702px, and at Courier's 0.6em advance (7.8px) that is exactly 90 monospace columns. The longest code line in this 64-block tutorial is 83 characters, comfortably inside 90. So the causal chain is real but runs the other way from the hypothesis: he decided code must show ~90 columns unbroken, shrank the mono to 13px to make that fit a 768px card, and the 90-character prose measure is the COLLATERAL DAMAGE, not a separate decision. Note also `overflow: none` on pre - that is not a valid CSS value, so it computes to `visible`: there is no scroll container at all, and a 91st column would visibly spill out of the card. He is relying entirely on author discipline. Heading scale is 32/20/18 against 16px body, with auto-numbering via `counter-increment` and the numbers dimmed to 14px #888. `del`/`ins` restyled to a yellow highlight is his diff-in-code device.",
   "transfers": "The counter-based section numbering (`counter-reset: h2` on body, `counter-increment` + `content: counter(h2)\".\"counter(h3)\" \"` in ::before) with the number dimmed and downsized - genuinely useful for long shader tutorials. The `del`/`ins` yellow-highlight code-diff treatment. The `tab-size: 4` on pre. The single-card article shell. Most valuable as a CAUTIONARY data point: it is the clearest proof that letting code set the container width wrecks the prose measure.",
   "doesNotTransfer": "Nothing visual. 13px Courier is close to unreadable, #eee/#fafafa flat grey is default-corporate, `overflow: none` is a bug, and the ~92-character prose measure is precisely what Rod's STYLE.md forbids. Cite it for the mechanism and the counters, not the look.",
   "confidence": "read-from-source"
  },
  {
   "site": "Cyanilux (Unity shader blog) - CORRECTION to prior evidence",
   "url": "https://www.cyanilux.com/tutorials/intro-to-shader-graph/",
   "confirmedHasComponent": true,
   "verbatimCss": ".content{margin:auto;width:70%;border:3px solid #303030;padding:30px;background-color:#202020;border-radius:20px;box-shadow:5px 5px 10px 0px #000000;transition:width 0.5s}\n@media screen and (min-width: 1200px){.header,.footer,.content{width:60%}}\n@media screen and (max-width: 1000px){.header,.footer,.content{width:85%}}\n@media screen and (max-width: 600px){.header,.footer,.content{width:90%;padding:10px}}\nbody{font-family:'Lato',sans-serif;background-color:#05323a;background:linear-gradient(#00000000 0%, rgb(0,0,0) 120%),repeating-conic-gradient(#062835 0% 25%, #042e3b 5% 50%) 0% / 60px 60px;background-attachment:fixed;color:#dddddd;line-height:170%;word-break:break-word}\n.highlight{line-height:120%}\n.highlight div{overflow:auto;padding-right:20px;background-color:#181818 !important}\n.highlight pre{background-color:#181818 !important}\n.highlight>pre{overflow:auto;padding:20px}\nimg{max-width:100%;object-fit:scale-down}\n.img-text{color:#888888;font-size:13px;text-align:center;margin:auto}",
   "howItWorks": "Corrects the note in the brief. The `width: 70%` figure is only the mid-range value, and more importantly THERE IS NO max-width ANYWHERE IN THE STYLESHEET on .content - I grepped all six max-width occurrences in the 13301-byte file and every one is either a media-query condition or `max-width: 100%` on images. The column is 60% above 1200px, 70% from 1000-1200px, 85% below 1000px, 90% below 600px, always fluid. So the measure is not ~95 characters, it is whatever the monitor is: at 1440px the content strip is 798px (~100 chars), at 2560px it is 1470px (~180 chars). Prose is Lato at the UA-default 16px with `line-height: 170%` and `word-break: break-word` on body. CODE: `.highlight > pre { overflow: auto; padding: 20px }` and `.highlight div { overflow: auto }` - the code sits in the SAME fluid column as prose with no width of its own and no font-size declaration, and simply scrolls. Longest code line in the post is 146 characters. So no, Cyanilux does not give code a wider measure; it has no measure discipline at all, and the wide-looking column is an absence of a decision rather than a decision about code.",
   "transfers": "Only as corrected evidence - the 70% number should be struck from the ledger and replaced with 'unbounded fluid, 60-90% by breakpoint, no max-width'. Structurally, the dark rounded card floating on a patterned fixed background (`repeating-conic-gradient` at 60px tiles under a darkening linear-gradient, `background-attachment: fixed`) is a real and cheap night-ground device worth keeping in the geometry pile.",
   "doesNotTransfer": "The palette is petrol/teal throughout (#05323a ground, #062835/#042e3b conic tiles, #202020 card, #181818 code) - a cool scheme end to end, disqualified under palette law; take the tiling and the card, not a single hex. `word-break: break-word` on body is a blunt instrument that will break words mid-syllable in prose. The unbounded percentage width is the anti-pattern this whole component exists to avoid.",
   "confidence": "read-from-source"
  },
  {
   "site": "Inigo Quilez - CONFIRMED but reframed",
   "url": "https://iquilezles.org/articles/smin/",
   "confirmedHasComponent": true,
   "verbatimCss": "body{background-color:#282828;color:#c0c0c0;font-size:1.125rem;line-height:1.8;font-family:sans-serif;font-weight:normal;text-align:left;text-justify:inter-word;display:flex;flex-direction:column;min-height:100vh}\ndiv#container{width:100%;max-width:120ch;min-height:100vh;padding-top:32px;padding-left:24px;padding-right:24px;margin-left:auto;margin-right:auto}\nh2{color:#ffffff;font-weight:normal}\nh3{color:#ffffff;font-weight:normal}\n.code{overflow:auto;font-family:Courier,sans-serif,monospace;font-size:1em;font-weight:bold;color:#dcdcdc;text-align:left;vertical-align:text-top;white-space:pre;line-height:1.35;height:auto}\n.formulaText{font-size:1.25em;font-family:\"Times New Roman\",Times,serif;padding-left:4px;padding-right:4px;line-height:1.5em}\n.partsL{display:flex;justify-content:flex-start;padding:0px;margin:0px;flex-wrap:wrap}\n.part,.part25,.part30,.part40,.part50,.part60,.part70{justify-content:flex-start;margin:0px;padding:0px;padding-bottom:32px;padding-right:32px}\n.part50{width:50%}\n@media only screen and (max-width:1280px){.part,.part25,.part30,.part40,.part50,.part60,.part70{padding-right:0px;width:100%}.code{font-size:0.8em;line-height:120%}}",
   "howItWorks": "The 120ch in the brief is real and I read it, but it is not a prose measure decision - it is a LAYOUT decision, and that reframing matters. `div#container { max-width: 120ch; padding-inline: 24px }` against body's 18px sans (Arial '0' advance ~10px) resolves to ~1200px, ~1152px of content, and prose runs at ~125-130 characters, which is indefensible as a reading measure. But the container is 120ch because iq's articles are built from flex rows of `.part25/.part30/.part40/.part50` columns - image beside code, formula beside diagram - and 120ch is the width needed to hold TWO of those side by side. On /articles/distfunctions/ every one of the 50 `.code` blocks lives inside a `.part1`/`.part2` flex column and is therefore roughly half that width; only on prose-led pages like /articles/smin/ do the `.code` divs become direct children of #container and get the full width. CODE vs PROSE: identical box, `.code { overflow: auto; white-space: pre; font-size: 1em }` - same 18px as prose, Courier, so ~106 columns, scrolling when longer. The one real code-specific adaptation is responsive, not spatial: `@media (max-width:1280px) { .code { font-size: 0.8em; line-height: 120% } }` - below 1280px the columns stack to 100% and the code font SHRINKS to 14.4px to keep more columns visible on a narrow screen.",
   "transfers": "The idea that a wide container can be a multi-column figure grid rather than a wide reading column - relevant to Rod because shader posts genuinely want code-beside-render pairs. The `.part25/.part30/.part40/.part50` flex-percentage ladder that collapses to `width: 100%` at one breakpoint. Shrinking the code font-size at narrow widths instead of adding a scrollbar.",
   "doesNotTransfer": "120ch as a prose measure - do not adopt it, it is 125+ characters and iq is not making a typographic claim. `font-family: Courier, sans-serif, monospace` lists sans-serif before monospace, which is a bug that will render 'monospace' code in a proportional face wherever Courier is absent. The #282828/#c0c0c0 flat charcoal with pink #ffa0a0 links is a neutral-grey dev scheme with no warmth. Body-level `display: flex` on <body> is fragile.",
   "confidence": "read-from-source"
  }
 ],
 "recommendation": "THE HYPOTHESIS IS MOSTLY REFUTED, and the refutation is more useful than a confirmation would have been. Of seven technical sites read from source, SIX give code the same measure as prose or narrower. Ciechanowski, Red Blob Games, Cyanilux, Catlike Coding and Inigo Quilez all put code in the identical column as prose; lisyarus makes code NARROWER than prose (640px code card inside a 760px column). Only Josh Comeau widens code, and only by one 32px gutter per side, which he re-adds as internal padding so the glyphs stay optically flush - the extra width is card chrome, zero extra columns of code. He then pairs it with `@container (min-width: 42.875rem) { pre { white-space: pre-wrap } }`, i.e. his code WRAPS. Nobody bleeds code out to fit long lines. Where a bleed class does exist (Red Blob's `.w-150b` at 1.5x, Ciechanowski's `.full_width`, lisyarus's `.image-banner`, Josh's `.full-bleed`), it is reserved for FIGURES, diagrams and interactive canvases, and I verified in the markup that none of them is applied to a `pre` on the pages I checked.\n\nWhat technical sites actually do instead, four devices, all cheap:\n1. SHRINK THE MONO. Universal - Catlike 13px code vs 16px prose (0.81x), Ciechanowski 1.05em vs 1.2em (0.875x), Red Blob `.9em`, Josh 16px vs 18px (0.89x). Same column, more columns of code.\n2. ACCEPT THE SCROLLBAR. `overflow-x: auto` on the code box, everywhere except Catlike (whose `overflow: none` is invalid CSS and does nothing).\n3. AUTHOR DISCIPLINE. lisyarus sizes his card to exactly 80 monospace columns (640px at the 13.33px UA default) and his longest line in the post is 78 chars. Catlike's box fits 90 and his longest is 83. They write to the column.\n4. SHRINK ON MOBILE, DON'T SCROLL. iq drops `.code` to 0.8em below 1280px.\n\nSo the 65-75ch vs 78/95/120ch conflict is not caused by code at all. Sorting the seven by craft sorts them by measure: the sites that made a real typographic decision are all at or near Rod's own spec - Ciechanowski ~32em (60-65 chars), Red Blob 66 by explicit declaration, Josh 42.875rem/18px (~70-76). The wide ones are wide because they never set a measure: Cyanilux has no `max-width` in its entire stylesheet (fluid 60-90%, so ~180 chars on a 2560px monitor), and iq's 120ch is a two-column figure-grid width that only doubles as a prose measure on his text-led pages. STYLE.md is right and should not be relaxed.\n\nADOPT: Red Blob Games as the primary. It is the only candidate that treats the measure as the constant and derives font size from it - `--body-linelen: 66` is literally a named variable, and `--font-size: calc(var(--body-flex) / (var(--body-linelen) / 2))` falls out of it. Plain custom properties and calc, zero framework, transcribes verbatim, and it comes with the rest of the well already solved: one `--line-height` rhythm unit shared by p/figure/ul/ol, `font-size-adjust: ex-height .48` so fallback fonts keep the measure, `hyphens: auto` on prose with `hyphens: none` on code, and a `.w-full` / `.w-150b` width ladder for figures. Take lisyarus's 80-column code card as the second half: size the code box from the monospace advance rather than as a fraction of the column, share that one number with images and blockquotes so the page has a single secondary rhythm, and write shader snippets to 80 columns. Take Josh Comeau's `grid-template-columns: 1fr min(measure, 100% - 2*gutter) 1fr` with `> * { grid-column: 2 }` and one `.full-bleed { grid-column: 1/4 }` if a bleed is wanted for renders and canvases - four lines and the cleanest bleed mechanism of the seven. Take Ciechanowski's compressed heading scale and asymmetric heading padding (0.75em above, 0.25em below, hierarchy from weight 600 and a family switch rather than point size), which suits an open, uncluttered page far better than a 1.25 modular scale.\n\nCOLOUR WARNING, none of these palettes transfer. Cyanilux is petrol/teal end to end (#05323a, #062835, #042e3b) and is disqualified outright; Josh Comeau's accent is pink/purple/blue, a cool UI accent; Ciechanowski is bright daylight paper (#F8F8F8/#444) with a blue #0181eb link; iq is neutral charcoal. The one warm accent in the set is lisyarus's link orange #c60. Geometry and mechanism only from all of them.\n\nTWO CORRECTIONS TO THE LEDGER: Cyanilux's \"width: 70% (~95ch)\" should read \"unbounded fluid, 60/70/85/90% by breakpoint, no max-width anywhere\" - the measure grows without limit. And iq's \"max-width: 120ch\" is confirmed as a literal declaration but should be relabelled a figure-grid width, not a prose measure. I did not verify the stripe ~78ch claim - it was not in scope here and no stripe URL was read."
}