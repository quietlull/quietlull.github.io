{
 "component": "toc-scroll-spy",
 "candidates": [
  {
   "site": "Astro Docs (Starlight theme) - article page right rail + mobile popup",
   "url": "https://docs.astro.build/en/guides/troubleshooting/",
   "confirmedHasComponent": true,
   "verbatimCss": "HTML (read from the served page):\n<div class=\"right-sidebar-panel sl-hidden lg:sl-block astro-mbtvktjh\">\n  <div class=\"sl-container astro-mbtvktjh\">\n    <starlight-toc data-min-h=\"2\" data-max-h=\"3\">\n      <nav aria-labelledby=\"starlight__on-this-page\">...</nav>\n    </starlight-toc>\nMobile twin:\n<mobile-starlight-toc data-min-h=\"2\" data-max-h=\"3\" class=\"astro-gl7qdvoz\">\n  <nav aria-labelledby=\"starlight__on-this-page--mobile\">\n    <details id=\"starlight__mobile-toc\"><summary class=\"sl-flex\"><div class=\"toggle sl-flex\">...\n\nCSS (from /_astro/middleware.Dyx8U14h.css, verbatim):\n.right-sidebar-container{width:calc(var(--sl-sidebar-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-width)) / 2);order:2;position:relative}\n.right-sidebar{border-inline-start:1px solid var(--sl-color-hairline);padding-top:var(--sl-nav-height);scrollbar-width:none;width:100%;height:100vh;position:fixed;top:0;overflow-y:auto}\n.right-sidebar-panel{min-height:100%;padding:1rem var(--sl-sidebar-pad-x);flex-direction:column;justify-content:space-between;display:flex}\n.right-sidebar-panel h2{color:var(--sl-color-white);font-size:var(--sl-text-h5);font-weight:600;line-height:var(--sl-line-height-headings);margin-bottom:.5rem}\n.right-sidebar-panel :where(a){font-size:var(--sl-text-xs);color:var(--sl-color-gray-3);overflow-wrap:anywhere;text-decoration:none;display:block}\n.right-sidebar-panel :where(a):hover{color:var(--sl-color-white)}\n\n/* the list + depth indent + active state */\nul{padding:0;list-style:none}\na{--pad-inline:.5rem;padding-block:.25rem;padding-inline:calc(1rem * var(--depth) + var(--pad-inline)) var(--pad-inline);border-radius:.25rem;line-height:1.25;display:block}\na[aria-current=true]{color:var(--sl-color-text-accent)}\n\n/* mobile popup */\nnav{z-index:var(--sl-z-index-toc);top:calc(var(--sl-nav-height) - 1px);border-top:1px solid var(--sl-color-gray-5);background-color:var(--sl-color-bg-nav);position:fixed;inset-inline:0}\n@media (width>=50rem){nav{inset-inline-start:var(--sl-content-inline-start,0)}}\nsummary{height:var(--sl-mobile-toc-height);border-bottom:1px solid var(--sl-color-hairline-shade);font-size:var(--sl-text-xs);outline-offset:var(--sl-outline-offset-inside);align-items:center;gap:.5rem;padding:.5rem 1rem}\nsummary::marker{display:none}\nsummary::-webkit-details-marker{display:none}\n.toggle{border:1px solid var(--sl-color-gray-5);background-color:var(--sl-color-black);user-select:none;cursor:pointer;border-radius:.5rem;flex-shrink:0;justify-content:space-between;align-items:center;gap:1rem;padding-block:.5rem;padding-inline:.75rem .5rem;line-height:1}\ndetails[open] .toggle{color:var(--sl-color-white);border-color:var(--sl-color-accent)}\ndetails[open] .caret{transform:rotate(90deg)}\n.display-current{white-space:nowrap;text-overflow:ellipsis;color:var(--sl-color-white);overflow:hidden}\n.dropdown{--border-top:1px;margin-top:calc(-1 * var(--border-top));border:var(--border-top) solid var(--sl-color-gray-6);border-top-color:var(--sl-color-hairline-shade);max-height:calc(85vh - var(--sl-nav-height) - var(--sl-mobile-toc-height));background-color:var(--sl-color-black);box-shadow:var(--sl-shadow-md);overscroll-behavior:contain;overflow-y:auto}\n\nTokens: --sl-mobile-toc-height:3rem; --sl-z-index-toc:4; --sl-sidebar-width:18.75rem; --sl-content-width:45rem;\nAccent: --sl-color-accent:#3d50f5 (light) / #3369ff (dark); --sl-color-accent-high:#0f1c8a / #b3c7ff\n\nJS (from /_astro/starlight-toc.CTCQXEge.js, de-minified faithfully):\nclass StarlightTOC extends HTMLElement {\n  _current = this.querySelector('a[aria-current=\"true\"]');\n  minH = parseInt(this.dataset.minH || '2', 10);\n  maxH = parseInt(this.dataset.maxH || '3', 10);\n  tocHeadingSelector = `h1#_top,:where(h2,h3)[id]`;   // built from minH..maxH\n  set current(link) {\n    if (link === this._current) return;\n    this._current?.removeAttribute('aria-current');\n    link.setAttribute('aria-current', 'true');\n    this._current = link;\n  }\n  init = () => {\n    const links = [...this.querySelectorAll('a')];\n    // getElementHeading(): walk backwards/up from any element to its owning heading\n    const setCurrent = (entries) => {\n      for (const { isIntersecting, target } of entries) {\n        if (!isIntersecting) continue;\n        const heading = getElementHeading(target);\n        if (!heading) continue;\n        const link = links.find(l => l.hash === '#' + encodeURIComponent(heading.id));\n        if (link) { this.current = link; break; }\n      }\n    };\n    const toObserve = document.querySelectorAll([\n      'main :where(h2,h3)[id]',\n      'main :where(h2[id],h3[id], .sl-heading-wrapper) ~ *:not(:has(h2[id],h3[id]))',\n      'main .sl-markdown-content > *:not(:has(h2[id],h3[id]))',\n      'main > *:not(:has(h2[id],h3[id]))'\n    ].join());\n    observer = new IntersectionObserver(setCurrent, { rootMargin: this.getRootMargin() });\n    toObserve.forEach(el => observer.observe(el));\n    // rebuilt on resize, debounced 200ms, inside requestIdleCallback\n  };\n  getRootMargin() {\n    const top = (document.querySelector('header')?.getBoundingClientRect().height || 0)\n              + (this.querySelector('summary')?.getBoundingClientRect().height || 0) + 32;\n    return `-${top}px 0% ${top + 53 - document.documentElement.clientHeight}px`;\n  }\n}\ncustomElements.define('starlight-toc', StarlightTOC);",
   "howItWorks": "Two DOM instances of the same list. Desktop: a fixed-position right rail (position:fixed, top:0, height:100vh, own overflow-y, hairline border on the inner edge) whose inner panel is a flex column. Mobile: the exact same <ul> inside a native <details>/<summary> pinned under the nav bar, where <summary> shows the CURRENTLY ACTIVE heading text (.display-current, ellipsis-truncated) and the caret rotates 90deg on [open]. Scroll-spy is one custom element per instance. Two non-obvious moves: (1) it observes not only the headings but every content block between headings, then walks backward/upward from whatever intersects to find its owning heading, so a long code block or figure still keeps its section lit; (2) rootMargin is computed at runtime into a thin detection band just under the sticky header: `-Npx 0% (N+53-viewportHeight)px`, where N = header height + mobile summary height + 32. Active state is written as aria-current=\"true\" on the <a> - the attribute IS the styling hook, so the CSS needs no class bookkeeping and the a11y story comes free. Nesting depth is a single CSS custom property: padding-inline-start:calc(1rem * var(--depth) + var(--pad-inline)).",
   "transfers": "Everything structural. The aria-current-as-style-hook pattern; the --depth indent formula (one property, any nesting depth); observing inter-heading content blocks rather than only headings; the runtime-computed rootMargin band tied to header height; the <details>/<summary> mobile popup with the active heading shown in the closed summary; the fixed rail + inner scroll + hairline inner border; max-height:calc(85vh - nav - tocbar) with overscroll-behavior:contain on the dropdown. All plain CSS custom properties, no framework needed - the custom element is ~40 readable lines of vanilla JS.",
   "doesNotTransfer": "COLOUR: --sl-color-accent is #3d50f5 / #3369ff, a cool indigo-blue used as a UI accent. Palette law says blue lives in the sky only, so the active-link colour must be re-picked (a warm ember/lantern hue). Also does not transfer: the Astro-scoped class hashes (astro-gl7qdvoz etc.) are build artifacts, the sl-* token names, and the base64 data-URI checkmark SVG used as a mask-image on the mobile active row (that one is artwork, and it is a generic tick - Rod would want his own mark or nothing). The sl-hidden/lg:sl-block utility pair is Starlight's own mini-utility layer, not Tailwind, but still worth replacing with a plain media query.",
   "confidence": "read-from-source"
  },
  {
   "site": "vuejs.org (VitePress default theme) - \"On this page\" outline with a sliding marker",
   "url": "https://vuejs.org/guide/essentials/reactivity-fundamentals.html",
   "confirmedHasComponent": true,
   "verbatimCss": "HTML (read from the served page):\n<div class=\"aside\"><div class=\"aside-container\">\n  <div class=\"VPContentDocOutline\">\n    <div class=\"outline-marker\"></div>\n    <div class=\"outline-title\">On this page</div>\n    <nav aria-labelledby=\"doc-outline-aria-label\">\n      <span id=\"doc-outline-aria-label\" class=\"visually-hidden\">Table of Contents for current page</span>\n      <ul class=\"root\"><li><a class=\"outline-link\" href=\"#declaring-reactive-state\">...</a>\n        <ul class=\"nested\">...\n\nCSS (from /assets/style.ColQyr9k.css, verbatim):\n.aside{flex-shrink:0;width:320px;padding-left:64px;display:none;position:relative}\n.aside{order:2;display:block}            /* at the wide breakpoint */\n.aside{padding-left:96px}                 /* at the widest breakpoint */\n.aside-container{width:224px;top:calc(var(--vt-nav-height) + var(--vt-banner-height,0px) + 24px);position:sticky;bottom:0}\n.aside-container::-webkit-scrollbar{display:none}\n.outline-link{color:var(--vt-c-text-2);text-overflow:ellipsis;white-space:nowrap;line-height:28px;transition:color .5s;display:block;overflow:hidden}\n.outline-link:hover,.outline-link.active{color:var(--vt-c-text-1);transition:color .25s}\n.outline-marker{opacity:0;background-color:var(--vt-c-green);z-index:0;border-radius:4px;width:4px;height:20px;transition:top .25s cubic-bezier(0,1,.5,1),opacity .25s,background-color .5s;position:absolute;top:32px;left:-12px}\n/* mobile dropdown variant */\n.VPLocalNavOutlineDropdown .outline-link{padding:2px 0;font-size:14px;font-weight:500}\n\nToken: --vt-c-green:#42b883 (also --vt-c-green-light:#42d392, --vt-c-green-dark:#33a06f)\n\nJS (from /assets/chunks/src.BgdEtdKT.js, de-minified faithfully):\nfunction useActiveAnchor(container, marker) {\n  const isAsideEnabled = useMediaQuery('(min-width: 1280px)');\n  const onScroll = throttleAndDebounce(setActiveLink, 100);\n  function setActiveLink() {\n    if (!isAsideEnabled.value) return;\n    const links = [].slice.call(container.value.querySelectorAll('.outline-link'));\n    const anchors = [].slice.call(document.querySelectorAll('.content .header-anchor'))\n      .filter(a => links.some(l => l.hash === a.hash && a.offsetParent !== null));\n    // pinned to last heading once scrolled to the bottom\n    if (anchors.length && window.scrollY + window.innerHeight >= document.body.offsetHeight - 1) {\n      activate(anchors[anchors.length - 1].hash); return;\n    }\n    for (let i = 0; i < anchors.length; i++) {\n      const [isActive, hash] = isAnchorActive(i, anchors[i], anchors[i + 1]);\n      if (isActive) { activate(hash); return; }\n    }\n  }\n  let prevActiveLink = null;\n  function activate(hash) {\n    prevActiveLink && prevActiveLink.classList.remove('active');\n    const link = prevActiveLink = hash == null ? null\n      : container.value.querySelector(`a[href=\"${decodeURIComponent(hash)}\"]`);\n    if (link) {\n      link.classList.add('active');\n      marker.value.style.opacity = '1';\n      marker.value.style.top = link.offsetTop + 33 + 'px';\n    } else {\n      marker.value.style.opacity = '0';\n      marker.value.style.top = '33px';\n    }\n  }\n  onMounted(() => { requestAnimationFrame(setActiveLink); window.addEventListener('scroll', onScroll); });\n  onUpdated(() => { activate(location.hash); });\n  onUnmounted(() => { window.removeEventListener('scroll', onScroll); });\n}\nconst PAGE_OFFSET = 56;\nfunction getAnchorTop(anchor) { return anchor.parentElement.offsetTop - PAGE_OFFSET - 15; }\nfunction isAnchorActive(index, anchor, nextAnchor) {\n  const scrollTop = window.scrollY;\n  if (index === 0 && scrollTop === 0) return [true, null];\n  if (scrollTop < getAnchorTop(anchor)) return [false, null];\n  if (!nextAnchor || scrollTop < getAnchorTop(nextAnchor)) return [true, anchor.hash];\n  return [false, null];\n}\nfunction throttleAndDebounce(fn, delay) { /* leading call + trailing setTimeout(delay) */ }",
   "howItWorks": "This is the counter-example to Starlight: NO IntersectionObserver at all. A throttled+debounced (100ms) scroll listener does plain arithmetic - it walks the .header-anchor list, compares window.scrollY against each anchor's parentElement.offsetTop - 56 - 15, and the first anchor whose band contains the scroll position wins, with a special case that pins the LAST heading once you hit the bottom of the document (which fixes the classic \"short final section never lights up\" bug). The visible active device is not a border on the link - it is a single free-floating .outline-marker div, 4x20px, border-radius 4px, position:absolute, left:-12px, that slides by having its inline style.top written to activeLink.offsetTop + 33, animated by transition:top .25s cubic-bezier(0,1,.5,1). Because it is one element rather than a per-link border, the highlight physically travels between items instead of blinking. The rail itself is not fixed - .aside-container is position:sticky with top:calc(nav-height + banner-height + 24px), so it flows with the article and stops under the header. The whole spy is gated behind a (min-width:1280px) media query, and the mobile form is a separate component: a <button> that toggles a .items flyout containing the same list, sized with a --vt-vh custom property set to window.innerHeight + Math.min(scrollY - 64, 0), with a \"Return to top\" link at the head of the list.",
   "transfers": "The sliding-marker device is the best geometry here and is pure CSS + one inline top value: a 4x20px rounded bar at left:-12px with transition:top .25s cubic-bezier(0,1,.5,1). Also transferable: the sticky (not fixed) aside-container with top:calc(header + gap); the bottom-of-document special case; the throttle-and-debounce-at-100ms scroll approach as a lightweight alternative if IntersectionObserver's rootMargin math proves fiddly; the mobile flyout's \"Return to top\" affordance; the 224px content width inside a 320px aside with 64-96px left padding (the gutter is what makes it read as a rail rather than a column).",
   "doesNotTransfer": "COLOUR: the marker is var(--vt-c-green) = #42b883, Vue brand green - a cool accent that violates the palette law outright. Take the 4x20px rounded bar and the easing, re-colour it warm. Also does not transfer: the ugly magic numbers (offsetTop + 33, PAGE_OFFSET 56, -15) which are hard-coded to VitePress's own header height and would need re-derivation; the data-v-* scope hashes; the Vue reactivity wrapper (useMediaQuery/onMounted) - the logic itself is framework-free but the transcription is not copy-paste. The scroll-math approach is also strictly worse than IntersectionObserver for main-thread cost on a page that already runs Three.js.",
   "confidence": "read-from-source"
  },
  {
   "site": "MDN Web Docs - \"In this article\" sticky right sidebar",
   "url": "https://developer.mozilla.org/en-US/docs/Web/CSS/position",
   "confirmedHasComponent": true,
   "verbatimCss": "HTML (read from the served page, server-rendered):\n<aside class=\"layout__right-sidebar reference-layout__toc\">\n  <nav class=\"reference-toc\">\n    <h2>In this article</h2>\n    <ul>\n      <li><a href=\"#try_it\" data-glean-id=\"toc_click: #try_it\">Try it</a></li>\n      <li><a href=\"#syntax\" ...>Syntax</a></li>\n      ...\n\nCSS (from /static/client/styles-reference-toc.9b9bd019efd16b86.css - this is the WHOLE file, 743 bytes, verbatim):\n.reference-toc h2{font-size:var(--toc-header-font-size,var(--font-size-large));font-weight:400;margin:0;padding:.75em 0}\n.reference-toc ul{display:grid;list-style:none;margin:0;padding:0}\n:is(.reference-toc ul) ul{padding-left:1em}\n.reference-toc a{box-shadow:-2px 0 0 var(--color-border-primary);color:inherit;display:inline-block;margin-left:2px;padding:.25em .5em}\n:is(.reference-toc a):not(:hover){text-decoration:none}\n:is(.reference-toc a):hover:not([aria-current=true]){color:var(--color-link-normal)}\n[aria-current=true]:is(.reference-toc a){background-color:var(--color-area-background);box-shadow:-2px 0 0 var(--color-area-highlight-border)}\n\nStickiness (from /static/client/270.66bae6059a1c23f9.css, verbatim):\n:is(.layout__2-sidebars,.layout__2-sidebars-inline) .layout__left-sidebar,\n:is(.layout__2-sidebars,.layout__2-sidebars-inline) .layout__right-sidebar{max-height:calc(100vh - var(--sticky-header-height));outline-offset:-2px;overflow-y:auto;position:sticky;top:var(--sticky-header-height)}\n:is(.layout__2-sidebars,.layout__2-sidebars-inline) .layout__right-sidebar{grid-area:right-sidebar}\n:is(.layout__2-sidebars,.layout__2-sidebars-inline) .layout__right-sidebar{margin-right:calc(var(--layout-side-padding-min)*-1);padding-left:var(--layout-sidebar-padding);padding-right:var(--layout-side-padding-min)}\n\nLayout CSS (from styles-reference-layout.632d2f3884f10ca3.css, verbatim):\n.reference-layout .reference-layout__toc{align-content:start;align-items:start;display:flex;flex-wrap:wrap;gap:.5rem;justify-content:space-between}\n@media (width < calc(1rem * 2 + 15rem + 2rem + 31rem)){.reference-layout .reference-layout__toc{--toc-header-font-size:var(--font-size-larger)}}\n\nTokens (styles-global.0babcbf439d7e39d.css, verbatim):\n:root{--sticky-header-height:calc(var(--navigation-height) + var(--breadcrumbs-bar-height));--z-index-sticky-header:100}\n:root{--color-area-background:var(--color-background-blue);--color-area-highlight-border:var(--color-blue-50);--color-area-link:var(--color-link-normal)}\n:root[data-current-area=learn]{--color-area-background:var(--color-background-orange);--color-area-highlight-border:var(--color-orange-50)}\n--color-blue-50:#0a5dbd; --color-blue-90:#ecf4fe; --color-blue-10:#072446;\n--color-orange-50:#e2782c; --color-orange-90:#fef3eb; --color-orange-10:#3f220d;\n#content{scroll-margin-block-start:var(--sticky-header-height)}\nhtml{scroll-behavior:smooth}\n@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n\nJS (from /static/client/index.b03d136913355719.js, module 70408, de-minified faithfully):\nfor (const toc of document.querySelectorAll('.generic-toc, .reference-toc, .document-toc, .blog-toc')) {\n  if (!(toc instanceof HTMLElement)) continue;\n  const links = [...toc.querySelectorAll('a')];\n  const sectionToLink = new Map();\n  for (const link of links.reverse()) {\n    const target = document.querySelector(`[id=\"${CSS.escape(decodeURIComponent(link.hash).slice(1))}\"]`);\n    if (!target) continue;\n    let section = target.closest('section');\n    while (section instanceof HTMLElement && !sectionToLink.has(section)) {\n      sectionToLink.set(section, link);\n      section = section.nextElementSibling;   // trailing siblings belong to the same link\n    }\n  }\n  const counts = new WeakMap();\n  const seen = new Set();\n  const options = { threshold: 0 };\n  const header = document.querySelector('header');\n  if (header instanceof HTMLElement) options.rootMargin = `-${header.clientHeight}px 0px 0px 0px`;\n  const observer = new IntersectionObserver((entries) => {\n    for (const { target, isIntersecting } of entries) {\n      if (!(target instanceof HTMLElement)) continue;\n      if (!seen.has(target)) { if (!isIntersecting) continue; seen.add(target); }\n      const link = sectionToLink.get(target);\n      if (!link) continue;\n      const n = (counts.get(link) ?? 0) + (isIntersecting ? 1 : -1);\n      link.ariaCurrent = n > 0 ? 'true' : null;\n      counts.set(link, n);\n    }\n  }, options);\n  for (const section of sectionToLink.keys()) observer.observe(section);\n}",
   "howItWorks": "The cheapest, most copyable version of the device. The rail line is not a pseudo-element or a container border - it is a box-shadow on every link: box-shadow:-2px 0 0 var(--color-border-primary) plus margin-left:2px. Stack the links in a grid and the individual 2px shadows abut into one continuous vertical line down the left edge. The active state then only has to recolour that same shadow to --color-area-highlight-border and add a faint tinted background - one property change, no extra DOM, no marker element, no layout shift. Nesting is one rule: :is(.reference-toc ul) ul{padding-left:1em}. The rail sticks via position:sticky;top:var(--sticky-header-height) with max-height:calc(100vh - var(--sticky-header-height)) and its own overflow-y:auto, and the same --sticky-header-height token is reused as #content{scroll-margin-block-start:...} so clicked anchors do not land under the header. The scroll-spy is IntersectionObserver over <section> elements (plus their trailing siblings, mapped back to the owning link), with rootMargin:-{headerHeight}px 0px 0px 0px and threshold 0. Its genuinely distinctive move is a REFERENCE COUNT per link (+1 on enter, -1 on exit, held in a WeakMap): MDN deliberately lights up EVERY section currently in the viewport, not just one, so a short section between two long ones is never skipped and there is no flicker while scrolling fast. Active state is written as element.ariaCurrent = 'true' | null.",
   "transfers": "Three things worth stealing outright. (1) box-shadow-as-rail: box-shadow:-2px 0 0 <colour> on each <a> with margin-left:2px and a grid list - a continuous rail with zero extra markup, and the active swap is a single colour change. (2) The multi-active reference-counting observer - no jitter, no \"which one wins\" arbitration, and it degrades gracefully on very long or very short sections. (3) The --sticky-header-height token reused for both top: on the sticky rail and scroll-margin-block-start on the content, which is the correct fix for anchor-under-header. Also: the --toc-header-font-size override at a narrow breakpoint, and the whole TOC stylesheet being 743 bytes of plain CSS with zero utility classes - it transcribes into SCSS verbatim.",
   "doesNotTransfer": "COLOUR: the default active accent is --color-area-highlight-border:var(--color-blue-50) = #0a5dbd with --color-area-background:var(--color-background-blue) = #ecf4fe/#072446 - a cool blue UI accent, banned. Note though that MDN already proves the recolour: :root[data-current-area=learn] swaps the same two tokens to --color-orange-50:#e2782c on --color-orange-90:#fef3eb, which is much closer to Rod's warm/lantern register - so the mechanism is explicitly built to be re-hued via two custom properties. Also does not transfer: MDN is server-rendered docs chrome and reads as \"default corporate\" visually (Inter + JetBrains Mono, flat grey), there is NO mobile popup form for this TOC (it is simply display:none below the two-sidebar breakpoint), and the data-glean-id attributes are Mozilla telemetry hooks.",
   "confidence": "read-from-source"
  },
  {
   "site": "brittanychiang.com - the ledger lead, sticky in-page jump-link rail",
   "url": "https://brittanychiang.com/",
   "confirmedHasComponent": true,
   "verbatimCss": "CAVEAT FIRST: this is a HOMEPAGE section nav, not an article table of contents. There is no /blog or article page with a TOC on the current site. The geometry and the spy logic are real and directly relevant; the \"table of contents\" framing is not.\n\nHTML (read from the served page):\n<div class=\"lg:sticky lg:top-0 lg:flex lg:max-h-screen lg:w-[48%] lg:flex-col lg:justify-between lg:py-24\">\n  ...\n  <nav class=\"nav hidden lg:block\" aria-label=\"In-page jump links\">\n    <ul class=\"mt-16 w-max\">\n      <li><a class=\"group flex items-center py-3 active\" href=\"#about\">\n        <span class=\"nav-indicator mr-4 h-px w-8 bg-slate-600 transition-all group-hover:w-16 group-hover:bg-slate-200 group-focus-visible:w-16 group-focus-visible:bg-slate-200 motion-reduce:transition-none\"></span>\n        <span class=\"nav-text text-xs font-bold uppercase tracking-widest text-slate-500 group-hover:text-slate-200 group-focus-visible:text-slate-200\">About</span>\n      </a></li>\n      ... #experience, #projects\n\nCSS - the ONLY two non-utility rules in the whole bundle (from /_next/static/css/1205f04d95fac248.css, verbatim):\n.active .nav-indicator{width:4rem;--tw-bg-opacity:1;background-color:rgb(226 232 240/var(--tw-bg-opacity))}\n.active .nav-text{--tw-text-opacity:1;color:rgb(226 232 240/var(--tw-text-opacity))}\n\nUtilities translated to plain CSS (from the class list above):\n/* rail container */  position:sticky; top:0; display:flex; max-height:100vh; width:48%; flex-direction:column; justify-content:space-between; padding-block:6rem;   /* all at >=1024px */\n/* link */            display:flex; align-items:center; padding-block:.75rem;\n/* indicator (rest) */ height:1px; width:2rem; margin-right:1rem; background:#475569 /* slate-600 */; transition:all 150ms;\n/* indicator (hover/focus-visible) */ width:4rem; background:#e2e8f0 /* slate-200 */;\n/* indicator (active) */ width:4rem; background:#e2e8f0;\n/* label */           font-size:.75rem; font-weight:700; text-transform:uppercase; letter-spacing:.1em; color:#64748b /* slate-500 */;\n/* label (active/hover) */ color:#e2e8f0;\n/* reduced motion */  motion-reduce:transition-none  ->  @media (prefers-reduced-motion:reduce){transition:none}\n\nJS (from /_next/static/chunks/pages/index-3745f4d077d34081.js, de-minified faithfully):\nuseEffect(() => {\n  const nav = document.querySelector('.nav');\n  if (!nav || window.innerWidth < 1024) return;\n  const ACTIVE = 'active';\n  const observer = new IntersectionObserver((entries) => {\n    entries.forEach((entry) => {\n      if (entry.isIntersecting) {\n        const prev = nav.querySelector(`a[href].${ACTIVE}`);\n        prev?.classList.remove(ACTIVE);\n        const next = nav.querySelector(`a[href=\"#${entry.target.id}\"]`);\n        next?.classList.add(ACTIVE);\n      }\n    });\n  }, { rootMargin: `0% 0% -${70}% 0%`, threshold: 0 });\n  sectionRefs.current.forEach(el => el && observer.observe(el));\n}, []);",
   "howItWorks": "A two-column split page: the left half is position:sticky;top:0 with max-height:100vh at >=1024px, and the jump-link rail sits inside it. The active device is a growing HAIRLINE, not a dot or a background: each link carries a 1px-tall, 2rem-wide span that transitions to 4rem wide and from slate-600 to slate-200 when the link is active OR hovered OR focus-visible. The same visual state serves all three, so hovering previews exactly what \"you are here\" looks like. The spy is a bare IntersectionObserver on the section elements with rootMargin:'0% 0% -70% 0%' and threshold 0 - that shrinks the detection viewport to its TOP 30%, so a section becomes active as its head enters the upper third rather than when it merely appears. Only one link is active at a time (explicit remove-then-add on a single .active class). The whole thing is hard-gated to desktop: it early-returns if window.innerWidth < 1024, measured once at mount and never re-checked on resize.",
   "transfers": "The growing-hairline indicator: h-px, 2rem -> 4rem width plus a brightness step, transitioned together, with hover/focus-visible sharing the active appearance. That is a warm, quiet, non-boxy device that suits an open-not-cluttered rail far better than a filled pill, and it is three CSS declarations. Also transferable: rootMargin:'0% 0% -70% 0%' as a dead-simple top-third detection band (much easier to reason about than Starlight's computed band); the uppercase/tracking-widest/xs label treatment for rail items; motion-reduce handling on the indicator transition; sticky top:0 with max-height:100vh on the whole rail column rather than on the nav itself.",
   "doesNotTransfer": "Tailwind utility soup - almost the entire component lives in class attributes, so it must be TRANSLATED rather than transcribed (I have done that translation above from the served class lists; the only authored CSS in her bundle is the two .active rules). COLOUR: the palette is slate-500/600 on slate-900 with a teal-ish cool cast, plus a teal accent elsewhere on the page - none of that transfers under the warm/blue-only-in-sky law; take the value RELATIONSHIP (dim rail line -> bright rail line, ~3 steps of lightness) not the hex. NO mobile popup form at all: below 1024px the nav is display:none and the spy never initialises, so this source contributes nothing to the mobile half of the brief. And, most importantly, it is NOT an article TOC - the ledger lead is a homepage section nav; if the ledger claims otherwise, correct it.",
   "confidence": "read-from-source"
  }
 ],
 "recommendation": "Adopt Starlight (docs.astro.build) as the structural base, MDN for the rail's visual trick, brittanychiang for the active-state feel, and VitePress only as a documented alternative. Reasoning:\n\n1. Starlight is the only one of the four that satisfies the ENTIRE brief in one place - sticky desktop rail AND a mobile popup, both driven by the same list and the same active state. Its two structural ideas are the ones worth taking wholesale: (a) active state written as aria-current=\"true\" on the <a>, so the styling hook and the accessibility hook are the same attribute and there is no class bookkeeping; (b) observing the content blocks BETWEEN headings, not just the headings, which is exactly the failure mode a shader post hits - a 200-line code block or a WebGL canvas sitting under an h2 would blank the active state in a naive heading-only observer. Its mobile popup is a native <details>/<summary> that shows the current heading in the closed summary - zero JS for the open/close, and the summary doubles as a \"you are here\" readout on small screens. Take the structure, re-hue the accent (#3d50f5 indigo is banned) and drop the base64 tick SVG.\n\n2. Take MDN's box-shadow-as-rail for the visuals: box-shadow:-2px 0 0 <colour> on each link, margin-left:2px, list as a grid. The abutting shadows form one continuous rail with no extra DOM and no pseudo-elements, and the active swap is literally one colour change on the same property. Their whole TOC stylesheet is 743 bytes of plain CSS - it transcribes into SCSS verbatim, no translation. MDN also already proves the warm recolour: their `learn` area swaps the same two tokens to #e2782c on #fef3eb, which is close to the lantern register. Also copy their --sticky-header-height token being reused for both the rail's `top:` and the content's `scroll-margin-block-start` - that is the correct fix for anchors landing under a sticky header, and it costs one variable.\n\n3. Take brittanychiang's growing-hairline for the active feel rather than a filled pill or a tinted background: a 1px line going 2rem -> 4rem plus a brightness step, with hover and focus-visible sharing the active appearance. It is quieter and more hand-drawn than a block highlight, it suits \"open not cluttered\", and it is three declarations. Her rootMargin:'0% 0% -70% 0%' (detect in the top 30% of the viewport) is also far easier to reason about and tune than Starlight's runtime-computed band - I would start there and only move to the computed band if the sticky top bar causes drift. Correct the ledger while you are in it: brittanychiang.com has NO article TOC. What it has is a homepage in-page jump-link rail. The device is real and relevant, the label in the ledger is wrong, and that is the same class of error that killed the stripe.dev lead.\n\n4. Do NOT adopt VitePress, but keep it on file as the documented alternative. Its sliding .outline-marker (4x20px rounded bar at left:-12px, transition:top .25s cubic-bezier(0,1,.5,1)) is a genuinely lovely device - the highlight travels between items instead of blinking - and if Rod wants the rail to feel \"alive\" that is the single best piece of geometry found in this pass. But it costs a scroll-math implementation full of magic numbers hard-coded to VitePress's own header (offsetTop + 33, PAGE_OFFSET 56, -15), and a throttled scroll listener on a page already running Three.js is the wrong trade. If the sliding marker is wanted, port the CSS and drive `top` from the IntersectionObserver's active link instead - that is a five-line hybrid and gets both.\n\nColour, across all four: every single source uses a cool accent - Astro indigo #3d50f5, Vue green #42b883, MDN blue #0a5dbd, Chiang slate/teal. The geometry transfers; not one of the hexes does. All four active states are pure CSS (colour, box-shadow, width, or an absolutely-positioned div) - there is NO artwork in any of them, with the single exception of Starlight's mobile checkmark, which is a base64 data-URI SVG used as a mask-image and should be dropped or replaced with Rod's own mark.\n\nSites checked and RULED OUT (do not re-spend time on these): joshwcomeau.com, ciechanow.ski, taniarascia.com (its TOC is inline in the article body, not a rail), ishadeed.com, redblobgames.com, lisyarus.github.io, catlikecoding.com - none carry a sticky TOC rail. webgpufundamentals.org has id=\"toc\" but it is a site-wide article index linking to other pages, not an in-page scroll-spy - it would have been a third false lead if cited from memory. blog.maximeheckel.com served no TOC markup on the shader post I fetched."
}