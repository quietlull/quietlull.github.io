{
 "component": "meta-chips (post meta / tag chips on article pages)",
 "candidates": [
  {
   "site": "ameye.dev — Alexander Ameye, Unity graphics/tech-art notes",
   "url": "https://ameye.dev/notes/edge-detection-outlines/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* read from the inlined <style> in the page head of ameye.dev/notes/edge-detection-outlines/ */\n.note-tags {\n  padding: 0;\n  display: flex;\n  justify-content: flex-start;\n  align-items: flex-start;\n  flex-wrap: wrap;\n}\n.note-tags li {\n  display: inline-block;\n  padding-right: 10px;\n  padding-bottom: 5px;\n  padding-top: 5px;\n}\n.note-tags a {\n  display: block;\n  padding-inline-start: 10px;\n  padding-inline-end: 10px;\n  font-size: 14px;\n  line-height: 30px;\n  text-decoration: none;\n  background-color: #fffefc80;\n  border: 2px solid var(--palette-offgray-600);\n  color: var(--palette-offgray-600);\n  border-radius: 10px;\n  transition: transform .1s;\n  font-family: arial-rounded-bold;\n  box-shadow: 1px 1px;\n}\n.note-tags a:before {\n  content: \"#\";\n  margin-right: .25rem;\n}\n.note-tags a:hover {\n  transform: scale(1.05);\n}\n.note-tags sup {\n  font-size: xx-small;\n  vertical-align: super;\n}\n.tags-header .note-tags li { padding-right: 0; }\n\n/* resolved tokens, read from the same inline style block */\n--palette-offgray-600: #1f293c;\n--palette-offwhite-100: #fcfcfa;  /* page background */\n\n/* markup, verbatim from the page */\n<footer class=\"note-footer\">\n  <ul class=\"note-tags\">\n    <li><a style=\"--tag-bg: #e1f5f6; --tag-text: #0885ff;\" href=\"/tags/graphics\">graphics</a></li>\n  </ul>\n</footer>",
   "howItWorks": "Chips are <a> inside <li> inside a flex-wrap <ul> in the post footer. Geometry is line-height-driven, not height-driven: line-height:30px sets the box height, padding-inline 10px sets the width, so the chip is exactly 30px tall with no vertical padding on the anchor (the <li> carries 5px top/bottom and 10px right for the gaps). A 2px solid border in the same colour as the text (#1f293c) rings a near-transparent warm fill (#fffefc at 50% alpha) that sits on a #fcfcfa page, so the chip reads as a slightly brighter patch, not a filled pill. Radius 10px on a 30px box is a soft rectangle, not a full pill. The '#' is a ::before pseudo, not content, so tag text stays clean. box-shadow:1px 1px with no blur and no colour (defaults to currentColor) is a hard 1px offset drop, which is what makes it read as a hand-cut sticker rather than a UI token. HOVER IS transform: scale(1.05) OVER .1s AND NOTHING ELSE — no fill, no colour change, no shadow change. The chip just pops toward you. Note a dead detail: each anchor carries inline --tag-bg / --tag-text custom properties that no shipped rule consumes; leftover from an abandoned per-tag colour scheme.",
   "transfers": "Everything structural: the line-height-as-height trick, 30px box, 10px inline padding, 2px border matched to text colour, 10px radius, the '#' ::before, the hard 1px 1px offset shadow, li-carried gaps, and above all the hover = scale(1.05) with a .1s transition. This is the exact non-fill hover Rod is looking for and it is one declaration. The translucent-fill-over-page-bg approach also transfers directly to a night ground: swap #fffefc80 for a warm ink at low alpha over the dark ground and the chip glows faintly instead of blocking out.",
   "doesNotTransfer": "Colour: --palette-offgray-600 #1f293c is a cool dark navy and the page is a light off-white — inverted from the night ground, and the navy is a cool neutral we would replace with a warm ink. font-family: arial-rounded-bold is a self-hosted rounded face we do not have and would not license; the ROUNDNESS is the intent, our Caveat/mono pairing carries that differently. The stray inline --tag-bg: #e1f5f6 / --tag-text: #0885ff are pale-cyan and blue and are dead code anyway — do not copy them. scale(1.05) needs a prefers-reduced-motion guard, which this site does not ship.",
   "confidence": "read-from-source"
  },
  {
   "site": "109ichiki.com — the ledger's own lead, verified",
   "url": "https://109ichiki.com/works/meidonohi/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* read from https://109ichiki.com/_astro/style.COBHKi4A.css */\n._tags_uowtk_75 {\n  display: flex;\n  gap: .5rem;\n}\n._tag_uowtk_75 {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  height: 1.3125rem;\n  padding: 0 .75rem;\n  margin-top: .46875rem;\n  border: solid 1px var(--color-border);\n  border-radius: 100vh;\n  font-size: var(--font-size-small);\n}\n\n/* resolved tokens, verbatim from :root in the same stylesheet */\n:root {\n  --font-ibm: \"IBM Plex Mono\";\n  --font-ja: \"Hiragino Kaku Gothic ProN\", \"Hiragino Sans\";\n  --font-size-small: .75rem;\n  --color-white-2: #f3f3ff;\n  --color-black-2: #101016;\n  --color-black-3: #565656;\n  --color-bg: var(--color-white-2);\n  --color-text: var(--color-black-2);\n  --color-border: var(--color-black-3);\n  font-family: var(--font-ibm), var(--font-ja), sans-serif;\n  font-size: clamp(14px, 1.1111111111vw, 16px);\n}\n/* mobile override, same stylesheet */\n@media (max-width: 768px) { :root { --font-size-small: .625rem; } }\n\n/* markup, verbatim from the work-detail page */\n<div class=\"_header_uowtk_67\">\n  <h1 class=\"_title_uowtk_71\">…</h1>\n  <div class=\"_tags_uowtk_75\">\n    <span class=\"_tag_uowtk_75\">…</span>\n    <span class=\"_tag_uowtk_75\">…</span>\n  </div>\n</div>",
   "howItWorks": "A true pill, and the most restrained of the four. Fixed height 1.3125rem (21px at the 16px root) with zero vertical padding — the flex centring does the vertical work, so the text can never push the box taller. Horizontal padding .75rem (12px) against a 21px height gives a wide, flat lozenge rather than a squat badge. border-radius:100vh is the always-full-pill idiom. The border is a single hairline in #565656, a mid grey against a #f3f3ff page and #101016 text, so the chip outline is deliberately QUIETER than the text it contains. Type is IBM Plex Mono at .75rem (12px desktop / 10px mobile) — that is the 'mono tags' the ledger recorded, and it is correct. Critically: these are <span>, not <a>. There is NO hover rule of any kind, because the chips are not interactive. The ledger's '109ichiki mono tags' lead is real for GEOMETRY only; it supplies no hover evidence whatsoever. Also worth correcting the ledger: these live on the /works/<slug>/ detail pages, not on the homepage — the homepage has no tags. Separately, the /works/ index has its own ._filterTypeItem_ / ._filterCategory_ category chips, which is a different component.",
   "transfers": "The whole geometry: fixed 21px height, zero block padding + flex centring so the box never grows, 12px inline padding, radius 100vh, hairline 1px border, .5rem flex gap, mono at 12px stepping down to 10px on mobile, and the border-quieter-than-text hierarchy. The 'metadata is not a button' posture is the real lesson — a static, non-interactive chip is one legitimate answer to the pill-fill-hover problem: if it does not hover, it cannot hover wrong.",
   "doesNotTransfer": "The palette is a light lavender-white page (#f3f3ff) with a neutral grey border — no warmth and no night ground, so every colour is replaced. It also gives us nothing for hover, which is half the brief; if we take this geometry we still need a hover treatment from elsewhere. IBM Plex Mono is Google-hosted and free, so that part is available, but the CSS-Modules hashed class names (_tag_uowtk_75) vary per build and must be renamed on adoption, same caveat already recorded for the 109ichiki caret source.",
   "confidence": "read-from-source"
  },
  {
   "site": "chriskirknielsen.com — personal dev site, heavily themed, all CSS inlined",
   "url": "https://chriskirknielsen.com/blog/going-full-circle-css-toggle-transitions/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* all read from the inlined <style> on the post page */\n.button {\n  --CTA-display: inline-flex;\n  --CTA-text-color: var(--_cta-text-color);\n  --CTA-bg-color: var(--_cta-bg-color);\n  --CTA-border-color: var(--_cta-border-color);\n  --CTA-border-radius: var(--_cta-border-radius);\n  --CTA-box-shadow: var(--_cta-box-shadow);\n  --CTA-padding: var(--_cta-padding-block) var(--_cta-padding-inline);\n  --CTA-transition-properties: color, text-decoration-color, text-underline-offset, text-decoration-thickness, background-color, border-color, box-shadow, opacity, filter, transform;\n  display: var(--CTA-display);\n  justify-content: center;\n  align-items: center;\n  gap: var(--_cta-padding-inline);\n  max-inline-size: 100%;\n  padding: var(--CTA-padding);\n  color: var(--CTA-text-color);\n  background: var(--CTA-bg-color);\n  border: var(--_cta-border-width) solid var(--CTA-border-color);\n  border-radius: var(--CTA-border-radius);\n  box-shadow: var(--CTA-box-shadow);\n  flex-shrink: 0;\n  line-height: 1;\n  position: relative;\n}\n@media (prefers-reduced-motion: no-preference) {\n  .button {\n    transition-property: var(--CTA-transition-properties);\n    transition-duration: var(--anim-short);\n    transition-timing-function: var(--easing-in-out);\n  }\n}\n.button.button--tag {\n  --CTA-padding: var(--_cta-padding-block) var(--size-xs);\n}\n.button.button--icon-only,\n.button.button--tag {\n  text-decoration: none;\n}\n.button:is(:hover, :focus-visible, :has(:focus-visible)) {\n  --CTA-text-color: var(--_cta-text-color-active);\n  --CTA-bg-color: var(--_cta-bg-color-active);\n  --CTA-border-color: var(--_cta-border-color-active);\n  --CTA-box-shadow: var(--_cta-box-shadow-active);\n}\n.flex-list {\n  gap: max(8px, var(--list-gap, var(--size-xs)));\n  flex-wrap: wrap;\n  list-style-type: \"\";\n  display: flex;\n}\n.fontSize-small { font-size: var(--typeScale-n1); }\n\n/* resolved tokens, verbatim */\n--_cta-border-width: var(--cta-border-width, 1px);\n--_cta-padding-block: var(--cta-padding-block, var(--size-2xs));\n--_cta-border-radius: var(--cta-border-radius, var(--_corner));\n--_corner: var(--corner, var(--size-xs));\n--_cta-bg-color: var(--cta-bg-color, var(--color-surface));\n--_cta-bg-color-active: var(--cta-bg-color-active, var(--color-surface));\n--_cta-border-color: var(--cta-border-color, var(--color-heading));\n--_cta-border-color-active: var(--cta-border-color-active, var(--color-accent));\n--_cta-text-color: var(--cta-text-color, var(--color-text));\n--_cta-text-color-active: var(--cta-text-color-active, var(--color-text));\n--_cta-box-shadow: var(--cta-box-shadow, none);\n--_cta-box-shadow-active: var(--cta-box-shadow-active, inset 0 0 0 1px var(--color-accent));\n--size-2xs: 4px;\n--size-xs: 8px;\n--corner: 0px;                     /* some themes */\n--corner: var(--size-2xs);          /* other themes */\n--anim-short: calc(.175s * var(--anim-f));\n--typeScale-n1: clamp(.8511rem, .7471rem + .5043vw, 1.1253rem);\n\n/* the amber-on-night theme variant, verbatim */\nhtml[data-theme=\"cyberpunk\"] .button--tag {\n  --CTA-border-radius: 4px;\n  --CTA-text-color: #edb238;\n  --CTA-bg-color: #0f0d17;\n  --CTA-border-color: #edb238;\n  --CTA-box-shadow: none;\n}\nhtml[data-theme=\"cyberpunk\"] .button--tag:is(:hover, :focus-visible) {\n  --CTA-text-color: var(--color-accent);\n  --CTA-bg-color: var(--color-surface);\n  --CTA-border-color: var(--color-accent);\n  --CTA-box-shadow: 0 0 1em color-mix(in hsl, var(--color-accent), transparent);\n}\nhtml[data-theme=\"cyberpunk\"] .button--tag { font-family: var(--_font-heading-family); }\n\n/* markup, verbatim from the post header */\n<div class=\"hero-tags\">\n  <p id=\"tags-description\" class=\"visually-hidden\">Tags:</p>\n  <ul class=\"flex-list | fontSize-small\" aria-describedby=\"tags-description\">\n    <li><a href=\"/tags/css\" class=\"button button--tag | p-category\">css</a></li>\n  </ul>\n</div>",
   "howItWorks": "The tag chip is a variant of the site's one button primitive: .button gives inline-flex + centring + line-height:1 + 1px border + token-driven radius, and .button--tag only overrides the padding to 4px 8px (tighter inline than a real button). Height is therefore purely content-derived — line-height:1 on ~14-18px fluid type plus 4px block padding plus 2x1px border, so roughly 22-26px. Radius comes from the theme's --corner, either 0px (square) or 4px. Every paintable property is routed through a --CTA-* indirection so a single :hover rule flips all four at once. THE HOVER IS NOT A FILL, AND THIS IS THE INTERESTING PART: --_cta-bg-color-active resolves to var(--color-surface), which is the SAME value as --_cta-bg-color. The background deliberately does not move. What changes is border-color to --color-accent plus box-shadow: inset 0 0 0 1px var(--color-accent) — an inner 1px ring in the accent, which visually thickens the outline from 1px to 2px in accent colour without shifting layout. Text colour also holds (--_cta-text-color-active is the same --color-text). So the chip 'ignites at the edge' and stays hollow. The cyberpunk theme then swaps that inner ring for an OUTER glow: box-shadow: 0 0 1em color-mix(in hsl, var(--color-accent), transparent) on an amber #edb238 outline over a #0f0d17 near-black — that is a literal glow-ignite-on-hover on a night ground, which is exactly what the ledger's HANA lead was reaching for. All transitions are wrapped in @media (prefers-reduced-motion: no-preference).",
   "transfers": "The hover model, which is the best answer in this batch: hold the background, move only border-color, and add box-shadow: inset 0 0 0 1px <accent> so the outline reads as doubling. Zero layout shift, no fill, one rule. The cyberpunk variant's outer-glow form (0 0 1em color-mix(in hsl, accent, transparent)) is the glow-ignite alternative and is already amber-on-night. Also transferable: padding 4px 8px with line-height:1 so the chip auto-sizes to type; the --CTA-* indirection pattern so one hover rule drives every variant; the flex-list gap: max(8px, …) floor; and the a11y furniture — visually-hidden 'Tags:' paragraph wired via aria-describedby, and the reduced-motion guard on the transition. Both are free and both are gates we already care about.",
   "doesNotTransfer": "His --color-accent values across themes are #18ffe3 cyan, #ac3d7e magenta, #f55a00 orange, #99c8c0 pale teal and #6d3c3c mauve. The cyan and magenta are exactly the cool UI accents the palette law forbids — the GEOMETRY and the glow MECHANIC transfer, the accent hue does not. The amber #edb238 on #0f0d17 in the cyberpunk theme is close to our twilight-festival ground and is the one pair worth keeping, but note its hover then jumps to that cyan accent, so the hover colour still has to be re-picked. --_font-heading-family and the corner-shape: bevel work are theme-specific and not part of the chip. The whole thing is coupled to his --CTA-* token system, so adopting it wholesale means importing that layer or flattening it.",
   "confidence": "read-from-source"
  },
  {
   "site": "kizu.dev — Roman Komarov, CSS-craft blog (the no-box answer)",
   "url": "https://kizu.dev/nested-links/",
   "confirmedHasComponent": true,
   "verbatimCss": "/* read from https://kizu.dev/s/style.1be369fd326ac4132f7deba8714b9fed.css */\n.TagList {\n  display: inline-block;\n  --link-position: relative;\n  --link-z-index: 9;\n  --link-hover-z-index: 10;\n  --link-color: var(--color);\n  --link-color--visited: var(--color);\n  --link-padding: 0.3em 0;\n  --link-underline-position: bottom 5%;\n  color: var(--color);\n  --color-a: 0.725;\n}\n.TagList > a::before {\n  position: absolute;\n  z-index: -1;\n  inset: 0 -.15em;\n}\n.ArticleList-Item > * > .TagList {\n  min-width: calc(var(--priority) * 100%);\n}\n.Link:focus-visible,\n.Link:hover,\na:focus-visible,\na:hover {\n  --underline-width: calc(2 * var(--link-underline-width));\n  --link-underline-color: var(--link-underline-color--hover);\n  color: var(--link-color--hover);\n  --code-color: var(--link-color--hover);\n  z-index: var(--link-hover-z-index, var(--link-z-index));\n}\n\n/* markup, verbatim from the post footer */\n<p class=\"Metadata Published\">\n  <a href=\"./\" class=\"u-url u-uid is-inherited-link\">Published on</a>\n  <time class=\"dt-published\" datetime=\"2015-02-10\">February 10, 2015</time>\n  with tags:\n  <span class=\"TagList\">\n    <a rel=\"category tag\" href=\"https://kizu.dev/tags/experiment/\" style=\"--underline: 0;\"><span aria-label=\"Tag\">#</span><span class=\"p-category\" style=\"--underline: 1;\">Experiment</span></a>\n    <a rel=\"category tag\" href=\"https://kizu.dev/tags/html/\" style=\"--underline: 0;\"><span aria-label=\"Tag\">#</span><span class=\"p-category\" style=\"--underline: 1;\">HTML</span></a>\n  </span>\n</p>",
   "howItWorks": "Deliberately NOT a chip — no border, no background, no radius. Tags are inline links inside a prose sentence ('Published on <date> with tags: #Experiment #HTML'), given only --link-padding: 0.3em 0 for vertical hit area and an absolutely positioned ::before at inset: 0 -.15em that widens the clickable/hover region .15em past each side without drawing anything. The '#' and the label are separate spans carrying inline --underline: 0 and --underline: 1, so the hash glyph is excluded from the underline and only the word gets decorated — a per-part underline mask driven by a custom property rather than by extra selectors. Hover is the site-wide link rule: --underline-width doubles (calc(2 * var(--link-underline-width))), the underline colour swaps to its hover token, the text colour swaps, and z-index lifts. No fill, no box, no motion. The <span aria-label=\"Tag\">#</span> also means the hash announces as 'Tag' to screen readers instead of being read as punctuation.",
   "transfers": "The whole device if we decide chips are too much furniture for a post header: hash-prefixed inline tags, 0.3em block padding for a comfortable target, a transparent ::before at inset 0 -.15em to extend the hit area past the glyphs, and hover = underline thickness DOUBLES rather than anything filling. The --underline: 0/1 custom-property masking so the '#' stays undecorated is a clean trick worth stealing on its own. The aria-label=\"Tag\" on the hash is free a11y. This is the cheapest possible answer to 'not a pill fill' and it is the most readable of the four.",
   "doesNotTransfer": "It gives us no chip geometry at all — no height, padding, radius, border or tracking, because there is no box. If the design calls for a visible chip this source answers a different question. Colours resolve through --color / --color-a: 0.725 alpha layers in his own theme system and are not read here as concrete hex. The hover depends on his site-wide --link-underline-width token, so the 'doubles' effect has to be re-anchored to whatever underline weight we set.",
   "confidence": "read-from-source"
  }
 ],
 "recommendation": "Adopt ameye.dev for GEOMETRY and chriskirknielsen.com for HOVER — they compose cleanly and between them cover the brief with nothing free-handed. ameye gives the box: line-height:30px as the height (no vertical padding, so the chip can never grow), 10px inline padding, 10px radius, a 2px border matched to the text colour, a '#' ::before, and box-shadow: 1px 1px — a hard, unblurred, currentColor offset that is the single most hand-made detail found in this batch and reads as a cut-and-stuck sticker rather than a UI token. He is also the closest match to Rod's own situation: a technical artist publishing Unity shader and render-graph tutorials, on a hand-rolled site, with giscus comments like ours. chriskirknielsen gives the hover Rod actually asked for: hold the background, move border-color to the accent, and add box-shadow: inset 0 0 0 1px <accent> so the 1px outline visually doubles without a single pixel of layout shift. His cyberpunk theme ships the glow-ignite variant of the same rule — box-shadow: 0 0 1em color-mix(in hsl, accent, transparent) on #edb238 amber over #0f0d17 near-black — which is the HANA lead the ledger wanted, on a night ground, in something very close to our palette. Take the mechanic, re-pick the accent hue (his default accents are cyan and magenta). Keep 109ichiki as the restraint reference and correct the ledger while you are there: those chips are real but they live on /works/<slug>/, not the homepage, and they are <span> with NO hover rule at all — 'mono tags' is sound evidence for geometry (21px fixed height, zero block padding, 12px inline, radius 100vh, IBM Plex Mono at 12px/10px, hairline border quieter than the text) and zero evidence for hover. kizu.dev is the fallback if a chip turns out to be too much furniture in the post header: hash-prefixed inline text, hover doubles the underline, no box to get wrong. Two ledger corrections owed. First, hana.b-rave.tokyo was fetched and contains NO tag or chip markup of any kind — the 'HANA glow-ignite' note is a nav-link hover recipe (already captured in sources/hana-nav.md) and is not a meta-chip source; chriskirknielsen's cyberpunk rule is the real, readable glow-ignite-on-a-chip. Second, three sites checked out as the rejected pattern and should be recorded as anti-evidence so nobody re-proposes them: piccalil.li .badge does hover { background-color: var(--color-dark) } with inverted text (and is not even used for post tags — only 'Advert' / 'Author' / 'Newsletter' labels); blog.ojisan.io ships a filled chip already and swaps the fill on hover; cyanilux.com .tag (background-color:#00aabb, border-radius:10px, padding:5px) is a permanently filled teal chip with no hover at all, and halisavakis.com .entry-tags a is a permanently filled black rectangle whose rule explicitly groups a and a:hover so nothing changes — both are tech-art blogs but neither has any craft to take, and halisavakis is stock WordPress 'writee' theme output. Also checked and empty: ronja-tutorials.com posts carry no tags, mxb.dev renders tags as plain comma-separated text with no chip."
}