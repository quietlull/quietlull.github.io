{
 "sites": [
  {
   "name": "dimden",
   "url": "https://dimden.dev/blog/13-optimizing-rendering-of-100k-html-nodes",
   "isLive": true,
   "layoutFamily": "Single full-bleed column, no shell or card. Body padding only; content capped, chrome uncapped.",
   "distinctiveMove": "A full-viewport animated CRT static field sits ON TOP of the whole page. Two stacked layers: body::before (opacity .2, 2s) and body::after (opacity .5, 0.8s), both position:fixed, inset 0, width/height 100%, pointer-events:none, z-index:1000, each animating background-position 0px 0px -> 0px 4px with animation-timing-function: steps(4) so it judders rather than slides. The first layer also carries box-shadow: inset 0 0 10em rgba(0,0,0,.4) as a screen vignette. Everything below is plain black-on-white prose; the entire personality of the page is those two pseudo-elements.",
   "measurements": "#blog{padding:35px} and NO max-width on the shell. Prose measure set in a page-level <style>: #content{max-width:1200px;display:block}. body{background:#000;color:#fff;font-family:\"Pixel NES\"} (self-hosted eot/woff/ttf @font-face) but #content is overridden to \"MS UI Gothic\",Tahoma,sans-serif at font-size:105% !important. #date{color:#2e8579;font-family:\"MS Gothic\",Tahoma,sans-serif}. hr{height:4px;background:#161719;border:none}. #logo{width:380px;font-size:60px;color:rgba(2,132,91,1);text-shadow:0 0 6px;image-rendering:pixelated;margin-left:auto;margin-right:auto} with #logo-n{transform:rotate(4deg);top:3px}. Links: rgba(15,150,102,1), hover rgba(15,150,102,.5); h2 rgb(2 132 124); #content a{color:#2f8f8a}. Footer: #footer{display:table} -> @media (min-width:600px){display:flex;justify-content:center}; #footer>div{padding:40px;display:inline-table} (3 columns: Find me there / About me / Public projects). Code: highlight.js 11.9.0 + atom-one-dark, overridden by code.hljs{background-color:#000!important} and pre{tab-size:4}. Scrollbar: ::-webkit-scrollbar{width:1em}, thumb border 1px solid #1b4834 on black. Only breakpoints in the whole sheet: 600px (footer) and 400px (#logo{width:100%}). Total blog.css = 3,404 bytes.",
   "howToCopy": "Greybox: one column, page padding 35px, inner text block max-width 1200px, left-aligned (NOT centered auto-margin - the column just starts at the padding edge). Title h1 at browser default size, one small colored date line under it, then prose. Bottom: a 3-cell flex row centered, each cell padding 40px, each a list of asterisk-prefixed links. Then bolt on the signature: two fixed full-screen overlay divs with pointer-events:none and z-index:1000, a repeating-linear-gradient standing in for the noise PNG, animated background-position 0 0 -> 0 4px with steps(4), durations 2s and 0.8s, opacities .2 and .5, and inset 0 0 10em rgba(0,0,0,.4) on the lower one. That alone reproduces the read.",
   "artworkOrCss": "HYBRID, and the split matters. The animation, stacking, opacity, judder (steps(4)) and vignette are 100% CSS - free to copy. The two textures (/overlay.png, /overlay2.png) are raster artwork. A greybox can substitute a repeating-linear-gradient and lose almost nothing. The 380px logo is also artwork (a pixel-font image).",
   "confidence": "read-from-source"
  },
  {
   "name": "Cyanilux",
   "url": "https://www.cyanilux.com/tutorials/urp-shader-code/",
   "isLive": true,
   "layoutFamily": "Single centered rounded card on a flat dark-teal ground. Percentage width, no max-width, width transitions between breakpoints.",
   "distinctiveMove": "Every subheading is a filled bar with a 10px accent stub on its left edge, not typography: h2:not(.title-no-margin):not(.title-undecorated), h3, h4 { margin-top:30px; padding:7px 20px; background-color:#282828; border-left:10px solid #00aabb; border-radius:7px }. Section rhythm comes from those bars, so a 6000-word tutorial stays scannable without a sticky TOC. Second device: the callout, a flex row whose left gutter is a pure-CSS barber stripe, with a circular exclamation chip.",
   "measurements": ".content{margin:auto;width:70%;border:3px solid #303030;padding:30px;background-color:#202020;border-radius:20px;box-shadow:5px 5px 10px 0px #000;transition:width .5s}. Breakpoints: @media(min-width:1200px) -> width 60%; @media(max-width:1000px) -> 85%; @media(max-width:600px) -> 90% + padding:10px. .header and .footer track the same widths; .header padding:20px. body{font-family:'Lato',sans-serif;background-color:#05323a;color:#dddddd;line-height:170%;word-break:break-word;background-attachment:fixed}. h1:not(.title-no-margin){text-align:center;padding:0 0 20px 0}. .post-metadata{margin:10px 0;display:flex;align-items:center;justify-content:center;flex-wrap:wrap}. .tag{display:inline-block;line-height:100%;background-color:#00aabb;border-radius:10px;padding:5px;margin-left:2px} with per-tag color set inline (#505050 category, #108010 HLSL, #00aabb URP); date/updated as two <time> elements separated by 10px and 5px spacer divs. Inline code .node{display:inline-block;color:#00aabb;background-color:#303030;padding:3px 7px;border:2px solid #161616;border-radius:10px}. Code blocks: .highlight>pre{overflow:auto;padding:20px}, .highlight pre{background-color:#181818!important}, .highlight div{overflow:auto;padding-right:20px;background-color:#181818!important}. Callout: .notice{margin:5px 0;display:flex;background-color:#282828;border-radius:10px;border:3px solid #303030}; .notice-left{margin-right:10px;border-radius:6px 0 0 6px;background:repeating-linear-gradient(45deg,#262626,#262626 12px,#202020 12px,#202020 24px)}; .notice-exclamation{margin:15px 10px;background-color:#00aabb;color:#202020;min-width:28px;height:28px;border-radius:100%;font-weight:700;font-size:20px;text-align:center}. Nav pill: .nav ul{background-color:#202020;border-radius:15px;border:3px solid #303030;box-shadow:5px 5px 10px 0 #000}, .nav li a{padding:5px 20px}, hover bg #008899. Index cards (for the sibling projects page): .card{margin-top:20px;border-radius:10px;background-color:#282828;line-height:120%}, .card-text{padding:12px}, hover -> #303030 and link -> #00ddff. Links #00aabb / hover #00ddff. Tables .tbl table{border:2px solid #151515;background-color:#202020}, th and even rows #191919, cell padding-left/right 5px. Total style.css = 13,301 bytes.",
   "howToCopy": "Greybox: one rounded card, width:70% margin auto (60% above 1200px, 85% below 1000px, 90% below 600px), border 3px, radius 20px, padding 30px, box-shadow 5px 5px 10px 0 black. Inside: centered h1 with 20px bottom padding, then a centered flex-wrap row of radius-10px 5px-padded tag pills followed by a date, then the body. Every h2/h3/h4 becomes a 7px-20px-padded filled bar, radius 7px, with border-left:10px accent, margin-top 30px. Code blocks get padding 20px and a darker fill. One callout: a flex row, radius 10px, 3px border, with a left gutter drawn as repeating-linear-gradient(45deg, dark, dark 12px, darker 12px, darker 24px) and a 28x28 circular accent chip at margin 15px 10px. TOC is just an inline nested <ul> under an h3, NOT sticky - copy that if the frozen spec's TOC should stay cheap.",
   "artworkOrCss": "100% CSS. There is not a single decorative image in the layout - the barber-stripe gutter, the tag pills, the heading bars, the code panels and the nav pill are all gradients, borders and radii. This is the cheapest-to-steal site in the set and the nearest structural peer to Rod's own tech-art posts. It is also the only shortlisted site that delivers the MinionsArt praise (rounded card + tags) as an actual on-site POST template rather than an index.",
   "confidence": "read-from-source"
  },
  {
   "name": "Mike Klubnika",
   "url": "https://mikeklubnika.com/logs/log_1",
   "isLive": true,
   "layoutFamily": "Single narrow document column presented as a physical sheet, flanked by fixed decorative banner strips. No cards, no grid.",
   "distinctiveMove": "The article column IS a sheet of paper lit in the middle. .docholder has hairline rules on the LEFT AND RIGHT ONLY (border:1px solid rgb(92,92,92) with border-top-width:0 and border-bottom-width:0) plus background:linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(26,26,26,1) 91%, rgba(0,0,0,1) 100%), so the sheet fades to pure black at top and bottom and the side rules appear to run off into the dark. Sections are divided by 1px DOTTED white rules, never solid. Second device: the post header is a single flex row, title left and date right (.lr{display:flex;flex-direction:row;justify-content:space-between}) - one line, no meta block.",
   "measurements": ".contents{display:flex;justify-content:center;min-height:100vh}. .docholder{max-width:58em;width:100%;border:1px solid rgb(92,92,92);border-top-width:0;border-bottom-width:0;background:linear-gradient(0deg,rgba(0,0,0,1) 0%,rgba(26,26,26,1) 91%,rgba(0,0,0,1) 100%);display:flex;flex-direction:column;justify-content:space-between;position:relative}. .doc{margin:15px;margin-top:10px;margin-bottom:10px;background-position:right bottom;background-repeat:no-repeat;background-image:url(../resources/page_bg_2.png)}. body{background-color:#000;color:#fff;font-family:monospace;overflow-x:hidden;margin:0;padding:0}. p{color:gainsboro;margin-top:2px;margin-bottom:2px}. h1{font-size:27px;font-family:bank;color:gainsboro;margin-top:.25em;margin-bottom:.75em}; h2{font-size:20px;font-family:bank;margin-top:0;margin-bottom:.75em}; @font-face bank = ../resources/fonts/F25_Bank_Printer.ttf. Separators: .pagespace{border-top:1px dotted #fff;margin-top:1.25em;margin-bottom:1em} and .footer{border-top:1px dotted #fff;margin-top:2.25em;margin-bottom:1em;flex:1;align-self:flex-end;text-align:center}. Header band: .header{font-size:25px;font-weight:bold;max-height:100px;font-family:bank;background:linear-gradient(0deg,#000 0%,#1a1a1a 75%,#000 100%)}; .headercontents{display:flex;flex-direction:row;justify-content:space-between;margin-left:5px;margin-right:5px;min-height:35px}; .headerlogo{display:flex;width:35px}. Footer link row: .footertext{margin-top:.75em;padding-bottom:.25em;display:flex;justify-content:space-between;background:linear-gradient(0deg,#000 0%,#1a1a1a 75%,#000 100%)}. Side strips: .banner{position:absolute;pointer-events:none;z-index:-1;width:100px;height:100%;overflow:hidden}; .bannerleft{left:0;transform:translateX(-100px);mask-image:linear-gradient(90deg,rgba(0,0,0,0) 25%,rgba(1,1,1,.3) 85%,rgba(255,255,255,.2) 98%)}; .bannerright is the 270deg mirror at right:0 translateX(100px). .wriksseal{position:absolute;bottom:50px;max-width:100px}. Sole layout breakpoint: @media(max-width:756px) -> banners display:none, .docholder{border:none;width:100%;min-width:100%}. Total main.css = 8,021 bytes.",
   "howToCopy": "Greybox: center a 58em-max column with display:flex;justify-content:center on the parent. Give the column left+right 1px hairlines only (set border-top-width and border-bottom-width to 0) and a vertical gradient that goes black -> #1a1a1a at 91% -> black, so the sheet dissolves at both ends. Inner padding via margin:15px 15px 10px. Header: a flex row, wordmark left and 35px logo right, on the same gradient, min-height 35px. Post header: one flex row, h2 title left, date paragraph right. Divide sections with border-top:1px dotted white at margin-top 1.25em / margin-bottom 1em. Footer: same dotted rule at margin-top 2.25em, then a flex space-between row of ellipsis-separated links with the copyright on the right. If you want the flanking strips, they are 100px absolute divs at z-index -1 with a mask-image gradient fade, and they must die below 756px.",
   "artworkOrCss": "BOTH, and cleanly separable - this is the useful lesson. The GEOMETRY is all CSS: the 58em column, the two hairlines, the top/bottom gradient wash, every dotted rule, the flex header and footer rows. The CHARACTER is raster artwork: banner.png in the two side strips, page_bg_2.png as a bottom-right page stain on .doc, and .wriksseal as a 100px absolutely positioned seal at bottom:50px. Strip the four images and the layout still reads as a document; that is the greybox.",
   "confidence": "read-from-source"
  },
  {
   "name": "Inigo Quilez",
   "url": "https://iquilezles.org/articles/raymarchingdf/",
   "isLive": true,
   "layoutFamily": "Very wide single column (measure set in ch, not px), with a wrapping percentage-width media wall below the prose. Flex column body, no grid anywhere.",
   "distinctiveMove": "The container measure is expressed in CHARACTERS: max-width:120ch. The column tracks the font rather than the viewport, so it never needs a px breakpoint for measure. Second device: syntax highlighting is HAND-AUTHORED spans (.codeK, .codeL, .codeS, .codeComment) with no highlighter library, and formula PNGs are dropped in with mix-blend-mode:screen so black-background images composite onto the page as if they had alpha. The whole site is 4.4KB of CSS.",
   "measurements": "Total style.css = 4,487 bytes. div#container{width:100%;max-width:120ch;min-height:100vh;padding-top:32px;padding-left:24px;padding-right:24px;margin-left:auto;margin-right:auto}. body{background-color:#282828;color:#c0c0c0;font-size:1.125rem;line-height:1.8;font-family:sans-serif;display:flex;flex-grow:1;flex-direction:column;min-height:100vh;padding:0;margin:0}. div#header{width:100%;display:flex;flex-direction:column;align-items:center;padding-bottom:3rem}. Media wall: .partsC{display:flex;justify-content:space-around;flex-wrap:wrap;padding:0;margin:0} with .part{padding-bottom:32px;padding-right:32px}, .part:last-child{padding-right:0}, and page-level override .part{width:45%;padding-bottom:64px}. Utility widths are literal: .part25{25%} .part30{30%} .part40{40%} .part50{50%} .part60{60%} .part70{70%} .part75{75%}. Code: .code{overflow:auto;font-family:Courier,sans-serif,monospace;font-size:1em;font-weight:bold;color:#dcdcdc;white-space:pre;line-height:1.35;text-align:left;vertical-align:text-top}; token colors .codeK #88a8c8, .codeL #c088a8, .codeS #da9f89, .codeComment #87b054, .codigoInText #40c040. Media: .myPicture{max-width:100%;height:auto}, .formula{max-width:100%;height:auto;mix-blend-mode:screen}, .formulaInline{vertical-align:middle;mix-blend-mode:screen}, .formulaText{font-size:1.25em;font-family:\"Times New Roman\",serif;padding:0 4px;line-height:1.5em}. figure{padding:0 32px 32px 0;display:inline-block;margin:auto}. li{padding-bottom:12px}. .tableCell{display:table-cell;padding:12px;padding-right:24px}. Links a{color:#ffa0a0;transition:.2s} hover #fff; a.menu{color:#c0c0c0} hover #fff. .socialMediaIcon{width:1.5rem;height:1.5rem;margin-left:.15rem;margin-right:.15rem}. Scrollbar ::-webkit-scrollbar{height:8px;width:8px;background:#282828}, thumb #505050 radius 4px. ONE breakpoint in the entire site: @media(max-width:1280px) - every .part* to width:100%, all .parts* to flex-direction:column, figure padding-right:0, and .code{font-size:.8em;line-height:120%}. Blog tokens also live in the same sheet: .blog_posts{padding-top:6rem}, .blog_post{padding-bottom:6rem}, .blog_post>.title{font-size:2rem;padding-bottom:1rem;color:#fff}, .blog_post>.date{padding-bottom:1.5rem}, .blog_post>.content{font-size:1.25rem}, .blog_page_title{font-size:2rem;display:flex;align-items:center;justify-content:space-between;padding-top:1rem}, .blog_nav{padding-top:2rem;padding-bottom:2rem}, .blog_calendar{display:grid;grid-template-columns:repeat(13,1fr);margin-left:auto;margin-right:auto}.",
   "howToCopy": "Greybox: body as a flex column, min-height 100vh, bg #282828 / text #c0c0c0, font-size 1.125rem, line-height 1.8. One centered container at max-width:120ch with padding-top 32px and 24px sides. Header is a centered flex column with 3rem bottom padding. Below the prose, a flex-wrap row with justify-content:space-around whose children are 45% wide with padding-bottom 64px and padding-right 32px - that is the whole media wall, no grid, no cards, no borders. Collapse everything to 100% at 1280px and shrink code to .8em/120%. If you want the prose rhythm too: article body is bare text with <br><br> between paragraphs (no <p> elements at all). Use 120ch ONLY if the column is hosting a media wall - for pure prose it is far too wide, and this is the site's honest weakness.",
   "artworkOrCss": "Pure CSS and hand-written markup. The one thing that LOOKS like artwork is the formula imagery, and it is actually a CSS trick: black-background PNGs composited with mix-blend-mode:screen instead of shipping transparent assets. Syntax colors are hand-typed span classes, not a highlighter. Nothing here needs an illustrator.",
   "confidence": "read-from-source"
  },
  {
   "name": "whey-isolate",
   "url": "https://whey-isolate.neocities.org/posts/2024-04-20-The-Pride-Morse-Manifesto",
   "isLive": true,
   "layoutFamily": "Three sticky rails over a fixed photographic ground: site nav left (160px), article center, in-page TOC right (160px). The shell is byte-identical between index and article; only the middle rail changes.",
   "distinctiveMove": "All three rails wear THE SAME chrome - a 10px solid accent border plus a hard, zero-blur offset shadow box-shadow:-10px 10px 0px var(--shadow-color) - so the page reads as three stickers laid on a starfield. The zero blur is the whole trick: it is a poster/screenprint shadow, not a material-design elevation. Both side rails are position:sticky;top:0 with different z-indexes (nav 3, main 2, #navbar 1) so the article panel tucks under the left rail and over the right. The article panel is deliberately TRANSLUCENT (--lighter-color:#464e66C4, note the C4 alpha) so the fixed starfield reads through the prose.",
   "measurements": "--sidebar-width:160px. body:not(footer){margin-left:10vw;margin-right:10vw;font-family:'Courier New';color:#d8dee9;text-align:left;margin-bottom:24px;padding-bottom:10px} -> @media(max-width:1100px){margin-left:0;margin-right:0}. main{margin-left:calc(var(--sidebar-width) + 30px);margin-right:calc(var(--sidebar-width) + 30px);padding:25px;margin-top:-10px;border-width:10px;border-style:solid;border-color:var(--accent-color);box-shadow:-10px 10px 0px var(--shadow-color);background-color:var(--lighter-color);position:relative;z-index:2} - i.e. 190px of clearance each side. nav{width:160px;padding:10px;position:sticky;top:0;float:left;margin-top:-10px;z-index:3;border:10px solid var(--accent-color);box-shadow:-10px 10px 0px var(--shadow-color);background-color:var(--lighter-color)}. #navbar (the TOC){width:160px;padding:10px;position:sticky;top:0;float:right;margin-top:-10px;z-index:1; same 10px border + -10px 10px 0 shadow}. .divider{height:10px;width:100%;background-color:var(--accent-color);box-shadow:0px 10px 0px var(--shadow-color)}. header{width:100%;background-color:var(--shadow-color);margin-top:-10px}; header img{width:80%;padding:20px;max-height:150px;object-fit:scale-down;display:block;margin:auto}. Figures: .captioned{margin:5px;padding:5px;border:5px solid var(--link-color);max-width:500px} with img{max-width:480px;max-height:480px} and p{max-width:480px}; .jCaptioned{max-width:480px} with p{max-width:460px}; .container-inline{display:inline-flex}. p{padding:0px 6px 0px}. Palette: --bg-color #2e3343, --starfield-color #222531, --darker-color #20242f, --lighter-color #464e66C4, --accent-color #d84f51, --link-color #d8d880, --shadow-color rgba(32,36,47,0.8). Ground: html{background-color:var(--starfield-color);background-image:url(images/sanctumbg.jpeg);background-blend-mode:screen;background-position:center;background-size:100% auto;background-repeat:no-repeat;background-attachment:fixed} and posts/style.css re-points it to /images/pleiades.jpg with background-position:60% 50%;background-size:200% auto. Breakpoints: 1100px (kill the 10vw gutters) and 1600px (.container -> flex-direction:column, .picture{order:-1}). footer{font-family:'Courier New';font-size:12px;height:24px;text-align:center;margin:auto}. Unused-but-present post slots in the sheet: #nextprev{text-align:center;margin-top:1.4em} and #disqus_thread{margin-top:1.6em}.",
   "howToCopy": "Greybox: body gutters at 10vw (0 below 1100px). A 10px-tall full-width accent .divider bar directly under the header, with box-shadow:0 10px 0 shadow. Then two 160px sticky rails, one float:left one float:right, both top:0, both padding 10px; and a center main with margin-left/right calc(160px + 30px) and padding 25px. Give ALL THREE the identical border:10px solid accent + box-shadow:-10px 10px 0px shadow (zero blur - this is the load-bearing detail) and pull each up with margin-top:-10px so the borders kiss the divider. z-index nav 3 / main 2 / TOC 1. Make the main panel background semi-transparent so the ground shows through. The TOC rail is just a hand-written anchor list starting with 'Return to top'. Figures get a 5px solid link-colored frame at max-width 500px with 480px images.",
   "artworkOrCss": "Mostly CSS. The 10px borders, hard zero-blur shadows, sticky rails, divider bars, translucent panel and the 5px image frames are all CSS. The two backgrounds ARE photographic artwork (images/sanctumbg.jpeg site-wide, /images/pleiades.jpg on post pages) - but the LOOK is CSS doing the work on them: background-blend-mode:screen over a flat #222531, background-attachment:fixed, background-size:200% auto. Any dark image would work. WARNING for anyone re-reading this site: the page links three stylesheets and the one that looks canonical (/style/style.css) is roughly 90% commented out - the live rules are in /style.css, and posts/style.css only swaps the background image.",
   "confidence": "read-from-source"
  },
  {
   "name": "Eve (eveofficial.com)",
   "url": "https://eveofficial.com/news/20260801200004.html",
   "isLive": true,
   "layoutFamily": "Fixed-width desktop-only centered column, JP serif, no bands and no cards. Every page in the site shares one .wf container width.",
   "distinctiveMove": "The article NARROWS against the site. The global container is .wf{width:1140px;margin:0 auto} on every page, and the news-detail template overrides just that one instance to width:1000px. That single 140px inward step is the entire 'good spacing' read - there is no other layout device. Everything else is generous, fixed, unapologetic vertical rhythm: date sits ABOVE the title, the header block clears 50px, the hero image clears 50px, and the article footer opens with a 1px rule at 60px margin plus 60px padding.",
   "measurements": "html{font:normal 16px/26px \"Yu Mincho\",YuMincho,\"Hiragino Mincho Pro\",\"MS PMincho\",serif;font-feature-settings:\"pkna\";color:#fff}. body{background:#111;min-width:1140px;position:relative;box-sizing:border-box} and <meta name=\"viewport\" content=\"width=1280\"> - there is NO mobile layout, the site just zooms out. .wf{margin:0 auto;width:1140px;position:relative} plus a clearfix ::after. Detail override: body#information.detail #contents > .wf{width:1000px}. Header block: #contents header{margin:0 0 50px}; #contents header h1{margin:25px 0 0;font-size:30px;line-height:42px} (so date -> 25px -> title -> 50px -> body). Hero media: #contents header .tn{margin:50px 0 0} with img{max-width:580px;height:auto}. Body rules: #contents .main hr{border-color:#2c2c2c}; link hover text-decoration:underline; image hover opacity:.6. Article footer: #contents footer{border-top:1px solid #2c2c2c;margin:60px 0 0;padding:60px 0 0}; footer ul.sns{float:right}; footer ul.sns li{margin:0 0 0 30px;width:200px;float:left}; footer ul.sns li a{padding:12px 0;text-align:center;display:block;line-height:1;transition:all .3s ease} with background:#2c2c2c url(icon.png) 20px 50% no-repeat / background-size:22px auto (24px for fb), hover opacity:.6; footer div.back{float:left}. Total style.css = 36,914 bytes for the whole site.",
   "howToCopy": "Greybox: one centered fixed-width column. Set the site container to 1140px and then narrow THIS template to 1000px - show both so the step is visible. Inside: a small date line, 25px gap, an h1 at 30px/42px, 50px gap, then optional hero image at max-width 580px with 50px above it, then 16px/26px prose. Close with a 1px hairline rule at 60px above and 60px of padding below it, then a flex/float row: a Back link on the left and two 200px share buttons on the right, each padding 12px 0, centered text, 30px apart, filled with a flat #2c2c2c and a 22px icon inset 20px from the left. Do NOT copy the fixed viewport - Rod's post page has to be responsive, so port the numbers into a max-width and keep the 25/50/60/60 rhythm.",
   "artworkOrCss": "Pure CSS geometry. The only images on the page are the content poster and small icon PNGs used as CSS backgrounds inside the share buttons (background-size:22px auto, positioned 20px 50%). Nothing about the layout requires artwork - it is entirely fixed widths and fixed vertical margins. That makes it the highest-signal SPACING reference in the set and the easiest to greybox honestly.",
   "confidence": "read-from-source"
  },
  {
   "name": "Maxime Heckel (anti-reference)",
   "url": "https://blog.maximeheckel.com/posts/on-crafting-painterly-shaders",
   "isLive": true,
   "layoutFamily": "Three-track CSS grid: centered prose column with full-bleed escape hatches on both sides. The same grid drives the header bar.",
   "distinctiveMove": "grid-template-columns: 1fr minmax(auto, 712px) 1fr applied to the article body AND to the page header, so any figure, code block or demo can grid-column:1/-1 and break the measure to full width while prose stays pinned at 712px, with no negative margins and no wrapper juggling. A second variant, 1fr minmax(auto, 663px) 1fr, is used for a narrower context. Fades are done with mask-image:linear-gradient(to bottom, rgba(0,0,0,0) 0%, black 100%) rather than an overlay div.",
   "measurements": "Read from the shipped HTML (Stitches CSS-in-JS, hashed classnames - there is no readable stylesheet). Article/header grid: grid-template-columns:1fr minmax(auto, 712px) 1fr (x2 occurrences); narrower variant 1fr minmax(auto, 663px) 1fr (x1). Header bar carrying that grid also has: background:var(--gray-000);width:100%;padding:8px 16px;margin-top:32px;height:88px, and @media(min-width:640px){padding:8px 32px}. Other caps present in the payload: max-width:1000px (x4), max-width:700px (x4), max-width:400px (x3), max-width:500px, max-width:1020px, max-width:202px. Other grids: repeat(auto-fit, minmax(250px, 1fr)) (x2), auto 1fr auto, repeat(2, minmax(0,1fr)), repeat(3,1fr), and repeat(9/16/33/36, 1fr) - the high-count ones drive the timeline widget Rod said is the one thing he likes. Card token: padding:var(--space-4);background:var(--gray-000);border:1px solid oklch(from var(--border-color) l c h / 33%);border-radius:var(--border-radius-2). Exactly one position:sticky in the whole document. Total HTML payload 1,421,621 bytes.",
   "howToCopy": "Greybox: make the article a grid with columns 1fr minmax(auto, 712px) 1fr, put every prose element in column 2, and let figures/code/demos span 1/-1 for full-bleed. Reuse the identical grid on the header bar (height 88px, padding 8px 16px rising to 8px 32px at 640px, margin-top 32px) so the nav aligns to the prose measure. Use mask-image:linear-gradient(to bottom, transparent, black) for any bottom fade instead of a gradient overlay div. Include this one as the CONTROL, not the target: it is the layout Rod called AI-feeling, so building it in greybox alongside the others is how the tell gets named. The 712px measure and the escape-hatch grid are the two things worth keeping; the rest is what makes it feel generated.",
   "artworkOrCss": "100% CSS - no artwork at all, which is arguably the diagnosis. Everything is tokens (var(--space-4), var(--gray-000), oklch(from var(--border-color) l c h / 33%)) rendered through Stitches into hashed classes like .c-PJLV-iXnkHG-css. There is no hand-made layer anywhere on the page, which is exactly the difference between this and dimden/Klubnika.",
   "confidence": "partial"
  }
 ],
 "notes": "METHOD: every site below was fetched with curl (raw HTML, real UA), then its actual stylesheets were fetched and read. No numbers are inferred from screenshots or descriptions. WebFetch was tried first and discarded - it converts to markdown and destroys the markup, which is precisely the \"is this CSS or artwork\" evidence needed. All eight candidates returned HTTP 200 with real content.\n\nCORRECTION TO THE SHORTLIST: the brief states dimden uses a \"same fixed 900px column as the home page\". That is wrong. Read from source: #blog{padding:35px} with NO max-width on the shell, and the prose column is capped at max-width:1200px via a page-level <style> block, not by blog.css. Do not carry the 900px figure forward.\n\nSECOND CORRECTION: the brief says Cyanilux has \"left-border callouts, rounded panels\". Half right. The left-border treatment is on EVERY h2/h3/h4 (border-left:10px solid #00aabb), not on the callouts. The callouts (.notice) use a 45deg repeating-linear-gradient barber-stripe gutter plus a circular chip instead.\n\nSIX sites read fully from source (dimden, Cyanilux, Klubnika, Inigo Quilez, whey-isolate, Eve) - comfortably past Rod's four-per-page-type floor - plus Maxime Heckel at partial confidence as the deliberate anti-reference.\n\nCOVERAGE AGAINST THE FROZEN SLOT LIST. None of the six carries all nine slots; the set has to be read as complementary:\n- header (title/desc/tags/meta/read-time): only Cyanilux has real tags + date + updated-date. NOBODY in the set shows a read-time. Nobody shows a description/standfirst. Klubnika's one-line flex title-left/date-right (.lr) is the tightest header in the set; Eve's date-above-title with 25px/50px gaps is the most generous.\n- hero media: only Eve has a real hero slot (header .tn{margin:50px 0 0}, img max-width:580px). whey-isolate has a header banner image (80% width, max-height 150px) but it is site chrome, not post hero.\n- TOC: two shapes only. Cyanilux = inline nested <ul> under an h3, not sticky, zero cost. whey-isolate = a 160px position:sticky;top:0 right rail. Those are the two real options; there is no third pattern in this set.\n- TL;DR: NOBODY has a styled TL;DR block. dimden writes \"TLDR:\" as the first sentence of the first paragraph with no styling at all. If Rod wants a TL;DR component, it has no provenance in this set - the nearest real snippet is Cyanilux's .notice callout, which could be remixed into one.\n- code blocks: three genuinely different approaches. Cyanilux = Hugo highlight, .highlight>pre{overflow:auto;padding:20px} on #181818. dimden = highlight.js atom-one-dark forced to a pure black background with pre{tab-size:4}. Inigo Quilez = hand-typed span classes (.codeK/.codeL/.codeS/.codeComment), no library at all.\n- takeaway: nobody has one. Cyanilux's post ends with an hr + a centered support line + two flat .footer-btn buttons, which is the closest structural analogue.\n- prev/next: NOT PRESENT on any of the six live post pages. whey-isolate's stylesheet carries an unused #nextprev{text-align:center;margin-top:1.4em}, and Cyanilux has a .pagination ruleset, but neither renders on the article. This slot has NO live provenance in this shortlist and will need a separate source before it gets built.\n- related posts: same - absent everywhere. Eve's footer offers only share buttons and a history.back() link.\n\nTHE RECURRING QUESTION, ANSWERED PER SITE. Pure CSS, nothing needed from an illustrator: Cyanilux, Inigo Quilez, Eve. Hybrid, with the geometry in CSS and the character in raster art that a greybox can drop: dimden (two noise PNGs behind an all-CSS animation) and Klubnika (banner strips, a page stain, a seal - all removable without breaking the sheet). Mostly CSS over photographic ground: whey-isolate (two JPEGs, but the look comes from background-blend-mode:screen + background-attachment:fixed over a flat color, so any dark image substitutes).\n\nTASTE READ, for picking which to greybox first. Against the WARM & HAND-MADE / ALIVE & PLAYFUL / OPEN & WONDROUS razor: Klubnika and whey-isolate are the two hand-made ones and they solve it in opposite directions - Klubnika by making the column a physical document (hairlines, dotted rules, page stain), whey-isolate by making every panel a sticker (10px borders, zero-blur offset shadows). Cyanilux is the most directly portable to Rod's actual content (a long tech-art tutorial with tags, code and callouts) and is the only one that delivers the MinionsArt praise - rounded card, tags, layout - as a real POST template rather than an index. Eve is the spacing ruler. Inigo Quilez is the minimum-viable proof that a 4.4KB stylesheet can carry a serious article, useful as a floor, and Rod already said it is not a site goal. Maxime is the control.\n\nONE GAP WORTH FLAGGING: all six are dark-ground. There is no warm/light post template in this set, which matters because the twilight-festival palette is not a dark-tech palette. Mateusz Urbanowicz (https://mateuszurbanowicz.com/2026/05/02/taking-good-photos/) is live (HTTP 200) and is the only warm/watercolour candidate on the shortlist, but I deprioritised it to a note rather than a card: it is WordPress.com on the h4 theme, its stylesheets are Jetpack/Gutenberg bundles behind concatenated /_static/?? URLs, and its only real layout numbers are the WP block defaults --wp--style--global--content-size:720px and --wp--style--global--wide-size:1340px. That is boilerplate, which Rod has explicitly ruled worthless as evidence. If a warm/light post reference is wanted, it should be sourced fresh rather than taken from here.\n\nCONVERGENCE ON MEASURE, since it is the one number that must be decided first: 712px (Maxime, and it is the only one tuned for prose), 1000px (Eve, at 16px/26px JP serif), 1200px (dimden), 58em (Klubnika, roughly 928px at 16px), 70%-of-viewport with no cap (Cyanilux - it genuinely grows unbounded on an ultrawide, which is the site's real flaw), and 120ch (Inigo Quilez, far too wide for prose and only defensible because it hosts a media wall). The honest cluster for a readable post column is 700-1000px; anything above that in this set is either hosting media or is a bug."
}