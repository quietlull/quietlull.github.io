{
 "pageType": "RESUME / CV page (new surface, no `_layouts/resume.html` exists today — the closest live thing is the `page` layout inside the shared `default.html` shell: fixed 72px topbar, `.container` + `.row` + `<main class=\"col-12 px-md-4\">`, tail-wrapper footer. So all four variants are additive; nothing has to be un-built.)\n\nFROZEN SLOT LIST (every variant must contain all nine; a variant may reshape a slot, never delete it):\nS1 site shell / nav — bench `top-bar` component, 72px fixed (favicon 3.75rem mark, wordmark, 4 nav links, 3 slap toggles)\nS2 name + role subtitle + one-line contact run (email / links)\nS3 summary paragraph (2-4 lines)\nS4 EXPERIENCE timeline — per role: date range, role, company, 2-4 line blurb, per-role skill tags\nS5 skills block (the full tag set, separate from the per-role tags)\nS6 education / secondary dated list (shorter, same timeline device)\nS7 selected projects (3 rows, each linking into the existing `/tech-art/` `/game-design/` project pages)\nS8 print/PDF affordance (either a `.no-print` link to a PDF, or the page itself IS the PDF)\nS9 footer — bench `site-footer` (single \"thanks for wandering by\" line + one link)\n\nSHARED GREY LANGUAGE (transcribed from `redesign-lab/landing-blockout.html`, so all four are comparable and none smuggles taste): page `#141414`, section blocks `#181818`, every reserved box `1.5px dashed #555` on `#1b1b1b`, labels 11px mono `#888` uppercase 0.14em, one 2px `#8a8a8a` fold marker. No colour, no radii except where the SOURCE specifies a radius (9999px pill vs 8px chip vs 0 — radius is geometry here, it is the tag device's identity), no type treatment beyond box heights that reserve the real leading.\n\nPRINT SIMULATION (all four): each variant renders twice on the bench — screen mode, and a paper column at A4 96dpi = 794 x 1123px with the browser default 12.7mm margin, giving a 698px LIVE MEASURE and a 1027px page depth. Anything wider than 698px must collapse in `@media print` or it is broken on paper. That single number is what separates variants B and C (fit natively) from A and D (must collapse).",
 "variants": [
  {
   "name": "A — Sticky Split Rail",
   "sourceSite": "Brittany Chiang v5 — https://brittanychiang.com/ (compiled Tailwind /_next/static/css/1205f04d95fac248.css + page HTML). This is the register Rod named, transcribed 1:1. Print block is NOT from this site (it has zero @media print rules) — the print half is grafted verbatim from zachleat.com/resume/index.css and labelled as such.",
   "geometry": "OUTER: max-width 1280px, margin-inline auto, min-height 100vh. Padding 24px inline / 48px block; at >=768px 48px / 64px; at >=1024px padding-block 0.\nSPLIT (>=1024px only): display:flex; justify-content:space-between; gap:16px. LEFT 48% = 614px at 1280 outer, position:sticky; top:0; max-height:100vh; padding-block:96px. RIGHT 52% = 666px, padding-block:96px (below 1024 the right column keeps padding-top:96px and the left becomes a static header).\nLEFT RAIL BLOCKS (S1 partial + S2 + S3): name box 48px tall (text-5xl 48px/1) -> 36px/40px below 640; subtitle box 20px; summary 3 lines at 14px/20px = 60px, 12px above; in-page nav 3 rows, each a 12px label bar 120px wide on a 24px row, 16px between; contact icon rail 5 boxes 24x24 with 20px gaps, 48px below the nav.\nRIGHT COLUMN RHYTHM: section gap margin-bottom 64px -> 96px at >=768 -> 144px at >=1024. scroll-margin-top 64px (96px at lg).\nROLE ROW (S4): <li> margin-bottom 48px. Inner grid-template-columns: repeat(8, minmax(0,1fr)); gap 32px at >=640 narrowing to 16px at >=768. DATE cell = col-span 2 (25% = 166px of the 666px column), one 12px/16px bar ~110px wide. BODY cell = col-span 6 (75% = 500px): role bar 16px on a 20px line, company bar same, blurb 2-3 lines 14px/20px starting 8px below, then the tag row 8px below that.\nTAG PILL: content 12px/20px + 4px block padding = 28px box height, 12px inline padding, border-radius 9999px, gaps 8px x 8px. A 6-tag row wraps to 2 lines inside 500px.\nHOVER DEVICE (geometry only, no colour): while the LIST is hovered every row gets opacity .5 and the hovered row opacity 1; the 'card' is an absolutely positioned sibling at inset -24px inline / -16px block with a 4px radius — i.e. it lives OUTSIDE the text bounds so the reading rhythm stays flush and no box exists at rest.\nS6 education: same 8-col row, 2 entries, no tags, li margin-bottom 48px.\nS7 projects: same 8-col row, thumb in cols 1-2 at 168px wide x 94.5px (aspect-video) with a 2px border and 4px radius, body in cols 3-8.\nS5 full skills block: one wrapping pill field, 500px wide, ~25 pills at the 28px box height = ~5 rows = 180px including 8px gaps.\nS8 + S9: PDF link row 20px, 48px above the footer; footer 80px, full width below the split.\nBELOW 1024px: one column, 24/48px padding, rail becomes a normal header.\nPRINT (grafted from zachleat): body max-width 55em, margin auto; every grid and the split flex forced to grid-template-columns:auto / display:block; topbar, toggles, in-page nav, footer display:none; ink #000 on #fff; a:after { content:\" \" attr(href) }.",
   "whyDistinct": "The only variant where the identity block (S2+S3) never scrolls — it is a sticky half of the viewport, so the page is permanently two-up rather than a document. It is also the only PROPORTIONAL date gutter (25% of the column, so it grows with the viewport) and the only one whose timeline reads as rows-in-a-table rather than paragraphs. Tests: does Rod actually want half the screen spent on a fixed bio, and does a 166px date cell still read at 1280 when the site's own topbar is already carrying identity?",
   "risk": "HONEST CAVEAT: this page cannot print. Zero @media print rules exist in the source — brittanychiang links a separate /resume.pdf instead — so the 48/52 sticky split, the 8-col grid and the 100vh sticky rail all land in the paper output unless the grafted zachleat print block is written first and tested first. The 666px right column also exceeds the 698px A4 live measure only slightly, but the 1280px outer does not fit at all. Rod's stated register and Rod's stated print requirement are in direct conflict here, and this variant resolves it by borrowing from a different site.\nAI TELLS: (1) the wrapping 9999px pill row is the single most generic component on the web right now — no pillar backs it; WARM & HAND-MADE would need the pill skin replaced by a paper/torn-edge treatment later or it reads as slop. (2) The source's mouse-follow radial-gradient spotlight (a fixed inset-0 div fed by a JS-written custom property) is gradient-on-dark and is explicitly contradicted by the de-glow principle — it is EXCLUDED from this variant, do not add it back. (3) The hover-dim + out-of-bounds card IS backed by ALIVE & PLAYFUL (interaction that rewards without becoming exploration) and is the one borrowed behaviour worth keeping. (4) Sticky rail + card-on-hover is also the exact shape of every AI-generated portfolio of the last two years, purely because this site is the thing they all copied."
  },
  {
   "name": "B — Print-First Measure",
   "sourceSite": "Zach Leatherman — https://www.zachleat.com/resume/ + its page-specific stylesheet https://www.zachleat.com/resume/index.css (4.1KB) and the page's inline <style>. The skill-chip geometry in S5 is a NAMED GRAFT from xeiaso.net/resume (this source has no tag device at all).",
   "geometry": "SHELL: .body-grid { display:grid; grid-template-columns: 1fr 14rem 2.5rem; grid-column-gap:1rem; padding-top:1rem } = main / 224px rail / 40px gutter, 16px gaps.\nCONTENT: .content-grid { display:grid; grid-template-columns: 1fr minmax(auto, 31em) 1fr; grid-column-gap:0; max-width:56.5217em (author's own comment: 1300px/23) }. Body 19px / line-height 1.65 / prose padding-inline 4px, so the MEASURE is a hard 31em = 589px self-centred inside a ~1300px frame — 589px sits inside the 698px A4 live measure with 109px to spare, which is the whole point of this variant.\nS2/S3: h1 block, then the .resume-title subtitle line, then the contact run; body rhythm is 1em = 19px between blocks.\nTOC (reshaped S1 in-page nav): .resume-toc { display:flex; gap:.5em 1em (9.5px x 19px); justify-content:center; margin-bottom:-2.33em (-44px); font-size:15px } — the negative margin pulls the first section up under it. Transcribed quirk, not an invention.\nS4 ROLE ENTRY (no date gutter at all): .resume-list > li { list-style:none; padding:.4em 0 = 7.6px }. Role line 19px; date nested UNDER it at 16px italic; location a separate 15px block line; subnote 13px; then ul.resume-desc { margin:.4em 0; font-size:16px } bullets at 16px x 1.65 = 26.4px per line. One role block = 31 + 26 + 25 + 3x26 + 15 = ~175px, and roles are only 15.2px apart (2 x .4em padding).\nS6 education: identical .resume-list, 2 entries.\nS7 projects: same list, each item a role line + one 16px desc line, no thumbnails (this source uses inline 60-72px favicons rendered small — that texture is ARTWORK and a greybox cannot fake it; reserve a 24x24 box per item and flag it as artwork-owed).\nS5 skills: wrapping chip field at 589px wide — chips grafted from xeiaso: 14px/20px text, 8px inside padding, 8px outside margin, 8px radius = 36px box, so ~25 chips = 4 rows = 176px including margins.\nRAIL (224px, right): .rail { padding:1em .5em (16px/8px); margin-bottom:1em; border-bottom:1px } — carries S8 (the PDF link) and the site links; it is the element the print block deletes.\nS9 .resume-invitation { border-top:1px; padding-top:1em; line-height:165% } then the 80px footer.\nBREAKPOINTS in the source's resume CSS: 450 / 500 / 560 / 980px.\nPRINT — the reason this variant exists, two separate blocks, transcribed: (1) body { max-width:55em; margin:0 auto; grid-template-columns:auto } + .content-grid { grid-template-columns:auto } + .rail, .footer, .table-of-contents, .resume-toc { display:none } + ink forced #000 on #fff; (2) .resume-print-link:after { content:\" at \" attr(href) } so every link prints its URL beside the text.",
   "whyDistinct": "The ONE variant where print is the primary target and screen is the fallback, and the only one where the same DOM is both the web page and the paper CV with no duplicate markup and no separate PDF file (S8 becomes 'this page, printed' rather than 'download a file'). It is also the only variant with NO date gutter whatsoever — the date is a nested 16px italic line under the role, which is why the print collapse is a no-op. Structurally it is a single narrow measure with a side rail, the opposite of A's two-up split and of D's app shell. Tests whether Rod's timeline still reads when dates are demoted below the role instead of ranged beside it.",
   "risk": "A 589px measure of 19px/1.65 prose with a right rail is, at greybox resolution, indistinguishable from a default blog post template — the risk is not an AI tell so much as a NO-DESIGN tell; nothing here carries ALIVE & PLAYFUL, and OPEN & WONDROUS gets no surface at all (there is no room for the lantern scene in a 589px column). It only survives if the later paper-morphism skin does real work. Second caveat: the source's only non-CSS texture is inline company favicons/avatars (from v1.indieweb-avatar.11ty.dev and /img/avatars/*.avif) — that is the one thing the greybox cannot fake, so if Rod likes this variant BECAUSE of the little marks beside each employer, that is artwork owed, not layout. Third: the -2.33em negative TOC margin is real but fragile; any change to the TOC font-size moves the first section.\nAI TELL AUDIT: the grafted 8px chip field is the only box-shaped element on the page and will read as bolted-on; a uniform chip field is the classic tell and no pillar backs it — the alternative is variant C's answer (skills as prose)."
  },
  {
   "name": "C — Bare Document",
   "sourceSite": "Ben Hoyt CV — https://benhoyt.com/cv/ (the whole site is ~2.5KB in one inline <style>; every number below is read from it). Nothing is grafted; S5 uses this source's own device (skills as prose under a small-caps head) rather than importing tags from elsewhere.",
   "geometry": "BODY: max-width 1000px; margin:0 auto; font-size 17px; line-height 1.6; word-spacing .1em.\n#content { margin:2em auto (34px); width:92.5%; max-width:700px } — the 92.5% is what guarantees breathing room at every width, and 700px sits inside the 698px A4 live measure (2px over, i.e. exact by construction).\nMENU STRIP (S1, this page's own, above the bench topbar decision): width 92.5%; height 5.5em = 93.5px; border-bottom 1px. Title 225% = 38px uppercase, letter-spacing .15em.\nTYPE / RHYTHM (this IS the layout — there is no other layout code): h1 200% = 34px, letter-spacing .02em, weight normal; h2 160% = 27.2px small-caps, margin 1.8em top (30.6px) / -0.2em bottom (-3.4px) — the NEGATIVE bottom margin tucks the following h3 up under the section head; h3 100% = 17px, margin 1.5em top (25.5px) / -0.5em bottom (-8.5px). h1,h2,p,ul,details all margin 1em (17px) 0. li { margin-left:1.2em (20.4px); margin-bottom:.5em (8.5px) }.\nS4 ROLE ENTRY: ONE h3 whose text is the whole header — 'Company - Role - Sep 2020 to now'. The date is the last third of the heading STRING; zero layout code carries the timeline. Then a plain ul of 2-4 bullets at 17px x 1.6 = 27.2px per line. Block height = 17 (h3) - 8.5 (negative bottom) + 17 (ul margin) + 3 x 27.2 = ~107px, with 25.5px of air before the next h3. Ten roles = ~1325px of timeline.\nS6 education: identical h3 + ul, 2 entries, under its own small-caps h2.\nS5 skills: NO tags. A small-caps h2 'Skills' + a ul of 4-6 comma-run lines at 17px/27.2px = 109-163px. This is the print-truest reading of the slot and the only variant that answers the tag question by refusing a box.\nS7 projects: same h3 + ul pattern, 3 entries, links inline in the bullets.\nS2/S3: h1 34px, one 17px contact paragraph, one 17px x 3-line summary = 82px.\nS8: a PDF link marked .no-print (this source ships /cv/ben-hoyt-cv-resume.pdf), plus a .print-only line that appears only on paper. S9: footer 80px.\nRESPONSIVE: the ENTIRE responsive strategy is the root font-size — 17px, then @media (max-width:750px) 15px, then @media (max-width:615px) 13px. Nothing else restyles. That means the greybox block heights above scale by 0.88 and 0.76 and the proportions are identical at every width.\nPRINT: @media print { .no-print{display:none} .print-only{display:block} a:link,a:visited{color:inherit-ink} } — that is the entire print stylesheet, because a 700px single column of prose has nothing to collapse.",
   "whyDistinct": "The only variant with NO second axis at all — no rail, no sidebar, no gutter, no grid, one 700px column and three type sizes. It is the only one whose printed output is correct BY CONSTRUCTION rather than by a print stylesheet, and the only one where the date is typography (part of the h3 string) instead of a slot. It is also by far the cheapest to build and maintain against the live Jekyll layouts. Tests the floor: how much of the resume's job is done by pure rhythm, and whether Rod's timeline needs a gutter at all.",
   "risk": "It will look UNSTYLED at greybox resolution — that is the honest caveat, and Rod should be warned before he opens it, because the failure mode of judging this variant is 'nothing is there'. The whole page carries WARM & HAND-MADE (the source's warm ink, the small-caps heads, the negative-margin tuck are all real craft) but gives ALIVE & PLAYFUL and OPEN & WONDROUS literally nowhere to land — no hover device, no scene, no interaction surface. Second caveat: the date-inside-the-heading move means the timeline is not machine-parseable and cannot be re-sorted, filtered, or given a per-role tag row without breaking the device — if Rod wants per-role skill tags (S4 explicitly lists them), this variant has to answer with a trailing bullet, not a tag row, and that is a real content compromise.\nAI TELLS: none — this is the anti-tell variant. Its inverse risk is the DEFAULT-BROWSER tell: an uncustomised 700px serif column reads as an unfinished page rather than a chosen one, and no pillar rescues that on its own."
  },
  {
   "name": "D — Doc Shell + Fixed Date Gutter",
   "sourceSite": "Tania Rascia — https://tania.dev/resume/ + https://tania.dev/styles.b1cb92fe3f8958ef5c2a.css (62KB). The timeline device is transcribed from the SAME site's home page (`ul.hero-eras span.era-dates`), not from its resume markdown, which is just rendered prose. Chips grafted from xeiaso.net/resume; print block grafted from zachleat (this source has zero @media print rules — its 260px sticky 100vh sidebar would land in the paper output as-is).",
   "geometry": "TOKENS (read from the source's custom properties): --content-width 760px; --sidebar-width 260px; --border-radius 8px; --layout-padding 1.5rem = 24px, rising to 2rem = 32px above 1020px. --main-layout-width = calc(260px + 760px) = 1020px.\nSIDEBAR (S1, in-page, persistent): min-width and width 260px; height 100vh; position:sticky; top:0; overflow-y:auto; border-right 1px; display:none below 1020px. Section head 16px mono; menu links 14px with 8px block / 12px inline padding = 34px rows; sub-links 13px on 28px rows. Reserve 6 section links + 3 sub-links = ~290px of rail.\nMAIN: flex-grow:1; margin:0 auto; max-width:760px; padding:24px (32px above 1020). Page-type templates widen to calc(760+260)=1020px with margin-left 24px.\nS2/S3: h1 40px/700 (2.5rem); .resume-title subtitle 20px on a 28px line; contact run 15.2px (.95rem); summary 3 lines at 16px/1.6 = 77px.\nSECTION HEAD (the rhythm device): h2 with border-bottom 1px and padding-bottom 8px — a hairline rule under every section head, which is what replaces A's 144px section gaps. Reserve 24px head + 8px pad + 1px rule + 24px after = 57px per section boundary.\nS4 TIMELINE ROW (the borrowed device): display:flex; align-items:baseline; gap:.75rem = 12px; the date span locked at width:90px AND min-width:90px, mono, 12.8px (.8rem). So a HARD 90px gutter + 12px gap leaves 658px of the 760px column for role + company + blurb. Row = date bar 13px tall in the 90px cell; role 16px/24px; blurb 2-3 lines 14.4px/1.6 = 69px; per-role chip row 36px. One role block ~150px, 32px between roles.\nS5 skills: wrapping chip field at 658px — 14px/20px text, 8px padding, 8px margin, 8px radius (the source's own --border-radius token happens to be the same 8px) = 36px boxes, ~25 chips = 4 rows = 176px.\nS6 education: same 90px-gutter flex row, 2 entries.\nS7 projects: same flex row with the date cell reused as a 90px thumbnail cell (90x51 at 16:9, 8px radius).\nS8: PDF link row 20px inside the sidebar's bottom section. S9: footer 80px inside the main column, not full-bleed (the sidebar runs past it).\nBREAKPOINTS: 600 / 800 / 1020 / 1360px, plus a max-width:799px block. Below 1020 the sidebar is display:none and main goes full width — which is also, verbatim, the fix this source is missing for print.\nPRINT (grafted from zachleat, mandatory here): sidebar display:none; the flex shell and every row flex forced to a single column; body max-width 55em; ink #000/#fff; a:after { content:\" \" attr(href) }. The 760px main column is 62px over the 698px A4 live measure, so the print block must also drop main's max-width, not just hide the rail.",
   "whyDistinct": "The only variant where the resume inherits the SITE's document shell rather than getting its own layout — one extra template, and it is the only one that gives the CV a persistent in-page table of contents (a second nav, alongside the bench top-bar). It is also the only FIXED-PX date gutter (90px, mono, hard-locked by both width and min-width) as opposed to A's proportional 25% cell, B's no gutter, and C's date-inside-the-heading. And it is the only one using a 1px rule under section heads instead of large vertical gaps to separate sections, which makes it the most compact of the four per role (~150px vs C's ~107px of prose but with tags included). Tests whether a fixed mono gutter is legible at 90px and whether a second nav is redundant against the topbar the site already ships.",
   "risk": "260px sticky sidebar + 8px-radius chips + mono labels + hairline section rules is the exact silhouette of every docs site and every AI-generated dashboard — this is the strongest AI-tell risk of the four. Pillar audit: the MONO is genuinely backed (the locked call is scribbly hand-drawn name + mono, so a mono date gutter is on-pillar); the 8px card radius, the chip field, and the persistent sidebar nav are backed by NOTHING and should each be either elevated by a pillar or deleted, per the impeccable rule that there is no neutral middle. Second caveat, structural not stylistic: the sidebar DUPLICATES the bench `top-bar` component, which already carries favicon + wordmark + 4 nav links + 3 slap toggles at 72px fixed — this variant ships two navigations on one page and Rod has to kill one. Third: the source has zero print rules and a 100vh sticky sidebar, so without the grafted print block this is the worst printer of the four, worse than A."
  }
 ],
 "openQuestions": "1. THE CENTRAL CONFLICT, needs Rod before any file is opened: he named brittanychiang.com as the register, and that page does not print at all — a grep of its compiled CSS returns zero @media print rules; the site ships a separate /resume.pdf instead. So \"the register I want\" and \"must survive being printed\" cannot both be satisfied by transcription alone. Which wins? Three resolutions exist and they change what gets built: (a) variant A + a grafted print stylesheet from zachleat (what I specced), (b) the page IS the PDF, zachleat's answer, which points at variant B, (c) a screen page plus a separately maintained PDF, which is what Brittany and Ben Hoyt both actually do and which makes S8 a link rather than a print problem.\n\n2. CROSS-SOURCE GRAFTS — provenance tier call. Variants A and D need a print block they cannot source from their own site (grafted from zachleat), and B and D need a tag device their own site does not have (chips grafted from xeiaso). Is a variant allowed more than one source when the slot is genuinely absent from the primary, and does that make it Remixed rather than True? I labelled every graft inline rather than silently blending. If Rod wants strict one-site-per-variant instead, B and D lose their tag block and the skills slot has to be answered variant-C-style (prose), which changes the comparison.\n\n3. PAPER SIZE. I sized every print simulation to A4 at 96dpi (794 x 1123px, 698px live measure at the 12.7mm default margin). US Letter gives 816 x 1056px and a 720px live measure. That 22px changes whether variant C's 700px column and variant B's 589px measure are comfortable or exact. Which does Rod print on?\n\n4. DOES THE RESUME KEEP THE TOPBAR TOGGLES? The bench `top-bar` ships three slap toggles (Breathing / Sparkler / Fireworks) and today `_includes/topbar.html:91` only renders the fireworks toggle for layouts home / section-about / section-landing / section-projects. A CV page with reward toggles is a taste call, not a layout one, and variant D's sidebar makes it worse by adding a second nav. Which nav survives on this surface?\n\n5. PER-ROLE TAGS vs PRINT. S4 asks for skill tags on each role. Only variant A's source has a per-role tag device natively; variant C structurally CANNOT have one without breaking its date-in-the-heading move. If per-role tags are non-negotiable, variant C should be rebuilt with a trailing bullet instead, and Rod should know that is a content compromise rather than a styling one.\n\n6. SOURCE READ GAP. The xeiaso.net measurement I was handed is truncated mid-line at `.lg\\\\:max-w-3xl{max-width:` and its date span is described as `text-md`, which is not a stock Tailwind class. The chip metrics I used (m-2 / p-2 / text-sm / rounded-lg = 8px margin, 8px padding, 14px/20px, 8px radius) and the 64rem container ARE fully read from source; the sibling column width and that date size are NOT. If Rod wants a fifth variant built on Xe's tag-cloud-first family (a flat 1024px column with only 16px side padding, where ~25 identical chips ARE the visual system and three type sizes 24/20/16px carry the whole hierarchy — a genuinely fifth layout family), that site needs re-reading first.\n\n7. NO SCENE ANYWHERE. None of the four reserves space for the Three.js lantern scene, because none of the four source sites has one and free-handing it would break the provenance law. Variants B and C have no room for it at all. If the resume is supposed to carry any of OPEN & WONDROUS, that is a slot Rod has to add to the frozen list explicitly, with a source, before build — and it conflicts head-on with the print requirement.\n\n8. TIMELINE DEPTH. Ten roles at variant C's rhythm is ~1325px of timeline; at variant D's ~150px per role with tags it is ~1500px plus 57px per section boundary. Nobody has told me how many roles, how many education entries, or how many projects actually exist. Real counts would let me give exact page depths instead of per-block heights, and would decide whether S4 needs a collapse/expand affordance — which print would then have to force open."
}