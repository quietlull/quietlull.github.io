{
 "pageType": "PORTAL (root /) — the chooser. NOTE: it is a 3-way, not a 2-way. The live `_layouts/portal.html` ships three destinations (Tech Art, Game Design, and a Ramblings link in `.portal-extras`), so every variant below routes three doors, with Ramblings demoted. FROZEN SLOT LIST, present in all four variants: (1) time-of-day greeting line, `.portal-greeting`, 0.85rem mono lowercase; (2) name `.portal-title` 3rem/2.25rem mobile; (3) role subtitle `.portal-subtitle` 1.25rem; (4) Door A Tech Art = icon + title + one-line description + CTA-with-arrow; (5) Door B Game Design = same four parts; (6) Ramblings secondary link = icon + label + arrow; (7) ambient Three.js layer (`three-background-general.min.js`, portal.html:8); (8) global chrome — top-bar (favicon, HOME + ARCHIVES nav only, and only TWO toggles since `_includes/topbar.html:91` excludes the fireworks toggle from the portal layout), site-footer, picture-frame, back-to-top. STATUS QUO being tested against (`_sass/pages/_portal.scss`): centred header, then `.portal-doors` = a symmetric `1fr 1fr` grid, max-width 900px, gap 2rem, each door `@include mx.glass` + `$radius-xl` + padding 3rem 2.5rem + `breathe-slow`, then a centred 2rem-radius pill for Ramblings. That baseline is a clean sweep of the AI tells: uniform card grid, literal glassmorphism, everything centred, both doors identical and breathing. All four variants are departures from it. SHARED GREY LANGUAGE (all variants, no exceptions): ground #E8E8E8, primary block #C9C9C9, emphasis block #A8A8A8, hairline 1px #9A9A9A, text-line bars #B5B5B5 at the real type size with real line-height. No colour, no accent, no gradient, no shadow except where a shadow is itself the transcribed measurement.",
 "variants": [
  {
   "name": "V1 — Sequential: one door on screen at a time",
   "sourceSite": "potg.art index, https://potg.art/ (measurements re-read by me from https://potg.art/_astro/about.DQG1MDvI.css in the local capture; every rule below was grepped from that file, not from the brief)",
   "geometry": "Grey ramp as defined in pageType. Unit note: the source uses html{font-size:calc(100/1440*1vw)} clamped to 1px above 1581px, so at a 1440 viewport every number below is px 1:1. Tokens verified: --margin-1:40, --margin-2:100, --margin-3:320 (at <=992px: 20/50/80); --max-width:1580px.\n\nSTATE 1 — fixed left column. section.contents{position:fixed; top:0; left:100px; height:100svh; z-index:2; display:flex; align-items:center} — above 1580px viewport, left:calc((100vw - 1580px)/2 + 152px). Inner .contents__wrap is flex-column, align-items:flex-start. Stack top-to-bottom with the source's exact gaps:\n  • greeting bar — 12px tall, line-height 1, letter-spacing .05em, max-width 320px (source .contents__ja-description metrics)\n  • gap 17px\n  • name block — 280x64 solid #A8A8A8 (source .contents__logo width:280)\n  • gap 50px\n  • subtitle — 32px/1.2, max-width 320px, letter-spacing .05em, 2 bars = 77px tall (source .contents__en-description)\n  • gap 50px\n  • DOOR A (Tech Art) — 44x44 icon square; gap 12px; title 32px/1.2 single bar; gap 12px; description 2 bars at 12px/1.4 capped 320px; gap 17px; pill 240x30, border-radius calc(infinity*1px), 1px solid, 14px label inset padding-left:10px, 18px arrow with margin-left:8px (source .parts-button, exact).\n\nSTATE 2 — Door B rises from the bottom edge. .work-nav__title{font-size:290px; line-height:1; letter-spacing:-.02em; position:fixed; left:0; right:0; bottom:-23px; margin-inline:auto; z-index:-1} = a 290px-cap-height wordmark block deliberately clipped 23px below the viewport floor. Its rest state is transform:translateY(100%). A second 240x30 pill sits fixed at bottom:32px, margin-inline:auto, opacity 0 at rest.\n\nTHE SWAP — one state class (source uses attribute [hide-contents]). On: every direct child of .contents__wrap goes opacity:0 + filter:blur(20px), transition-property opacity,filter, duration .5s, ease, with transition-delays 0ms / 50ms / 100ms / 100ms down the stack (logo / en-desc / ja-desc / link — verified exactly); .contents gets pointer-events:none. Simultaneously the 290px wordmark goes translateY(0) and its pill goes opacity:1, both .5s ease. Driver: .scroller__item blocks of height:100svh each — 2 scenes = 200svh total document.\n\nRAMBLINGS (slot 6) — does not take a scene. It is a persistent third 240x30 pill pinned fixed at bottom:32px, left:100px (flush to --margin-2, so it reads as rail furniture, not as a peer of the two doors), with its 18px icon in the 10px label inset.\n\nMOBILE <=750px — everything goes static, exactly as the source: .contents{position:static; width:calc(100vw - 100px); margin-inline:auto; margin-top:109px; margin-bottom:65px}, the art/scene block gets a fixed height of 500px, and the second door's link becomes margin-top:24px / margin-bottom:136px. Top bar stays fixed at top:40px, width calc(100vw - 80px), max-width 1580, 12px nav items.",
   "whyDistinct": "It is the only variant in which the two primary doors are never co-visible. The other three all lay the choice out for comparison; this one refuses comparison and makes the portal a sequence of single offers. It is also the only variant that puts the chooser hard against the left margin and leaves the entire right two-thirds as uninterrupted field for the Three.js lantern scene, and the only one whose transition is a real state machine rather than a hover. Worth transcribing for one specific reason: in the source, nothing about the doors is baked into an image — the pills are 1px borders and the 290px wordmark is live text — so the whole device survives being greyboxed, which is not true of V4.",
   "risk": "Three honest problems. (1) It costs 200svh of scrolling to serve a page whose stated job is one click and which Rod ranked LAST priority — a scroll-gated chooser is the most friction of the four, and the second door is below the fold by construction. (2) Real a11y exposure, not cosmetic: the hidden state is opacity:0 + blur, which leaves Door A focusable while invisible, and Door B is only revealed by scroll — this needs an explicit tab-order and visible-focus decision (WCAG 2.4.3, 2.4.7, 2.1.1) before it is built, plus a prefers-reduced-motion path since the whole device is a 500ms transform+filter. (3) The greeting slot is the weakest fit — a time-of-day line wants to be the first thing read, but the source's stack puts the 12px line third. AI tell: no card grid, no glass, no gradient, no centred popup — it dodges all four. What it does flirt with is a different tell, the 2023-24 agency giant-clipped-wordmark, which reads as borrowed-cool rather than made-by-hand. ALIVE & PLAYFUL backs the state swap; OPEN & WONDROUS backs the open field. Neither pillar backs the 290px wordmark itself, and WARM & HAND-MADE actively argues against it. If Rod likes this one, the wordmark is the part to replace with something drawn."
  },
  {
   "name": "V2 — Stacked block-per-destination, printed-rule rhythm",
   "sourceSite": "ZUTOMAYO MART_BOTH store chooser, https://zutomayo.net/mart_both/ (I re-extracted every rule below from the local copy of https://zutomayo.net/themes/zutomayo/_assets/css/app.css — the selectors are namespaced .mart2026_detail / .mart-for-live_detail, which is why a naive grep misses them)",
   "geometry": "Grey ramp as defined in pageType, with one addition this variant needs: the ground is a repeating texture tile at background-size:60%, greyboxed as a 2px #DEDEDE dot on the #E8E8E8 ground so its presence is testable without smuggling warmth in.\n\nBLOCK MODULE, used four times. .block{max-width:500px; margin:0 auto; padding:100px 0} — at <=768px this becomes padding:40px 16px 80px. Inside, in order:\n  • identity mark — width:80vw, max-width:300px, display:block, margin:0 auto 50px (so the mark-to-caption gap is a hard 50px)\n  • caption — text-align:center, margin-bottom:20px, one 16px bar\n  • button row — display:flex; gap:20px 5px; justify-content:center. flex-wrap:wrap ONLY at <=768px, so on desktop the row never wraps.\n  • button — width:40vw; max-width:340px; height:55px; border:1px solid; font-size:14px; display:flex; align-items:center; padding-left:70px; padding-right:40px. The 70px left inset reserves a 44x32 icon absolutely positioned at left:10px, top:50% translateY(-50%); the 40px right inset reserves a 14x14 arrow at right:10px. At <=768px width:90vw. Hover = solid fill swap (background and label invert), NOT a scale and NOT a glow. A single-button block uses margin:auto on the button (this is the source's own .mart2026_detail__complete case, verified).\n\nDIVIDER between blocks — a full-bleed band, height:50px, repeat-x, with border-top:1px and border-bottom:1px. This is the entire section rhythm: no block has a border, a radius, or a background of its own. Separation is a printed rule, not a card edge.\n\nPAGE ASSEMBLY, four blocks top to bottom:\n  1. HEADER block — greeting caption bar (slot 1) at 16px, then the name as the 300px-max mark (slot 2), then the subtitle caption (slot 3), no button row. Divider.\n  2. TECH ART block — 300px mark = the door icon+title fused (slot 4 icon + title), caption = the one-line description, button row = TWO 340x55 buttons at 5px column gap.\n  3. GAME DESIGN block — identical module (slot 5), TWO buttons.\n  4. RAMBLINGS block — same module but the single-button case, margin:auto, one 340x55 button (slot 6).\nTotal desktop height per full block = 100 + 150 + 50 + 19 + 20 + 55 + 100 = 494px, plus the 50px divider; four blocks ~ 2100px document.\n\nCONTENT CONSEQUENCE of the two-button row, which is the reason to transcribe it rather than collapse to one CTA: it lets the portal absorb one level of the existing section-nav. Tech Art = [PROJECTS][ABOUT]; Game Design = [PROJECTS][BLOGS] — which is exactly the real asymmetry in `_includes/section-nav.html`, where only game-design carries BLOGS. The portal becomes a deep-link router instead of a section router.",
   "whyDistinct": "The only variant where the two doors are never side by side AND never sequential — they are peers stacked in reading order, each one a full room with its own sub-doors. It is also the only variant that gives each destination more than one exit, and the only one whose separation device is a printed horizontal rule rather than an edge, a border, or negative space. Against the status quo it removes the card entirely: nothing on the page has a background or a radius, which is the single biggest departure from the current glass 1fr-1fr grid.",
   "risk": "(1) Repetition is the mechanism, so it is also the failure mode: four near-identical 500px modules can read as a template rather than a composition, and the third and fourth repeats are where it will either click or die. (2) It makes the portal the LONGEST of the four at ~2100px, which fights hardest against 'last priority, one choice' — arguably the wrong direction for this page even if the module is good. (3) A 500px column on a 1920 screen leaves ~1400px of empty field; that is only a virtue if the Three.js scene is doing real work out there, so this variant is coupled to a decision Rod has not made. AI tell: it dodges card-grid, glass, gradient-on-dark and centred-popup, but the button itself — 1px rectangle, icon inset left, arrow inset right, fill-on-hover — is close to generic app-store furniture, and at greybox stage it is indistinguishable from one. The thing that saves it in the source is that the icons and the divider strip are hand-drawn images, not CSS. So: WARM & HAND-MADE backs this variant only if Rod draws the 44x32 icons and the 50px divider strip; if those stay as CSS defaults the variant has no pillar backing at all and should be dropped. Flag on the two-button row: it is a content change (portal absorbs section-nav), so it needs Rod's yes on the information architecture before it is a fair layout test."
  },
  {
   "name": "V3 — Type-scale only, no container, no layout system",
   "sourceSite": "lyra.horse, https://lyra.horse/ (verified by reading the complete page CSS — it is 367 bytes in one inline <style>, there is no external stylesheet, and I have the full text: three rules total, .bigLink, .underline, and a .badge block)",
   "geometry": "Grey ramp as defined in pageType. The spec here is a RATIO, not a geometry, and that is the entire point — the source has no container, no max-width, no font stack, no spacing scale and no media queries whatsoever.\n\nTYPE SCALE, the only design decision on the page: door links at font-size:2em, font-weight:bold. Everything else at 1em. 2:1, nothing in between, and no third size anywhere on the page. At a 16px UA root that is 32px vs 16px; door line box = 32 x 1.2 = 38.4px tall, description line box = 19.2px.\n\nFLOW, using default UA box model with no resets (body margin 8px, h1 margin-block 0.67em, p margin-block 1em, hr margin-block 0.5em with a 1px inset border, full-viewport-width body):\n  • h1 — the name (slot 2), left-aligned, default h1 sizing\n  • one 1em paragraph — greeting and subtitle MERGED into a single plain sentence (slots 1 + 3), e.g. the time-of-day phrase and the role in one line\n  • <hr>\n  • three door groups, each exactly [1em icon glyph square, inline] + [2em bold link on its own line] + <br> + [one ~15-word 1em description line directly beneath]: Tech Art (slot 4), Game Design (slot 5), Ramblings (slot 6). Group-to-group separation is the default 1em paragraph margin — no custom gap.\n  • <hr>\n  • everything else demoted below the rule as h3 + ul at 1em: Archives, About, contacts, the two toggles. Nothing below the second rule is allowed to be 2em.\nRule of the variant, stated as a constraint rather than a measurement: exactly three things on the page are big, and every other element is 1em. That rule is the design.\n\nICON SLOT — the source has no icons above the fold (its only images are 88x31 pixel-art badges far below, image-rendering:pixelated with a 1px outline on hover). So each door's icon becomes a single inline 1em glyph-scale square before the link, at text scale. Anything larger breaks the 2:1 rule and turns this into a different variant.\n\nONE DEVIATION, flagged rather than hidden: the true transcription is full-viewport-width body, which at 1920px gives ~230-character measure. Build it faithfully first. The optional knob is a 66ch measure on the flow container — but toggling it is a real change to what the variant tests, so it must be a labelled A/B and not a silent fix.",
   "whyDistinct": "It is the control. It is the only variant with no container, no columns, no gaps, no block heights and no state — the other three are all arguing about geometry and this one asserts that a portal has no geometry problem to solve. Its function in the set is to be the sanity check the other three must beat: if V1's 200svh scroll, V2's 2100px stack, or V4's constellation cannot visibly out-serve 367 bytes of type scale on a page whose job is one click, that is a real finding about the page, not a failure of the variant. It also directly tests Rod's own framing — 'the portal is NOT the landing page and is LAST priority' — by being the only proposal that spends nothing on it.",
   "risk": "(1) It is the only variant that gives the Three.js lantern scene nothing to be composed around, so adopting it is close to deciding the portal has no atmosphere layer — that is a much bigger decision than a layout pick and should be surfaced as such, not absorbed. (2) It deletes the staged entrance the live page currently has (`fade-in-up` at 0.1s/0.25s/0.45s/0.7s/0.9s/1.1s in `_sass/pages/_portal.scss`), because a page with no layout has nothing to stagger. (3) Merging the greeting and the subtitle into one sentence is the one place I compressed the frozen flow rather than reshaping it — both slots survive as content, but they stop being separately styleable, and Rod should confirm that trade. AI tell: none. This is the only one of the four that flirts with zero classic tells, which is exactly why it belongs in the set. The inverse risk is the real one and it is not small: deliberate plainness reads as taste only if the rest of the site is confident, and reads as unfinished otherwise — and next to the current glass cards it will look like a regression to anyone who is not in on the argument. Pillar audit is brutal and honest: ALIVE & PLAYFUL backs nothing here, OPEN & WONDROUS backs nothing here, WARM & HAND-MADE backs it only weakly (plain text is not the same as hand-made). It serves the job and almost nothing else. Keep it as the measuring stick; be suspicious if it wins on comfort rather than on argument."
  },
  {
   "name": "V4 — Orbital constellation, position-as-hierarchy",
   "sourceSite": "Space Jam 1996, https://www.spacejam.com/1996/ (still live; I re-read the served HTML — 6.8KB, and the total CSS on the document is one rule, .footer-links{margin:5px}. Every size below is an HTML width/height attribute I pulled off the actual <img> tags)",
   "geometry": "Grey ramp as defined in pageType, on a tiled ground (the source is body background=bg_stars.gif over bgcolor #000; greyboxed as a repeating 4px dot tile on #E8E8E8 — no dark ground, no gradient, because a dark starfield in a blockout is a colour decision wearing a geometry costume).\n\nFIELD — a 500px-wide square-ish stage, centred (source outer <table width=500>). Above it a 488x60 band (source's real header table dimensions) carrying the greeting bar (slot 1) and the subtitle bar (slot 3) as two 50x11 label strips — 50x11 is a real size from the source, its fast.gif/break.gif labels.\n\nCENTRE MARK — 272x165 (source p-jamlogo.gif, exact). This is the name (slot 2). It is the only element on the page allowed to be that big.\n\nSATELLITES — absolutely positioned inside the 500px field, deliberately unaligned to any grid, no two the same size. All eleven of these are real measured sizes from the source; I am assigning Rod's slots to the three largest and populating the rest from the same measured set so the constellation keeps its density:\n  • Tech Art (slot 4) — 131x56 (p-pressbox)\n  • Ramblings (slot 6) — 104x67 (p-sitemap)\n  • Game Design (slot 5) — 95x77 (p-lunartunes)\n  • remaining measured sizes available for the secondary population (Archives, About, contacts, the two toggles): 94x72, 83x83, 67x63, 63x52, 62x62, 58x52, 55x67, 49x57.\nEach satellite is one block with its label baked in — in the source every door is literally <a><img alt=\"...\"></a>, so the title, the icon and the description are all inside a single mark. In greybox each satellite is one solid rect with a 1em bar under it standing in for the description, and the icon/title are NOT separate boxes.\n\nPLACEMENT RULE — orbital, not gridded: no satellite shares an x or a y with another, none aligns to the centre mark's edges, and the gaps between them are irregular by 12-40px. The composition IS the design; if the satellites snap to a grid this variant has become V2 with extra steps.\n\nRESPONSIVE — the source has NO media queries, no max-width, and no responsive behaviour at all, so this is the one measurement I cannot transcribe and Rod has to decide. Two options to bench: (a) scale the whole 500px field with a single transform so the composition is preserved intact at every width, or (b) let it reflow to a single column below 600px, which destroys the composition but keeps the doors reachable. I recommend benching (a) and treating (b) as the accessibility fallback.",
   "whyDistinct": "The only variant where the doors are neither in reading order nor the same size — hierarchy is carried by position and area instead of by type scale or by sequence. It is also the only one that composes AROUND the Three.js scene rather than sitting on top of it: the irregular gaps between satellites are where lanterns live, which makes it the single variant whose layout has a reason to want the atmosphere layer. And it is the only one that answers 'is the device CSS or artwork' with 'artwork', which makes it the honest extreme of the set.",
   "risk": "(1) The load-bearing caveat: the source is 100% artwork and greyboxing it proves only the composition. A grey rect constellation will look fine and tell Rod almost nothing about whether the finished thing works, because in the original every door's identity lives entirely inside its GIF. Do not let a good blockout here be read as a validated design — this is the one variant where the blockout and the build are different risks. (2) Population problem: Rod has three real doors and the composition wants eight to eleven bodies. Either four-plus satellites get invented (content Rod does not have) or the field reads sparse — that is a content decision, not a layout one, and it should be settled before this is built. (3) The responsive answer does not exist in the source and has to be originated, which is the one place this variant leaves the transcription. (4) Cost: it is by far the most expensive of the four, on a page Rod ranked LAST priority — that tension is real and I am not going to soften it. AI tell: it dodges all four classic ones outright, but it flirts with a different tell, retro-web pastiche, where the irregularity is quoted rather than meant. The defence is that the irregularity has to come from Rod's own drawing, not from randomised offsets — a jittered grid would be exactly the pastiche version. Pillar backing is the strongest in the set: WARM & HAND-MADE backs it directly (the marks must be drawn, there is no CSS shortcut), and OPEN & WONDROUS backs the orbital field and the lantern gaps. This is the most pillar-aligned and the most expensive variant simultaneously, which is the actual decision Rod is being asked to make."
  }
 ],
 "openQuestions": "PROVENANCE — none of the four is Claude-originated; all four are transcriptions (potg.art / ZUTOMAYO / lyra.horse / Space Jam), so they land as Remixed-theirs and leave the <25% Claude-idea budget untouched. They still need rows in `redesign-lab/element-tracker.md` before anything is built, per the provenance law. Two references I deliberately did NOT turn into variants, and why: neal.fun (a 3-column 285:107 grid is meaningless for three doors, and it is 100% per-tile artwork — copying the grid gets nothing unless you can draw 30 tiles) and dimden.dev (its live-content boxes make the root a hub that shows what is behind each door, which directly contradicts Rod's 'the portal is NOT the landing page' — it would have the portal compete with the section landings). Say the word if either should be benched anyway.\n\nBLOCKING QUESTIONS, roughly in the order they gate the build:\n\n1. Is the portal a 2-way or a 3-way chooser? The brief says two-way; the shipping code (`_layouts/portal.html`) has three destinations, with Ramblings in `.portal-extras`. I built all four as 3-way with Ramblings demoted. If Ramblings is being cut from the portal, V1 and V4 get simpler and V2 loses a whole block.\n\n2. Does the portal keep the Three.js ambient scene? V4's composition depends on it, V2's 500px column assumes it fills the empty field, V1 leaves two-thirds of the viewport for it, and V3 makes it pointless. This one answer reorders the whole set, so it is worth settling before you look at geometry.\n\n3. Is glassmorphism being retired site-wide or only here? The current doors are `@include mx.glass` + `$radius-xl` + `breathe-slow`. None of the four variants has a card surface at all. If some bordered surface is allowed to survive, V2 is the variant that changes most.\n\n4. V2 only: may the portal absorb one level of section-nav (Tech Art = [PROJECTS][ABOUT], Game Design = [PROJECTS][BLOGS], matching the real asymmetry in `_includes/section-nav.html` where only game-design carries BLOGS)? That is an information-architecture change, not a layout change, and V2's two-button row is not a fair test without a yes.\n\n5. V4 only: the satellite population. Three real doors, a composition that wants eight-plus bodies. Invent satellites, or accept a sparse field?\n\n6. V4 only: responsive. The source has zero media queries, so this has to be originated rather than transcribed — scale the 500px field with one transform (composition intact) or reflow to a column below 600px (composition destroyed, doors reachable)?\n\n7. V3 only: keep the faithful full-bleed body (~230-character measure at 1920px) or allow a 66ch cap? Toggling it changes what the variant tests, so it should be a labelled A/B rather than a quiet fix.\n\n8. V1 only: it needs an explicit tab-order, visible-focus and prefers-reduced-motion decision before build. The hidden state is opacity:0 + blur(20px), which leaves Door A focusable while invisible, and Door B is revealed only by scroll (WCAG 2.4.3, 2.4.7, 2.1.1). This is a genuine defect risk, not a polish item.\n\nTWO THINGS I VERIFIED THAT MAY SURPRISE YOU: the fireworks toggle does NOT render on the portal — `_includes/topbar.html:91` limits it to layouts home / section-about / section-landing / section-projects, so the portal top bar has two toggles, not three. And the portal nav branch is HOME + ARCHIVES only (`_includes/topbar.html:42-53`). V1's bottom rail and V4's satellite population both assume that two-toggle bar; confirm it stays true.\n\nFiles: live layout `C:/Users/Rod/Documents/ProjectFiles/Website/_layouts/portal.html`, live geometry `C:/Users/Rod/Documents/ProjectFiles/Website/_sass/pages/_portal.scss`, top-bar branch `C:/Users/Rod/Documents/ProjectFiles/Website/_includes/topbar.html`. Verified reference sources in `C:/Users/Rod/AppData/Local/Temp/claude/C--Users-Rod-Documents-ProjectFiles-Website/d1d0c775-c102-4c14-b21e-98f9e13d1b9f/scratchpad/portal/` (potg.css, ztmy.css, neal.css, lyra.css, dimden.css, spacejam.html). No portal blockout file exists yet in `redesign-lab/` — the only blockouts present are `landing-blockout.html` and `layout-blockouts.html`, so this page type starts from scratch."
}