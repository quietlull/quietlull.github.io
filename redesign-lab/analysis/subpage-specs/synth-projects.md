{
 "pageType": "PROJECTS index / work grid (`_layouts/section-projects.html`, served at `/tech-art/projects/` and `/game-design/projects/`, ~12 items). FROZEN SLOT LIST every variant must carry, read from the live layout: (1) page title + description, (2) search field (`#post-search`), (3) filter chips (`_includes/filter-pills.html`), (4) a PINNED set distinguished from the rest (live: separate `.pinned-section` + `.badge-pinned` + `<hr>`), (5) the item grid with thumbnail-or-`<video>` face (`_includes/post-media.html`), title, description, role/engine line, date + read-time, tag badges, WIP badge, hover takeaway overlay, (6) empty state, (7) a \"View all\" terminal action. GREYBOX LANGUAGE for all four, one language only: `redesign-lab/foundations.css` `.ph` box (flat `#0d1430` fill, 45deg 10px/20px hatch at 3% white, 1px border at 10% white, uppercase `data-label`), plus value-only variation (100% / 60% / 40% / 30% of one grey) for state and depth. NO hue anywhere, no gold, no type treatment, no real faces. Shared shell reserved in every variant: sticky `.top-bar` 72px, `picture-frame --open` (1px stroke, inset 20px, 15px at <=768), `.site-footer`, and the Three.js ground behind everything.",
 "variants": [
  {
   "name": "V1 - STICKER PANEL (island of chromed cards)",
   "sourceSite": "MinionsArt Tutorials - https://minionsart.github.io/tutorials/ (read from /tutorials/style.css, the in-page <template id=\"test\">, and Builder.js CreateTypeDiv())",
   "geometry": "PAGE: cards sit on a lighter panel floating on the darker page ground - the only variant where the card has its own background and edge. PANEL: content width 1000px, padding 35px (content-box, so 1070px outer), border-radius 10px, centred, 40px below the top bar. HEAD (inside panel): title box 44px tall, 18px gap, description box 2 lines at 16px/1.55 = 50px, then 24px. SEARCH: 400px block centred > 300px inner > input 200px x 34px, 5px top padding, 24px below the head. FILTER ROW: display:grid, grid-template-columns auto x5 (5-up), width 500px, margin-left/right auto, gap 10px, padding 15px; each chip 28px tall, 14px text, 5px padding, 10px radius, 2px border. GRID: 3 columns (auto auto auto), gap 20px, padding 15px, max-width 1000px -> 3x300 + 2x20 = 940 + 30 padding = 970 inside 1000 (30px slack). CARD: 300px wide, 15px radius, box-shadow 5px 10px rgba(grey) with ZERO blur and ZERO spread - the single load-bearing line of the whole variant. THUMB: 300x160 (15:8), radius 15px 15px 0 0. TITLE BAND: absolute, bottom 0, full width, veil at 60% grey, text 22px/1.0, 5px padding + 10px margin = ~44px band. CHIP CLUSTER: absolute top 0 right 0, floated right, each chip 14px text / 10px line-height / 5px padding / 10px radius / 2px border / 5px margin - tags AND the pinned marker AND the WIP marker all live here, which is how pinned survives without a second grid. DATES: 14px, first at top 5% left 5%, second absolute at top 15% left 5%. BODY: block 300px wide, max-height 100px, margin-top 20px; description 280px wide, 14px/1.0, 10px margin. CARD TOTAL ~300x300, so 12 items = 4 rows x 300 + 3x20 = 1260px of grid. TAKEAWAY OVERLAY: reserved as a full-card 300x300 veil at 70% grey. VIEW ALL: one more chip at filter geometry (200px x 28px, 10px radius, 2px border) centred 20px below the grid. EMPTY STATE: one 300x300 cell with a centred 14px label. PAGE TOTAL ~1650px + shell (about 1.9 screens at 900px).",
   "whyDistinct": "The only variant where a card is an OBJECT - own fill, own edge, own hard offset shadow - and the only one that puts the whole page inside a fixed-width island panel rather than letting the grid touch the viewport. It also collapses pinned/tags/WIP into one chip cluster baked into the thumbnail corner, so it is the only variant with zero metadata below the title band and no separate pinned band at all.",
   "risk": "Uniform 3-up card grid with a drop shadow is a flagged AI tell. Pillar-backed here on one specific condition: the shadow must stay 5px 10px with NO blur, which reads as a sticker on paper (WARM & HAND-MADE, and consistent with the locked paper-morphism-over-glass call). The moment anyone adds a blur radius it becomes material-design and the tell is real. Second risk: 1000px is FIXED, so at 1440+ the page reads as a small island with dead margins, and the panel fill fights the Three.js lantern ground behind it - this is the only variant that puts an opaque plane over the scene. Third: pinned as a corner chip is a genuine downgrade in prominence from today's separate pinned section - it is reshaped, not deleted, but Rod should confirm that is acceptable. Idea origin: theirs (panel, grid, card, shadow, chip cluster, filter row, search block); Claude (routing PINNED and WIP through their existing chip geometry, and the View-all chip placement).",
   "name_note": ""
  },
  {
   "name": "V2 - FRAMELESS SQUARE FIELD (no card at all)",
   "sourceSite": "109ichiki (1:09 Portfolio) Works - https://109ichiki.com/works/ (read from /_astro/style.COBHKi4A.css)",
   "geometry": "ROOT: font-size clamp(14px, 1.1111vw, 16px). PAGE: padding 20px 20px 157.5px; content side padding 56px; 12-column spine, repeat(12, 1fr). HEADING BAND: display flex, justify-content space-between, align-items flex-end, padding 157.5px 56px 60px, closed with a 1px rule. Title box 57px tall on the left; description box max 360px, 15px/1.75 = 2 lines = 52px, on the right. FILTER BLOCK: grid repeat(12,1fr), padding 37.5px 56px. Column 1 = a 15px '(Filter)' label. Columns 2-3 = the search field, 30px tall, radius 100vh, 1px border, 14px side padding (borrowed verbatim from their category pill). Columns 4-12 = the type row: flex, gap 7.5px, 15px items separated by a literal '/' glyph, active state = 1px underline at 3px offset (NOT a pill). 22.5px lower, the category row: pills 30px tall, 0 14px 1px padding, 1px border, radius 100vh, active INVERTS to a solid grey fill. Focus-visible outline 2px at 1px offset. COUNT LINE: 15px uppercase, padding 0 56px, 37.5px below the filter block; the View-all sits at its right end as one more 30px radius-100vh pill. GRID: 37.5px below the count line. 6 columns, stepping 6 -> 5 (1280) -> 4 (1024) -> 3 (768) -> 2 (576). NO gap property anywhere: each cell carries padding 20px, which doubles to a 40px visual gutter and keeps hit targets touching. CELL: aspect-ratio 1, object-fit contain, inner link padding calc(15px * --_square-ratio) (7.5px base at <=768). PINNED: pinned items set that inner padding to 0 so they FILL their square while everything else is inset - the pin is expressed through their own per-item padding lever, no extra markup. CAPTION: fit-content chip, 2px 8px padding, 4px radius, 15px margin-top, centred, 15px type, inverts on hover. TAGS + WIP: a second row of identical chips, gap 6px, 6px below the caption. TAKEAWAY: reserved as a caption-chip-width block that replaces the caption on hover, same 2px/8px/4px geometry. MEASURED AT 1440: usable width 1440 - 40 outer - 112 side = 1288; 1288/6 = 214.7px cell; image box 174.7px after the 20px cell padding; row = 214.7 + 22 caption + 20 tags = ~257px. 12 items = 2 rows = 514px of grid. PAGE TOTAL ~1160px - the shortest of the four by a wide margin. EMPTY STATE: one 214.7px square cell, caption chip reading the empty label.",
   "whyDistinct": "The only variant with NO card whatsoever - no border, no fill, no shadow, no gap property - where the gutter is made of padding inside each cell and the artwork sits directly on the page ground. It is also the only one that filters in two different grammars at once (a text/slash row for type, invert-pills for category) and the only one where the pin is a spacing change rather than a badge or a span.",
   "risk": "Two tells at once. (a) A uniform square grid is the flagged uniform-card-grid tell; it survives only as an exact transcription and only if the contain-fit inset actually varies per item - if every thumbnail ends up the same crop it collapses into a stock gallery. (b) More serious and specific to this site: frameless images floating directly on a LIVE Three.js lantern ground is the exact failure that got `edge-rails` REJECTED on 2026-08-13 ('painted dividers read as stickers over the live scene'). 109ichiki gets away with it because its ground is flat #f3f3ff. Also: 12 items at 6-up is only two rows and ~1160px of page - 109 runs 17 items plus a barcode/frame furniture layer to carry the page, so this variant may simply read thin at Rod's volume, and stepping to their 4-up (1024) breakpoint early is the obvious mitigation. Finally its 20px outer padding collides with picture-frame's 20px inset. Idea origin: theirs (everything structural); Claude (using the per-item padding lever for PIN, and putting search in columns 2-3 - 109 has no search at all, so that placement is the weakest-sourced element here).",
   "name_note": ""
  },
  {
   "name": "V3 - ZERO-GUTTER MOSAIC + RIGHT RAIL",
   "sourceSite": "Kaito Note Works - https://kaitonote.com/works/ (read from /assets/css/style.css + the inline Tailwind block)",
   "geometry": "ROOT: html font-size calc(100 / 1920 * 1vw), so 1rem = 1 design pixel at 1920 and every number below is a design pixel that scales fluidly. PAGE GRID: grid-template-columns 135 / 412 / 412 / 412 / 412 / 135 (= 1918 of 1920). Under 992px it becomes 40 / 224 x4 / 40. TITLE: columns 2-4, margin-top 96 (base) / 160 (md) / 240 (lg), size 76 / 112 / 160, line-height 1.0, tracking -2. Description box 24px under it, 2 lines at 16/1.4 = 45px. FILTER RAIL: column 5 (col 4 at md), margin-left auto margin-right 0, margin-top 160. Search first: 240 x 40 field, 16px text, 24px above group one. Then three labelled groups: label 13-14px, ul margin-top 24, items margin-top 16 at 16px (21px base), active item at full grey and the rest at 60% - groups 40px apart. The 'ALL' item at the head of group one IS the View-all in its resting form; a second 21px View-all line sits 56px below the grid. MOBILE FILTER: a fixed 100x100 round button at inset auto 0 40 0 opening a 540-tall sheet. WORKS GRID: ul spans columns 2-6 = all four content columns = 1648px at 1920. grid-template-columns repeat(3, minmax(0,1fr)) with NO gap - tiles butt edge to edge. Tile = 1648/3 = 549.33 x 312 (their images ship at exactly 549x312). Block margin-top 56 / 104 / 204; padding-bottom 256. PINNED: li gets col-span-2 row-span-2 = 1098.67 x 624, and nothing else - the mosaic breaks rhythm with zero extra markup. This is the ONLY reference of the four that ships a real pinned mechanism. With 12 items and 2 pinned: 2 x 4 cells + 10 x 1 cell = 18 cells = 6 rows x 312 = 1872px of grid. TILE CAPTION: absolute, bottom 16, left 24, right 24. Title 21 (md) / 32 (lg), leading 1.4. Category tag 8px below at 14px, leading 1.3. Role/engine line 6px under that at 14px. WIP marker at the tile's top-left, 24/16 inset, same 14px. REST-STATE VEIL: a flat grey scrim over every tile that clears on hover - this is the blockout stand-in for their three-layer frost (black 20% + #e6e6e6 in mix-blend-mode:color-burn + a white gradient with backdrop-filter blur(12px), with a duplicate <img> above the stack). TAKEAWAY: the veil is where it goes - text at caption geometry, revealed instead of the crisp image. PAGE TOTAL ~2600px at 1920.",
   "whyDistinct": "The only variant that abandons a horizontal control row entirely - filters live in a vertical right-hand rail as three labelled lists, which is precisely what keeps the mosaic uninterrupted. It is also the only near-full-bleed layout (1648px of tiles, no centred column), the only one where tiles TOUCH, and the only one whose pinned state is a real grid span rather than a badge or an inset trick.",
   "risk": "Glassmorphism. Their rest state is literally backdrop-filter: blur(12px) plus mix-blend-mode: color-burn, which is a flagged AI tell AND directly contradicts the locked adoption call (paper-morphism over glass) - so it is NOT pillar-backed and the blockout deliberately renders it as a flat opaque grey veil, deferring the blur decision to the aggregate stage where Rod can kill it. Note also that the gallery entry for Kaito says 'great shader effects'; there is no shader and no canvas on that page, the whole effect is four divs, so nothing here needs GPU budget. Structural risks: the rail costs a whole 412px column that the mosaic never gets back, and at <992px the rail collapses to a fixed 100x100 round button plus a 540px sheet - a floating round button over the live scene is another sticker-over-scene hazard. Finally the calc(100/1920 * 1vw) root means every number scales with the window including type, which is a real commitment and conflicts with any fixed-px component already in the bench. Idea origin: theirs (grid spine, zero-gutter mosaic, col-span-2/row-span-2 pin, rail, veil, mobile sheet); Claude (search as the rail's first item - Kaito has no search - and the terminal View-all line).",
   "name_note": ""
  },
  {
   "name": "V4 - NARROW COLUMN, MOTION-FIRST 2-UP",
   "sourceSite": "TUYU Works - https://tuyu.moe/works/ (read from /css/common202c.css + /css/page202c.css)",
   "geometry": "ROOT font-size 12px; body text 14px. WRAPPER: width 90%, max-width 1000px, margin 0 auto (>=768px); on mobile 90% with no max. HEAD BAND: their #works has no h1 at all - the filter row's own 150px top margin IS the first thing on the page - so the title lives inside that reserved 150px band: padding-top 150px, title box 44px, description box 44px, then 48px to the controls. Mobile: 130px top, 50px bottom. SEARCH: 300 x 32, centred, a 1px BOTTOM RULE only (no box, no radius), 14px text at 3px letter-spacing, 40px above the filter row. FILTER ROW: ul flex, justify-content center, align-items center; each item padding 0 18px, 14px text, 3px letter-spacing, item box 20px tall. Their active state is colour-only (#745bc7 -> #ad57de) which a greybox cannot carry, so it is rendered as the same swap in VALUE: active at 100% grey, resting at 45%. Mobile: 0 12px padding, 11px text, 1px tracking. Then 120px of air to the list (their exact number). LIST: width 86% of the wrapper = 860px max, centred. ul flex + flex-wrap, margin 0 0 -5% -5%; li width 45%, margin 0 0 5% 5% -> 2-up with a 43px gutter and 387px items. CARD: a padding-top 56.25% aspect box (387 x 217.7), flat fill, NO frame, NO radius, NO shadow, NO play badge - the face is the moving image itself, which is what the live site already does via post-media.html's <video autoplay muted loop playsinline>. CAPTION: glued only 5px beneath the box - title 16px/1.4 = 22px; tag + role/engine line 6px below at 12px with 3px tracking = 17px; date + read-time on the same line, right-aligned. Row height = 217.7 + 5 + 22 + 6 + 17 = 268px, row gutter 43px. PINNED: li width 95% instead of 45% - a full-row 817 x 460 embed - reusing the only sizing lever the family has. WIP: 12px/3px-tracking word at the head of the tag line. TAKEAWAY: reserved as a full-cover veil on the aspect box at 70% grey. WITH 2 PINNED + 10 NORMAL: 2 x (460 + 45 caption + 43) + 5 x (268 + 43) = 1096 + 1555 = 2651px of list. VIEW ALL: one centred item at exact filter-row geometry (14px, 3px tracking, 0 18px padding) placed 120px below the list. EMPTY STATE: one 45%-width aspect box with a centred 14px label. PAGE TOTAL ~3100px.",
   "whyDistinct": "The only variant where the card FACE is motion at 16:9 rather than a still in a box, the only one with a narrow centred single-column measure (860px of list inside a 1000px wrapper), the only one with no chips, no pills and no borders anywhere in the controls, and the only one that spends 150px/120px of pure vertical air on a control row that is nothing but centred text.",
   "risk": "Two honest problems. (a) PERFORMANCE: TUYU's distinctiveness is that every card is a live 16:9 embed; 12 of those is a straight hit on the perf hard constraint, and it only works if they are self-hosted muted loops (what post-media.html already emits) rather than 12 iframes - if they degrade to stills, this variant loses the exact thing that makes it a different family. (b) The centred-narrow-column-with-centred-controls silhouette is close to the generic template look, and a 2-up of 16:9 boxes with a caption underneath can read as a video channel page rather than a portfolio. Pillar backing for the motion face is real (ALIVE & PLAYFUL, and it is what the site already ships), but nothing backs the centring itself - it is inherited, not chosen. Also 3100px is the tallest of the four and 12 items become 7 rows, which is a lot of scroll for a page whose whole job is 'not hundreds'. Idea origin: theirs (wrapper, 150/120 rhythm, text filter row and its metrics, 86%/45%/5% list maths, 56.25% aspect box, 5px caption glue); Claude (pinned as li width 95%, the search field's rule-underline placement - TUYU has no search - and the View-all line reusing filter-row metrics).",
   "name_note": ""
  }
 ],
 "openQuestions": "1. PINNED is the weakest link across the board: only Kaito Note ships a real pinned mechanism (col-span-2 row-span-2). V1 demotes it to a corner chip, V2 to a zero-inset cell, V4 to a full-width row. All three are reshapes of the source's own levers rather than deletions, but all three are LESS prominent than today's separate `.pinned-section` with its own h2 and `<hr>`. Rod should say whether losing the pinned band is acceptable before any of these is built.\n2. SEARCH is the least-sourced element in the whole set: three of the four references have no search field at all (only MinionsArt does, at 200px inside a 300px div inside a 400px block). V2/V3/V4's search placements are Claude-originated placements dressed in the source's own component geometry. Per the provenance law that is an ASK, not a decision - or the search input gets cut from those three and only MinionsArt's is kept.\n3. FRAMELESS vs THE LIVE SCENE: V2 (and to a lesser degree V3's mobile round button) puts unframed elements directly over the Three.js lantern ground. That is the exact failure mode that killed `edge-rails` on 2026-08-13. Worth deciding as a site-wide law before judging V2 on its own merits.\n4. ITEM COUNT vs COLUMN COUNT: at ~12 items, V2's 6-up gives only two rows and a ~1160px page, while V4 gives seven rows and a ~3100px page. Neither is wrong, but they answer 'how big should this page feel' oppositely and Rod has not set that number for projects the way he set the landing spacing.\n5. VIEW ALL at 12 items may be redundant - if the grid already shows everything, the control has nothing to reveal. It is carried in all four because the flow is frozen, but it may be a slot that should be renegotiated rather than designed.\n6. picture-frame collision: V2's 20px outer page padding sits exactly on picture-frame's 20px inset. One of the two numbers has to move.\n7. V3's calc(100/1920 * 1vw) root font-size scales ALL type with the window. It is what makes the design-pixel numbers work, but it conflicts with every fixed-px component already in the bench (top-bar, favicon, site-footer) - adopting V3 is adopting that root, not just that grid.\n8. Not built as files - this is the spec pass only. The blockout HTML (one page, four variants on a switcher, `.ph` greybox only, provenance panel per PAGE-PROCESS.md step 2) plus the clean-agent numeric re-check in step 3 are still outstanding."
}