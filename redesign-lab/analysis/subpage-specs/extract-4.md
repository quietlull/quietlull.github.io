{
 "sites": [
  {
   "name": "dimden.dev",
   "url": "https://dimden.dev/",
   "isLive": true,
   "layoutFamily": "fixed 900px two-column (675 content + 225 rail), stacked bordered panels, full-bleed image background behind",
   "distinctiveMove": "The page is a STACK OF IDENTICAL BORDERED PANELS (.box), and one of those panels is a live status terminal: a monospace black panel holding a table of service names each followed by a coloured OK/FAIL span. There is also a tiny 'mood' chip (a 100px bordered pill with inline colour) sitting inline next to the name heading. This is the closest thing in the whole set to Rod's bio + status block, and the 88x31 webtile row is the closest thing to a badge/trophy wall.",
   "measurements": "#container width: 900px, margin 0 auto (hard fixed, not max-width). #main-layout: display flex, row, align-items flex-start. #maintd width 675px, #menutd width 225px, both flex-shrink 0 (675+225 = exactly 900, NO gap). .box: background #000000a8, border 2px solid #383838, border-radius 4px, padding 10px 15px, margin 5px; panels are separated by inline style margin-top:10px. Firefox override bumps .box background to #000000d3. .terminal: background black, font-family 'Perfect DOS VGA 437 Win', color lightgray. #statuses > td width 250px. .status-ok colour #36fda0 with text-shadow 0 0 20px #36fda0; .status-fail #ff5858 with matching shadow. #mood-div: float left, width 110px, position relative, bottom 6px, margin-right 5px; #mood: width 100px, font-size 13px, padding 2px 0, border 1px solid rgb(41,124,92), text-align center. #webtiles-container: display flex, row, gap 10px, align-items flex-start; #webtiles-iframe 250x270px with the same 2px #383838 / 4px radius as .box. Mobile breakpoint at max-width 920px: #container goes width 100% / max-width 900px / padding 0 8px and #main-layout switches to display:block. Body font stack 'RussianGothic', 'MS UI Gothic', 'NEC_APC3', Tahoma with image-rendering: pixelated. Glow is CSS: --body-text-shadow: 0 0 20px #ffffff4a, 0 0 20px #00ffab45; --link-color #2c9080.",
   "howToCopy": "Greybox it as: one 900px fixed centred column, split flex 675 / 225 with zero gap. Fill the 675 side with 5-6 grey rectangles all sharing border 2px + radius 4px + padding 10px 15px, stacked with 10px gaps. Make the 4th rectangle the status block: black fill, monospace, a 2-column table with 250px cells, each row 'label ......... OK' where OK is a single accent-coloured token with a 20px blur glow. Put a 100px x ~20px bordered pill inline at the top-left of the first panel for the status/mood chip. At the bottom of the 675 column, a flex row with 10px gap holding 250x270 boxes for the trophy wall. The 225 rail is just more of the same boxes. Collapse to one column under 920px.",
   "artworkOrCss": "CSS, almost entirely. The panel look (border, radius, translucent black fill), the glow (text-shadow), the status colours and the mood pill are all pure CSS. The only artwork is decorative and separable: the background image (/images/bg.png), the letter-by-letter logo (individual PNGs per letter with negative margins), the moon-icon PNG used as a section bullet, and the 88x31 webtile GIFs. The 88x31 tiles are artwork by definition, but the WALL that holds them is a plain flex row with gap 10px.",
   "confidence": "read-from-source"
  },
  {
   "name": "109ichiki",
   "url": "https://109ichiki.com/profile/",
   "isLive": true,
   "layoutFamily": "12-column CSS grid hero (text in cols 1-4, graphic in cols 5-12) followed by a 1fr 1fr body split by hairline rules; draggable window panels floating over the graphic half",
   "distinctiveMove": "The bio hero puts prose in the LEFT THIRD and hands the right two-thirds to a cluster of four DRAGGABLE WINDOW PANELS, hand-placed at absolute offsets with explicit z-index 1-4 so they overlap like scattered polaroids. Each panel is a double-bordered box with a 2rem title bar that has cursor:grab. This is the single best structural answer in the set to 'trophy case with 29 items' that is not a boring grid: a scatter of small titled windows the user can push around. The body below then splits 1fr 1fr with a 1px border-left separating fact-lists from a decorative grid canvas.",
   "measurements": "Root type base: font-size clamp(14px, 1.1111111111vw, 16px), so every rem below is fluid. --grid-columns: 12 (drops to 6 under 768px). --padding-outside: 20px (15px mobile). --padding-content: 3.5rem (2rem under 1024px, .875rem under 768px). --global-header-height 4.5rem. Type scale: --font-size-small .75rem, --font-size-base-ja .875rem, --font-size-base-en .9375rem, --font-size-h3 1.1875rem, --font-size-medium 1.525rem, --font-size-large 2.1375rem, --font-size-h2 3.5625rem, --font-size-barcode 3rem, --line-height 1.75. Colours: --color-white-1 #fff, --color-white-2 #f3f3ff, --color-white-3 #a4a4a4, --color-black-1 #000, --color-black-2 #101016, --color-black-3 #565656; light theme bg = white-2, text = black-2, border = black-3; [data-theme=dark] swaps to bg black-2 / text white-1 / border white-3. Page: ._page_ padding 20px 20px 9.84375rem (=157.5px at 16px). Hero ._kv_: display grid, grid-template-columns repeat(12, 1fr), padding 9.84375rem var(--padding-content) 6.09375rem (157.5px top / 97.5px bottom), border-bottom 1px solid var(--color-border). ._kvHeading_ grid-column 1/5 grid-row 1. ._kvContent_ grid-column 1/5 grid-row 2. ._kvGraphic_ grid-column 5/-1, grid-row 1/3, display flex, align-items center. ._mainDesc_ margin-top 3.75rem (60px). Body ._body_: display grid, grid-template-columns 1fr 1fr, grid-template-rows auto auto, border-bottom 1px solid border. ._subSection_ grid-column 2, border-left 1px solid border. ._bodyGrid_ grid-column 1, grid-row 2, border-top 1px solid border. ._subContent_ margin-top 2.34375rem (37.5px). ._subHeading_ display flex, align-items center, gap .375rem, text-transform uppercase. Window panels: ._kvDialogWrapper_ position absolute, width 60%, height 100%, inset 0, margin 0 auto, z-index 2, pointer-events none. ._kvDialog_ position absolute, pointer-events auto; nth-of-type(1) 17.5rem x 21.25rem at top -4.375rem / right -8.75rem, z-index 1; (2) 18.125 x 21.25rem at top 13.125rem / right .625rem, z-index 2; (3) 20rem x 18.75rem at top -1.25rem / right 13.75rem, z-index 3; (4) 21.25rem x 23.125rem at top 7.5rem / right 22.5rem, z-index 4. Panel chrome ._root_3fli2_: border 1px solid var(--color-border), border-radius .1875rem; ._inner_ display grid, grid-template-rows 2rem 1fr, border 1px solid var(--color-white-1), border-radius 2px (a genuine DOUBLE border, outer dark + inner white). ._header_ display flex, space-between, padding 0 .5rem 1px .625rem, border-bottom 1px solid border, cursor grab; :active cursor grabbing. [data-blink=true] runs a .1s step-end infinite blink. Stickers: ._sticker1_/._sticker2_ position absolute, width 10.625rem (170px), z-index 1; sticker1 at left 2.5rem / top -3.75rem. Decorative ._kvGrid_ position absolute, width 56vw, height 31.25rem, top 0 right 0. Dashed rule ._line_: height 1px, repeating-linear-gradient to right, border colour 0 to .3125rem then transparent to .625rem. Spacing rhythm is a strict 7.5px unit: 2.34375rem=37.5px, 3.75rem=60px, 6.09375rem=97.5px, 9.84375rem=157.5px. Motion: --ease-out-expo cubic-bezier(.16,1,.3,1), --duration-base-transform .4s, --duration-hover-material 0s.",
   "howToCopy": "Greybox the hero as a 12-column grid with padding 157.5px 56px 97.5px and a 1px bottom rule. Text block spans columns 1-4 only (heading, then bio with margin-top 60px). Columns 5-12 are an empty graphic well. Over that well, absolutely position 4 rectangles at roughly 280x340, 290x340, 320x300, 340x370 px, overlapping, each with z-index 1..4 ascending, each drawn as: outer 1px border radius 3px, inner 1px border radius 2px, and a 32px-tall title bar strip across the top separated by a 1px rule. Below the hero, a 1fr 1fr grid with a 1px border-left down the middle: right half stacks two labelled fact-lists (uppercase heading with a 6px gap icon, list starting 37.5px below), left half is an empty grid canvas with a 1px top border. Use the 7.5px spacing unit for every gap. Set the root font-size to clamp(14px, 1.111vw, 16px) so the whole thing breathes with the viewport.",
   "artworkOrCss": "CSS for everything structural. The double-bordered draggable window, the 32px title bar, the hairline 1px section rules, the dashed divider (repeating-linear-gradient, not an image), the blink animation and the pill buttons (border-radius 100vh, invert-on-hover) are all CSS. Artwork is confined to the CONTENTS of the panels (webp photos inside <picture>) and the two stickers (170px images). The barcode motif Rod likes is NOT artwork either: it is a webfont, 'Libre Barcode 128' from Google Fonts, set at --font-size-barcode 3rem. The decorative grid overlay is a <canvas>, not an image. Drag is real JS: the dialog class grabs [data-js=\"dialog-box-header\"] and the bundle ships GSAP Observer.",
   "confidence": "read-from-source"
  },
  {
   "name": "Mike Klubnika",
   "url": "https://mikeklubnika.com/about",
   "isLive": true,
   "layoutFamily": "single narrow framed column (58em) with a lit vertical gradient inside 1px side rails, bio as plain prose; sibling pages use a tile-grid + preview-panel catalogue",
   "distinctiveMove": "Two things. (1) The whole site is one 58em column with 1px side borders and a vertical gradient that is BLACK at the very top and bottom and lifts to #1a1a1a at 91% - the column reads as a lit strip of paper in a dark room, and the about page is just prose dropped into it with no cards at all. (2) The catalogue component (on / and /games, same stylesheet) is a working trophy case: a wrap of 64x64 tiles, hover scales to 1.3, and the selected tile gets a white drop-shadow glow while a 333x250 preview panel swaps image, name and date from the tile's data- attributes. That is exactly the 'click an achievement, read its detail' interaction, already built in ~60 lines.",
   "measurements": ".docholder: max-width 58em (=928px at 16px), width 100%, border 1px solid rgb(92,92,92) on LEFT AND RIGHT ONLY (border-top-width 0, border-bottom-width 0), background linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(26,26,26,1) 91%, rgba(0,0,0,1) 100%), display flex column, justify-content space-between. .contents: display flex, justify-content center, min-height 100vh. .doc: margin 15px (top and bottom overridden to 10px), display flex column, justify-content flex-end, text-align left. Body: background black, color white, font-family monospace, overflow-x hidden, all margins/padding 0. p colour gainsboro, margin-top 2px, margin-bottom 2px. a colour gainsboro. h1 font-size 27px, font-family 'bank', colour gainsboro, margin .25em top / .75em bottom. h2 font-size 20px, font-family 'bank', margin-top 0, margin-bottom .75em. Catalogue: .catalogue display flex row, min-height 372px. .catalogueentry 64x64px, margin 4px, cursor pointer, transition .15s ease-out; :hover transform scale(1.3). .entryselected filter drop-shadow(0 0 5px rgba(255,255,255,.555)) saturate(175%) brightness(1), transform scale(1.3). .cataloguepreview 333x250px, margin-top 25px, margin-bottom 55px, z-index 2. .cataloguetop display flex wrap, justify-content left, margin-left 45px, margin-top 12px, margin-bottom 30px. .cataloguebottom justify-content right, margin-left 40px, margin-right 7px. .cataloguefeaturedlabel position absolute, transform translate(-35px,-35px), width 475px, z-index -2, pointer-events none. .badge_container display flex, column-gap 2.29%, children 88x31px. Side banners: .banner position absolute, width 100px, height 100%, z-index -1, pointer-events none; .bannerleft transform translateX(-100px) with mask-image linear-gradient(90deg, transparent 25%, ~.3 at 85%, ~.2 at 98%); .bannerright mirrors at 270deg.",
   "howToCopy": "Greybox as ONE centred column, max-width 928px, min-height 100vh, with a 1px left and right rail and nothing on top or bottom. Paint it with a vertical gradient that is pure black at 0% and 100% and #1a1a1a at 91%, so the column glows in the middle and dies at both ends. Inset the content 15px. Set everything monospace, body text at gainsboro not white, h2 at 20px with .75em bottom margin. For the trophy case, drop a flex-wrap row of 64x64 grey squares with 4px margins into that column, plus one 333x250 rectangle to the side as the detail panel; on hover scale a square to 1.3, and give the 'selected' square a 5px white drop-shadow. Locked achievements are trivially the same square at reduced brightness. Optionally hang a 100px-wide strip outside each rail, pushed out by translateX(±100px), faded in with a mask-image gradient.",
   "artworkOrCss": "Mixed, and the split matters. The FRAME is CSS: the 58em width, the 1px side rails, and the vertical gradient that makes the column glow are all CSS, no image. The BANNERS flanking the column are artwork (banner.png) but their soft fade into the dark is CSS (mask-image linear-gradient), so you get the whole effect from one tileable strip. The paper texture behind the doc is artwork (page_bg_2.png, background-position right bottom, no-repeat). In the catalogue, the tiles and preview are artwork (per-item images pulled from data-preview) but every bit of the CASE - the wrap, the 64px cell, the hover scale, the selected glow - is CSS, and the wiring is a tiny vanilla script (gameselect.js) reading data-index / data-name / data-date / data-preview off each tile.",
   "confidence": "read-from-source"
  },
  {
   "name": "potg.art",
   "url": "https://potg.art/about/",
   "isLive": true,
   "layoutFamily": "full-bleed bands on a fixed background gradient; oversized display title, right-aligned narrow intro column, then a wide-margin credits list",
   "distinctiveMove": "An asymmetric bio: the intro text block is capped at 640px but pushed to the RIGHT with margin-left:auto, so it sits against the right edge of a 1580px field while the huge 200px display title anchors the left. Then the credits section swaps to a very wide 320px inline margin, so the page visibly narrows and widens between sections instead of holding one column. The credits rows themselves are a three-part flex baseline row: year, a 70px-wide 1px-tall bar, then names - a spacing device that costs nothing and reads as hand-set.",
   "measurements": "Sizing system: html font-size: calc(100 / var(--base-vw) * 1vw), with --base-vw 1440 at <=1580px, 1100 at <=992px, 390 at <=750px, and font-size pinned to 1px above 1580px. Net effect: 1rem == 1 design pixel. --max-width: 1580px. --margin-1: 40rem, --margin-2: 100rem, --margin-3: 320rem (i.e. 40 / 100 / 320px). Sections compute width as calc(100vw - var(--margin-inline) * 2), max-width 1580px, margin-inline auto. section.first uses --margin-inline: var(--margin-2) (100px). section.my and section.client use --margin-3 (320px). .first__wrap: max-width 640rem, margin-left auto, display flex column, gap 23rem (14rem under 750px). .first__text-en: font-size 22rem, font-weight 500, line-height 1.8, letter-spacing -.02em, font-family Hellix. .my__comments__text: font-size 14rem, font-weight 300, line-height 2, letter-spacing .05em. page-header__title: font-size 200rem (100rem under 750px), font-family Dahlia, font-weight 400, line-height 1. section.kv: margin-top 213rem, height 620rem, position relative, overflow hidden (60rem / 280rem on mobile). section.my: margin-top 200rem, display flex row, gap 40rem. section.client: margin-top 100rem. .client__column: display flex row, gap 20rem, align-items baseline (switches to column on mobile). .client__div-bar: min-width 70rem, height 1rem, background color-mix(in srgb, var(--color-black-100) 40%, transparent 60%), transform translateY(-5rem); display:none on mobile. .client__year font-size 16rem, weight 500, line-height 1. Colours: --color-black-100 #28224D (a violet-navy, not black), --color-white-100 #ffffff. .container position relative, max-width 100svw, min-height 100svh, overflow hidden. .gradients: position fixed, z-index -1, width 100vw, height 100lvh, aspect-ratio 1/1, background-image url(/gradients.png), background-size cover, centred - a fixed background plate behind everything.",
   "howToCopy": "Greybox as a 1580px field. Put a 200px-tall display title at top left. Under it, a 640px-wide text block pushed hard right with margin-left:auto, internal gap 23px, body text at 22px / line-height 1.8. Leave 213px of air, then a full-width 620px-tall image band. Leave 200px, then narrow the field dramatically to 320px side margins for the remaining sections. Credits are baseline-aligned flex rows: 16px year, 20px gap, a 70x1px rule nudged up 5px, 20px gap, names. Note the two-tier margin system (100px for the intro band, 320px for the lower bands) is the whole trick - do not use one container width. If you copy the rem-as-px scheme, set html{font-size: calc(100 / 1440 * 1vw)} and then write plain pixel numbers as rem.",
   "artworkOrCss": "Mostly CSS with one artwork dependency. The asymmetric column, the two-tier margin system, the baseline credit rows and the 70x1px bar (a div with a color-mix background, not an image) are all CSS. The oversized title is a webfont (Dahlia for display, Hellix for text) - type, not lettering artwork. The one real artwork piece is the ambient background wash: .gradients is a fixed full-viewport div painting /gradients.png at background-size cover, so the soft colour field behind the whole site is a bitmap, not a CSS gradient. Worth knowing for Rod: if you want that atmosphere behind a three.js canvas, potg proves a single fixed cover-sized PNG is enough and it costs nothing to composite.",
   "confidence": "read-from-source"
  },
  {
   "name": "melonking.net",
   "url": "https://melonking.net/melon",
   "isLive": true,
   "layoutFamily": "full-viewport flex shell of framed panels (tilted nav rail + main frame + optional side window) with a draggable splitter; content inside is a wall of pill-shaped icon tiles",
   "distinctiveMove": "The trophy wall solves the '29 items would be a giant grid' problem by HIDING THE LABEL UNTIL HOVER: each .bloxBlock is a 26px-tall pill holding just a pixelated 26px icon, with its <span> label set to display:none and revealed to display:inline on hover. So the whole collection collapses to a compact row of coloured pills and only expands where the cursor is. Second move: the nav rail is rotated -2deg with a warm gold drop-shadow, so a plain panel reads as a hand-placed object rather than a UI column.",
   "measurements": "#limiter: height 100%, width 100%, position relative, max-width 1700px, margin auto. #wrapper: display flex, height 100%, width 100%, padding 50px 20px 25px 20px, box-sizing border-box. #nav-outer: height 90%, margin-top 60px, margin-right 10px, transform rotate(-2deg), box-shadow 30px 20px 60px -5px rgba(195,156,22,0.521), padding 16px, z-index 5. #nav: height 100%, width 140px, border none. #mainframe-outer: flex 10 1 auto, border-radius 15px, border 3px double #0000a6, box-shadow 10px 10px 60px -5px rgba(11,67,223,0.54), z-index 10, position relative. #extra-window-outer: width 300px, flex 1 1 auto, transform rotate(1deg), box-shadow 10px 10px 60px -5px rgba(199,232,244,0.594), z-index 5 (display:none by default). .handler (splitter): width 20px, cursor ew-resize, flex 0 0 auto, z-index 30, ::after draws images/knob.png at background-size 40px, 40px wide, centred. #main-forum-thread-badge: position absolute, bottom -10px, right -10px, width 100px, image-rendering pixelated. Trophy pills - .bloxBlock: background-color #1e0e64, padding 4px 4.8px, border 2px dotted #8a5bfe, border-radius 50px, height 26px, display flex row, align-items center, justify-content center, gap 3px, text-align center. .bloxBlock img: height 26px, image-rendering pixelated, display inline-block, vertical-align middle. .bloxBlock span: display none. .bloxBlock:hover span: display inline. NOT FOUND: the .window, .picture-frame-1 and .picture-frame-6 classes used in the markup are not defined in any stylesheet the page loads (/styles/common.css 6.2KB, /styles/flood.css 1.2KB, forum.melonland.net/melonland_common_anims.css, .../melonland_common_fonts.css, or the 3 inline <style> blocks) - I could not read those frame styles, so I am reporting no numbers for them.",
   "howToCopy": "Greybox the trophy case as a flex-wrap row of 26px-tall pills: border-radius 50px, 2px DOTTED border, dark fill, 26px square icon inside, 3px gap, and a label span that is display:none until :hover. Twenty-nine of these wrap into three or four tidy rows and stay quiet until touched; a locked one is the same pill desaturated. For the page shell, greybox a max-width 1700px flex row with padding 50px 20px 25px: a 140px rail rotated -2deg with a warm 60px-blur shadow, a flex:10 main panel with 15px radius and a 3px double border, and a 300px side panel rotated +1deg. Put a 20px ew-resize splitter between them. The -2deg / +1deg tilts plus coloured shadows are the entire 'handmade' read.",
   "artworkOrCss": "CSS for the parts that matter here. The pill shape, the dotted border, the hover-reveal label, the panel tilts, the double border and the coloured drop-shadows are all CSS. Artwork is the tile ICONS (per-item GIF/PNG/SVG, rendered pixelated at 26px) and the splitter knob (knob.png as a background-image) and the corner badge PNG - all decorative payload, none of it load-bearing for the layout. Caveat: the .window / .picture-frame-N frame treatment IS likely artwork (border-image) but I could not find the rules, so treat that as unread rather than assumed.",
   "confidence": "partial"
  },
  {
   "name": "sadgrl.online",
   "url": "https://sadgrl.online/about",
   "isLive": true,
   "layoutFamily": "fixed sticky-rail shell (header + left sidebar + centred prose + optional right metadata rail) driven by named grid areas, with an 800px prose measure",
   "distinctiveMove": "ONE grid shell serves two different page shapes by swapping only grid-template-areas: .withMetadata gives 'sidebar main metadata', .withoutMetadata gives 'sidebar main main'. The about page uses the second, so the prose reclaims the right rail that post pages spend on metadata - the chrome never moves, only the middle changes. The bio itself is split purely by h2 headings ('About this site' / 'About the author' / 'Archives'), which is the cheapest possible version of Rod's intro/more split.",
   "measurements": ":root: --header-height 150px, --sidebar-width 250px, --metadata-width 270px. Colours --black #111, --white #ddd, --accent-green rgb(181,222,181), --accent-purple #d4bfff, --accent-font 'Trona'. .container: height 100vh, display grid, grid-template-columns var(--sidebar-width) 1fr var(--metadata-width), grid-template-rows var(--header-height) 1fr, background var(--black). .withMetadata grid-template-areas: 'header header header' / 'sidebar main metadata' / 'footer footer footer'. .withoutMetadata: 'header header header' / 'sidebar main main' / 'footer footer footer'. .header: grid-area header, position fixed, top/left/right 0, height var(--header-height), background var(--black), z-index 1000. .sidebar: grid-area sidebar, position fixed, top var(--header-height), left 0, bottom 0, width var(--sidebar-width), background-color #111, overflow-y auto, transform translateX(0), transition transform .3s ease (slides off-canvas on mobile via a .sidebar-open toggle class). .main-content: grid-area main, grid-column 1 / -1, width 100%, max-width 800px, min-width 320px, padding 2em, margin 0 auto. .metadata: grid-area metadata, position fixed, top var(--header-height), right 0, bottom 0. Fluid type steps: --size-step-0 clamp(1.0925rem, 0.95rem + 0.25vw, 1.125rem) (~17px), --size-step-1 clamp(1.25rem, 1.15rem + 0.35vw, 1.5rem) (~20-24px), --size-step-2 clamp(1.5rem, 1.3rem + 0.5vw, 1.875rem) (~24-30px). Breakpoint: at max-width 1250px .container collapses to grid-template-columns 1fr.",
   "howToCopy": "Greybox as a CSS grid with named areas: columns 250px / 1fr / 270px, rows 150px / 1fr. Fix the header and both rails with position:fixed so only the middle scrolls. Give the middle a max-width of 800px with 2em padding and margin auto, and note the deliberate grid-column: 1 / -1 override that lets the prose ignore the rail track when the metadata area is unused. Build the About variant by swapping ONE property - grid-template-areas - to let 'main' span the last two columns. Body text at clamp(~17px), headings at the next two clamp steps. Collapse to a single column at 1250px and slide the sidebar out with a transform.",
   "artworkOrCss": "CSS, entirely. There is no artwork doing structural work anywhere on this page - the shell, the rails, the measure and the sidebar slide are all grid and transform. The only images are the site logo (webp, max-width 500px) and a ::before decoration on the sidebar. This is the plainest reference in the set, and that is its value: it isolates the sticky-rail-plus-prose device with nothing else attached, so you can see whether the geometry alone carries the About page before any atmosphere is added.",
   "confidence": "read-from-source"
  }
 ],
 "notes": "METHOD: every URL below was probed with curl (status + effective URL), then its HTML and its actual stylesheets were downloaded and read. Every number reported is copied out of a stylesheet I fetched; where I could not read something I said \"not found\" rather than estimating. Working files are in C:\\Users\\Rod\\AppData\\Local\\Temp\\claude\\C--Users-Rod-Documents-ProjectFiles-Website\\d1d0c775-c102-4c14-b21e-98f9e13d1b9f\\scratchpad\\about\\ (raw .html + .css, plus ichiki.pretty.css and potg.pretty.css which are the minified sheets re-expanded for reading).\n\nSIX SITES REPORTED, all live and all CSS-read. Five are read-from-source; melonking is marked partial because three classes used in its markup (.window, .picture-frame-1, .picture-frame-6) are not defined in any stylesheet the page loads, so I report no numbers for them.\n\nDEAD OR UNREADABLE - do not cite these, and note two are wrong in the shortlist I was handed:\n- https://109ichiki.com/about/ -> 404. The real page is /profile/. (Reported above under the correct URL.)\n- https://kaitonote.com/about/ -> 404. Real page is /profile/, which is live but built in Tailwind, so its layout numbers live in HTML utility classes rather than readable CSS; dropped as weaker evidence than the six above.\n- https://www.cyanilux.com/about/ and /about -> both 404.\n- https://dimden.dev/about -> 404. dimden has no separate About page; the HOME page IS the about page (bio, mood, status, links), which is why it is reported at https://dimden.dev/.\n- https://blog.maximeheckel.com/about -> 404 (it was the anti-reference anyway).\n- https://mateuszurbanowicz.com/about/ -> 302s to an unrelated 2018 fountain-pen post, i.e. no About page. Dropped.\n- https://eveofficial.com/profile/ -> redirects to the site root. No profile page. Dropped.\n- https://tuyu.moe/about/ -> page returns 200, BUT its stylesheets are unreadable at that path: the page links \"css/commonc8ff.css?124\" relative to /about/, and https://tuyu.moe/about/css/commonc8ff.css returns text/html (48405 bytes, an HTTrack-mirrored HTML doc), not CSS. The real sheet exists one level up at https://tuyu.moe/css/commonc8ff.css (text/css, 25879 bytes) but that is the root sheet, not provably the one styling the About page. Per the method, I am reporting NO numbers for tuyu rather than guessing which sheet applies.\n- https://whey-isolate.neocities.org/about -> live and readable, but dropped as a trophy-case reference on the evidence: its badge wall is 3 GIFs and the entire rule is \".badge { text-align: center }\". Too thin to count.\n\nON THE THREE THINGS ROD ACTUALLY NEEDS:\n\n1. Bio split intro/more. Best evidence is sadgrl (split by plain h2 headings inside one 800px measure - the zero-cost version), potg (split by GEOMETRY instead: a 640px block shoved right with margin-left:auto against a 1580px field, so intro and body read as different objects without any card), and 109ichiki (split by GRID: bio in columns 1-4 of 12, everything else in 5-12).\n\n2. Trophy case, 29 items, 5 categories, some locked. Three genuinely different mechanisms, all read from source, none of them a generic card grid:\n   - Klubnika: 64x64 tile wrap + a 333x250 detail panel, tiles carry data-name/data-date/data-preview, hover scale(1.3), selected gets drop-shadow(0 0 5px rgba(255,255,255,.555)). This is the closest match to \"click an achievement, read its detail\" and it is about 60 lines of vanilla JS.\n   - melonking: 26px pill tiles whose LABEL is display:none until :hover. This is the answer to \"29 items would swamp the page\" - the wall stays compact and only expands under the cursor. Locked state is just a desaturated pill.\n   - 109ichiki: four draggable titled windows scattered at absolute offsets with ascending z-index. Best if the trophy case should feel like objects on a desk rather than a grid; scales badly past ~6 items, so it suits the 5 CATEGORIES rather than the 29 achievements.\n   A useful combination falls out of these three: 5 category windows (109ichiki), each holding a wrap of hover-labelled pills (melonking), with a shared detail panel (Klubnika).\n\n3. Status block. Only one real precedent in the set and it is exact: dimden's .box.terminal - a black monospace panel holding a table of 250px cells, each row ending in a single accent token (.status-ok #36fda0 / .status-fail #ff5858, both with a 20px blur text-shadow). He also has the small version, a 100px bordered pill (#mood) sitting inline beside the name heading.\n\nCROSS-CUTTING, RELEVANT TO THE HEAVY THREE.JS SCENE: two of the six put their whole page in front of a full-viewport background plate and neither costs anything - potg uses a fixed 100vw x 100lvh div painting one cover-sized PNG at z-index -1, dimden uses a fixed pixelated background image plus a 0.2-opacity fixed overlay via body:before. Both are proof that the content layer can be plain flow/grid over a full-bleed backdrop without any of the layout having to know the backdrop exists - which is the arrangement Rod needs for water + dock + scroll camera.\n\nARTWORK-VS-CSS SUMMARY (the recurring question): the load-bearing devices in all six are CSS, not artwork. Specifically worth knowing - 109ichiki's barcode motif is a Google webfont (Libre Barcode 128 at 3rem), not an image; Klubnika's glowing column is a CSS linear-gradient, and his flanking banner strips are one tileable PNG faded by a CSS mask-image; melonking's \"handmade\" read is two transforms (rotate(-2deg) and rotate(1deg)) plus coloured box-shadows. The only genuinely irreplaceable artwork across all six is atmospheric background bitmaps (potg's gradients.png, dimden's bg.png) and per-item icons/thumbnails inside the trophy tiles."
}