{
 "pageType": "RAMBLINGS / blog index (post LIST) — greybox blockout, 4 variants. All numbers quoted at a 1440px viewport. FROZEN SLOTS every variant must carry: (1) page title + optional page description, (2) search/filter control, (3) per-post DATE, (4) per-post TITLE, (5) per-post BLURB, (6) per-post TAGS, (7) pinned-post state, (8) optional thumbnail, (9) empty state, (10) shared shell: 72px top-bar reserve, site-footer, picture-frame. SHARED GREYBOX LANGUAGE (identical across all four, lifted from the accepted precedent C:/Users/Rod/Documents/ProjectFiles/Website/redesign-lab/landing-blockout.html:23-28 so the four stay comparable): page #141414; block fill #1b1b1b; block border 1.5px dashed #555; structural rules/hairlines solid #333; the ONE emphatic grey #8a8a8a (landing-blockout.html:90 .fold) is the single stand-in for every source site's accent, so an accent still reads as a distinct element without smuggling hue; labels are mono 9-11px uppercase, #666-#999. NO colour, no radius beyond what a source specifies, no type treatment. Every source accent (#00ddff Cyanilux, #2aa198 dimden, #a40000 Eve, #d84f51 whey-isolate) collapses to #8a8a8a — that collapse is the rule that killed the 2026-08-16 batch and is enforced here. STATUS QUO for comparison, measured from C:/Users/Rod/Documents/ProjectFiles/Website/_layouts/ramblings.html and _sass/layout/_blogspreview.scss:1-160: Bootstrap .container (1320px at xxl) + col-12 px-md-4; .posts-vertical flex column, gap 1rem/16px; each row = 150px image rail (120px <=md, 100px <=sm) + flex text, min-height 120px, border-radius 10px ($radius-lg), @include mx.glass (GLASSMORPHISM, already shipped), card-hover, per-6n breathe-slow keyframe, hover translateX(4px) + img scale(1.05); text stack = h5 title, 120-char truncated blurb, calendar-icon date. Row pitch ~136px. NO TAG SLOT EXISTS TODAY — tags are a new requirement in every variant. Note the status quo is already a near-copy of Cyanilux's /contents/ compact row (image rail left, text right, hover image zoom), which is WHY no variant below is transcribed from Cyanilux: it would be the fifth tweak of the thing already on the site, violating the distinct-families rule. Cyanilux is used instead as the tag-chip donor in variant D and as the honest baseline.",
 "variants": [
  {
   "name": "A — RULE LOG (full-bleed, no container)",
   "sourceSite": "dimden — /blog, https://dimden.dev/blog ; CSS read from https://dimden.dev/css/blog.css",
   "geometry": "NO container and no max-width — the research read blog.css in full and found only `#blog { padding:35px }` constraining the column, so the list is viewport-wide minus 35px (at 1440 = 1370px live measure). Top-bar reserve 72px. HEADER: a 380x100 centred greybox block (dimden's centred 380px-wide pixelated logo) = page-title slot, with a 44px-tall x 520px centred block under it at 12px gap = page-description slot, then 35px air. SEARCH: one full-width 40px-tall greybox band (1.5px dashed #555) inset to the 35px padding, 24px above the first rule — this is a deviation, dimden has no search; the geometry is transcribed instead from the live site's own `.blog-controls` input-group (_layouts/ramblings.html:22-32). PINNED GROUP: pinned posts are the first N rows, closed by a DOUBLE rule (two 4px rules 8px apart) instead of a single one — built from dimden's own hr, doubled; no new device. ROW, repeated 8x: [4px full-width solid #8a8a8a rule, 8px margin above and below (browser hr default .5em)] -> [30px-tall title block, width 62% of column] -> 12px gap -> [16px-tall date block, width 180px] -> 10px gap -> [blurb: 2 lines at 16px/1.4 = 45px tall, width 78% of column] -> 8px gap -> [tag run: 3 blocks each 16px tall x 64/88/72px wide, 12px apart, LEFT-aligned, no chip radius] -> 32px air (dimden's literal <br><br> after the date). Row pitch 195px. Density: 4.6 rows per 900px of scroll. THUMBNAIL: none — dimden's list has no image, so the thumbnail slot is served by a 100x100 greybox square floated right of the title, top-aligned, 24px from the right padding edge, present on only 3 of the 8 rows to prove ragged rows still hold the left edge. EMPTY STATE: one 4px rule + a 120px-tall centred 400px block + one 4px rule. FOOTER: 4px rule, 35px air, 24px-tall centred link row.",
   "whyDistinct": "The only variant with NO container at all — every other one centres a column. Its structural weight lives entirely in a 4px horizontal rule against the page, not in a box, a border, or a card. It is the maximum-air, minimum-furniture end of the spectrum (4.6 rows visible per 900px, the second-loosest) and the only one where the row has no bounding shape whatsoever, so the blurb and tags float free rather than sitting inside anything.",
   "risk": "HONEST CAVEAT: dimden's real list carries only title + date. The blurb slot, the tag run and the search band are ADDITIONS to the transcription — the research note is explicit that this is the layout to argue AGAINST if Rod wants blurbs, because the whole visual weight is the rule and adding two more text lines per row dilutes exactly what makes it work. Treat the 45px blurb and 16px tag run as the thing being tested, not as proven. SECOND CAVEAT: full-bleed collides with the existing `picture-frame` component (fixed, inset 20px, rx 8) — 20px frame inset + 35px page padding = 55px effective gutter, and dimden has no frame, so either the frame goes to `--open` or the 35px shrinks. Rod's call. THIRD: the research flags a CONTRADICTION with the shortlist's '900px column everywhere' claim; the 900px figure is UNVERIFIED for this page and was not used here. AI TELL: none of the four classics (no uniform card grid, no centred popup, no glassmorphism, no gradient-on-dark) — this variant actively deletes the glassmorphism the live site ships. The inverse risk applies instead: so bare it can read as unfinished/unstyled rather than as a deliberate lighter register. Pillar backing is partial — WARM & HAND-MADE backs a raw log over a catalogue, but nothing in the three-axis razor backs bareness for its own sake, and OPEN & WONDROUS is not served by a flat list of rules."
  },
  {
   "name": "B — TERMINAL LEDGER (58em rails, zebra)",
   "sourceSite": "Mike Klubnika — /logs, https://mikeklubnika.com/logs ; CSS read from https://mikeklubnika.com/static/style/main.css",
   "geometry": "Centred column max-width 58em = 928px at a 16px root, width:100%, with 1px #5c5c5c borders on the LEFT AND RIGHT ONLY (border-top-width:0; border-bottom-width:0) — vertical rails, no box. Inner content margin 15px, with 10px top and 10px bottom (.doc). Top-bar reserve 72px. HEADER: h1 block 27px tall; h2 group headings 20px tall with 0.75em (15px) bottom margin. SEARCH: a 28px-tall greybox input spanning the 928px minus 15px inset each side, sitting 15px under h1 — DEVIATION, Klubnika has no search; geometry taken from the live site's own #post-search. ROW (2-line, the one deliberate deviation): line 1 is Klubnika's exact flex line — `display:flex; flex-direction:row; justify-content:space-between; font-size:12px; margin-top:5px; margin-bottom:5px` — LEFT cell = a 96px date block + 8px + a title block 45% of column width, RIGHT cell = a 92px relative-age block ('17 days ago'), the one field no other site in the set has. Line 2, indented 104px to align under the title, = a 12px-tall blurb block at 60% column width, with 2 tag blocks (each 12px x 52px, 8px apart) pushed right by the same space-between. Line gap 4px. Row height 12+4+12 = 28px + 5px/5px margins = 38px pitch. Zebra: every other row gets background rgba(235,235,235,0.0333) spanning BOTH lines, full 898px inner width, no rule between rows. Density: 23.7 rows per 900px — by far the densest of the four, ~5x variant A. SECTION GROUPING: a 1px DOTTED #8a8a8a divider with margin-top 1.25em (20px) / margin-bottom 1em (16px) (.pagespace) between year or month groups; 8 rows shown as 3 groups (3/3/2) to prove the rhythm. PINNED: pinned posts are their own first group above the first dotted divider, headed by a 20px h2 — no new device, just Klubnika's own heading+divider grouping. THUMBNAIL: reserved as a 40x40 greybox square in the LEFT cell before the date on 3 of 8 rows, which pushes the row to 44px — Klubnika has no thumbnail, so this is the slot most at odds with the source and is flagged as such on the page. EMPTY STATE: one dotted divider + a 60px-tall 12px-labelled block. PAGE GROUND: the vertical gradient behind the sheet (.docholder linear-gradient 0deg, black 0% -> #1a1a1a 91% -> black 100%) is CSS and is reproduced in GREY ONLY as a #0f0f0f -> #1b1b1b -> #0f0f0f vertical ramp; the paper texture (resources/page_bg_2.png) and the two 100px masked side banners are ARTWORK and are greyboxed as two 100px-wide dashed placeholder strips outside the rails, labelled ART OWED.",
   "whyDistinct": "The density outlier and the only variant that is a TABLE rather than a feed: two columns with a right-aligned relative-age field, zebra tint instead of a rule, ~23.7 rows per 900px against 4.6-5.7 for the other three. It is also the only one whose row separation is a background fill, not a line — so nothing on the page is a border except the two side rails. It tests whether 'lighter register' means airier (A, C, D) or means MORE POSTS AT ONCE.",
   "risk": "The 2-line row is a real deviation: Klubnika's row is ONE 12px monospace line at ~22px tall and carries no blurb and no tags. Doubling it to 38px halves the density that is the entire reason to pick this family, so if Rod wants blurbs this variant is arguing with itself — the honest test is whether he'd rather drop the blurb here than drop the density. The 40x40 thumbnail is the weakest graft in the whole set. AI TELL: none of the four named tells. The tell it flirts with is a fifth one — the monospace-terminal / 'log printout' skin is a developer-portfolio cliché and reads as machine-cold, which is a direct fight with the WARM & HAND-MADE pillar; ALIVE & PLAYFUL gets nothing from it either. Only OPEN & WONDROUS is arguably served, and weakly. Also 58em is font-size-relative, so it is 928px only at a 16px root — if the redesign's root size moves, the container moves with it, unlike the fixed columns in C."
  },
  {
   "name": "C — HAIRLINE ROWS (1140px fixed, category-word tags)",
   "sourceSite": "Eve — OFFICIAL SITE /news/, https://eveofficial.com/news/ ; CSS read from https://eveofficial.com/css/style.css (36,914 bytes, 1,654 lines)",
   "geometry": "Fixed centred column: `.wf { margin:0 auto; width:1140px }` — a hard 1140px, no percentage, no max-width. Top-bar reserve 72px (Eve's own masthead is 251px absolute + `#wrapper { padding:251px 0 0 }` — NOT copied, our 72px top-bar replaces it). PAGE TITLE BAR: full 1140px band, background #1b1b1b, padding 37px 0, holding a 26px-tall centred label block ~220px wide; page-description = a 20px-tall centred 560px block 12px under it, inside the same band. Then 80px of air (`margin:0 0 80px`). SEARCH: a 40px-tall x 360px greybox input right-aligned to the 1140px column, 24px above the list's top hairline — DEVIATION, Eve has no search; geometry from the live site's own #post-search. LIST: 1px solid #333 hairline on TOP of the list, then each row `padding:30px 0` with a 1px #333 bottom hairline. ROW INTERNALS, all sharing one left edge at x=0: [date line, 16px tall: a 30px x 16px accent block at #8a8a8a (Eve's red category word — THIS IS THE TAG SLOT, and it is the one native solution in the whole set: 1 to 3 tag blocks of 48/64/56px, each with an 8px right margin, sitting inline BEFORE a 140px date block)] -> 15px gap (`.date { margin:0 0 15px }`) -> [title block, 20px font on 28px line-height, width 66% of column = 752px] -> 15px gap (same rhythm, reused) -> [blurb, 2 lines at 16px/24px = 48px, width 72% = 820px]. Row content 16+15+28+15+48 = 122px, + 30/30 padding + 1px = 183px pitch. Density 4.9 rows per 900px. THUMBNAIL ROWS: identical left edge, `padding-right:270px` reserved and a 240x180 greybox positioned absolute top:0 right:0; row min-height 180px so a picture row is 240px pitch. Show 3 of 8 rows with a thumbnail so the constant left edge is visible against ragged right. PINNED: the pinned posts are a separate list with its own top hairline, closed by 60px of air (Eve's pager `margin:60px auto 0`) before the main list's top hairline — no invented device. HOVER (specify, do not colour): a 1px underline grown from `background-size: 0% 1px -> 100% 1px` over .3s under the title block — a background-image gradient, not a border, because a border cannot animate width. PAGER: a 1000px-wide centred band 60px below the last hairline, holding two 18px greybox arrow squares at each end. EMPTY STATE: top hairline, 120px-tall row, bottom hairline. GROUND: flat #141414, no texture — Eve's list has no background image at all.",
   "whyDistinct": "The only variant where the TAG is native to the source rather than grafted on (Eve's inline category word), and the only one that solves the thumbnail by RESERVING space rather than by a rail or a float — every row keeps the same left edge whether or not it has a picture, which is the opposite of the status quo's 150px image rail that shifts all text right. It is also the only variant with a real pager and the only one with a full-width title band. Rhythm is one number reused: 15px between every stacked field, 30px above and below every row.",
   "risk": "1140px is FIXED and Eve ships `<meta name=\"viewport\" content=\"width=1280\">` with `body { min-width:1140px }` — the site is desktop-only by declaration and has NO responsive column. Any mobile behaviour in this variant is invented, so the blockout must show 1140px only and the responsive rule is an OPEN QUESTION, not a spec. Second caveat: the blurb line is an addition (Eve rows are date + title only); it is grafted using Eve's own 15px gap so the rhythm survives, but it is still not in the source. AI TELL: this is the one variant that flirts with a named tell — 'hairline-divided list on near-black with generous padding' is the single most common shape in AI-generated 'minimal blog' output, and 30px/30px + 80px header air is exactly the default-generous spacing that reads as machine-tasteful. Pillar check: OPEN & WONDROUS backs the air, and the animated 0%->100% underline is a genuinely non-default mechanism worth stealing on its own. But WARM & HAND-MADE and ALIVE & PLAYFUL get nothing here — this is the coldest, most corporate-editorial option of the four, and if Rod's read is 'personal register', this is the variant most likely to read as a press-release feed."
  },
  {
   "name": "D — STICKY RAIL SHEET (10vw gutters, 3 bordered panels)",
   "sourceSite": "whey-isolate (Neocities), https://whey-isolate.neocities.org/ ; CSS read from https://whey-isolate.neocities.org/style.css (7,969 bytes). Tag chips borrowed from a SECOND source: Cyanilux, https://www.cyanilux.com/css/style.css?v=12",
   "geometry": "Page gutters `margin-left:10vw; margin-right:10vw` = 144px each side at 1440, so the content band is 1152px; `margin-bottom:24px`. Top-bar reserve 72px. THREE PANELS, all wearing the identical skin: 10px SOLID border in the single greybox emphatic grey #8a8a8a, and a HARD un-blurred offset shadow `-10px 10px 0` at #0d0d0d (whey's rgba(32,36,47,.8) desaturated). LEFT RAIL: width 160px, padding 10px, `position:sticky; top:0; margin-top:-10px`, z-index 3 — holds the section nav as 5 stacked 28px greybox blocks, 8px apart. RIGHT RAIL: identical 160px sticky panel, z-index 1 — holds the SEARCH (one 28px block) plus a tag-filter list of 8 stacked 22px blocks, 6px apart. This is the only variant where search and tag-filter are PERSISTENT and pinned, and it is what those rails are actually for on the source site. CENTRE SHEET: `margin-left:calc(160px + 30px)` and the same on the right = inset 190px each side, `padding:25px`, z-index 2 — inner width 1152-380 = 772px, text measure 772-50 = 722px. ENTRY, repeated 8x: [DATE as a CENTRED heading block, 24px tall x 200px wide — whey's `<h3 id=centertext>` YYYY/MM/DD, and it is the only variant where the date is centred and outranks the title] -> 12px gap -> [paragraph with `text-indent:50px`: line 1 opens with a HEAVY-weight block ~42% of the measure (whey's bolded lede clause — THIS IS THE TITLE SLOT, transcribed, not invented), then the blurb continues as 2 more full-measure lines at 16px/24px, total 72px] -> 8px gap -> [tag chips, BORROWED FROM CYANILUX `.tag`: display:inline-block, border-radius 10px, padding 5px, margin-left 2px, line-height 100% — 3 chips of 56/72/64px at 22px tall, left-aligned at the 50px indent] -> 28px between entries (whey separates entries with a bare <br>; 28px is our reading of it and is flagged as INFERRED, not read). Entry pitch 158px. Density 5.7 rows per 900px. PAGE TITLE: a 32px-tall x 320px centred block at the top of the sheet, 20px above the first date, with a 20px x 480px description block under it. PINNED: the pinned entry keeps the same skin but its 10px border becomes 10px DASHED — a modifier of the source's own border, not a new device. THUMBNAIL: a 240x160 greybox centred inside the sheet directly under the date heading on 3 of 8 entries (the source's own images sit inline in the paragraph flow); it pushes those entries to 330px. EMPTY STATE: the sheet with one centred 24px date-shaped block reading NOTHING YET and a 2-line paragraph. FOOTER: sits OUTSIDE the 10vw gutters (`body:not(footer)`), full width, 24px tall.",
   "whyDistinct": "The only three-column variant, the only one with anything sticky, and the only one where search and tag-filtering are permanently visible instead of a control at the top that scrolls away. It is also the only variant whose entry has NO separate title field — the bold lede clause IS the title, which makes it a genuine diary/changelog register rather than an article index, and the only one that could mix 'new post' with 'updated the bookshelf' in one stream without looking broken. Structurally it is the furthest thing on the list from the status quo's single flex column.",
   "risk": "TWO-SOURCE VARIANT — the tag chips are Cyanilux, not whey-isolate; whey's stream has no tags at all. That is a declared cross-graft, not a hidden one, but it means this variant's provenance is Remixed at best. The 28px inter-entry gap is INFERRED from a bare <br>, not read from CSS — do not report it as measured. The research did not surface a mobile rule for the two sticky rails, so their collapse behaviour below ~900px is UNSPECIFIED and is an open question for Rod, not something to invent; at 1152px of content minus 380px of rails the sheet is already only 772px wide, so this variant degrades earliest of the four. The title-as-bold-clause is the biggest content bet: it makes titles unlinkable as standalone headings and would need the Jekyll loop restructured, and it may simply not survive posts with long titles. AI TELL: the 10px solid border + hard un-blurred offset shadow is neobrutalism, which IS a genuine generated-landing-page tell of the last few years — but here it is backed by a locked call, the impeccable-adoption note's 'paper-morphism over glass', and by the WARM & HAND-MADE pillar (sticker/xerox, not a soft card). It is also the only variant that positively deletes the glassmorphism the live site currently ships, replacing it rather than just removing it. Counter-risk: in pure greybox, three heavy 10px borders plus three offset shadows are the loudest thing on the page and may read as chrome rather than as layout, which is the exact way a blockout can lie about what it will feel like once the palette lands."
  }
 ],
 "openQuestions": "1. WHY NO CYANILUX VARIANT. Cyanilux is the only reference in the set that natively carries all four of Rod's fields on one row (thumb + title + blurb + tag chips + date), so it looks like the obvious pick. It was deliberately NOT made a variant because the live site is already a near-copy of Cyanilux's /contents/ compact row: 150px image rail left + flex text right, 10px radius, hover image scale — see _sass/layout/_blogspreview.scss:75-104 against the research's `.card-compact { display:flex } / .card-image-box { min-width:200px; overflow:hidden } / .card-image-small { height:100% -> 110% on hover }`. Building it would produce a fifth tweak of the status quo and break the distinct-families rule. Cyanilux instead donates the tag chip to variant D and stands as the honest control. ROD'S CALL: do you want it built anyway as an explicit STATUS-QUO CONTROL panel in the same greybox, so the four variants are judged against a same-language baseline rather than against memory of the live page? Recommendation: yes, one control, labelled as such.\n2. THE TAG SLOT IS NEW. No tag renders on this page today (_layouts/ramblings.html has no tag loop; the class .post-tag exists only on the post layout). Of the five references only Cyanilux (coloured chips) and Eve (inline category word) have a native tag mechanism; dimden, Klubnika and whey-isolate have none, so in variants A, B and D the tag run is grafted. Variant C is the only one where tags are native. Is the tag a FILTER (persistent, as in D's right rail) or a LABEL (inline, as in A/B/C)? That single answer changes three of the four variants, and it should be settled before the clean-agent verification pass, not after.\n3. THE BLURB IS THE CONTESTED FIELD. Three of four sources (dimden, Klubnika, Eve) carry NO blurb — their row is date + title. Every blurb in this set is an addition. If the blurb is truly required, variants A and B are arguing against their own reason to exist, and the honest set shrinks toward C and D. Worth asking Rod directly: is the blurb load-bearing on ramblings, or is it a habit carried over from the projects grid?\n4. PICTURE-FRAME CONFLICT. The global picture-frame component is fixed at inset 20px, rx 8. Variant A is full-bleed by definition and variant D uses 10vw gutters — both fight the frame. The element-tracker already notes the assembly uses `--open` (stroke only) because the opaque band crops the scene. Confirm `--open` before these are built, or A and D will be measured against a gutter that will not exist.\n5. NUMBERS I DID NOT READ, flagged so they are not mistaken for measurements: variant D's 28px inter-entry gap (inferred from a bare <br>); the mobile collapse of D's two sticky rails (no rule found); any responsive column for variant C (Eve declares viewport width=1280 and min-width:1140px — it has none). The research also explicitly CONTRADICTS the shortlist's claim that dimden's blog uses a 900px column; blog.css has no such rule and the 900px figure was not used in variant A.\n6. PAGINATION. The live page renders every personal-tagged post with no pager (ramblings.html:36-87). Only Eve has one, so only variant C specifies it (1000px band, 60px above, 18px arrows). If Rod wants a pager on all four, three of them need a source that does not exist yet — ask before inventing one.\n7. DENSITY IS THE REAL AXIS. Rows visible per 900px of scroll: A 4.6, C 4.9, D 5.7, B 23.7. B is the outlier and the only one testing 'lighter register = more at once' rather than 'lighter register = more air'. If Rod reads all four and B is instantly out, the remaining three are separated by CONTAINER (none / 1140 fixed / 3-panel sticky) rather than by density, and a fourth density point may be worth adding."
}