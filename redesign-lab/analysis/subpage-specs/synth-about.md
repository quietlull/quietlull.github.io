{
 "pageType": "ABOUT (`_layouts/section-about.html`, served at /tech-art/about/ and /game-design/about/). Frozen slot list every variant must carry, taken from the layout + `_sass/layout/_aboutmecontainer.scss` + `_javascript/modules/components/achievements.js`: (1) section title h1; (2) portrait slot `.profile-container` (currently `flex:0 0 300px`, collapses via `:empty` because no portrait exists yet); (3) `bio_intro`; (4) `bio_more`; (5) status block (NEW - no equivalent renders today); (6) trophy-case header + subtitle + `.trophy-summary` (mono count + 4px progress bar) + 29 badges across 5 categories (explorer 7, reader 6, interactor 8, secret 3, meta 5) with unlocked / locked / secret states; (7) a per-badge detail affordance; (8) the live three.js ground (water + dock + scroll camera, `three-background-scene.min.js`) plus enough scroll length for the camera move - today that is `.scene-spacer` at the page bottom; (9) shared chrome: 72px fixed top bar, 80px footer, back-to-top, picture-frame `--open`. Status quo being compared against: one centred Bootstrap column of glass `.text-container` panels (25px padding, radius-lg, breathe animation) with the trophy case as a single FLAT `repeat(auto-fill, minmax(135px,1fr))` grid at 10px gap (110px minmax under md) - categories are not expressed at all, and the opaque panel stack sits directly on top of the scene.",
 "variants": [
  {
   "name": "A. PANEL STACK 900",
   "sourceSite": "dimden.dev (https://dimden.dev/ and /about.html) - read-from-source measurements in the brief; Rod's own gallery note for this site is \"stacked bordered panels ... personality through framing rather than typography\"",
   "geometry": "Fixed 900px centred column (`width:900px; margin:0 auto` - hard fixed, NOT max-width), starting y=112 (72px bar + 40px air). Inside: `display:flex; flex-direction:row; align-items:flex-start`, left cell 675px, right rail 225px, both `flex-shrink:0`, ZERO gap (675+225=900 exactly). Every block is the same panel: 2px solid border, 4px radius, padding 10px 15px, stacked with 10px margin-top. LEFT COLUMN, top to bottom: P1 title panel h=64 (title row 34px with a 100x20px bordered pill floated left, `position:relative; bottom:6px; margin-right:5px`, holding the one-line status chip); P2 bio_intro h=140 (6 text rules at 20px pitch + 20px padding); P3 bio_more h=180 (8 rules); P4 STATUS TERMINAL h=152 - darker fill than the panel ground, monospace, a 2-column table with 250px first cells, 6 rows at 22px, each row `label .......... TOKEN` where the right token is a single 40x14 chip (dimden's `#statuses > td{width:250px}` + `.status-ok/.status-fail`); P5 trophy header h=78 (title 24 + subtitle 16 + summary row = mono count + 4px x 480px progress bar); P6-P10 = five CATEGORY panels, one per cat, each = 18px label row + 8px + a badge wall + 20px padding. Badge wall is dimden's `#webtiles-container` at true size: 88x31 tiles, `display:flex; flex-wrap:wrap; gap:10px`, which fits exactly 6 per row in the 645px content width (6x88+5x10=578; 7 would be 676 > 645). So explorer 7 -> 2 rows (h=72), reader 6 -> 1 row (h=31), interactor 8 -> 2 rows (h=72), secret 3 -> 1 row (h=31), meta 5 -> 1 row (h=31). Panel heights 118/77/118/77/77. Locked = same 88x31 tile at lower luminance; secret = same tile with no interior marks. Detail affordance = the tile's own 250x270 expanded box (dimden's `#webtiles-iframe` size, same 2px/4px chrome) opening in the 225 rail. RIGHT RAIL 225px: portrait box 225x225, then a 225x90 progress/count box, then a 225x120 links box, each the same panel with 10px gaps - rail total ~455. Left column total 1191 (664 for P1-P5 incl. gaps, 517 for the five category panels). Page: 72 bar + 40 + 1191 + 40 + 720 scene-spacer band + 80 footer = ~2143 at 1920x1080. Scene budget: the 900px column is opaque, leaving 510px of live water down each side at 1920 and the full 720px spacer band at the bottom. Breakpoint at max-width 920px: column goes `width:100%; max-width:900px; padding:0 8px` and the flex row becomes `display:block` (rail drops under the left column).",
   "whyDistinct": "The only variant where the page is a STACK OF IDENTICAL FRAMES and the achievement categories are expressed as five separate framed panels rather than one grid - the case is furniture, not a gallery. It is also the only variant with a persistent side rail (225px) and the only one where the status block is a multi-row service table rather than a single line. Fixed-px, no fluid root.",
   "risk": "Closest of the four to the CURRENT live page (a centred stack of opaque panels), so it risks being a greyscale re-ship of the status quo - the honest question it asks is whether framing alone is enough of a change. AI tells it flirts with: (a) uniform card grid - the 88x31 wall is strictly uniform, and five near-identical panels compound it; the defence is Rod's own gallery note that a strict grid survives when the ground behind it is atmospheric, plus WARM & HAND-MADE (88x31 webtiles are personal-web furniture, not a component library); (b) nothing backs the panel-on-panel density under OPEN & WONDROUS - this variant occludes the heaviest scene on the site the most of the four, 900px of solid column straight down the middle of the water and dock. If Rod wants the scene to matter, this is the variant that loses."
  },
  {
   "name": "B. SCATTER DESK",
   "sourceSite": "109ichiki (https://109ichiki.com/profile/) - read-from-source; Rod's gallery note: \"oversized PROFILE_ label + bio left, artwork panel floating right on a wireframe grid ... vast empty space below the fold\"",
   "geometry": "Fluid root: `html{font-size:clamp(14px, 1.1111vw, 16px)}`; all px below are at the 16px cap. Page padding 20px 20px 157.5px. HERO band: `display:grid; grid-template-columns:repeat(12,1fr)`, padding 157.5px 56px 97.5px, 1px bottom rule. Title block = grid-column 1/5, row 1. bio_intro = grid-column 1/5, row 2, margin-top 60px, ~6 rules at 28px pitch. Graphic well = grid-column 5/-1, rows 1/3, `display:flex; align-items:center` - left EMPTY as the scene well. Over it, `position:absolute; width:60%; height:100%; inset:0; margin:0 auto; z-index:2; pointer-events:none` wrapper holding FOUR draggable panels at 109ichiki's verbatim sizes and offsets (children get `pointer-events:auto`): (1) PORTRAIT 280x340 at top -70 / right -140, z1; (2) TROPHY MASTER 290x340 at top 210 / right 10, z2; (3) STATUS 320x300 at top -20 / right 220, z3; (4) BIO_MORE 340x370 at top 120 / right 360, z4. Panel chrome is their genuine DOUBLE border: outer 1px border at 3px radius, inner 1px border at 2px radius, inner `display:grid; grid-template-rows:32px 1fr`, the 32px title bar separated by a 1px rule, padding 0 8px 1px 10px, `cursor:grab` / `:active grabbing`. Trophy-master body = the summary row (count + 4px bar) then a 3-column tile grid at 64x64 with 4px margins (cell borrowed and named from Mike Klubnika's `.catalogueentry`; 3x64+2x8=232 inside 264 of usable width), clipped to the panel with the rest reached by drag-scroll. Hero band min-height 560 (the scatter spans top -70 to 490). BODY band under the hero: `display:grid; grid-template-columns:1fr 1fr; grid-template-rows:auto auto`, 1px bottom rule. Right cell (grid-column 2) carries `border-left:1px` and holds the FIVE category blocks stacked - each = an uppercase heading row (`display:flex; align-items:center; gap:6px`) then content at margin-top 37.5px then a row of 64px tiles at 4px margins; at 1440 the half-cell is 644px wide so every category fits one row (8 tiles max per row; interactor's 8 exactly fills it). Category block ~157 tall, five = 785. Left cell (grid-column 1, row 2, `border-top:1px`) is a 500px-tall scene canvas well - 109ichiki puts their decorative `<canvas>` in exactly this cell at width 56vw / height 500px, so the live water goes where their canvas goes. Every gap on the page is a multiple of their 7.5px unit (37.5 / 60 / 97.5 / 157.5). Portrait may alternatively occupy their sticker slot: `position:absolute; width:170px; z-index:1; left:40px; top:-60px`. Page total ~1600 at 1440 wide; scroll length for the camera comes from the 157.5px bottom pad plus the body band. Under 768px `--grid-columns` drops to 6, padding-outside 15px, padding-content 14px, and the scatter must fall back to a stack (their own mobile behaviour).",
   "whyDistinct": "The only variant where the trophy case is a MOVABLE OBJECT rather than a region of the page - a scatter of overlapping titled windows the visitor pushes around, with hand-placed z-index 1-4 instead of a grid. It is also the only asymmetric hero (text confined to 4 of 12 columns) and the only variant that gives the scene a dedicated bordered CELL (bottom-left, 1px top rule) rather than margins or a tail band.",
   "risk": "Two real hazards. (1) The drag interaction collides head-on with the scene's own click-to-ripple on the water - a drag that starts on a panel and ends on the water either eats the ripple or fires it; that is a genuine interaction conflict, not a style note, and it is the reason to prototype this one before believing it. (2) AI tell: floating draggable OS-style windows are a heavily-used trend right now and overlapping panels read as slop the moment the offsets are off by 20px - the offsets here are verbatim, so the tell is only avoided as long as nobody 'tidies' them. ALIVE & PLAYFUL backs the drag; nothing backs the desktop-pastiche framing, so the title bars must not grow close/minimise buttons. Secondary risk: the five category blocks in the right half read as a list, which partly overlaps variant D's move - the 1px border-left and the tile rows are what keep them apart."
  },
  {
   "name": "C. LIT STRIP CATALOGUE",
   "sourceSite": "Mike Klubnika (https://mikeklubnika.com/about, catalogue component shared with / and /games) - read-from-source",
   "geometry": "ONE centred column, `max-width:58em` (928px at 16px), `width:100%`, borders on LEFT AND RIGHT ONLY (1px, `border-top-width:0; border-bottom-width:0`), painted with a vertical MONOCHROME luminance ramp: darkest at 0% and 100%, lifting to the light grey at 91%, so the strip glows in the middle and dies at both ends. Column is `display:flex; flex-direction:column; justify-content:space-between`; its wrapper is `display:flex; justify-content:center; min-height:100vh`. Content inset 15px (10px top and bottom). Type rhythm from their sheet: h1 27px with .25em top / .75em bottom margin, h2 20px with 0 top / .75em bottom, paragraph rules at 2px top/bottom margin. Order down the strip: title 27px -> PORTRAIT block 333x250 (their `.cataloguepreview` box reused as the portrait frame, margin-top 25 / margin-bottom 55) -> bio_intro ~14 rules -> h2 -> bio_more -> STATUS block (named borrow from dimden: monospace 2-column table, 250px first cells, 6 rows at 22px, h=152 - Klubnika's about page has no status device, the whole site is already monospace so the borrow sits flush) -> TROPHY CATALOGUE. The catalogue is their working case: `min-height:372px`, tiles 64x64 with 4px margin (72px pitch), `cursor:pointer`, `transition:.15s ease-out`, `:hover transform:scale(1.3)`, selected = `scale(1.3)` plus a 5px drop-shadow ring; the detail panel is 333x250 and swaps its contents from the tile's data attributes (their `gameselect.js` reads data-index / data-name / data-date / data-preview - about 60 lines of vanilla JS, already written). Categories use their TWO-BAND alternation rather than a grid: band 1 `justify-content:left; margin-left:45px; margin-top:12px; margin-bottom:30px`, band 2 `justify-content:right; margin-left:40px; margin-right:7px`, alternating down five bands. Usable width 898px = 12 tiles per row, so every category is a single row: explorer 7, reader 6, interactor 8, secret 3, meta 5. Band height 124 each (14px label + 8 + 64 tile + 30 bottom + 8 slack), five bands = 632. Detail panel below at 333x250 with 25/55 margins. Locked = the same 64px square at reduced brightness; secret = the same square with its interior blank (both are one filter, no new geometry). Flanking the rails: `position:absolute; width:100px; height:100%; z-index:-1; pointer-events:none` strips pushed out by `translateX(±100px)` with a `mask-image` gradient that fades transparent at 25% to nearly solid at 98% - in this blockout those strips are the SCENE reveal channel, so the water fades in from behind the column edges instead of being covered. Column content ~1790; page = 72 bar + 1790 + 80 footer ~1950 at 1920x1080, with the column's `space-between` pushing a ~720px empty tail to the bottom to give the scroll camera its travel. Everything outside the 928px column at 1920 is 496px of live water on each side, uncovered.",
   "whyDistinct": "The only single-column variant with NO panels at all - bio is bare prose dropped into a lit strip, and the only drawn boundaries on the entire page are two 1px vertical rails. It is also the only one where the case is a select-and-preview catalogue (one tile selected, one detail panel swapping) instead of 29 simultaneously-legible items, and the only one whose page-level device is a light gradient rather than a border.",
   "risk": "AI tell, named plainly: gradient-on-dark. Here it is not decoration bolted on, it IS the transcribed device (their `.docholder` background is exactly this ramp), and the blockout keeps it monochrome so the greybox rule holds - but if it reads as atmosphere-smuggling in review, flatten it to a single grey fill and judge the column geometry alone; that is a one-line change and the variant survives it. Second tell: the 64px hover-scale-1.3 tile wrap is a uniform card grid, defended only by the atmospheric-ground argument and by the two-band alternation breaking the rectangle. Third, honest: Klubnika's whole identity is monospace-everything and a dead-black room, which is a taste import that fights WARM & HAND-MADE - the blockout is monospace anyway (that is the blockout face), so this variant will look more 'right' in greybox than it will in the aggregate. Re-face it before believing it."
  },
  {
   "name": "D. BAND DRIFT",
   "sourceSite": "potg.art (https://potg.art/about/) - read-from-source CSS archived at redesign-lab/sources/potg-typography.md",
   "geometry": "Full-bleed bands over the scene, no container box anywhere; the field is `--max-width:1580px` and the rhythm comes from three inset tokens: `--margin-1:40px`, `--margin-2:100px`, `--margin-3:320px` (dropping to 20 / 50 / 80 at max-width 992px). Fluid root: `html{font-size:calc(100 / 1440 * 1vw)}` so 1rem = 1px at 1440 and every number below scales with the window, pinned to 1px at >=1581px. BAND 1 header: `margin-top:239px`, `width:calc(100vw - 200px)`, `margin-inline:auto`, `max-width:1580px`; title 200px at line-height 1 anchored LEFT (100px under 750px), sub-label 16px at `margin-top:11px`. BAND 2 intro: `max-width:640px; margin-left:auto; display:flex; flex-direction:column; gap:23px` - the reading column is pushed hard against the RIGHT edge of the 1580 field, leaving ~800px of open scene on the left at 1440. Inside it: portrait block 640 wide x 400 tall (640 and the 23px gap are read; the 400 height is DERIVED, flag it), then bio_intro at 22px / line-height 1.8, then a softened secondary paragraph at 16px / line-height 1.7 at ~70% ink. Band 2 total ~792. BAND 3 bio_more: section header is their baseline-aligned pair - `display:flex; flex-direction:row; align-items:baseline; gap:12px`, main 40px, sub 12px - then a `display:flex; flex-direction:column; gap:20px` comment stack at 14px / line-height 2.0, ~284 tall. BAND 4 status: their contact device used verbatim as the status block - `max-width:560px; height:148px; border:1px solid; border-radius:calc(infinity * 1px)`, one word at 120px inside with `padding-left:40px` (mobile 293x64 at 40px). BAND 5 trophy = their client list, which is the whole point of this variant: the band is inset by `--margin-3` 320px on BOTH sides (so at 1440 the content is 800px wide, and the page visibly NARROWS after the wide bands above), header is the same 40px/12px baseline pair, then five rows - one per category - each `display:flex` with a 16px category label, then a hairline divider `min-width:70px; height:1px` at ~40% ink, then the achievement names at 13px / line-height 1.5 wrapping with `gap:20px`. Rows are ~40-60px tall depending on wrap, 20px between rows, band ~380. Locked names sit at the same 40% ink as the divider; secret names render as their placeholder glyph at the same size - no boxes, no tiles, no icons anywhere in this variant. Detail affordance = the row expands in place (adds one 13px line at gap 20px). Page total ~2700 at 1440, bottom padding 157.5px. Scene budget: the ~800px left of the intro column, the 320px channels each side of the trophy band, and the space beside the 560px pill are all live water - the page never draws a filled rectangle over the scene, only text.",
   "whyDistinct": "The only variant where the page WIDTH CHANGES between sections (1580 header -> 640 right-aligned intro -> 800 inset trophy band) instead of holding one column, and the only one where the achievements are a CREDITS LIST - names and hairlines, no tile, no icon, no box. Also the only one that treats the status as a single oversized graphic word rather than a table or chip.",
   "risk": "AI tell it flirts with hardest: 200px display title plus acres of void is the house style of every generated luxury-agency page, and the 560x148 outlined pill at 120px type is a hero-CTA shape. OPEN & WONDROUS genuinely backs the void (this variant occludes the scene least of the four by a wide margin), but nothing in the pillars backs the didone-elegance register - potg's polish is the opposite of WARM & HAND-MADE, so if this wins on layout it must be re-faced in the aggregate or it will read as a stranger's site. Second risk: 29 names with no icons removes the collectible feeling entirely - the trophy case stops looking like a case; that may be the correct restraint or it may kill the feature, and only Rod can call it. Third, mechanical: the whole scale depends on the fluid-rem trick, so any hand-set px in the aggregate will drift out of the system at non-1440 widths. One derived number to flag: the 400px portrait height (the 640px measure and the 23px gap are read from source, the height is not)."
  }
 ],
 "openQuestions": "SHARED CONSTANTS (all four, non-negotiable): the grey language is `landing-blockout.html`'s, reused exactly per `analysis/2026-08-16-subpage-sources.md` - ground #141414, scene block #181818, placeholder #1b1b1b with 1.5px DASHED #555, chrome bar #1f1f1f with 1.5px solid #666 bottom, row-header band #1d1d1d, body text #d9d9d9, box label #666/11px/.14em uppercase, section tag #bbb, measurement note #777/10px right-aligned, scene label #5c5c5c/12px/.2em, and `ui-monospace, \"SF Mono\", Consolas, monospace` as the ONLY face. Dashed 1.5px = placeholder, solid = real structure. No colour, no accent, no second face, in any variant. Shared chrome in all four: 72px fixed top bar, 80px footer, back-to-top, picture-frame in `--open` (stroke-only) so the band does not crop the scene.\n\nOPEN QUESTIONS - these change geometry, so they are worth answering before the clean-agent pass:\n\n1. WHAT IS THE STATUS BLOCK? A multi-row live status (dimden's service table: 6 rows, 250px cells, an OK/FAIL token per row) or a single \"currently\" line? Variants A, B and C all reserve a 152px table; D reserves a 560x148 single-word pill. If it is one line, A/B/C are over-built by ~120px each; if it is six rows, D's pill cannot hold it and D needs a second device.\n\n2. IS THERE A PORTRAIT AT ALL? `.profile-container` is `flex:0 0 300px` with `&:empty{display:none}` - today it collapses, meaning no portrait image exists. A reserves a 225x225 rail box, B a 280x340 window (or a 170px sticker), C a 333x250 frame, D a 640x400 block. If Rod is not drawing one, four blocks come out and the columns all get shorter.\n\n3. DO THE 3 SECRET ACHIEVEMENTS OCCUPY SLOTS? Today they render as visible-but-dimmed (`.secret{opacity:.3}`). If they become hidden until earned, every count drops 29 -> 26 and C's band 4 and A's secret panel change row counts.\n\n4. HOW MUCH SCENE MAY BE COVERED? This is the single biggest divider and it is a Rod call, not a layout call. Measured occlusion at 1920: A covers a 900px opaque column top to bottom (worst), C covers 928px but only with a translucent lit strip and hands the water two 100px masked reveal channels, B covers ~60% of the hero's right half with 4 floating panels but gives the scene a dedicated 500px-tall cell, D never draws a filled rectangle over the scene at all (best). Answering this probably decides the winner on its own.\n\n5. FLUID ROOT OR FIXED PX? B (`clamp(14px, 1.1111vw, 16px)`) and D (`calc(100/1440*1vw)`, capped at 1px >=1581) both scale their entire type and spacing system with the viewport; A (900px hard) and C (58em) do not. This is a site-wide decision that outlives the About page - if it is fixed px, B and D have to be re-specified rather than tuned.\n\n6. DOES THE CASE NEED A DETAIL PANEL? C transcribes a working one (333x250, swapped from data attributes by ~60 lines of vanilla JS, already written by Klubnika); A opens a 250x270 box in the rail; B relies on the panel body; D expands the row in place. A detail panel adds ~330px of vertical space to any variant that adopts it.\n\n7. CATEGORY GROUPING IS NEW. `achievements.js:232` renders the 29 badges FLAT (\"ACHIEVEMENTS array is already ordered by category - render flat\"). All four variants express the 5 categories structurally, which is a reshape of an existing section (allowed) but it is a render-path change, not just CSS - worth confirming Rod wants categories visible before any of this is built.\n\n8. NOT VERIFIED BY ME: the potg.art fetch this session returned no stylesheet, so its numbers come from `redesign-lab/sources/potg-typography.md` (captured 2026-06-11, tier True, verbatim CSS). If potg has been rebuilt since June those numbers are stale - the clean-agent pass should re-measure /about at 1440 before variant D is trusted."
}