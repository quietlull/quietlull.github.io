{
 "sites": [
  {
   "name": "potg.art (index)",
   "url": "https://potg.art/",
   "isLive": true,
   "layoutFamily": "full-viewport fixed art field + two pinned text panels that swap by scroll state (a genuine two-door chooser, doors sequential rather than side-by-side)",
   "distinctiveMove": "The two doors are never on screen together. Door A (logo + 32px blurb + pill button to /about) is fixed to the left edge over the floating artwork; on scroll it blurs out (filter: blur(20px) + opacity 0, 0.5s ease, 0/50/100ms stagger down the stack) and Door B rises: a 290px 'WORK' wordmark that animates translateY(100%) -> 0 from the bottom edge while its own pill fades in. One page, one choice at a time.",
   "measurements": "REAL, read from https://potg.art/_astro/about.DQG1MDvI.css. Unit system: html{font-size:calc(100 / var(--base-vw) * 1vw)} with --base-vw 1440 (desktop) / 1100 (<=992) / 390 (<=750), clamped to font-size:1px above 1581px -- so every rem below equals px at a 1440 viewport. --max-width:1580px. --margin-1:40 / --margin-2:100 / --margin-3:320 (mobile 20/50/80). section.contents: position:fixed, height:100svh, left:var(--margin-2) (=100px); above 1580px left:calc((100vw - 1580px)/2 + 152px). Stack inside: .contents__logo width 280; .contents__en-description 32/1.2, max-width 320, letter-spacing .05em, margin-top 50; .contents__ja-description 12, line-height 1, margin-top 17; .contents__link margin-top 50. Pill .parts-button: width 240, height 30, border-radius calc(infinity*1px), border 1px solid #28224D, label 14 (padding-left 10), arrow svg 18 wide + margin-left 8. Door B .work-nav__title: font-size 290, letter-spacing -.02em, line-height 1, position:fixed bottom:-23, color #fff, z-index:-1; .work-nav__link fixed bottom:32, margin-inline:auto. Scroll scenes: .scroller__item height 100svh each. Header fixed at top:var(--margin-1)=40, width calc(100vw - 80px), max-width 1580, nav items 12px; on index the header logo is opacity:0 above 751px. Mobile (<=750): everything goes static -- contents width calc(100vw - 100px), margin-top 109, margin-bottom 65; art block height 500; work-nav link margin-top 24 / margin-bottom 136. Palette tokens: --color-black-100:#28224D, --color-white-100:#ffffff.",
   "howToCopy": "Greybox it as: fixed left column at x=100px, full 100svh, containing a 280px logo block, 50px gap, a 32px/1.2 line capped at 320px, 17px gap, a 12px line, 50px gap, a 240x30 pill (1px border, fully rounded, 14px label + 18px arrow). Second door = a 290px wordmark clipped by the bottom edge, starting translateY(100%). Wire one state class: adding it blurs+fades the left column (20px blur, 500ms, 0/50/100ms stagger) and slides the wordmark to translateY(0) while a second 240x30 pill fades in 32px off the bottom.",
   "artworkOrCss": "Split, and the split is the lesson: the ATMOSPHERE is artwork (a fixed full-screen section.pickup image layer, plus blurred duplicate images at filter:blur(20rem) opacity .3-.5 used as glow), but every part of the CHOOSER is CSS and live text -- the pills are 1px borders, the giant WORK is real text in the Dahlia webfont, and the door swap is a CSS transform+filter transition. Nothing about the two doors is baked into an image.",
   "confidence": "read-from-source"
  },
  {
   "name": "ZUTOMAYO - MART_BOTH (store chooser)",
   "url": "https://zutomayo.net/mart_both/",
   "isLive": true,
   "layoutFamily": "stacked centred 500px blocks, one block per destination, a pair of buttons inside each block",
   "distinctiveMove": "The purest 'this page exists only to route you' page in the set: two identical blocks (MART, MART for LIVE), each = logo artwork, one caption line, then two side-by-side outline buttons (member / non-member). Identity is carried entirely by the logo image; the choice itself is unstyled bordered rectangles with an icon inset left and an arrow inset right. Doors hover-fill rather than glow.",
   "measurements": "REAL, read from https://zutomayo.net/themes/zutomayo/_assets/css/app.css?26081001. .mart2026_detail: max-width 500px, margin 0 auto, padding 100px 0 (<=768px: 40px 16px 80px). .logo: width 80vw, max-width 300px, margin 0 auto 50px. .cap: text-align center, margin-bottom 20px. .mart2026_detail__wrap: display flex, gap 20px 5px, justify-content center, flex-wrap:wrap only <=768px. .btn_mart: font-size 14px, width 40vw with max-width 340px, height 55px, padding-left 70px, padding-right 40px, border 1px solid #211815, color #211815, display flex align-items center; :before icon 44x32 at left:10px; :after arrow svg 14x14 at right:10px; hover background #636683 + #fff text; <=768px width 90vw. Page ground: background-color #ffffff plus a fixed repeating texture image bg_texture.jpg at background-size 60%. The section below is fenced off by a repeat-x img_border.jpg strip, height 50px, border-top/bottom 1px solid #010. Body font 'Zen Old Mincho', serif.",
   "howToCopy": "Two centred blocks, each max-width 500px with 100px of vertical padding: a 300px-max logo, 50px gap, one centred caption line, 20px gap, then two 340x55 outline buttons side by side with 5px column gap. Reserve 70px of left padding in each button for a ~44x32 icon and 40px right for a 14px arrow. Hover = solid fill, not scale. Behind everything, one fixed repeating paper texture at 60% size.",
   "artworkOrCss": "Both, cleanly separated: the wordmark logos (SVG/PNG), the paper texture, the divider strip and the button icons are all IMAGES; the layout and the doors are plain CSS -- flex row, 1px border, fixed height. The 'warm print' feel comes from a repeating jpg texture, not from any CSS gradient.",
   "confidence": "read-from-source"
  },
  {
   "name": "neal.fun",
   "url": "https://neal.fun/",
   "isLive": true,
   "layoutFamily": "pure chooser grid, no hero, no about text -- the root is nothing but doors",
   "distinctiveMove": "Every door is a bespoke SVG illustration at a fixed aspect ratio, dropped into a completely dumb 3-column grid. The site invests 100% in per-door artwork and 0% in layout invention; the only interaction is a 2.3% hover scale with a soft shadow.",
   "measurements": "REAL, read from the inline <style> blocks in the served HTML (13,166 bytes of CSS total; Nuxt inlines it, there is no external stylesheet). .posts: display grid, grid-template-columns repeat(3,1fr), grid-gap 22px 22px, justify-content center, max-width 1430px, padding 0 25px, width 100%. Breakpoints: <=1280px gap 15px 16px; <=1130px grid-template-columns 1fr 1fr and max-width 900px, gap 15px 18px; <=600px single column, gap 11px, padding 0 8px. Door card .page-link: aspect-ratio 285/107, border-radius 15px, background #e7e7e7 (placeholder under the art), contain:strict, min-width 100%, transition transform .07s linear + box-shadow .07s linear; :hover transform scale(1.023) with box-shadow 3px 6px 6px 0 rgba(0,0,0,.11); :active scale(1.011) with rgba(0,0,0,.15). Page shell .container: min-height 100vh, padding-top 52px, colour #000, RobotoLight. .main-logo row height 64px, gap 6px. .socials flex, gap 12px, margin-top 14px (becomes a 2-col grid on mobile).",
   "howToCopy": "Greybox: a 1430px-max grid, 25px side padding, 3 equal columns, 22px gutters, each cell a 285:107 rounded rectangle (15px radius) filled with its own illustration. Collapse to 2 columns at 1130px (and shrink max-width to 900px), 1 column at 600px. Hover = scale(1.023) + a low soft shadow over 70ms. No section headers, no intro copy above the grid beyond a 64px logo row.",
   "artworkOrCss": "ARTWORK. Each door is literally <a class=\"page-link\"><img src=\"/link-images/<slug>.svg\" alt=\"Wiki Spy\"></a> -- the title, illustration and composition of every tile live inside the SVG. The CSS contributes only a grey box, a radius, and the hover transform. Copying this layout in a greybox gets you nothing unless you can draw 30 tiles.",
   "confidence": "read-from-source"
  },
  {
   "name": "lyra.horse",
   "url": "https://lyra.horse/",
   "isLive": true,
   "layoutFamily": "single unstyled default-flow column (no container, no max-width, no grid)",
   "distinctiveMove": "Hierarchy done purely with type scale and an <hr>: four .bigLink doors at 2em bold, each immediately followed by a <br> and one plain-language sentence saying what is behind the door; everything else on the site is demoted below a horizontal rule into <h3> + <ul> lists. The page makes the choice obvious by making exactly four things big and the other twenty things small.",
   "measurements": "REAL, read from source: the entire page CSS is 367 bytes in one inline <style> and there is NO external stylesheet. The only rules are .bigLink{font-size:2em;font-weight:bold;color:#0078B1}, .underline{text-decoration:underline}, and a .badge block (a{display:inline-block;line-height:0}, img{image-rendering:pixelated}, &:hover img{outline:solid 1px #000}). No container width, no max-width, no font stack, no spacing scale -- default UA styles and full-viewport-width body. Whole document is 14,704 bytes.",
   "howToCopy": "Greybox as: h1, one intro line, <hr>, then four repeats of [2em bold link on its own line + one ~15-word description line beneath it], then <hr> and the demoted lists. The 'design' is the 2em/1em ratio and the rule that nothing else on the page is allowed to be big. Use this as the sanity check on how little a portal actually needs.",
   "artworkOrCss": "Neither, and that is the point -- the doors are raw anchor text at 2em. The only images on the page are 88x31 pixel-art webring badges far below the fold, rendered with image-rendering:pixelated and a 1px black outline on hover.",
   "confidence": "read-from-source"
  },
  {
   "name": "dimden.dev",
   "url": "https://dimden.dev/",
   "isLive": true,
   "layoutFamily": "fixed 900px column, flex-row of bordered boxes (hub, not a two-way split)",
   "distinctiveMove": "Each destination is a live box rather than a link: the Blog box lists recent posts with arrow bullets, Projects, Chat (a working terminal box), Stars, Online. The root shows you what is behind each door instead of naming it, all inside a never-fluid 900px frame under two fixed full-screen PNG overlays.",
   "measurements": "REAL, read from https://dimden.dev/css/main.css?9 (18,447 bytes). #container,#backside-container{width:900px;margin:0 auto} -- fixed, not max-width; on mobile it becomes width:100%;max-width:900px;box-sizing:border-box;padding:0 8px. #main-layout{display:flex;flex-direction:row;flex-wrap:nowrap;align-items:flex-start} and switches to display:block on mobile. .box{background-color:#000000a8;border:2px solid #383838;border-radius:4px;color:#fff;padding:10px 15px;margin:5px} (mobile bg #000000d3). body{margin:0;margin-top:10px;overflow-x:hidden}. body:before = fixed 100%x100% url(/images/overlay.png), opacity .2, z-index 1000, pointer-events none; body:after = the same geometry with /images/overlay2.png at opacity .5. Section headings observed in the served HTML: Welcome to DIMDEN.DEV!, Blog, Projects, Chat, Stars, Online!, plus small .centerbox widgets.",
   "howToCopy": "Greybox: a hard 900px centred column, inside it a flex row of boxes with 5px margins, 10px 15px padding, 2px #383838 borders and 4px radius over a near-black translucent fill. Give the two real doors (Blog, Projects) a heading plus 4-8 live rows of their own content so the box argues for itself. Two fixed full-viewport texture overlays on top of everything at .2 and .5 opacity.",
   "artworkOrCss": "The CRT/atmosphere layer is ARTWORK -- two tiled PNGs (overlay.png, overlay2.png) pinned fixed over the whole viewport, plus a hand-written 100-step @keyframes flickering opacity animation emitted inline in the HTML head. The boxes themselves are plain CSS: one border, one radius, one translucent background. Nothing about the frame is an image.",
   "confidence": "read-from-source"
  },
  {
   "name": "Space Jam (1996, still live)",
   "url": "https://www.spacejam.com/1996/",
   "isLive": true,
   "layoutFamily": "artwork field with no layout system at all (nested <table> + <center>, sizes as HTML attributes)",
   "distinctiveMove": "A portal made of nothing but eleven hand-drawn planet GIFs scattered in orbit around a central logo, each one an <a><img alt=\"...\"> -- the composition IS the drawing. It is the extreme answer to 'is the device CSS or artwork', and it still ships in 2026 at 6.8KB of HTML.",
   "measurements": "REAL, read from source. Total CSS on the page: one rule, .footer-links{margin:5px}. Everything else is HTML attributes: outer <table width=500 border=0>; centre logo img 272x165; doors (w x h) -- Press Box 131x56, Jam Central 55x67, Planet B-Ball 62x62, Lunar Tunes 95x77, The Lineup 63x52, Jump Station 58x52, Junior Jam 49x57, Warner Studio Store 94x72, Stellar Souvenirs 83x83, Site Map 104x67, Behind the Jam 67x63. Ground: <body bgcolor=\"#000000\" background=\"img/bg_stars.gif\" text=\"#ff0000\" link=\"#ff4c4c\" vlink=\"#ff4c4c\">. No max-width, no media queries, no responsive behaviour whatsoever.",
   "howToCopy": "Greybox as an absolutely-positioned constellation inside a ~500px square: one ~270x165 centre mark and 8-11 satellites between 49x52 and 104x83, deliberately unaligned to any grid, over a tiled star background. If Rod wants the two-way portal to feel hand-made rather than laid-out, this is the geometry to copy -- irregular sizes, orbital placement, zero grid.",
   "artworkOrCss": "100% ARTWORK. Every door is a GIF with an alt attribute; the only CSS in the document is a 5px margin on the footer links. There is no device to steal here except the composition itself.",
   "confidence": "read-from-source"
  }
 ],
 "notes": "METHOD: every URL was fetched with curl (raw HTML) and every stylesheet was downloaded and grepped; no number below came from a screenshot or a description. Local copies are in C:\\Users\\Rod\\AppData\\Local\\Temp\\claude\\C--Users-Rod-Documents-ProjectFiles-Website\\d1d0c775-c102-4c14-b21e-98f9e13d1b9f\\scratchpad\\portal\\ (html + extracted css per site) if you want to re-measure.\n\nWHAT COUNTS AS THIS PAGE TYPE: I only kept pages whose whole job is routing. Six qualify, and they fall into three shapes worth naming for the greybox: (a) SEQUENTIAL two-door - potg.art, one door on screen at a time, swapped by a CSS state; (b) BLOCK-PER-DESTINATION - zutomayo mart_both and neal.fun, each door a self-contained unit in a centred stack or dumb grid; (c) TYPOGRAPHIC - lyra.horse, doors are just the only big text on the page.\n\nTHE ARTWORK-VS-CSS ANSWER, which is the recurring trap: it splits almost perfectly by site. neal.fun and Space Jam are pure artwork - copying their layout gives you an empty grid, because the identity of every door lives inside an SVG/GIF. dimden hides its whole atmosphere in two tiled PNG overlays over plain 2px-bordered boxes. zutomayo is a hybrid: logos + paper texture are images, the buttons are 1px borders. potg.art and lyra.horse are the two where the distinctive move is genuinely CSS/type and therefore greyboxable end to end - potg's blur-out/rise-in door swap (blur(20px) + translateY(100%), 500ms, staggered 0/50/100ms) and lyra's 2em-bold-plus-one-sentence hierarchy. If only one thing gets copied for Rod's portal, potg's swap is the one that survives a greybox.\n\nCHECKED AND REJECTED (all live, none is this page type): eveofficial.com (news homepage, not a chooser), harumakigohan.com (WordPress homepage with news/disco/series sections), tuyu.moe (menu + JP/EN gate but a normal homepage), kaitonote.com, unseen.co, 109ichiki.com (landing pages with full nav), sadgrl.online (blog home), kibty.town, heckscaper.com (currently behind an Anubis proof-of-work interstitial - could not read the real page), auberylis.moe (homepage; it does carry an interesting flashy/calm MODE toggle implemented by swapping stylesheets via a `flashy`/`calm` attribute, if a two-way mode switch ever becomes relevant), mikeklubnika.com (homepage with a catalogue widget, not a chooser). minionsart.github.io returned 404 at the root - only /tutorials/ exists.\n\nCAVEAT ON THE SHORTLIST I WAS GIVEN: it was a SUB-PAGE shortlist (post templates, project indexes), not portals - none of those URLs is a portal page. Four of the six sites here (potg.art, zutomayo, dimden, and by association mikeklubnika's scene neighbour lyra.horse, which is linked from mikeklubnika.com's badge wall) are already inside Rod's reference gallery or one hop from it, so provenance ties back to sites he has already tiered. neal.fun and Space Jam are new to the ledger and would need his sign-off before anything is derived from them."
}