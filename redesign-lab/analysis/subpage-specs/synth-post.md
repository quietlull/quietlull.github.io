{
 "pageType": "POST / article page — the SHARED template (project write-ups AND ramblings both use it). Frozen slots that must exist in every variant: header (title/desc/tags/meta/read-time), hero media, TOC, TL;DR, prose, code blocks, takeaway, prev/next, related posts. All four variants are specced at a 1440x900 reference viewport, in the ONE grey language already accepted for `landing-blockout.html` and recorded in `redesign-lab/analysis/2026-08-16-subpage-sources.md`: ground #141414, hero/scene block #181818, placeholder box #1b1b1b + 1.5px dashed #555, bar/chrome #1f1f1f + 1.5px solid #666, row-header band #1d1d1d, body #d9d9d9, box label #666 11px .14em uppercase, note #777 10px right-aligned, face ui-monospace/Consolas ONLY. Dashed 1.5px = placeholder, solid = real structure. No colour, no accent, no second face, no real type treatment anywhere. Global chrome (top-bar, site-footer, picture-frame --open) is the same in all four and is NOT what these variants are testing.",
 "variants": [
  {
   "name": "V·CYANILUX — Contained Card + Heading Bars",
   "sourceSite": "Cyanilux, https://www.cyanilux.com/tutorials/urp-shader-code/ (read-from-source; style.css 13,301 bytes, zero decorative images)",
   "geometry": "LAYOUT FAMILY: one centred rounded card on a flat ground; percentage width with NO max-width, width transitions between breakpoints (transition: width .5s).\n\nCARD: width 70% (= 1008px @1440), margin auto, border 3px solid #666 (their 3px #303030 — border WEIGHT is geometry, keep it), border-radius 20px, padding 30px, background #181818, box-shadow 5px 5px 10px 0 #000. Inner content width = 1008 - 60 = 948px. Breakpoints exactly as theirs: >=1200px -> width 60%; <=1000px -> 85%; <=600px -> 90% AND padding 10px. Header block and footer block track the same widths; header padding 20px.\n\nHEADER (inside card): h1 box 948x56, text-align centre, padding 0 0 20px 0. Desc box 948x40. Meta row = .post-metadata: display flex, align-items centre, justify-content centre, flex-wrap wrap, margin 10px 0, row height 30px — inside it, tag pills at padding 5px, border-radius 10px, margin-left 2px, each ~90x24; then date <time> + 10px spacer div + updated <time> + 5px spacer div (their literal spacer-div construction); read-time as a third 90x24 pill on the same run.\n\nHERO MEDIA: 948 x 533 box (16:9) at margin-top 30px, no border of its own (their images are bare inside the card).\n\nTL;DR: transcribed from their .notice callout — flex row, margin 5px 0, border-radius 10px, border 3px solid #666, fill #1b1b1b, block height ~96px. Left gutter 24px wide drawn as repeating-linear-gradient(45deg, #1b1b1b, #1b1b1b 12px, #232323 12px, #232323 24px), border-radius 6px 0 0 6px, margin-right 10px. Chip: 28x28, border-radius 100%, margin 15px 10px. Text area padding 12px.\n\nTOC: their actual device — a plain nested <ul> under an h3 bar, NOT sticky, NOT a rail. Block 948 wide, 24px indent per level, 26px rows, 8 rows = 208px, plus the 32px h3 bar above it.\n\nSECTION RHYTHM = THE HEADING BARS (the whole point of this variant): every h2/h3/h4 is a filled bar, not typography — padding 7px 20px, border-radius 7px, border-left 10px solid #666, margin-top 30px, fill #1f1f1f. h2 bar 36px tall, h3/h4 32px. Prose between bars: 18px type at 170% line-height = 30.6px line box, paragraph blocks of 4-6 lines (122-184px), 16px between paragraphs. A 6000-word article stays scannable off the bars alone, which is why their TOC can stay cheap.\n\nCODE BLOCKS: 948 wide, padding 20px, overflow auto, fill #101010, min-height 180px, 24px code rows. Inline code chip: 24px tall, padding 3px 7px, border 2px solid #161616, border-radius 10px, display inline-block.\n\nTAKEAWAY: a second .notice callout, identical geometry to the TL;DR, at margin-top 30px after the last prose block.\n\nPREV/NEXT: their .nav pill construction — a flex space-between row of two pills, each border-radius 15px, border 3px solid #666, box-shadow 5px 5px 10px 0 #000, li a padding 5px 20px, pill 140x32, row margin-top 30px.\n\nRELATED POSTS: their .card index cards — 3 full-width stacked cards, each margin-top 20px, border-radius 10px, fill #1e1e1e, .card-text padding 12px, line-height 120%, card height 84px. Block total 3x104 = 312px.\n\nTOTAL ARTICLE HEIGHT (typical write-up, 1440): header 146 + hero 563 + tldr 96 + toc 240 + 5 bar-and-prose sections ~1100 + 2 code blocks 400 + takeaway 96 + prevnext 62 + related 312 + card padding 60 = ~3075px.",
   "whyDistinct": "The only variant where the article has a VISIBLE CONTAINER EDGE — everything else is a bare column on the ground. It is also the only one whose section rhythm comes from filled UI bars rather than whitespace, rules or boxes, which is what lets its TOC stay a cheap inline list instead of a sticky rail. Nearest structural peer to Rod's tech-art posts in the whole set, and the only shortlisted site that delivers the MinionsArt-style rounded-card-plus-tags as an actual POST template rather than an index. Cheapest to steal: 100% CSS, not one decorative image in the layout.",
   "risk": "Flirts with TWO named AI tells at once. (1) Rounded card + soft drop shadow + pill tags on dark is the bootstrap/panel look, and box-shadow 5px 5px 10px is the generic soft-shadow tell. Pillar backing is PARTIAL: WARM & HAND-MADE backs a bordered container only if it reads as paper (paper-morphism over glass, per the impeccable-adoption note) — a glossy radius-20 panel does not qualify on its own, so this needs Rod's call rather than a claimed defence. (2) The heading-bar device reads unmistakably as 'documentation site', which serves the project write-ups but may fight ALIVE & PLAYFUL on the ramblings half of the shared template. Second-order risk: the card's own edge competes with the picture-frame's fixed inset border — two rectangles inside each other. Honest note: at 70% of 1440 the prose runs ~1008px minus padding, well past the 65-75ch prose rule quoted in the rejected-batch README.",
   "confidence": "read-from-source"
  },
  {
   "name": "V·KLUBNIKA — Document Sheet, Dotted Rules",
   "sourceSite": "Mike Klubnika, https://mikeklubnika.com/logs/log_1 (read-from-source; main.css 8,021 bytes)",
   "geometry": "LAYOUT FAMILY: a single narrow document column presented as a physical sheet lit in the middle, flanked by fixed decorative strips. No cards, no grid, no boxes — every division is a rule.\n\nCOLUMN: parent .contents { display:flex; justify-content:center; min-height:100vh }. Sheet max-width 58em (= 928px at 16px root), width 100%. Border 1px solid #5c5c5c with border-top-width:0 and border-bottom-width:0 — LEFT AND RIGHT HAIRLINES ONLY. Background linear-gradient(0deg, #141414 0%, #1e1e1e 91%, #141414 100%) so the sheet dissolves into the ground at both ends and the side rules appear to run off into the dark. display flex, flex-direction column, justify-content space-between, position relative. Inner padding via margin: 15px left/right, 10px top, 10px bottom -> usable width 898px.\n\nHEADER BAND (above the sheet): min-height 35px, max-height 100px, flex row justify-content space-between, margin-left/right 5px, on the same 0deg gradient (#141414 -> #1e1e1e 75% -> #141414). Wordmark left, 35px square logo right.\n\nPOST HEADER — the reshape: no meta BLOCK, only left/right pairs (.lr { display:flex; flex-direction:row; justify-content:space-between }). Row 1: h1 title left (27px, margin-top .25em, margin-bottom .75em = 6.75px / 20.25px) and date paragraph right, one line. Row 2: tags as a plain text run left, read-time right, 22px. Row 3: desc, full width, 2 lines x 22px. Header stack ~112px.\n\nDIVIDER LANGUAGE = THE STRUCTURE: 1px DOTTED #666, never solid, at margin-top 1.25em (20px) / margin-bottom 1em (16px) — 37px of rhythm per boundary. Every single slot boundary in this variant is one of these. Zero borders, zero fills, zero radii anywhere in the article body.\n\nHERO MEDIA: 898 x 505 (16:9), directly under the first dotted rule.\n\nTOC: a dotted-ruled block after the hero, built from their .lr pair — 8 rows x 22px = 176px, each row a leader pair (label left, section number right). Not sticky.\n\nTL;DR: a bare 3-line paragraph block (66px) sandwiched between two dotted rules. No box, no chip, no gutter — the rules ARE the container.\n\nPROSE: paragraph margin-top 2px / margin-bottom 2px (their literal values — the tightest paragraph rhythm of the four). Body 16px monospace, line box 22px. h2 20px with margin-top 0 / margin-bottom .75em. So micro-rhythm is 2px and macro-rhythm is the 37px dotted rule; there is nothing in between.\n\nCODE BLOCKS: 898 wide, padding 15px, fill #101010, hairline 1px solid #5c5c5c on TOP AND BOTTOM only (the sheet's own hairline logic rotated 90deg), min-height 200px, 22px rows.\n\nTAKEAWAY: same bare-paragraph-between-dotted-rules treatment as the TL;DR, immediately before the footer rule.\n\nRELATED POSTS: 3 .lr rows (title left, date right), 24px each = 72px, under one dotted rule.\n\nPREV/NEXT: their .footertext device — flex space-between row on the 0deg gradient band, margin-top .75em (12px), padding-bottom .25em (4px), 32px tall, prev left / next right.\n\nFOOTER: dotted rule at margin-top 2.25em (36px) / margin-bottom 1em, then flex:1 align-self flex-end text-align centre; ellipsis-separated link run with copyright right.\n\nFLANKING STRIPS (default OFF, see risk): .banner position absolute, pointer-events none, z-index -1, width 100px, height 100%, overflow hidden; left strip transform translateX(-100px) with mask-image linear-gradient(90deg, rgba(0,0,0,0) 25%, rgba(1,1,1,.3) 85%, rgba(255,255,255,.2) 98%), right strip the 270deg mirror at right:0 translateX(100px). In greybox they are 1.5px dashed placeholder rails (artwork owed).\n\nSOLE BREAKPOINT in the entire sheet: <=756px -> banners display:none, sheet border:none, width:100%, min-width:100%.\n\nTOTAL ARTICLE HEIGHT (typical): header 112 + rule 37 + hero 505 + rule 37 + toc 176 + rule 37 + tldr 66 + rule 37 + prose ~1150 + 2 code 430 + rule 37 + takeaway 66 + rule 37 + related 72 + prevnext 36 + footer rule 52 = ~2900px.",
   "whyDistinct": "The only variant where the article is a PHYSICAL SHEET with visible left and right edges that fade out top and bottom, and the only one where 100% of the structure is drawn with rules instead of boxes, bars or containers. It has the tightest paragraph rhythm of the four (2px) and the loosest section rhythm (37px), which is the exact inverse of V·CYANILUX. Its header is also the only one collapsed into left/right pairs with no meta block at all — the strongest test of whether Rod's 5-part header (title/desc/tags/meta/read-time) can survive without a dedicated meta row. Geometry and character are cleanly separable here, which is the useful lesson: strip their four raster images (banner.png, page_bg_2.png, the wriksseal) and the layout still reads as a document. That residue IS the greybox.",
   "risk": "The top/bottom gradient wash is literally GRADIENT-ON-DARK, a named AI tell. The defence is real but needs stating: here the gradient is geometry, not decoration — a vertical fade whose job is to dissolve the sheet's edges into the ground, and OPEN & WONDROUS backs a page that fades into the live scene rather than sitting on top of it. Second and larger risk: the flanking 100px strips are functionally identical to the `edge-rails` component Rod REJECTED on 2026-08-13 ('painted dividers read as stickers over the live scene'). They must ship OFF by default with a toggle, or the variant walks into a decision Rod already made. Third: 58em = 928px of monospace prose is ~110ch, well past the 65-75ch rule quoted in `archive/2026-08-16-rejected-subpage-tests/README.md`. Fourth, honest: the 2px paragraph margin is very tight for a rambling — it suits log entries, and Rod's ramblings may not be log entries.",
   "confidence": "read-from-source"
  },
  {
   "name": "V·IQ — 120ch Column + Wrapping Media Wall",
   "sourceSite": "Inigo Quilez, https://iquilezles.org/articles/raymarchingdf/ (read-from-source; style.css 4,487 bytes — the entire site)",
   "geometry": "LAYOUT FAMILY: one very wide column whose measure is set in CHARACTERS, not pixels, with a wrapping percentage-width media wall below the prose. Flex column body, no grid anywhere, no container chrome at all.\n\nCOLUMN: body { display:flex; flex-direction:column; flex-grow:1; min-height:100vh; margin:0; padding:0; font-size:1.125rem (18px); line-height:1.8 }. div#container { width:100%; max-width:120ch; min-height:100vh; padding-top:32px; padding-left:24px; padding-right:24px; margin-left:auto; margin-right:auto }. At 18px sans the measure lands ~1080-1200px @1440 — and because it is expressed in ch it tracks the FONT, so this variant needs no px breakpoint for measure at any width. That is the whole trick.\n\nHEADER: div#header { width:100%; display:flex; flex-direction:column; align-items:center; padding-bottom:3rem (48px) }. Title box 100% x 64, desc box 100% x 36, then a single centred 24px meta run carrying tags + date + read-time inline. Header stack 172px.\n\nHERO MEDIA: .myPicture { max-width:100%; height:auto } -> full container box, 1080 x 608 (16:9), bare, no border.\n\nTL;DR: transcribed from their literal utility-width vocabulary (.part25/.part30/.part40/.part50/.part60/.part70/.part75 — actual classes in their sheet). TL;DR is a .part75 block: 75% of container = 810px, standing alone above the prose, height 90px, no box, no border, just a sub-width run of text. Sub-width-block-in-flow is their device; a boxed callout is not.\n\nTOC — the signature reshape: transcribed from .partsC { display:flex; justify-content:space-around; flex-wrap:wrap; padding:0; margin:0 } with .part { padding-bottom:32px; padding-right:32px } and .part:last-child { padding-right:0 }. The TOC becomes a HORIZONTAL WRAPPING WALL at the top of the article, not a rail and not a list: 6 entries as .part30 blocks (30% = 324px each), 3-up, wrapping to 2 rows, each row 24px text + 32px bottom padding. Block height 112px.\n\nPROSE: 18px / 1.8 = 32.4px line box, paragraph separation one blank line (32px). Measure 120ch — deliberately the WIDEST of the four and the exact opposite of V·KLUBNIKA.\n\nCODE BLOCKS: .code { overflow:auto; white-space:pre; font-size:1em; line-height:1.35 (24.3px); text-align:left; vertical-align:text-top } — no padding, no border, no radius in their sheet. Greybox: full-container 1080 wide, fill #101010, min-height 200px, 24px rows, flush to the measure. Their syntax highlighting is HAND-AUTHORED spans (.codeK/.codeL/.codeS/.codeComment) with no highlighter library at all — in greybox that collapses to ONE flat tint (see open questions).\n\nTAKEAWAY: a .part70 block (70% = 756px) at the end, padding-bottom 64px.\n\nRELATED POSTS = THE MEDIA WALL PROPER: .partsC flex-wrap space-around with their page-level override .part { width:45%; padding-bottom:64px; padding-right:32px }. 4 related posts -> 2-up x 2 rows, each thumbnail 486 x 274 (16:9) + 20px caption + 64px bottom padding. Block height 2 x 358 = 716px. No cards, no borders, no shadows — images and gaps only.\n\nPREV/NEXT: a two-cell .part50 row (50% each = 540px), 24px lines, prev left cell / next right cell, padding-bottom 32px.\n\nFOOTER: full container width, 48px.\n\nTOTAL ARTICLE HEIGHT (typical): 32 top pad + header 172 + hero 608 + tldr 90 + toc 112 + prose ~1250 + 2 code 400 + takeaway 154 + related 716 + prevnext 56 + footer 48 = ~3640px.",
   "whyDistinct": "The only variant with NO container chrome whatsoever and the only one whose measure is font-relative rather than viewport-relative, so it is the only one that needs zero breakpoints for the reading column. It is also the only one that treats TOC and RELATED POSTS as the SAME mechanism — one wrapping percentage wall, reused at two scales (30% for TOC entries, 45% for related tiles) — which is the cheapest possible responsive story in the set: two numbers instead of a component. Widest prose of the four (120ch vs 58em vs 70% vs 1200px), and the only one that gives hero media and related media the same visual weight, which suits a project write-up carrying many process shots.",
   "risk": "120ch is far past the 65-75ch prose rule quoted in `archive/2026-08-16-rejected-subpage-tests/README.md`, and this is the sharpest real conflict in the batch: iq gets away with it because his content is code and formula images rather than paragraphs, which is exactly Rod's tech-art case and exactly NOT his ramblings case — and this is a SHARED template. Rod must rule on it. AI-tell exposure: the wrapping 45% media wall is one step from the uniform card grid. Two defences, both from Rod's own material: his harumaki note says a strict grid survives when the ground behind it is atmospheric (our ground is the live scene, same defence), and the wall items here carry no card, border, radius or shadow at all — images and gaps only, which is also the 109ichiki `works` move he already praised. Separate note for the aggregate, not the blockout: their formula PNGs use mix-blend-mode:screen so black-background images composite as if they had alpha — a genuinely useful trick for shader captures over the scene, but it is a colour decision and must stay out of the greybox.",
   "confidence": "read-from-source"
  },
  {
   "name": "V·DIMDEN — Uncapped Left, No Shell",
   "sourceSite": "dimden, https://dimden.dev/blog/13-optimizing-rendering-of-100k-html-nodes (read-from-source; blog.css 3,404 bytes total)",
   "geometry": "LAYOUT FAMILY: no shell, no card, no centring. Page padding only; CONTENT CAPPED, CHROME UNCAPPED. The article hangs off the left padding edge and the right side is deliberately bare.\n\nSHELL: #blog { padding:35px } and NO max-width on the shell at all. The text block is capped separately by a page-level override: #content { max-width:1200px; display:block } with NO margin auto — so at 1440 the article occupies x=35 to x=1235 and 205px of ground is left empty on the right. That asymmetry IS the layout. Header, rules and footer run the full 1440 minus the 35px padding; only the prose stops at 1200.\n\nHEADER: logo block 380px wide x 60px tall, margin-left auto + margin-right auto — the ONE centred element on the page (their #logo, 380px, and the only thing that gets its own breakpoint). Then h1 at browser-default size (32px). Then a SINGLE meta line, 20px, carrying date + tags + read-time separated by inline dots. Then desc, one line, 20px. Header stack ~150px total. No meta block, no chips, no pills.\n\nRULES: hr { height:4px; border:none } — a 4px SOLID BLOCK, not a hairline and not dotted. Section rhythm = 24px + 4px rule + 24px = 52px.\n\nHERO MEDIA: 1200 x 675 (16:9) under the first hr, left-aligned to the 35px padding edge like everything else.\n\nTOC: transcribed from their footer construction — #footer { display:table } upgraded at @media (min-width:600px) to { display:flex; justify-content:center }, with #footer > div { padding:40px; display:inline-table }. Reused at the TOP of the article as a 3-column TOC block: 3 cells, 40px padding each, each cell an asterisk-prefixed list of 20px rows, block height ~200px. Same device, different slot — a reshape, not an invention.\n\nTL;DR: a bare paragraph with no box and no gutter, marked only by a 4px hr above and below. 3 lines x 22px = 66px.\n\nPROSE: font-size 105% of 16px = 16.8px (their literal !important override), line-height normal ~22px, measure 1200px (~95ch). Paragraph gap 16px.\n\nCODE BLOCKS: highlight.js panel with the background force-flattened to pure black regardless of theme (code.hljs { background-color:#000 !important }) and pre { tab-size:4 }. Greybox: 1200 wide, fill #0d0d0d — the only value in the batch darker than the ground, and it is geometry-neutral — padding 16px, min-height 220px, 22px rows.\n\nTAKEAWAY: identical bare-paragraph-between-4px-hrs treatment as the TL;DR.\n\nRELATED POSTS: their own blog-index device (dimden.dev/blog, in Rod's gallery) — a pure text list with NO THUMBNAILS AT ALL: 5 rows, each a 22px title line plus a 16px 'POSTED ON <date>' sub-line, 38px per row, block 190px. Proof an index does not need cards.\n\nPREV/NEXT: a two-item asterisk list, left-aligned at the padding edge, 20px rows, 40px block.\n\nFOOTER: the 3-cell flex row, centred at >=600px, 40px padding per cell, ~200px tall.\n\nSIGNATURE OVERLAY (default OFF, see risk): two stacked full-viewport layers, body::before and body::after — position:fixed, inset:0, width/height 100%, pointer-events:none, z-index:1000; both animate background-position 0px 0px -> 0px 4px with animation-timing-function: steps(4) so it JUDDERS rather than slides; durations 2s and 0.8s, opacities .2 and .5; the lower layer also carries box-shadow: inset 0 0 10em rgba(0,0,0,.4) as a screen vignette. In greybox the texture is a repeating-linear-gradient standing in for their two raster PNGs — the animation, stacking, opacity, judder and vignette are 100% CSS and free to copy; only the texture is artwork.\n\nBREAKPOINTS — the entire stylesheet has exactly TWO: 600px (footer table -> flex) and 400px (logo width 100%). Nothing else, because the measure is a max-width that simply stops applying below 1200.\n\nTOTAL ARTICLE HEIGHT (typical): 35 pad + header 150 + 52 + hero 675 + 52 + toc 200 + 52 + tldr 66 + 52 + prose ~1100 + 2 code 440 + 52 + takeaway 66 + 52 + related 190 + prevnext 40 + footer 200 = ~3300px.",
   "whyDistinct": "The only variant with NO centring and NO container — the reading column is anchored to the left padding edge and 205px of the right is deliberately dead, while the chrome above and below runs full-bleed past it. It is also the only one whose section structure is a 4px SOLID block rule (versus a bar, a dotted hairline and nothing at all in the other three), the only one that carries a full-viewport animated overlay as the page's entire personality, and the only one whose related-posts slot has no imagery whatsoever. It is the closest of the four to a 'the site never changes width between page types' discipline, which is Rod's own note on this site in the gallery. Cheapest sheet in the set at 3.4KB.",
   "risk": "The overlay is the danger, not an AI tell — it is the opposite of one. Two fixed full-screen layers at z-index 1000 sit ON TOP of Rod's live Three.js lantern scene: it will fight the de-glow principle (the lantern needs night, not a lit film over it), it duplicates the mistake edge-rails was rejected for (a decorative layer reading as a sticker over the scene), and two animated full-viewport composites are a standing per-frame cost against the perf hard constraint. Ship OFF by default with a toggle. Second risk: 1200px at 16.8px is ~95ch, past the 65-75ch rule. Third, and the one Rod should judge with his eyes: the left-anchored asymmetry with a dead right margin reads as 'broken layout' to some viewers — the pillar defence is OPEN & WONDROUS and it is the same defence Rod already accepted in his own gallery note on 109ichiki ('vast empty space below the fold rather than filler sections'), but it is a defence, not a certainty. Fourth: this variant has the weakest header of the four — one meta line has to carry date, tags and read-time, which may not survive a project write-up that also needs engine and role chips.",
   "confidence": "read-from-source"
  }
 ],
 "openQuestions": "1. THE STICKY-RAIL FAMILY IS MISSING, and that is the biggest gap in this batch. All four sourced sites put the TOC in the flow; none has a side rail — yet the LIVE post page today has one (`_layouts/post.html` -> `#panel-wrapper.col-xl-2` holding `related-posts.html` + a `position-sticky` `#toc-wrapper`, plus `#toc-bar`, `#toc-solo-trigger` and `dialog#toc-popup`). So none of these four can be compared against the status quo on its single most distinctive feature. I did NOT free-hand a rail into one of them, because that breaks the provenance law. The sourced candidate already measured in-repo is stripe.dev's blog post (`archive/2026-08-16-rejected-subpage-tests/README.md`, measured @1440 with getComputedStyle): 24-column grid at 58.375px, container 1425px, children on subgrid; sticky metadata rail exactly 6 columns = 350px at top:60px; prose exactly 12 columns = 701px (~78ch); the remaining 6 columns left as bare right margin so the layout is deliberately NOT centred; section rhythm 56px; 1px rules. Rod picked that reference himself. ASK: add it as a fifth variant for the rail family, or drop the rail from the post template entirely?\\n\\n2. PROSE MEASURE CONFLICT — three of four variants blow past the 65-75ch rule (V·KLUBNIKA 58em ~110ch mono, V·IQ 120ch, V·DIMDEN 1200px ~95ch; only V·CYANILUX at <=1000px viewport lands near it). This is not a transcription error, it is what these sites actually do. For a SHARED template carrying both code-heavy write-ups (wide helps — code lines and shader captures want room) and ramblings (wide hurts), which rule wins? Related doc-drift note: memory points at `STYLE.md` in the repo root and it is NOT there — the 65-75ch rule survives only as a quotation inside the rejected-batch README. Worth a /sync-docs pass.\\n\\n3. SHARED-TEMPLATE HEADER STATE. Today the TL;DR block only renders when `page.description` exists AND the tags contain `project`, and the `.post-project-meta` chip row only renders when `page.engine` or `page.role` exists. So the ramblings state of this page has NO TL;DR and NO chip row. Do the blockouts reserve those as always-present boxes, or does each variant need a second 'rambling' state where the header collapses? It materially changes header geometry in V·CYANILUX (the centred meta run loses two pills) and V·KLUBNIKA (row 2 of the left/right pairs empties out entirely).\\n\\n4. TWO FIXED FULL-VIEWPORT LAYERS ARE IN PLAY and both duplicate a decision Rod already made: V·KLUBNIKA's flanking 100px banner strips and V·DIMDEN's two-layer CRT overlay. `edge-rails` and `seam-band` were both REJECTED on 2026-08-13 for exactly this ('painted dividers read as stickers over the live scene'). I have specced both OFF by default with a toggle. Confirm that is what Rod wants, or cut them from the variants outright.\\n\\n5. CODE-BLOCK TINTING. iq hand-authors four token colours with no highlighter library; Cyanilux ships highlight.js 11.9.0 + atom-one-dark; dimden force-flattens the hljs background to pure black. Blockout rule 1 says ONE grey language, so I have specced every code block as a single flat fill with plain grey line-bars and no token differentiation, not even as greys. Confirm — if Rod wants token structure visible in the greybox, that is a deliberate exception and should be written down, since the last batch died for smuggling exactly this kind of thing in.\\n\\n6. REFERENCE VIEWPORT. `landing-blockout.html` is specced at 1920x1080 with exact px; the stripe.dev and brittanychiang measurements were taken at 1440. I have specced all four of these at 1440 so they compare against the measured references. If the sub-page batch must sit next to the landing blockout at 1920, every px number above scales and I should restate them.\\n\\n7. GLOBAL CHROME COVERAGE. Only three bench components are consumed by any of these variants — `top-bar/`, `site-footer/` and `picture-frame/` (in `--open`, since the faithful opaque band crops the lantern scene). Everything else in each variant is a greybox placeholder. Note that `picture-frame`'s fixed inset (20px, 15px at <=768) removes 40px of usable width from EVERY variant above; my numbers assume the frame is on. Confirm, because it changes V·CYANILUX's 70% card and V·DIMDEN's 35px padding edge in particular.\\n\\n8. TOC-AS-FLOW vs TOC-AS-RAIL is really one question about scroll length: these articles blockout at 2900-3640px, i.e. 3-4 screens. An in-flow TOC that scrolls away is fine at that length and is what all four sources do; the current sticky rail exists for much longer pieces. Does Rod actually have posts long enough to need the rail, or was it inherited from Chirpy?"
}