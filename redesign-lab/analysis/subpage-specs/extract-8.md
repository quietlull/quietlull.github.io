{
 "sites": [
  {
   "name": "Brittany Chiang (current v5)",
   "url": "https://brittanychiang.com/",
   "isLive": true,
   "layoutFamily": "sticky half-rail + scrolling half (two-up split, no visible cards)",
   "distinctiveMove": "Hover on any experience row dims ALL its siblings to 50% opacity and lifts the hovered one back to 100%, while a pseudo-layer inset BEYOND the text bounds fades in as the 'card'. The card is not a box in the flow, it is an absolutely-positioned div at -inset-x-6/-inset-y-4, so the reading rhythm stays flush and the card only exists on hover. Dates sit in a fixed 2-of-8 grid column as 12px uppercase tracked mono-ish label; skills are pill tags below the prose.",
   "measurements": "READ FROM /_next/static/css/1205f04d95fac248.css (compiled Tailwind) + page HTML. Outer: mx-auto min-h-screen max-w-screen-xl = max-width 1280px; px-6 (24px) / py-12 (48px), md:px-12 (48px) / md:py-16 (64px), lg:py-0. Breakpoints present: 640/768/1024/1280/1536. Split at >=1024px: `lg:flex lg:justify-between lg:gap-4` (gap 1rem/16px); left = `lg:sticky lg:top-0 lg:max-h-screen lg:w-[48%] lg:py-24` (width 48%, sticky top 0, padding-block 96px); right = `pt-24 lg:w-[52%] lg:py-24` (52%). Section rhythm: mb-16 (64px) -> md:mb-24 (96px) -> lg:mb-36 (144px), scroll-mt-16 (64px) / lg:scroll-mt-24. Each <li class=\"mb-12\"> = 48px between roles. Row grid: `sm:grid-cols-8` = repeat(8,minmax(0,1fr)), `sm:gap-8` (32px) narrowing to `md:gap-4` (16px); date cell col-span-2 (25%), body col-span-6 (75%). Type: name text-4xl (36px/40px) -> sm:text-5xl (48px/1); date text-xs (12px/16px) semibold uppercase tracking-wide; role text-base 16px font-medium leading-tight; blurb mt-2 text-sm (14px/20px); nav label text-xs bold uppercase tracking-widest (letter-spacing .1em). Skill pill: `rounded-full` (9999px) `px-3` (12px) `py-1` (4px) `text-xs` (12px) `leading-5` (20px) on bg-teal-400/10, text-teal-300. PRINT: grep of the compiled CSS returns ZERO `@media print` rules - this page does not survive printing at all; the site links a separate /resume.pdf instead.",
   "howToCopy": "Greybox it as: 1280px max container, 24/48px page padding, then a >=1024px two-column flex at 48% / 52% with a 16px gap, left column position:sticky; top:0; max-height:100vh; padding-block:96px. Right column = list of roles, 48px apart, each an 8-col grid with the date in cols 1-2 and content in cols 3-8, 32px gap collapsing to 16px at md. Section gaps 64 -> 96 -> 144px. Below 1024px the whole thing stacks to one column and the left rail becomes a normal header. For the hover: put `opacity:.5` on every row when the LIST is hovered, `opacity:1` on the row itself, and add an absolutely positioned sibling div at inset -24px/-16px for the hover background. Tags: 12px text in 9999px-radius pills, 12px/4px padding.",
   "artworkOrCss": "Pure CSS. The hover 'card', the sibling dimming, the mouse-follow spotlight (a radial-gradient div at `pointer-events-none fixed inset-0 z-30 lg:absolute` whose position JS writes into a CSS custom property) and the pill tags are all CSS. The only raster assets are project thumbnails (aspect-video, rounded, 2px border) and the korok/TARDIS easter-egg cursor PNGs. Nothing about the timeline device is artwork - it is fully reproducible in a greybox.",
   "confidence": "read-from-source"
  },
  {
   "name": "Zach Leatherman resume",
   "url": "https://www.zachleat.com/resume/",
   "isLive": true,
   "layoutFamily": "single narrow prose column centred inside a 3-track grid, with a site rail that is deleted on print",
   "distinctiveMove": "It ships a PAGE-SPECIFIC stylesheet (/resume/index.css, 4.1KB) whose whole job is print survival. Two separate `@media print` blocks: one collapses the site shell (body -> max-width 55em, grid-template-columns:auto, .rail/.footer/.table-of-contents/.resume-toc display:none, colours forced to #000 on #fff), the other rewrites links so the URL prints next to the text: `.resume-print-link:after { content: \" at zachleat.com\" attr(href); }`. So the same DOM is a web page and a paper CV with no duplicate markup.",
   "measurements": "READ FROM https://www.zachleat.com/resume/index.css and the page's inline <style> (53KB). Prose: `.content-main { font-size: 1.1875em (=19px); line-height: 1.65; color:#525252; padding: 0 .25rem }`. Page grid: `.tmpl-page .content-grid { display:grid; grid-template-columns: 1fr minmax(auto, 31em) 1fr; grid-column-gap: 0; max-width: 56.5217391304em (author's comment: 1300px/23) }` - so the measure is 31em at 19px ~= 589px inside a 1300px frame. Site shell: `.body-grid { display:grid; grid-template-columns: 1fr 14rem 2.5rem; grid-column-gap: 1rem; padding-top: 1rem }` (main / 224px rail / 40px gutter). `.rail { padding: 1em .5em; margin-bottom: 1em; border-bottom: 1px solid #dedede }`. Resume internals: `.resume-list > li { list-style:none; padding: .4em 0 }`; `ul.resume-desc { margin:.4em 0; font-size:0.8421em (16px/19) }`; `.resume-date { font-family: Lato, sans-serif; font-style: italic }`; `.resume-location { display:block; font-size:0.7895em (15px/19); italic }`; `.resume-subnote { font-size:0.6842em (13px/19); color:#666 }`; `.resume-toc { display:flex; gap:.5em 1em; justify-content:center; margin-bottom:-2.33em; font-size:0.789em }`; `.resume-invitation { border-top:1px solid #ddd; padding-top:1em; line-height:165% }`. Breakpoints in the resume CSS: 450px, 500px, 560px, 980px. PRINT: `body { max-width: 55em; margin: 0 auto; grid-template-columns: auto }` + `.tmpl-page .content-grid { grid-template-columns: auto }`.",
   "howToCopy": "Greybox: 1300px outer frame, `grid-template-columns: 1fr minmax(auto, 31em) 1fr` so the prose column is a hard ~590px measure that self-centres, 19px/1.65 body text. Put a 224px right rail and a 40px gutter beside it via `grid-template-columns: 1fr 14rem 2.5rem; gap: 1rem`. Roles are a plain <ul> with list-style:none and .4em vertical padding per item - no date gutter at all, the date is a nested 16px italic line under the role. Then add the print block verbatim: hide rail/footer/TOC, set body max-width:55em, collapse every grid to `grid-template-columns:auto`, force #000/#fff, and use `a:after { content: \" at \" attr(href) }` to print URLs.",
   "artworkOrCss": "CSS for every layout device. The only images are company favicons/avatars pulled inline into the text (60-72px intrinsic, rendered small, from v1.indieweb-avatar.11ty.dev and /img/avatars/*.avif) - that texture IS artwork/data, not CSS, and is the one thing a greybox cannot fake. Everything else (measure, rail, print collapse, URL-expansion on print) is CSS.",
   "confidence": "read-from-source"
  },
  {
   "name": "Ben Hoyt Resume/CV",
   "url": "https://benhoyt.com/cv/",
   "isLive": true,
   "layoutFamily": "single narrow column, warm serif, no chrome (the whole site CSS is ~2.5KB in one inline <style>)",
   "distinctiveMove": "The dates are not a column, not a tag, not a badge - they are the last third of the <h3> heading text itself: `Canonical - Senior Engineer and Manager - Sep 2020 to now`. Zero layout code carries the timeline, which is exactly why it prints perfectly. Warm palette (#301800 ink on white, #e87000 / #c04000 links) with small-caps <h2> section heads, so hierarchy comes from font-variant rather than rules or boxes.",
   "measurements": "READ FROM the single inline <style> in the page. `body { background:white; color:#301800; font-family: Georgia, \"Bitstream Charter\", \"Times New Roman\", serif; font-size:17px; line-height:1.6; word-spacing:0.1em; margin:0 auto; max-width:1000px }`. `#content { margin: 2em auto; width: 92.5%; max-width: 700px }`. `#menu { width:92.5%; height:5.5em; border-bottom:1px solid #d0c8c0 }`, site title 225% uppercase letter-spacing .15em. Headings: `h1 { font-size:200%; letter-spacing:.02em; font-weight:normal }`, `h2 { font-size:160%; font-variant:small-caps; margin:1.8em 0 -0.2em }`, `h3 { font-size:100%; margin:1.5em 0 -0.5em }`. Body rhythm `h1,h2,p,ul,details { margin: 1em 0 }`, `li { margin-left:1.2em; margin-bottom:.5em }`. Links `text-decoration:none; border-bottom:1px dotted` -> solid on hover; a:link #e87000, a:visited #c04000. Responsive: `@media (max-width:750px) { body{font-size:15px} }`, `@media (max-width:615px) { body{font-size:13px} }`. PRINT: `@media print { .no-print{display:none} .print-only{display:block} a:link,a:visited{color:#301800} }` - and a `/cv/ben-hoyt-cv-resume.pdf` link marked .no-print.",
   "howToCopy": "Greybox: one 700px column centred in a 1000px body, 17px Georgia at 1.6 line-height, 92.5% width so there is always breathing room on narrow screens. Section heads at 160% in small-caps with 1.8em top margin and a NEGATIVE -0.2em bottom margin so the following <h3> tucks up under it. Each role = an <h3> whose text is `Company - Role - Start to End`, followed by a plain <ul> of bullets. Ship the two-class print swap (.no-print / .print-only) and flatten link colours to the ink colour in print. Scale the whole page down by dropping the root font-size (17 -> 15 -> 13px) instead of restyling anything.",
   "artworkOrCss": "100% CSS, and barely any of it. There is no artwork on this page at all - no icons, no avatars, no tags, no rules other than one 1px border under the menu. This is the cheapest of the six to reproduce and the only one whose printed output is guaranteed correct by construction.",
   "confidence": "read-from-source"
  },
  {
   "name": "Tania Rascia resume",
   "url": "https://tania.dev/resume/",
   "isLive": true,
   "layoutFamily": "fixed sticky left sidebar + centred prose column (app-shell doc layout)",
   "distinctiveMove": "The resume is just rendered markdown dropped into the site's normal doc shell, so the CV inherits the sidebar, the mono type scale and the 8px card radius for free - the 'resume page' costs one extra template. Only two things are page-specific: a `.resume-title` subtitle line (1.25rem muted) under the h1, and `#article-resume .resume-title + p` at .95rem for the contact line. On the home page the same site uses a 90px fixed date gutter (`ul.hero-eras span.era-dates { min-width:90px; width:90px }`) which is the reusable timeline device.",
   "measurements": "READ FROM https://tania.dev/styles.b1cb92fe3f8958ef5c2a.css (62KB). Custom props: `--content-width:760px; --sidebar-width:260px; --post-sidebar-width:260px; --border-radius:8px; --layout-padding-vertical:1.5rem; --layout-padding-horizontal:1.5rem` rising to `2rem` at the wide breakpoint; `--main-layout-width: calc(260px + 760px)` = 1020px. `.sidebar { min-width:var(--sidebar-width); width:260px; height:100vh; position:sticky; top:0; overflow-y:auto; border-right:1px solid var(--color-border); display:none }` (shown only at >=1020px). `.main-content { flex-grow:1; margin:0 auto; max-width:760px; padding: 1.5rem 1.5rem }`; `.main-content.page { max-width: calc(760px + 260px) = 1020px; margin-left:1.5rem }`. Type: `.hero.hero-page h1 { font-size:2.5rem; font-weight:700 }`, `#article-resume .resume-title { font-size:1.25rem; color: muted }`, `.resume-title + p { font-size:.95rem }`, `.sidebar-section h2 { font-size:1rem; font-family: \"Google Sans Code\", monospace }`, `.sidebar-menu a { font-size:14px; padding: 8px .75rem }`, `.sidebar-sub-links a { font-size:13px }`. `:is(.main-article,.page-article) h2 { border-bottom:1px solid var(--color-border); padding-bottom:.5rem }` - section rules. Fonts: Outfit (headings), system sans (body), Google Sans Code (mono). Breakpoints: 600 / 800 / 1020 / 1360px, plus max-width:799px. PRINT: ZERO `@media print` rules in the stylesheet - the 260px sidebar and its 100vh sticky positioning will land in the printed output.",
   "howToCopy": "Greybox: 260px sticky full-height left sidebar (border-right, own scroll) + 760px prose column, page-type templates widening to 1020px, 1.5rem padding stepping to 2rem above 1020px, 8px radius on every card. Section heads get a 1px bottom border with .5rem padding-bottom. For the timeline itself copy the home-page pattern rather than the resume markdown: a flex row per entry, `align-items:baseline`, `gap:.75rem`, with the date span locked at `width:90px; min-width:90px` in mono at .8rem, muted colour. Below 1020px the sidebar is display:none and the content goes full width - which is also the fix this site is missing for print.",
   "artworkOrCss": "All CSS. The sidebar avatar/logo SVGs and the small 16-24px link icons are assets, but no layout device depends on artwork. Note the mono font (Google Sans Code) is doing real work on the date gutter - swap it for any mono and the device still reads.",
   "confidence": "read-from-source"
  },
  {
   "name": "Xe Iaso resume",
   "url": "https://xeiaso.net/resume",
   "isLive": true,
   "layoutFamily": "single wide column, no rail, tag-cloud + dated headings",
   "distinctiveMove": "Skill tags are the whole visual system - a wrapping flex of ~25 identical 8px-radius chips at 14px, each with 8px margin AND 8px padding, forming a dense block above the experience list. Then every role is a bare <h3> 'Company - Role' with the date as a separate 16px <span> line underneath ('2024-09 - present, Ottawa, CAN'), ISO-ish date format, no gutter, no rules. Extremely flat: three type sizes (24 / 20 / 16px) carry the entire hierarchy.",
   "measurements": "READ FROM https://xeiaso.net/styles.css (87KB, Tailwind build) + page HTML. Container: `<div class=\"px-4 py-2 mx-auto bg-bg-hard ... lg:max-w-5xl\">` and in the CSS `.lg\\:max-w-5xl { max-width: 64rem }` = 1024px above the lg breakpoint, with `.px-4 { padding-left:1rem; padding-right:1rem }` and `.py-2 { padding-top:.5rem; padding-bottom:.5rem }` - i.e. only 16px side padding on a 1024px column. Tag chip: `class=\"m-2 p-2 text-sm ... rounded-lg\"` resolving to `.m-2{margin:0.5rem}` `.p-2{padding:0.5rem}` `.text-sm{font-size:0.875rem;line-height:1.25rem}` `.rounded-lg{border-radius:0.5rem}` - so 8px outside, 8px inside, 14px/20px text, 8px radius. Headings: `.text-2xl{font-size:1.5rem;line-height:2rem}` for 'Experience', `.text-xl{font-size:1.25rem;line-height:1.75rem}` for each role, date span at text-md, body paragraphs all `.mb-4{margin-bottom:1rem}`. Sibling widths also defined: `.lg\\:max-w-3xl{max-width:48rem}`, `.lg\\:max-w-[65ch]`, and an `[...]{width:80ch}` rule. PRINT: ZERO `@media print` rules in styles.css.",
   "howToCopy": "Greybox: one 1024px column with only 16px of side padding, everything left-aligned, uniform 16px paragraph spacing (`margin-bottom:1rem` on literally every block). Skills = `display:flex; flex-wrap:wrap` of chips at `margin:8px; padding:8px; font-size:14px; line-height:20px; border-radius:8px`, filled with a subtle surface colour rather than an accent. Roles = h3 at 20px reading 'Company - Role', then a 16px span on its own line reading 'YYYY-MM - YYYY-MM, City, COUNTRY', then paragraphs. Do NOT copy the print behaviour - there is none.",
   "artworkOrCss": "Pure CSS/Tailwind, zero artwork in the resume itself (the site's mascot art lives on other pages). This is the cleanest evidence in the set that a 'skill tag' is nothing more than a padded inline-flex span with a radius - no image, no gradient, no border.",
   "confidence": "read-from-source"
  },
  {
   "name": "Seirdy resume",
   "url": "https://seirdy.one/resume",
   "isLive": true,
   "layoutFamily": "single very narrow semantic column; all screen layout scoped inside @media screen",
   "distinctiveMove": "Every layout declaration on the site lives inside `@media screen { ... }`, so print gets the browser's default document flow with nothing to undo - print survival by omission rather than by override. On top of that there is a real `@media print` block that hides the TOC, permalink asides, backlinks, footer and nav, plus a page-break rule OUTSIDE the media query: `figure, blockquote, section[itemprop=mentions], li { break-inside: avoid }`. Dates lead each heading ('June-August 2019: Software Engineering Intern, SAP Ariba, Palo Alto') so no CSS carries the timeline.",
   "measurements": "READ FROM the page's inline <style> (5.2KB, the entire site CSS). `html { font: 100%/1.5 sans-serif; overflow-y: scroll; text-size-adjust: none }` = 16px at 1.5. Inside `@media screen`: `body { margin:auto; max-width:40em; padding:0 14px }` (~640px), `.e-content, .narrow { margin:auto; max-width:34em }` (~544px measure), `body > :not(main), main > :not(article), article > :not(h2):not(h3) { padding: 0 .5em }`, `article > h2 { margin:.25em 0; padding:.25em 0 }`, `h1 { margin: 0 0 .25em }`, `dd { padding: .25em .25em .5em 1.75em }`, `ol, ul { padding-left: 1.75em }`, `blockquote { border-left: 3px solid; padding-left: 1.25em }`. Tap targets are enforced with `input, summary, li > a, nav li > a { padding: .75em .25em }`. Narrow fallback `@media (max-width: 272px) { body { padding: 0 6px; hyphens: auto } }`. Full media list: screen, print, max-width:272px, prefers-color-scheme:dark, prefers-contrast:more/less/not. NOTE: the CV content itself is stale (an internship search dated 2023) - cite it for the print/markup technique, not for content shape.",
   "howToCopy": "Greybox: `html { font: 100%/1.5 sans-serif }`, body max-width 40em with 14px side padding, and an inner content measure of 34em. Then the technique that matters: wrap EVERY layout rule you write in `@media screen { }`, add a short `@media print { }` that hides nav, TOC, permalinks and footer, and put `break-inside: avoid` on li, figure and blockquote at top level so roles never split across pages. Dates go at the front of the heading string, not in a column.",
   "artworkOrCss": "100% CSS, and no images anywhere in the document. Also worth noting for Rod's a11y bar: this page is the strongest of the six on that axis (explicit tap-target padding, prefers-contrast and prefers-color-scheme handling, microdata + microformats on every block) and it costs 5KB total.",
   "confidence": "read-from-source"
  },
  {
   "name": "Brittany Chiang v4 (archived, still served)",
   "url": "https://v4.brittanychiang.com/",
   "isLive": true,
   "layoutFamily": "centred single column with vertical tab-rail for jobs (the pre-2023 version of the site Rod named)",
   "distinctiveMove": "The experience section is a vertical TAB rail: company names stacked in a left list with a sliding 2px highlight bar, and one job panel shown at a time on the right - the opposite trade from the current site (which shows every role at once). Useful as the counter-proposal: it hides 90% of the content behind interaction, which is why the newer site abandoned it, and it is unprintable by definition.",
   "measurements": "PARTIAL - only the critical inline styled-components CSS is served in the HTML (21.8KB); the tab-rail component CSS is client-side and was NOT readable from the delivered document, so no tab widths are reported. What I DID read: `main { margin:0 auto; width:100%; max-width:1600px; min-height:100vh; padding:200px 150px }` stepping down to `200px 100px`, `150px 50px`, `125px 25px`; `main.fillHeight { padding: 0 150px }` -> `0 100px` -> `0 50px` -> `0 25px`; `section { margin:0 auto; padding:100px 0; max-width:1000px }` -> `80px 0` -> `60px 0`. Fonts: Calibre (400/500) + SF Mono, self-hosted woff2. PRINT: not determinable from the served CSS.",
   "howToCopy": "Only the shell is safe to copy: 1600px page frame with very large 150px side padding, sections capped at 1000px with 100px of vertical padding collapsing to 80 then 60px. That 1000px/100px rhythm is the widely-copied part. Do not greybox the tab rail from this - either read the public source repo first or use the current site's always-visible list instead, which is what Rod actually named.",
   "artworkOrCss": "CSS (styled-components) for the shell and the tab highlight; the only assets are self-hosted Calibre woff2 files and project images. Flagged here because a screenshot of this page and of brittanychiang.com look like the same designer but the experience DEVICE is completely different - tabs vs an always-open dimming list.",
   "confidence": "partial"
  }
 ],
 "notes": "METHOD: every site below was fetched with curl (real HTTP, real user-agent) and its stylesheets fetched separately and parsed. Every number in `measurements` was read out of a served CSS file or inline <style> block. Where a value could not be read I said so rather than inferring it. Working files are in C:\\Users\\Rod\\AppData\\Local\\Temp\\claude\\C--Users-Rod-Documents-ProjectFiles-Website\\d1d0c775-c102-4c14-b21e-98f9e13d1b9f\\scratchpad (bc.html/bc.css, zach.html/zach-resume.css/zach-inline.css, benhoyt.html, tania.html/tania.css, xe.html/xe.css, seirdy.html, v4.html).\n\nSIX sites read from source, one (v4) partial. That clears the four-site bar with margin.\n\nTHE HEADLINE FINDING FOR ROD: the site he named as the register, brittanychiang.com, has ZERO `@media print` rules in its compiled stylesheet. It solves \"must survive printing\" by linking a separately-authored /resume.pdf. So the two requirements he stated cannot both be satisfied by copying that one site - the register comes from brittanychiang.com, the print behaviour has to come from somewhere else. The three sites that actually print are zachleat.com/resume (override strategy: a dedicated 4KB page stylesheet with two @media print blocks, collapses the grid to `auto`, forces #000/#fff, and expands links with `content: \" at \" attr(href)`), benhoyt.com/cv (two-class .no-print/.print-only swap over a 700px serif column), and seirdy.one/resume (omission strategy: ALL screen layout wrapped in `@media screen` so print falls back to browser defaults, plus `break-inside: avoid` on li/figure/blockquote). Those three are cheap to combine with the Chiang register because none of them constrain the screen design.\n\nTHREE DISTINCT TIMELINE DEVICES were found, and they are cleanly separable in a greybox:\n1. Fixed date gutter - brittanychiang (2 of 8 grid columns = 25%, 32px gap collapsing to 16px) and tania.dev's home page (`width:90px; min-width:90px` mono span in a baseline-aligned flex row). Costs layout code, reads as a timeline, needs a stacking fallback below the breakpoint.\n2. Date as a separate line under the role - zachleat (`li.resume-date`, 16/19 italic Lato) and xeiaso (16px span under a 20px h3). Zero layout code.\n3. Date inside the heading string - benhoyt (\"Canonical - Senior Engineer and Manager - Sep 2020 to now\") and seirdy (\"June-August 2019: Software Engineering Intern, SAP Ariba, Palo Alto\"). Zero CSS, prints flawlessly, but no visual timeline.\n\nSKILL TAGS: two sites ship them and both are trivially copyable and pure CSS - brittanychiang `border-radius:9999px; padding:4px 12px; font-size:12px; line-height:20px` on a 10%-alpha accent fill, and xeiaso `border-radius:8px; margin:8px; padding:8px; font-size:14px/20px` on a surface fill. Zachleat instead marks skills semantically with `<strong class=\"p-skill\">` inline in the sentence and gives them no visual treatment at all - worth showing Rod as the third option.\n\nARTWORK vs CSS (the recurring question): none of the six timeline devices is artwork. The single place artwork does the work is zachleat's inline company avatars (60-72px favicons pulled from indieweb-avatar and /img/avatars/*.avif, set inline in the running text) - that texture is the one thing a greybox cannot fake, and it is also the one thing that will cost print quality.\n\nDEAD ENDS, reported honestly and NOT counted: jamesg.blog/cv returns HTTP 200 with a <title>cv</title> but its <main id=\"main\"> is empty - no CV content in the delivered HTML, so it was dropped after fetching. leerob.com/resume returns 200 but serves the homepage markup (canonical https://leerob.com, no experience section) - dropped. 109ichiki.com/profile/ IS live and was fetched, but it is a bio + links + dialog-box KV, with no dated career list, so it does not qualify as a CV page (still potentially useful to Rod as a sub-page shell reference: mono heading + Libre Barcode 128 motif, Astro scoped CSS, canvas grid overlay). Confirmed 404 on: jvt.me/cv, mikemai.net/resume, hugogiraudel.com/cv, kilianvalkhof.com/cv, jgthms.com/resume, stefanjudis.com/cv, lea.verou.me/cv, aaron-gustafson.com/resume, hidde.blog/cv, drewdevault.com/cv.html, tobiasahlin.com/about, cassie.codes/about, dimden.dev/about, cyanilux.com/about, blog.maximeheckel.com/about, kaitonote.com/about, unseen.co/about, 109ichiki.com/about, yannesidibe.com/about.\n\nTASTE CAVEAT: this page type skews dev-default. Of the six, the two closest to Rod's warm/hand-made axis are benhoyt.com/cv (Georgia serif, #301800 ink, #e87000 links, small-caps section heads, 2.5KB of CSS, no boxes anywhere) and seirdy.one/resume (5KB, semantic, no chrome). Neither is playful. If Rod wants the CV to feel like the rest of the redesign rather than like a dev resume, the warmth will have to come from his own type and palette laid over one of these skeletons - I found no live CV page in this pass that is both warm/hand-made AND has the Chiang-register timeline, which is itself a useful negative result."
}