{
 "sites": [
  {
   "name": "Cyanilux — Recent Posts / Contents",
   "url": "https://www.cyanilux.com/recent/",
   "isLive": true,
   "layoutFamily": "single centred panel (percentage-width, not px) + full-width stacked post cards; the /contents/ sibling is the same panel with heading-grouped COMPACT cards (image rail left, text right)",
   "distinctiveMove": "The only site in the set that carries all four of Rod's fields on one row — thumbnail, title, blurb, colour-coded tag chips, date + a second '(updated …)' date. The whole card is clickable (inline onclick + cursor:pointer) and the hover state changes card background AND title colour together. On /contents/ the compact card's thumbnail zooms inside its clipped rail on hover (height 100%→110%, bottom 0→5%, transition .4s) — an image crop animation with zero JS.",
   "measurements": "REAL, read from https://www.cyanilux.com/css/style.css?v=12 (13,301 bytes, full read). Container: .header/.footer/.content { margin:auto; width:70% } → 60% at min-width 1200px, 85% at max-width 1000px, 90% + padding 10px at max-width 600px. NO px max-width anywhere. Panel: .content { padding:30px; border:3px solid #303030; border-radius:20px; background:#202020; box-shadow:5px 5px 10px 0 #000 }. Card: .card { margin-top:20px; border-radius:10px; background:#282828; line-height:120% }; .card-text { padding:12px }; hover .card{background:#303030} and h2 a → #00ddff. Image: .card-image { max-width:90%; max-height:300px; margin-inline:auto } on .card-image-box-2 { background:#151515; border-radius:10px 10px 0 0 }. Tags: .tag { display:inline-block; border-radius:10px; padding:5px; margin-left:2px; line-height:100% } with per-tag inline background-color (URP #00aabb, Shader Graph #101010, HLSL #108010, 2D #2d88ff). time { color:#888 }. Body: Lato 400, line-height:170%, colour #dddddd. Compact variant (/contents/): .card-image-box { min-width:200px; border-radius:10px 0 0 10px; overflow:hidden; background:#151515 }; .card-image-small { height:100%; object-fit:cover; transition:height .4s, bottom .4s } → hover { height:110%; bottom:5% }; .card-compact { margin-top:6px; display:flex } on desktop, image collapses to a 150px-tall full-width top image under 600px.",
   "howToCopy": "Greybox: percentage-width panel (70% / 60% over 1200px), 30px padding, 20px radius, 3px border. Inside it stack rows 20px apart: 700x300-ish image block with only the top corners rounded (10px 10px 0 0), then a 12px-padded text block holding h2 title, one 2-line blurb, then a row of 10px-radius 5px-padded chips followed by the date. For the archive variant flip to flex: 200px-min image rail on the left with 10px 0 0 10px radius, text right, rows only 6px apart. Prove the hover by overflowing the rail and scaling the image to 110%.",
   "confidence": "read-from-source",
   "artworkOrCss": "ALL CSS. Even the page background is CSS: body { background: linear-gradient(#00000000 0%, rgb(0,0,0) 120%), repeating-conic-gradient(#062835 0% 25%, #042e3b 5% 50%) 0% / 60px 60px; background-attachment: fixed } — a 60px checkerboard darkened toward the bottom, no image file. The only artwork on the index is the per-post preview PNG and the round site logo. Tag colours are inline style attributes per post, not classes."
  },
  {
   "name": "dimden — /blog",
   "url": "https://dimden.dev/blog",
   "isLive": true,
   "layoutFamily": "full-bleed single column, no container — rule-separated chronological rows, title + date only",
   "distinctiveMove": "The list has no card, no box, no blurb, no tag: every post is just a 30px teal title, a plain white date line, and a fat 4px near-black rule between rows. Both the title and the date are the same link, so the whole two-line block is one target. External posts (his diary subdomain) sit in the same list with no visual distinction — the register is 'log', not 'catalogue'. The whole page is faked into a CRT with two animated full-screen overlays.",
   "measurements": "REAL, read from https://dimden.dev/css/blog.css (full read, ~4.3KB). #blog { padding:35px } and NOTHING else constrains width — I found no max-width/width rule for the blog column in blog.css, so the list is viewport-wide minus 35px. (This CONTRADICTS the shortlist note that the blog uses the same fixed 900px column as the home page; the blog page loads only /css/blog.css, and that file has no such rule. Treat the '900px everywhere' claim as unverified for this page.) .title { font-size:30px; color:#2aa198 } hover #1b4834. Date link inherits a { color:#fff; text-decoration:none } hover #0f9666. Row separator: hr { height:4px; background-color:#161719 !important; border:none } (browser default margin .5em auto ≈ 8px above/below). Row internals are literal <br><br> after the date. body { background:#000; color:#fff; font-family:'Pixel NES' }. Logo image is 380px wide, centred, image-rendering:pixelated. Scrollbar restyled: ::-webkit-scrollbar { width:1em }, thumb 1px solid #1b4834 on black.",
   "howToCopy": "Greybox: no container at all, 35px page padding. Repeat [4px solid #161719 full-width rule] → [30px title link] → [16px date link] → 2 line-breaks of air. Do NOT add a card, a blurb slot, or a border-radius anywhere; the entire visual weight is the 4px rule against black. If Rod wants blurbs this is the layout to argue AGAINST — it is the pure 'lighter register' end of the spectrum.",
   "confidence": "read-from-source",
   "artworkOrCss": "MIXED, and the split matters: the row rhythm is 100% CSS (a bare hr). The CRT look is ARTWORK — body:before uses /overlay.png at opacity .2 and body:after uses /overlay2.png at opacity .5, both position:fixed, 100%x100%, pointer-events:none, z-index:1000, animated with animation:Static 2s / 0.8s steps(4) infinite that shifts background-position 0px→4px. The inset vignette (box-shadow: inset 0 0 10em rgb(0 0 0 / 40%)) IS CSS. So: two PNGs + a steps() keyframe, not a shader and not a CSS-generated grain."
  },
  {
   "name": "Mike Klubnika — /logs (All Logs)",
   "url": "https://mikeklubnika.com/logs",
   "isLive": true,
   "layoutFamily": "fixed-max 58em 'document' page with hairline side rails, holding a dense two-column zebra-striped log table",
   "distinctiveMove": "Each entry is one 12px monospace line: '[30 Jul 2026] Title' pushed left, '17 days ago' pushed right, via flex space-between — a relative-time column that no other site in this set has. Alternate rows get an almost-invisible tint (3.3% white) instead of a rule, so the list reads as a terminal printout rather than a feed. The whole page sits in a bordered sheet with vertical light rails outside it.",
   "measurements": "REAL, read from https://mikeklubnika.com/static/style/main.css (8,021 bytes, full read). Column: .doccontents { max-width:58em; width:100%; border-width:1px; border-top-width:0; border-bottom-width:0; border-color:rgb(92,92,92) } → 928px at a 16px root, with 1px vertical rails only. .doc { margin:15px; margin-top:10px; margin-bottom:10px }. .docholder { background: linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(26,26,26,1) 91%, rgba(0,0,0,1) 100%); border:1px solid }. Rows: .ue { display:flex; flex-direction:row; justify-content:space-between; margin-top:5px; margin-bottom:5px; font-family:bank; font-size:12px }; .uebg { background-color: rgba(235,235,235,0.0333) } on every other row; .entrytext { font-family:monospace }. Section divider: .pagespace { border-top:1px dotted white; margin-top:1.25em; margin-bottom:1em }. Headings: h2 { font-size:20px; margin-bottom:.75em; color:gainsboro }, h1 27px. Body monospace, background #000, colour #fff, p colour gainsboro. Breakpoint 756px kills the side banners and the sheet border, 600px hides .hide-mobile.",
   "howToCopy": "Greybox: centre a 928px (58em) column with 1px #5c5c5c borders on the LEFT AND RIGHT ONLY (no top/bottom), inner 15px margin. Rows are 12px monospace flex lines, 5px margin top and bottom, alternating rgba(235,235,235,0.033) background. Left cell = '[DD Mon YYYY] Title', right cell = relative age. Separate sections with a 1px DOTTED white rule at 1.25em/1em. Row height ends up ~22px — this is the densest option in the set.",
   "confidence": "read-from-source",
   "artworkOrCss": "MIXED. Zebra rows, the 58em rails, the dotted divider and the vertical page gradient are all CSS. The paper/console texture is ARTWORK: .doc { background-image:url('../resources/page_bg_2.png'); background-position:right bottom; background-repeat:no-repeat }. The two glowing side rails are also artwork but CSS-faded: .banner { position:absolute; width:100px; height:100%; z-index:-1 } with background-image:url('../resources/banner.png') and mask-image: linear-gradient(90deg, rgba(0,0,0,0) 25%, rgba(1,1,1,.3) 85%, rgba(255,255,255,.2) 98%) — an image made to feel ambient by a CSS mask, which is a technique worth stealing on its own."
  },
  {
   "name": "Eve — OFFICIAL SITE /news/",
   "url": "https://eveofficial.com/news/",
   "isLive": true,
   "layoutFamily": "fixed 1140px centred column under a tall 251px static masthead; hairline-divided list rows, optional right-aligned thumbnail",
   "distinctiveMove": "Hairline discipline: one 1px #2c2c2c line on top of the list and under every row, 30px of air above and below each row, date stacked ABOVE the title with 15px between them, and a red (#a40000) category word inline with the date. Hover does not colour the title — it wipes an underline out from a zero-width background-image (background-size: 0% 1px → 100% 1px over .3s). Rows that have a thumbnail simply reserve padding-right:270px and absolutely position a 240x180 image at the row's top-right, so ALL rows keep the same left edge.",
   "measurements": "REAL, read from https://eveofficial.com/css/style.css?ver=260816230345 (36,914 bytes, 1,654 lines; layout rules read directly). Container: .wf { margin:0 auto; width:1140px; position:relative }; body { background:#111; min-width:1140px }; the page declares <meta name=\"viewport\" content=\"width=1280\"> — desktop-only, no responsive column. #wrapper { padding:251px 0 0 } because #header is position:absolute, height:251px (logo 221x141 PNG, nav items 16px, margin-left 29px between). Page title: article#contents > h1 { background:#161616; margin:0 0 80px; padding:37px 0; text-align:center; font-size:26px }. List: ul:first-of-type { border-top:1px solid #2c2c2c }; li { border-bottom:1px solid #2c2c2c; padding:30px 0 }; li a { display:block }; .date { margin:0 0 15px; display:block; line-height:1 }; .date span { margin:0 8px 0 0; color:#a40000 }; .ttl { font-size:20px; line-height:28px; background-image:linear-gradient(#fff,#fff); background-position:0% 100%; background-repeat:no-repeat; background-size:0% 1px } → hover 100% 1px, transition all .3s ease-out. Thumbnail rows: li.tn a { padding-right:270px; min-height:180px; position:relative }; li.tn .img { width:240px; height:180px; position:absolute; top:0; right:0 } hover opacity .6. Pager: #contents ul.pager { margin:60px auto 0; width:1000px }, next/back arrows are 18px PNGs. Detail template column: 1000px.",
   "howToCopy": "Greybox: 1140px fixed centre column. Full-width dark title bar (37px vertical padding, 26px centred label) then 80px of air. List = top hairline #2c2c2c, then rows of 30px/30px padding with a bottom hairline; inside each row a 14-16px date line (with an 8px-gapped accent-coloured category word), 15px gap, then a 20px/28px title. For picture rows keep the same left edge and reserve 270px on the right for a 240x180 block. Build the hover as a 1px gradient background growing 0%→100% width, not a border.",
   "confidence": "read-from-source",
   "artworkOrCss": "CSS for everything structural — dividers, the row rhythm, and notably the hover underline, which is a background-image gradient sized 0%→100%, so it animates smoothly where a border/text-decoration could not. ARTWORK is confined to chrome: the header logo (/images/blk/header_logo.png, 221x141), the nav separator (header_sep.png positioned at 0 21px), and the pager arrows (ico_arrow_l/r.png at 18px). No texture, no background image on the list itself — the whole page is #111 flat."
  },
  {
   "name": "whey-isolate (Neocities) — home / updates log",
   "url": "https://whey-isolate.neocities.org/",
   "isLive": true,
   "layoutFamily": "sticky-rail sandwich: 160px sticky nav left + 160px sticky nav right + centre 'sheet', over a fixed screen-blended background image",
   "distinctiveMove": "The register itself: entries are a centred date heading (h3, YYYY/MM/DD) followed by ONE paragraph whose first clause is bolded as the lede and whose links go straight into the relevant page — no titles, no tags, no read-more. It is a diary/changelog voice rather than an article index, and it mixes 'new blog post' with 'updated the bookshelf' in one stream. Visually every panel (both rails and the sheet) wears the same 10px solid red border with a hard, un-blurred offset shadow (-10px 10px 0) — a sticker/xerox look, not a soft card.",
   "measurements": "REAL, read from https://whey-isolate.neocities.org/style.css (7,969 bytes, full read). Page: body:not(footer) { margin-left:10vw; margin-right:10vw; font-family:'Courier New'; color:#d8dee9; margin-bottom:24px }. Rails: --sidebar-width:160px; nav and #navbar { width:160px; padding:10px; border:10px solid var(--accent-color) /*#d84f51*/; background:var(--lighter-color) /*#464e66C4*/; box-shadow:-10px 10px 0 rgba(32,36,47,.8); position:sticky; top:0; margin-top:-10px } (left z-index:3, right z-index:1). Sheet: main { margin-left:calc(160px + 30px); margin-right:calc(160px + 30px); padding:25px; border:10px solid #d84f51; box-shadow:-10px 10px 0 rgba(32,36,47,.8); z-index:2 } → 190px inset each side. Entries in markup: <h3 id=centertext> date, then <p style=\"text-indent:50px\"> with a bolded lede, separated by <br>. Palette vars: --bg-color:#2e3343, --starfield-color:#222531, --darker:#20242f, --lighter:#464e66C4, --accent:#d84f51, --link:#d8d880, --shadow:rgba(32,36,47,.8). Title glow keyframe: text-shadow 0 0 10px var(--link-color) → 0, 1.5s ease-in-out alternate.",
   "howToCopy": "Greybox: 10vw page gutters, two 160px sticky columns pinned top:0, centre column inset 190px from each side with 25px padding. Give all three the same 10px solid accent border and the same hard offset shadow -10px 10px 0 (no blur) so they read as cut paper. Entries: centred date heading, then a single paragraph with text-indent:50px, blank line between. This is the reference for 'personal register, lighter than the projects grid' taken to its logical end.",
   "confidence": "read-from-source",
   "artworkOrCss": "MIXED and cleanly separable. The panel language — thick border + hard offset shadow + sticky rails — is pure CSS and is the part worth copying. The atmosphere is ARTWORK: html { background-image:url('images/sanctumbg.jpeg'); background-blend-mode:screen; background-color:#222531; background-size:100% auto; background-repeat:no-repeat; background-attachment:fixed } — a photo/starfield JPEG screen-blended against a flat colour, which is exactly the 'image faded by a CSS blend' trick rather than a drawn-in-Photoshop composite. Title glow is a CSS keyframe on text-shadow, not a graphic."
  },
  {
   "name": "MinionsArt — /tutorials/ index",
   "url": "https://minionsart.github.io/tutorials/",
   "isLive": true,
   "layoutFamily": "fixed 1000px dark panel holding a 3-column card grid, with a tab-bar filter row that docks into the panel",
   "distinctiveMove": "The filter tab bar: buttons sit ABOVE the panel and the active one takes background-color:#353635 with border-radius:10px 10px 0 0 — the same colour as the panel below, so the selected tab visually merges into the sheet (a real folder-tab, not a pill). Cards are data-driven (title, date, 'Released:' + optional '*Updated*' date, description, type + tag slugs) and every label wears a 4-way text-shadow outline, which is what gives it the game-UI feel Rod likes.",
   "measurements": "REAL, read from https://minionsart.github.io/tutorials/style.css (14,960 bytes) plus header.html, Builder.js and TutData.json (152KB of post records). Panel: .mainbody { width:1000px; padding:35px; border-radius:10px; background:#353635; margin-inline:auto; display:table }. Grid: .grid-container { display:grid; grid-template-columns:auto auto auto; gap:20px; padding:15px; max-width:1000px } (3 x 300px + 2 x 20px gap + 30px padding = 970px). Tile: .unit_card { width:300px; background:#000; border-radius:15px; box-shadow:5px 10px rgba(0,0,0,.3); margin-top:5px; transition:.3s; display:grid }. Wide variant used elsewhere: .post_card { width:990px; min-height:400px; border-radius:15px; padding:10px; opacity:.9; background-size:cover }; .post-title-holder { width:990px; display:flex; justify-content:space-between }; .post_description { padding:20px; padding-top:5px }. Tabs: .tab { display:flex; justify-content:center; overflow:hidden }; .tab button { padding:14px 16px; border-radius:10px; border:none; transition:.3s }; .tab button.active { background:#353635; border:2px solid #353635; border-radius:10px 10px 0 0 }. Banner: .header-img { background-image:url(Images/BGTwittedec.png); height:200px; background-size:cover }; .header2 { font-size:40px; font-family:round; text-shadow:#8846be 3px 3px 1px, #8846be 1px 1px 1px, #8846be -1px -1px 1px }. Dates: .dates { color:#966eff; margin:5px }, second date colour #d46eff. Label outline: text-shadow: rgb(7,59,136) -1px -1px 0, 1px -1px 0, -1px 1px 0, 1px 1px 0 (used on .postTags, .typefilter, tab buttons; #21190f variant on .postLink). Search box 200px wide inside a 300px centred holder. NOTE: the index's card markup is cloned from a JS <template> in Builder.js, so I read the CSS and the data schema, not a server-rendered card.",
   "howToCopy": "Greybox: 1000px panel, 35px padding, 10px radius, dark fill. Above it a centred row of 14px/16px tab buttons where the active one is the panel colour with only the top corners rounded. Inside, a 3-column grid of 300px tiles, 20px gap, 15px padding, each tile 15px radius with a HARD offset shadow (5px 10px, no blur radius given so it stays crisp). Tile contents in order: image, title, 'Released: MM/YYYY' in accent purple, one-line description, then a tag row prefixed by a small icon. Outline every label with a 4-way 1px text-shadow.",
   "confidence": "read-from-source",
   "artworkOrCss": "MIXED, and this is the one where the tell is a pseudo-element image: the tag marker is ARTWORK injected by CSS — tag { content:url(/tutorials/Images/tag.png); max-height:20px; top:3px; position:relative }, i.e. a custom element whose entire body is a PNG. Same trick for every filter icon in header.html (<godot></godot>, <urp></urp> … are bare custom tags filled by CSS content images). The banner is artwork (BGTwittedec.png, 200px tall, cover). Everything else — the folder-tab merge, the 3-col grid, the offset shadow, the text outline — is CSS."
  },
  {
   "name": "ZUTOMAYO — /news/",
   "url": "https://zutomayo.net/news/",
   "isLive": true,
   "layoutFamily": "full-bleed bands with fixed 80px side gutters (no centred max-width) + a floating fake-OS window pinned bottom-right",
   "distinctiveMove": "Two things. (1) The recent-news panel is a draggable-looking desktop window fixed to the viewport, 335x582, that collapses to a 46px title bar with a CSS height transition — the 'little fake popup windows, very internet-core' Rod flagged. (2) In-body links are inverse chips: black background, white text, 0.2rem side padding, hover to purple #8027d0 — a highlighter/redaction treatment instead of underlines.",
   "measurements": "REAL, read from https://zutomayo.net/themes/zutomayo/_assets/css/app.css?26081001 (469,771 bytes — I grepped the exact selectors rather than reading the whole file, so a later override is possible). Rows: each entry is a <section class=\"ztmy-detail-list ztmy-topics ztmy-detail-wrap ztmy-mb-25\">; .ztmy-mb-25 { margin-bottom:25px !important }; first item .ztmy-under-inner { padding:80px 80px 0 80px } and .ztmy-topics:not(.ztmy-topics-num0) .ztmy-under-inner { padding:0 80px }. Head: .ztmy-topics-head { margin-bottom:15px } holding a [YYYY.MM.DD] paragraph then an h3 title, both font-family zenkakubold ('.f-tt--b'). Rule: .ztmy-topics-more { padding:0 0 1rem 0; border-bottom:solid 1px #000000 } — the divider hangs off the VIEW MORE row, not the section. Body copy block: .ztmy-topics-cont { border-top:solid 1px #000; border-bottom:solid 1px #000; padding:1rem 0; word-break:break-all }. Links: .ztmy-topics-cont a { display:inline; background-color:#000; color:#fff; padding:0 .2rem; transition:background-color .3s } hover #8027d0. Container: .ztmy-container--inner { max-width:100% } (genuinely no centred column). Page height floor: .ztmy-wrap { min-height:calc(100vh - 54px - 94px) }. Floating window: .ztmy-pcmove-news { position:fixed; z-index:300; bottom:100px; right:40px; width:335px; height:582px; overflow:hidden; transition:height .3s; background-image:url('../img/news_pc_window.png'); background-size:100%; background-repeat:no-repeat; background-position:top }; collapsed .windowmini { height:46px }; .header-bar { position:absolute; top:0; left:0; width:100%; height:45px; cursor:pointer }.",
   "howToCopy": "Greybox: drop the centred container entirely — 80px left/right gutters, 80px top only on the first item, rows 25px apart, each row = [YYYY.MM.DD] line, 15px gap, title, then a VIEW MORE line with a 1px black rule beneath it. Then pin a 335x582 panel at bottom:100px right:40px whose top 45px is a click target that toggles the height to 46px with a .3s transition. Render links as filled black chips with 0.2rem padding.",
   "confidence": "partial",
   "artworkOrCss": "THE WINDOW IS ARTWORK. The entire 'fake popup' chrome — title bar, borders, close-button styling — is a single PNG (news_pc_window.png) used as a background-image at background-size:100%; only the collapse (height 582px→46px, transition .3s) and the 45px invisible header hit-area are CSS. If Rod wants that device he is choosing to draw a window graphic, not to style one. By contrast, the list itself — 80px gutters, 1px rules, the inverse-chip links — is all CSS and copyable as geometry."
  },
  {
   "name": "Mateusz Urbanowicz — blog home",
   "url": "https://mateuszurbanowicz.com/",
   "isLive": true,
   "layoutFamily": "sticky 16% left rail (logo, title, nav, 'recent posts' list, socials) + wide right column of posts",
   "distinctiveMove": "The rail carries the identity block AND a plain 'recent posts' text list, so the wide column can stay editorial. Fluid type and fluid spacing throughout: every size is a clamp() and every spacing step is min(px, vw), so the page breathes with the viewport instead of stepping at breakpoints. INCLUDED WITH A WARNING: under the hood this is a stock WordPress/Gutenberg block theme (wp-container-* generated classes), i.e. close to the 'default template' evidence Rod has called worthless — cite it for the rail geometry and the fluid-spacing scale only, not as a craft reference.",
   "measurements": "REAL, read from the inline generated CSS in the served HTML (163,189 bytes; there is no single hand-written stylesheet). --wp--style--global--content-size: 720px; --wp--style--global--wide-size: 1340px. Spacing scale: --wp--custom--spacing-unit:10, --wp--custom--spacing-increment:2.2, so spacing-20 = min(22px, 2.2vw), spacing-30 = min(44px, 4.4vw), spacing-40 = min(66px, 6.6vw), spacing-50 = min(88px, 8.8vw). Left column: <div class=\"wp-block-column\" style=\"flex-basis:16%\"> with .wp-container-3 { position:sticky; top:calc(0px + var(--wp-admin--admin-bar--position-offset,0px)); z-index:10 }; the inner header group is sticky too (.wp-container-1), padding-top/bottom var(--wp--preset--spacing--20) with 0 side padding. Rail stack gap: .wp-container-core-group-is-layout-35cd6667 > * + * { margin-block-start:8px }; nav gap 7px, column-direction; socials gap 0 10px. Columns stack below 782px (.wp-block-columns flex-wrap:wrap until min-width:782px). Site logo 160x160 PNG; site title font-size:clamp(23.54px, 1.471rem + ((1vw - 3.2px) * 1.516), 39px) in EB Garamond, weight 500, colour var(--wp--preset--color--theme-3) = #7b7b7b. Preset sizes: small clamp(14px…16px), medium clamp(26px…20px), large clamp(22.041px…36px), x-large clamp(33.419px…60px), xx-large clamp(40px…74px); x-small flat 14px (used by the rail's recent-posts list). Post loop is .wp-block-post-template (flex container, gap 1.25em, li width 100%).",
   "howToCopy": "Greybox: two columns, left flex-basis 16% and position:sticky top:0, right column free. Prose measure 720px, wide media 1340px. Use a fluid spacing scale of min(22px,2.2vw) / min(44px,4.4vw) / min(66px,6.6vw) / min(88px,8.8vw) rather than fixed section padding, and clamp() the headings. Rail children 8px apart, nav links 7px apart, 14px text for the recent-posts list.",
   "confidence": "partial",
   "artworkOrCss": "CSS layout, artwork identity. The rail/column geometry and the fluid clamp/min() scales are CSS (machine-generated by Gutenberg). Everything that makes the site feel hand-made — the watercolour illustrations and the 160x160 logo — is uploaded ARTWORK; there is no CSS texture, gradient, or drawn device anywhere in the layout. Useful as proof that a warm-feeling blog index can be geometrically plain and carry all its warmth in the images."
  }
 ],
 "notes": "METHOD: every URL here was fetched with curl (real HTTP, desktop UA) and every number was read out of the actual stylesheet, not from a screenshot or a description. Local copies of the HTML + CSS are in C:\\Users\\Rod\\AppData\\Local\\Temp\\claude\\C--Users-Rod-Documents-ProjectFiles-Website\\d1d0c775-c102-4c14-b21e-98f9e13d1b9f\\scratchpad\\blogidx\\ (dimden-blog.html/.css, klubnika-logs.html + klubnika.css, cyanilux-recent.html + cyanilux-contents.html + cyanilux.css, eve-news.html + eve.css, whey-home.html + whey.css, minions.html + mheader.html + minions.css + builder.js + tutdata.json, zutomayo-news.html + ztmy.css).\n\n8 sites returned, all live as of this pass (all HTTP 200). Six are read-from-source with the stylesheet read end to end; two are marked partial and why: ZUTOMAYO's app.css is 469KB so I grepped the exact selectors instead of reading it whole (a later cascade override is possible), and Mateusz has no hand-written stylesheet at all — the numbers come from WordPress's generated inline CSS.\n\nCORRECTION TO THE SHORTLIST: the brief says dimden's blog uses \"the same fixed 900px column as the home page, so the site never changes width between page types\". /blog loads only /css/blog.css, and that file contains no width or max-width for the blog column — just #blog { padding:35px }. The blog index is viewport-wide. Do not carry the 900px claim into the measure phase without re-checking the home page's own stylesheet.\n\nTWO SHORTLIST URLS ARE WRONG for this page type: https://whey-isolate.neocities.org/posts/ returns 404 — there is no post index; the site's HOME page is the post/updates list, so I measured that instead. And in the earlier pass the Eve and ZUTOMAYO news pages are easy to swap: eveofficial.com/news/ is a 6.6KB hand-rolled page, zutomayo.net/news/ is 508KB.\n\nCOVERAGE AGAINST ROD'S BRIEF (date + title + blurb + tags, personal register): only Cyanilux carries all four fields, and it does it in a card. MinionsArt carries title + date + blurb + tags but only because the index is JS-built from a JSON file — there is no on-site post template behind it. Eve, dimden, Klubnika and ZUTOMAYO are date + title only, no blurb, no tags. Whey-isolate has neither titles nor tags — it is a dated paragraph diary. So the honest read is: the \"date + title + blurb + tags\" list Rod described is closer to the Cyanilux/MinionsArt card family than to the Japanese-indie hairline-row family, and the row family (Eve is its cleanest instance) is what actually reads as \"lighter than the projects grid\".\n\nTHREE DISTINCT GEOMETRIES WORTH BENCHING AS VARIANTS: (1) hairline rows, no card — Eve: 1140px column, 1px #2c2c2c top rule + per-row bottom rule, 30px/30px row padding, date 15px above a 20px/28px title, hover = a 1px gradient underline growing 0%→100% width. (2) dense log lines — Klubnika: 58em/928px column with 1px side rails only, 12px monospace rows at 5px margins, alternate rows tinted rgba(235,235,235,0.033), left \"[date] title\" / right relative age. (3) cards with tags — Cyanilux: percentage-width panel (70%, 60% over 1200px) at 30px padding / 20px radius, cards 20px apart at 10px radius with a 12px-padded text block and 10px-radius 5px-padded tag chips, plus the compact archive variant with a 200px image rail and a CSS-only hover crop-zoom (height 100%→110%, bottom 0→5%, .4s).\n\nRECURRING \"CSS OR ARTWORK\" ANSWER, since it is invisible in screenshots: the row geometry is ALWAYS CSS on every one of these sites — nobody draws their list. What is artwork is the atmosphere and the novelty devices: dimden's CRT is two PNG overlays (overlay.png at 0.2, overlay2.png at 0.5) animated with steps(4), ZUTOMAYO's fake desktop window is one PNG (news_pc_window.png) at 335x582 with only the collapse animated in CSS, MinionsArt's tag marker and every filter icon are PNGs injected via `content: url(...)` on bare custom elements, Klubnika's paper is page_bg_2.png plus side banner.png rails softened by a CSS mask-image gradient, and whey-isolate's starfield is a JPEG screen-blended over a flat colour. The two purely-CSS atmospheres in the set are Cyanilux (repeating-conic-gradient 60px checkerboard under a darkening linear-gradient, background-attachment:fixed) and whey-isolate's panel language (10px solid border + hard un-blurred offset shadow -10px 10px 0). If Rod wants the internet-core window or the CRT grain, that is a decision to commission a graphic, not to write CSS.\n\nONE MORE PATTERN WORTH STEALING regardless of which geometry wins: MinionsArt's filter tabs dock into the panel — the active button takes the panel's own background (#353635) with border-radius:10px 10px 0 0 so it merges with the sheet below. That is a real folder-tab, buildable in ~4 lines, and it is the site Rod called \"one of the reasons I'm starting my own\"."
}