{
 "sites": [
  {
   "name": "MinionsArt Tutorials",
   "url": "https://minionsart.github.io/tutorials/",
   "isLive": true,
   "layoutFamily": "Fixed 1000px rounded panel floating on black, containing a 3-up grid of fixed 300px cards",
   "distinctiveMove": "Tag chips are baked into the TOP-RIGHT CORNER OF THE THUMBNAIL itself (not under the title), each chip carrying its own hand-picked fill + brighter border colour pair; and every card sits on a hard offset drop shadow with ZERO blur (box-shadow: 5px 10px, no blur radius), which is what makes the 'card look' Rod praised read as sticker-like rather than material-design.",
   "measurements": "READ FROM /tutorials/style.css + the <template id=\"test\"> in the page + Builder.js. PANEL: .mainbody width 1000px, padding 35px, border-radius 10px, background #353635 on html background black. GRID: .grid-container display:grid, max-width 1000px, grid-template-columns: auto auto auto (3-up), gap 20px, padding 15px (3x300 + 2x20 = 940, +30 padding = 970 inside the 1000px panel). CARD: .unit_card width 300px, border-radius 15px, box-shadow 5px 10px rgba(0,0,0,0.3) — no blur, no spread — background black, transition 0.3s. THUMB: .preview-image-holder 300x160px (15:8), border-radius 15px 15px 0px 0px, opacity .9 -> 1 on hover. TITLE BAND: .centeredbox position absolute, bottom 0, left 0, width inherit, background rgba(0,0,0,0.6); .centeredSmaller font-size 22px, line-height 1, padding 5px, margin 10px, text-align left. DATES: .date font-size 14px position relative top 5% left 5%; .ogdate font-size 14px position absolute top 15% left 5%, color darkgrey. BODY: .bottom-holder width 300px, max-height 100px, margin-top 20px; .description width 280px, font-size 14px, line-height 1, margin 10px. TAG CHIP: .typefilter / .postTags font-size 14px, line-height 10px, padding 5px, margin 5px, border-radius 10px, float:right; parent .typeOfPost { position:absolute; top:0%; right:0% }. Tag colour pairs are inline styles from Builder.js CreateTypeDiv(): built-in #416eb3 / border #8193ff; 3d #c31f8b / #ff00f6; gameplay #ca9243 / #ffc470; shader-graph #5ad2b7 / #92ffe7; urp #41b34e / #8193ff; godot #5ecd68 / #91ff9b; other #666666 / #bdbdbd — all with 2px solid borders. FILTER ROW: .buttons display:grid, grid-template-columns: auto auto auto auto auto (5-up), width 500px, gap 10px, padding 15px, margin-left/right auto. SEARCH: input width 200px inside a 300px div inside a 400px #modS, padding-top 5px. Grid is JS-built from /tutorials/TutData.json; filtering is URL-param driven (?type=slug) and rebuilds #output.",
   "howToCopy": "Greybox: a 1000px panel, radius 10, 35px padding, sitting on a darker page ground. Inside it a 3-column grid of 300px cells with a 20px gap and 15px of grid padding. Each card: 300px wide, radius 15, and the single most important line — box-shadow: 5px 10px rgba(0,0,0,0.3) with NO blur radius. Top 300x160 thumbnail with radius on the top two corners only. Pin the title to the thumbnail's bottom on a rgba(0,0,0,0.6) band at 22px/1. Float the tag chips at the thumbnail's top-right: 14px text, 10px line-height, 5px padding, 10px radius, 2px border, each tag a different saturated fill with a lighter border of the same hue. Description block below the thumb at 280px wide, 14px/1. Above the grid, a centred 5-up auto grid of filter buttons, 500px wide, 10px gap, with a 200px search field centred above it.",
   "artworkOrCss": "OVERWHELMINGLY CSS — this is the good news for Rod. The card, the offset shadow, the corner radii, the title band and the tag chips are all pure CSS; the tag colour pairs are inline styles written in JS, so they are values, not images. The only real artwork is (a) the small per-tag PNG icons, which are custom elements like <urp>, <godot>, <tag> styled with `content: url(/tutorials/Images/*.png)` at max-height 20px, and (b) the thumbnails themselves. The display face is a licensed TTF (BRLNSDB.TTF / Berlin Sans Demi, aliased as font-family 'round'). Nothing structural in the card depends on an image.",
   "confidence": "read-from-source"
  },
  {
   "name": "109ichiki (1:09 Portfolio) — Works",
   "url": "https://109ichiki.com/works/",
   "isLive": true,
   "layoutFamily": "Full-bleed 6-up square-tile grid inside a fixed SVG page frame — zero card chrome, gutters made of per-cell padding",
   "distinctiveMove": "Each item carries an inline `--_square-ratio` custom property computed from its image aspect, and the card link pads itself by calc(0.9375rem * var(--_square-ratio)). Square-ish images therefore get inset and panoramic ones fill, so wildly different artwork reads at equal optical weight inside identical square cells. There is no card at all — no border, no shadow, no background — and the entire hover affordance is the caption chip inverting.",
   "measurements": "READ FROM /_astro/style.COBHKi4A.css. ROOT: font-size clamp(14px, 1.1111111111vw, 16px); --padding-outside: 20px; --padding-content: 3.5rem (56px); --grid-columns: 12; --font-size-base-en .9375rem (15px); --font-size-h2 3.5625rem (57px); --line-height 1.75; --color-bg #f3f3ff, --color-text #101016, --color-border #565656. PAGE: padding var(--padding-outside) var(--padding-outside) 9.84375rem (157.5px bottom). HEADING BAND: display flex, justify-content space-between, align-items flex-end, padding 9.84375rem (157.5px) 3.5rem 3.75rem (60px), border-bottom solid 1px var(--color-border). H1 = 57px IBM Plex Mono, uppercase. FILTER: display grid, grid-template-columns repeat(12, 1fr), padding 2.34375rem (37.5px) 3.5rem; '(Filter)' label in col 1, .filterContent grid-column 4/-1. Type row: flex, gap .5em, 15px, margin-left auto, items separated by a literal '/' via :not(:last-of-type):after; checked = text-decoration underline, thickness 1px, text-underline-offset .1875rem, colour-mix 60% currentColor. Category row: flex, gap .5em, margin-top 1.40625rem (22.5px); each label height 1.875rem (30px), padding 0 .875rem (14px) .0625rem, background var(--color-bg), border solid 1px, border-radius 100vh; checked inverts (background var(--color-text), colour var(--color-bg)). Focus-visible: outline solid 2px, offset 1px. BODY: padding-top 2.34375rem (37.5px); count line padding 0 3.5rem, uppercase; grid margin-top 2.34375rem (37.5px). GRID: grid-template-columns repeat(6,1fr) -> repeat(5) @1280 -> repeat(4) @1024 -> repeat(3) @768 -> repeat(2) @576. NO gap property anywhere — the gutter is .card { padding: var(--padding-outside) } = 20px per cell = 40px visual. .item { display: contents } so the li dissolves. CARD LINK padding calc(.9375rem * var(--_square-ratio)) (15px * ratio; .46875rem at <=768). IMAGE: aspect-ratio 1, img object-fit contain, width/height 100%. CAPTION: width fit-content, padding .125rem .5rem (2px 8px), margin .9375rem (15px) auto 0, border-radius .25rem (4px), background var(--color-bg), font-size 15px, text-align center; hover swaps colour/background. 17 live items. Dark theme via :root[data-theme=dark].",
   "howToCopy": "12-column page at 56px side padding. Heading band: 157.5px of air above the H1, 60px below, closed with a 1px rule. Filter block into columns 4-12, right-aligned: row one is plain text separated by '/' characters with a 1px underline (offset 3px) as the active state; row two, 22.5px lower, is 30px-tall fully-round outline pills (border-radius 100vh, 14px side padding) that INVERT to solid when active. Small uppercase count line, then 37.5px, then the grid. Grid is 6-up with NO gap — put 20px of padding inside each cell instead, which doubles to a 40px gutter and keeps hover targets touching. Every thumbnail sits in a 1:1 box with object-fit:contain directly on the page background, no frame. Caption is a fit-content 15px chip with 2px/8px padding and 4px radius, centred 15px below the image, inverting on hover. Steps down 6 -> 5 -> 4 -> 3 -> 2 at 1280/1024/768/576.",
   "artworkOrCss": "ENTIRELY CSS — no decorative images at all. Two things that look like artwork are not: the barcode motif is the Libre Barcode 128 Google web font rendering the string 'myWorks', and the blinking terminal cursor after the H1 is `._main:after { content: \"_\" }` with a 1s step-start keyframe. The page frame is an inline <svg> rect + mask whose dimensions are written by JS, so it is a runtime device rather than an asset. The only bitmaps on the page are the works themselves, served from a Sanity CDN with ?w=800/400 srcset.",
   "confidence": "read-from-source"
  },
  {
   "name": "Kaito Note — Works",
   "url": "https://kaitonote.com/works/",
   "isLive": true,
   "layoutFamily": "Zero-gutter 3-up mosaic with 2x2 feature spans, filters parked in a right-hand vertical rail",
   "distinctiveMove": "Pinned/featured items are nothing but `col-span-2 row-span-2` on the li, so the mosaic breaks rhythm with no extra markup. And every tile is frosted AT REST by a three-layer CSS stack — black at 20%, a grey layer in mix-blend-mode: color-burn, and a white gradient with backdrop-blur(12px) — with a duplicate copy of the same image sitting above the stack, so hovering reveals the crisp original from behind its own fog.",
   "measurements": "READ FROM /assets/css/style.css + the inline Tailwind block in the page. ROOT: html { font-size: calc(100 / var(--base-vw) * 1 * 1vw) } with --base-vw: 1920 — so 1rem = 1px at a 1920px viewport and every value below is a design pixel that scales fluidly. PAGE GRID: .base-grid { grid-template-columns: 135rem 412rem 412rem 412rem 412rem 135rem } (135px outer gutters, four 412px content columns = 1918 of 1920); @media (width < 992px) it becomes 40rem 224rem x4 40rem. H1 'Works': col-start-2 col-end-4, font-size 76rem base / 112rem md / 160rem lg, line-height 1, tracking-2, block margin-top 96/160/240rem. FILTER RAIL: md:col-start-4 / lg:col-start-5, ml-auto mr-0, md:mt-[160rem]; group label h2 = Noto Serif Display, italic, weight light, text-4 (14rem) base / text-3 (13rem) md; ul margin-top 24rem; items margin-top 16rem, text-7 (21rem) base / text-5 (16rem) md, buttons at opacity-60 with a data-current attribute for the active one; groups 40rem apart. Three groups: Type (ALL/CLIENT/PERSONAL), Field (ALL/VIDEO/WEB), Role (ALL/MOTION/DESIGN/DEVELOPMENT). MOBILE FILTER: a fixed 100x100rem round FILTER button at inset auto 0 40rem 0, opening a 540rem-tall sheet with backdrop-blur(10px) and a bg-gradient #FFFFFF33 -> #FFFFFF1A plus a grey mix-blend-color-burn layer. WORKS GRID: ul spans col-start-2 / col-end-6 (all four content columns), md:grid md:grid-cols-3 = repeat(3, minmax(0,1fr)); there is NO gap class on the ul and no .works-list rule exists in any stylesheet — tiles touch edge to edge. 1648/3 = 549.33px per tile, and the images ship at exactly width=549 height=312. Block margin-top 56/104/204rem, padding-bottom 256rem. FEATURED: li.col-span-2.row-span-2. TILE CAPTION: md:absolute bottom-[16rem] left-[24rem] right-[24rem]; title margin-top 16rem, text-8 (26rem) base / text-7 (21rem) md / text-9 (32rem) lg, leading 1.4; category tag margin-top 8rem, text-4 (14rem), leading 1.3. OVERLAY STACK (desktop only): z-[4]/z-[6] duplicate <picture>, z-[3] bg-black opacity-20, z-[2] bg var(--color-gray) #e6e6e6 mix-blend-color-burn, z-[1] bg-gradient-to-br from-[#FFFFFF33] to-[#FFFFFF1A] backdrop-blur-[12px] with translateZ(0). TYPE SCALE: --font-size-1..14 = 11,12,13,14,16,18,21,26,32,43,64,85,128,256rem. 23 items live.",
   "howToCopy": "Set the root font-size to calc(100/1920 * 1vw) and then write plain design-pixel numbers as rem. Page = 135px gutters + four 412px content columns. Works list spans all four columns as a 3-up grid with NO gap so tiles butt together; give one item col-span-2 row-span-2 and the mosaic does the rest. Rest state per tile: image, then black at 20%, then a #e6e6e6 layer in mix-blend-mode:color-burn, then a white gradient (#FFFFFF33 -> #FFFFFF1A) with backdrop-filter: blur(12px); place a second copy of the image above the stack and clip it in on hover. Caption absolutely pinned 16px above the tile bottom, 24px in from both sides: 21-32px title, then a 14px category tag 8px below it. Put the filters in a right-hand rail as three labelled vertical lists (serif italic labels, 16px items at 60% opacity) rather than a chip row — that is what keeps the mosaic uninterrupted.",
   "artworkOrCss": "ALL CSS, and this specifically corrects the gallery note. Rod logged Kaito as 'great shader effects — feels interactive', but there is NO shader and no canvas on this page. The entire frosted/interactive quality is mix-blend-mode: color-burn plus backdrop-filter: blur(12px) plus a duplicated <img>, layered over an ordinary microCMS JPEG/AVIF. It is fully greyboxable with four divs and zero assets.",
   "confidence": "read-from-source"
  },
  {
   "name": "TUYU — Works",
   "url": "https://tuyu.moe/works/",
   "isLive": true,
   "layoutFamily": "Narrow centred 1000px column, 2-up 16:9 video cards, centred plain-text filter row",
   "distinctiveMove": "Every card IS a live 16:9 YouTube embed, built with the classic padding-top:56.25% aspect box over a black fill, with the caption glued only 5px beneath it — there is no poster image, no play badge, no card frame. The filter row above is pure centred text with a colour-only active state (#745bc7 -> #ad57de), no pill and no border, given an enormous 150px/120px of vertical air to carry it.",
   "measurements": "READ FROM /css/common202c.css + /css/page202c.css. Root font-size is 12px (the sheet double-declares px then rem: 1.1666666667rem = 14px). CONTAINER: .wrapper width 90%, max-width 1000px, margin 0 auto (>=768px); mobile .wrapper width 90% with no max. FILTER ROW: #works .works_sub_menu margin 150px auto 120px (mobile 130px auto 50px); ul display flex, justify-content center, align-items center; li a/span padding 0 18px, font-size 14px, letter-spacing 3px, colour #745bc7, font-family 'Florsn01', transition all 0.3s linear; .works_menu.active { color: #ad57de }. Mobile: padding 0 12px, 11px, letter-spacing 1px. LIST: #works .works_list width 86% (of the 1000px wrapper = 860px max), margin 0 auto; ul display flex, flex-wrap wrap, margin 0 0 -5% -5%; li width 45%, margin 0 0 5% 5% — a 2-up with a 5% (~43px) gutter, ~387px tiles. Mobile: .works_list width calc(100% - 90px), single column, li margin-bottom 50px. MEDIA BOX: .works_movie position relative, width 100%, padding-top 56.25%, background #000, overflow hidden; iframe position absolute, top 0, right 0, width 100%, height 100%. Source iframes are 560x315. CAPTION: .works_detail margin-top 5px, position relative; .date font-size 13px, colour #745bc7, letter-spacing 1.5px; .tit font-size 14px, margin-bottom 5px; .meta font-size 12px. (Mobile drops to 11px/12px/10px expressed in vw: 2.9333vw / 3.2vw / 2.6667vw.) 13 items live — the closest of the whole set to Rod's '~12 items, not hundreds'.",
   "howToCopy": "1000px max wrapper at 90% width, then inset the grid to 86% of that (860px). Two-up via width:45% + margin 0 0 5% 5% on the items with a matching margin: 0 0 -5% -5% on the flex list — the negative-margin gutter trick, which is worth copying because it keeps the outer edges flush. Each cell: a div with padding-top:56.25%, background #000, overflow:hidden, holding an absolutely-positioned iframe or video at 100%/100%. Caption 5px below in three lines — 13px date in a single accent colour with 1.5px tracking, 14px title, 12px meta. Filter row is centred text only: 14px, 3px letter-spacing, 18px apart, one accent colour, a brighter accent for active, 150px of air above and 120px below. Collapse to one column with 50px between items on mobile.",
   "artworkOrCss": "The GRID is all CSS and completely bare — worth knowing, because the watercolour character Rod likes about TUYU is artwork (images/logo.png, images/menu_tuyu.png) and it all lives in the header and drop-down nav, NOT in the works list. The works page itself is a plain 2-up of black 16:9 boxes. The one non-CSS ingredient in the works region is the filter row's licensed display face, 'Florsn01'. If Rod copies this layout he inherits none of the watercolour — that has to come from his own assets.",
   "confidence": "read-from-source"
  },
  {
   "name": "Mike Klubnika — Games",
   "url": "https://mikeklubnika.com/games",
   "isLive": true,
   "layoutFamily": "58em bordered 'document' column with masked artwork rails outside it; index itself is a zebra-striped row list",
   "distinctiveMove": "The page is framed as a physical document — a 58em column with 1px borders on the LEFT AND RIGHT ONLY (border-top/bottom-width: 0) over a vertical black -> #1a1a1a -> black gradient — flanked by two 100px decorative rails pushed fully outside the column with translateX(±100px) and faded inward with a mask-image gradient. Against that frame the index is deliberately plain: a zebra list at 3.3% alpha. On the homepage the same site shows the cleanest possible 'View all': .lr { display:flex; justify-content:space-between } holding an <h2> and one text link.",
   "measurements": "READ FROM /static/style/main.css + both /games and / markup. FRAME: .docholder max-width 58em (~928px at 16px), width 100%, border-style solid, border-width 1px, border-top-width 0, border-bottom-width 0, border-color rgb(92,92,92), background linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(26,26,26,1) 91%, rgba(0,0,0,1) 100%); display flex column, justify-content space-between. .doc margin 15px with margin-top/bottom 10px, background-image page_bg_2.png at right bottom, no-repeat. .contents display flex, justify-content center, min-height 100vh. ROWS: .ue display flex, flex-direction row, justify-content space-between, margin 5px 0, font-family 'bank', font-size 12px; alternating .uebg background rgba(235,235,235,0.0333). .entrytext font-family monospace. h2 font-size 20px, margin-top 0, margin-bottom 0.75em. RULES: .pagespace border-top 1px DOTTED white, margin 1.25em 0 1em; .footer border-top 1px dotted white, margin-top 2.25em, margin-bottom 1em. HEADER: .header font-size 25px, max-height 100px, same 0deg gradient (75% stop), font-family 'bank'; .headerlogo width 35px; .headercontents min-height 35px, margin 0 5px. RAILS: .banner position absolute, width 100px, height 100%, z-index -1, pointer-events none, overflow hidden; .bannerleft left 0, transform translateX(-100px), mask-image linear-gradient(90deg, rgba(0,0,0,0) 25%, rgba(1,1,1,0.3) 85%, rgba(255,255,255,0.2) 98%); .bannerright mirrored at 270deg; both display:none under 756px, where .docholder also drops its border. HOMEPAGE FEATURED BLOCK: .lr flex row space-between (h2 'Catalogue' + <a href=\"/games\" class=\"importantlink\">view all</a>); .cataloguepreview 333x250px, margin-top 25px, margin-bottom 55px, z-index 2; .cataloguepreviewsubtitle flex row space-between, background black, font-size 15px, transform translate(-3px, 217px), margin 0 15px; .catalogueentry 64x64px, margin 4px, transition 0.15s ease-out, hover transform scale(1.3); .entryselected filter drop-shadow(0 0 5px rgba(255,255,255,0.555)) saturate(175%) brightness(1) with scale(1.3); .cataloguetop margin-left 45px, margin-top 12px, margin-bottom 30px; .cataloguebottom margin-left 40px, margin-right 7px, justify-content right. 24 games in the index, 20 catalogue tiles on the home page.",
   "howToCopy": "928px (58em) column, 1px border on the left and right edges ONLY, with a top-to-bottom dark gradient and 15px of inner margin — that alone is 80% of the character. Section header = a flex row, space-between, h2 left and a bare 'view all' text link right. Featured/pinned block = one 333x250 preview with a caption bar pinned across its bottom (flex space-between, solid black, 15px) plus a wall of 64x64 thumbnails at 4px margins that scale(1.3) over 0.15s on hover and swap the big preview. Full index below = a zebra list: flex rows, space-between, 5px apart, 12px monospace, date bracketed on the left, title link, relative age on the right, alternating rows at rgba(235,235,235,0.0333) — a 3.3% alpha, far lighter than a typical zebra. Separate blocks with 1px DOTTED rules. Reserve 100px of space outside each edge for a decorative rail and fade it inward with mask-image, and drop the rails and the border below 756px.",
   "artworkOrCss": "SPLIT, and this is the site where the distinction matters most. CSS: the document frame, the side-only borders, the gradient, the zebra rows, the dotted rules, the mask-image fade, the 1.3x hover scale. ARTWORK: the entire CRT character is absolutely-positioned PNGs/GIFs stacked over the preview — tv2_small.png and screen_grunge.png as overlays sized 475px wide and translated (-35px,-35px) over a 333x250 preview, tv_static.gif as the loading layer, and featured_label.png as a literal drawn 'FEATURED' badge; the side rails are banner.png; the document texture is page_bg_2.png; the display face is a real TTF (F25_Bank_Printer.ttf). A greybox of this site gets the frame and the list rhythm and NONE of the look — everything Rod reacted to ('looks awful and I LOVE it') is drawn, not styled.",
   "confidence": "read-from-source"
  },
  {
   "name": "Unseen Studio — Projects",
   "url": "https://unseen.co/projects/",
   "isLive": true,
   "layoutFamily": "Full-viewport WebGL canvas for the tiles, with a fixed DOM pill-filter bar above and a fixed CTA below",
   "distinctiveMove": "A floating capsule filter bar: a fit-content rounded container holding outline pills, with a separate white capsule element positioned behind them that slides to the active pill, and a live per-filter COUNT rendered as a 10px superscript inside each pill.",
   "measurements": "HONEST LIMIT FIRST: the project grid has NO CSS geometry to read. There are ZERO project tiles in the DOM (I counted 0 matches for any tile class) — the tiles are drawn into <canvas id=\"gl\"> which is `fixed`, fills the viewport at z-40. So there is no container width, no grid-template-columns and no gap for the grid, and I will not invent any. What I DID read from the theme stylesheet: FILTER BAR: .project-filters position fixed, left 0, top 0, margin-top 5rem (80px), width 100%, z-index 50. .project-filters__title 'Selected Projects' display none <768 / block >=768, font-size 3rem -> 4rem (64px), font-weight 400, line-height 1.2, margin-bottom 1rem, centred, letter-spacing negative (t-ls--2). .project-filters__filter >=768: border-radius 12.5rem, flex-direction row, padding .4rem (6.4px), width fit-content, margin 0 auto, z-index 10; mobile it is a 15.625rem column. .project-filters__filter__bg (the sliding capsule): background #fff, position absolute, height/width 100% >=768; mobile 7.5625rem x 2.875rem, border-radius 1.875rem, left 50% translateX(-50%). BUTTONS >=768: font-size .8rem (12.8px), padding .325rem .875rem (5.2px / 14px), margin-right .1875rem (3px), border 1px solid #e7e7e7, border-radius 12.5rem, colour #212121, background none, transition all .4s ease-in-out; mobile font-size 1.5rem, padding .625rem 1.375rem, letter-spacing -.0225rem, margin-bottom .5rem. ACTIVE: background #212121, border 1px solid #212121, colour #faf6f4 (a warm off-white, not pure white). HOVER (non-active): background and border #cacaca. COUNT BADGE: button div font-size .625rem (10px), margin-left .1875rem (3px). Mobile toggle: 7.5625rem x 2.875rem, font-size 1.375rem, chevron .625rem x .375rem, margin-left .5625rem; overlay rgba(0,0,0,.5) fixed full-screen. CTA: .project-grid-cta position fixed, bottom 9rem (144px), 15vh >=1024, left 0, width 100%, text-align center; p font-size .8rem, font-weight 600, margin-bottom .5rem. Filters are All / Branding / Digital / Motion / Experiment.",
   "howToCopy": "Copy the chrome, not the grid — the grid is not copyable as CSS. Fixed bar 80px from the top, full width: a centred 64px/400-weight 'Selected Projects' title with tight negative tracking, 16px above a centred fit-content capsule (border-radius 12.5rem, 6.4px padding). Inside it, pills at 12.8px with 5.2px/14px padding, 3px apart, 1px #e7e7e7 borders, fully rounded, transitioning all .4s ease-in-out; active inverts to #212121 on #faf6f4. Add a 10px count 3px after each label — that is the detail worth stealing, it makes the filter feel like it knows the collection. Put a white capsule div behind the pills and animate its position to the active one. Then pin a small centred CTA (12.8px, weight 600) 144px off the bottom, 15vh on wide screens. For Rod, his water/lantern scene stands in for their canvas behind all of it.",
   "artworkOrCss": "NEITHER, for the grid — the tiles are WebGL geometry in canvas#gl, so there is nothing in the DOM or the CSS to reproduce. For the chrome it is 100% CSS with no images: the capsule, the sliding white background, the pill borders and the inversion are all plain properties. Treat this site as a filter-bar and CTA reference only, and do not report grid measurements from it.",
   "confidence": "partial"
  },
  {
   "name": "dimden — Gallery",
   "url": "https://dimden.dev/gallery",
   "isLive": true,
   "layoutFamily": "Fixed 900px two-column shell (675px main + 225px sidebar), single-column stack of bordered boxes",
   "distinctiveMove": "Page-type chrome that never changes width or shape: the same 900px frame, the same exact 675/225 split and the same two animated fullscreen noise layers sit over the gallery, the blog and the home page alike, so an index reads as the same room as a post. Both overlay layers are stepped animations (steps(4)) rather than smooth ones, which is what makes the grain read as film rather than as a CSS gradient.",
   "measurements": "READ FROM /css/main.css?9 + the page's inline <style>. SHELL: #container width 900px, margin 0 auto; mobile #container width 100%, max-width 900px, box-sizing border-box, padding 0 8px. #main-layout display flex, flex-direction row, flex-wrap nowrap, align-items flex-start. #maintd width 675px, flex-shrink 0; #menutd width 225px, flex-shrink 0 — exactly 675 + 225 = 900. ITEM: .box background #000000d3, border 2px solid #383838, border-radius 4px, padding 10px 15px, margin 5px; #maintd > .box img/iframe width 100%, image-rendering auto (the site is otherwise image-rendering: pixelated). CREDIT LINE: .gl-credit display flex, flex-wrap wrap, align-items center, gap 8px 12px, margin 4px 0 18px; buttons font-size 13px, padding 2px 10px, border 1px solid #555, border-radius 3px. OVERLAYS: body:before background-image /images/overlay.png, opacity 0.2, position fixed, 100% x 100%, z-index 1000, pointer-events none, box-shadow inset 0 0 10em rgb(0 0 0 / 40%), animation Static 2s infinite steps(4); body:after /images/overlay2.png, opacity 0.5, animation Static 0.8s infinite steps(4). LOGO: .letter display inline, height 60px; #logo padding-top 5px, margin-right 50px, animation flickering 0.07s infinite driven by a hand-written 50-stop opacity keyframe table (values 0.36 to 1.34). #wires 922x126, position absolute, top 1px, z-index -5. MOBILE: #main-layout becomes display block, #maintd 100%, #menutd becomes a fixed right drawer at width min(82vw, 260px), height 100vh, background #000000f2, border-left 1px solid #383838, transform translateX(100%) with transition .2s ease-in-out. 92 gallery entries — a stack, not a grid.",
   "howToCopy": "WEAK as a work grid and I want to be straight about that — the gallery is a single-column stack of boxes, not a card grid, so take the SHELL and not the list. Fix the content at 900px centred, split it with flex into a 675px main and a 225px sidebar with align-items:flex-start, and keep that identical split on the index and the detail page so the site never changes width between page types (this is the actual lesson, and it is the same lesson as dimden's blog). Each item is a .box: 2px #383838 border, 4px radius, near-opaque black fill (#000000d3), 10px/15px padding, 5px margin. Over everything, two fixed fullscreen layers at opacity .2 and .5, pointer-events:none, z-index 1000, both animated with steps(4) at 2s and 0.8s — the stepping is the whole trick.",
   "artworkOrCss": "SPLIT, and inseparably so. CSS: the 900px shell, the 675/225 flex split, the .box border and radius, the mobile drawer, and both animations. ARTWORK: everything with personality is a PNG — overlay.png and overlay2.png supply the actual grain, plus bg.png, wires.png (922x126) and mewo.png, and the logo is one PNG per letter (D1.png, IM.png, D2.png, E.png, N.png) nudged together with inline negative margins of -12px and -5px. The flicker and the static are CSS animations DRIVING those PNGs; neither half works alone, so a greybox of this page looks like nothing at all.",
   "confidence": "read-from-source"
  }
 ],
 "notes": "METHOD AND HONESTY\n\nAll 7 sites fetched live over HTTP this pass (all 200) and every number below was read out of the actual stylesheet, not from a screenshot or a description. Raw HTML + CSS saved to C:\\Users\\Rod\\AppData\\Local\\Temp\\claude\\C--Users-Rod-Documents-ProjectFiles-Website\\d1d0c775-c102-4c14-b21e-98f9e13d1b9f\\scratchpad\\proj\\ (109ichiki.html, minions.html, klubnika.html, klub_home.html, dimden.html, unseen.html, kaito.html, tuyu.html and the matching css_*.css, plus builder.js). Nothing is dead; nothing needed replacing. Where a number does not exist I said so rather than inventing it.\n\nTHREE FINDINGS THAT CORRECT THE SHORTLIST\n\n1. Unseen Studio's project grid is WebGL, not CSS. Zero project tiles exist in the DOM — the tiles live in canvas#gl. It is still worth keeping, but ONLY as a filter-bar and CTA reference. Any future note claiming a container width or column count for unseen.co/projects is fabricated.\n\n2. Kaito Note has no shader. The gallery note says \"great shader effects — feels interactive\"; there is no canvas on /works/. The frosted-at-rest, crisp-on-hover effect is mix-blend-mode: color-burn + backdrop-filter: blur(12px) + a duplicated <img>. Four divs, zero assets, fully greyboxable. This is probably the single most useful correction in the batch.\n\n3. .works-list on Kaito and several Tailwind-looking class names have no CSS rule at all — they are JS hooks. The grid geometry is only `md:grid-cols-3` with no gap class, which is why the tiles touch.\n\nTHE CSS-OR-ARTWORK ANSWER, RANKED (this was the recurring question)\n\nPure CSS, safe to greybox and it will actually look like the reference: 109ichiki (the barcode is a web font, the cursor is content:\"_\"), Kaito Note, MinionsArt (only the 20px tag icons and the thumbnails are images), TUYU's grid, Unseen's filter capsule.\n\nArtwork-dependent, a greybox will look like nothing: Mike Klubnika (CRT overlays, grunge, tv_static.gif, a drawn FEATURED badge, banner rails, an F25 Bank Printer TTF) and dimden (overlay.png/overlay2.png grain, per-letter logo PNGs, wires.png). For both of these, copy the FRAME geometry and accept that the character has to come from Rod's own assets.\n\nMAPPED AGAINST WHAT ROD ACTUALLY NEEDS ON THIS PAGE\n\nCards with thumbnail: MinionsArt is the reference (300x160 thumb, 300px card, and the no-blur 5px/10px offset shadow is the entire \"card look\" he praised). Video instead of thumbnail: TUYU, via padding-top:56.25% over black with the iframe absolutely filling it. Tags: MinionsArt puts them at the thumbnail's top-right with per-tag fill+border colour pairs (exact hex values captured); Kaito puts one category tag 8px under the title. Pinned items: Kaito, as literally `col-span-2 row-span-2` — the cheapest mechanism in the whole set; Klubnika does it as a big featured preview above a thumbnail wall. Filter chips: three genuinely different recipes worth benching against each other — 109ichiki's 30px outline pills at border-radius 100vh that invert when active (all-CSS, accessible, uses real radio inputs with focus-visible outlines), Unseen's floating capsule with a sliding white background and a 10px live count per filter, and TUYU's no-chip-at-all centred text row with a colour-only active state. \"View all\": Klubnika's homepage has the cleanest possible version — a flex row, space-between, h2 left and a bare text link right. ~12 items: TUYU is at 13, Chris Kalafatis at 7; 109ichiki 17, Kaito 23, Klubnika 24, dimden 92.\n\nTWO GUTTER TRICKS WORTH BENCHING\n\n109ichiki uses NO gap and instead puts 20px of padding inside each grid cell, doubling to a 40px visual gutter while keeping hover targets touching. TUYU uses the negative-margin trick (items at width:45% margin 0 0 5% 5%, list at margin 0 0 -5% -5%) so the outer edges stay flush. Both are more interesting than a plain gap and neither was in the existing measurements note.\n\nDROPPED\n\nChris Kalafatis (chriskalafatis.com/projects) — live and I read its CSS, but it is not a work grid at all: .projects_images_c is position:fixed and each .projects_img is 65vh tall by 120vh WIDE (deliberately wider than the viewport), a scroll-driven cinematic filmstrip of 7 items with a fixed 16rem-title layer and a bullet rail. No cards, no tags, no filters, no \"view all\". It cannot answer this page type, so I left it out rather than padding the count. Its root is html{font-size:.6944vw} at desktop if it is ever wanted for the giant-type treatment.\n\ndimden is included to reach a solid set and because the never-changing 675/225 shell is a real lesson, but be aware it is a single-column stack, not a grid — it is the weakest of the seven for this specific page type. The five I would actually build benches from are MinionsArt, 109ichiki, Kaito Note, TUYU and Klubnika."
}