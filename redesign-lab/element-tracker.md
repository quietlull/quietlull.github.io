# CANONICAL ELEMENT LEDGER — provenance + sourcing

**This is the single source of truth for the redesign.** Governed by the Code Provenance Contract (memory: `project_code_provenance.md`). Organized by FINAL-SITE SURFACE (not by prototype) because the final site is by surface; the 8 rework designs + `ref-*.html` captures are *source candidates* that feed these rows.

Companion docs (do NOT duplicate them here):
- `element-inventory.md` — per-design source catalog (what each of the 8 designs offers).
- `references.md` — consolidated site references (USED-as-inspiration organized + REVISIT links). Source-code wells kept separately: `scraped-norikura.md`, `refs-floating-and-palette.md`.
- `rework-<name>-ledger.md` — per-rework AS-BUILT ledgers (each rework's elements by Tier/Idea/Source; source candidates feeding the surface rows below). Built: `rework-hana-ledger.md`, `rework-harumaki-ledger.md`, `rework-merodev-yanne-ledger.md`, `rework-stephan-ledger.md`, `rework-norikura-ledger.md`.
- memory `project_visual_references.md` — palette + de-glow principle + atmosphere rules.
- memory `project_reference_site_breakdowns.md` — full per-site teardowns (HANA bloom recipe etc).

## LAB TOOLING (claude-coded, NEVER ships — exempt from provenance, flagged for honesty)
- **`picker.html` + `lab-solo.js` (2026-06-12)** — the element PICKER: iframe compositor where Rod settles the final design. Each rework page gets `?solo=<slot>` mode (lab-solo.js, central per-page selector maps — NO markup tagging); picker stacks transparent per-slot iframes over a swappable bg frame. Per-slot design chips; **Tune** tab = per-element mt/mb/zoom/gap/maxw (Alt-click an element in a frame to focus its controls); **Type** tab = role-based font overrides (display/body/mono/hand) broadcast to all frames. Picks/tunes/fonts persist in localStorage `lab-picker-v1`; **Copy picks** exports the JSON spec used for final assembly. Element code in rework pages is UNTOUCHED (only a head script tag was added to the 5 pages).

## Columns
- **Tier** — `True` (verbatim from a real source, source cited) / `Remixed` (True code changed or combined, parents + change cited) / `Slop` (fabricated, no source — NOT shippable).
- **Idea** — whose concept: `mine` (Rod) / `theirs` (a reference) / `claude` (my invention — a standing debt, must be revisited). `?` = not yet determined. A trailing `?` = provisional, confirm during re-sourcing.
- **Source** — URL / `ref-*.html` / live-site `file:line`. Blank `—` until proven. Candidate leads live in Notes as `lead:`.

## TRUST RESET (2026-06-09)
Per Rod: **100% of code is Slop until re-proven from a real source.** Old `COPIED`/`adapted` labels are NOT trusted. Every row below starts `Slop / —`. Notes preserve prior leads so re-sourcing is fast. We re-prove one element at a time: find real snippet -> set Tier + Source + Idea -> confirm lab code matches.

**CULLED 2026-06-09:** A2/B1/B2 element sets removed (A2 = HANA fork; B1/B2 = no reference basis). Remaining skins: hana, harumaki, norikura, stephan, merodev-yanne.

## Per-rework ledgers (how each rework's elements are handled)
Each `rework-*.html` is a **source candidate**, not the final site — so each gets its own as-built ledger, `rework-<name>-ledger.md`, classifying every element it contains by the same Tier/Idea/Source rules. When an element from a rework is chosen for the final site, its proven row is **promoted up** into the by-surface sections below. First ledger: `rework-hana-ledger.md` (2026-06-09).

## METRIC ROLLUP (maintain as we go)
- **[VERIFY] This rollup is STALE (last recomputed 2026-06-09) and must not be cited as evidence.**
  Rows have been added since without it being updated, so the claude-idea ratio below cannot
  currently answer "are we still under ~25%" - the one question the rollup exists to answer.
  Recount from the tables before any ship decision that leans on it.
- Total tracked elements: ~60 (see sections).
- Tier counts: **True 2 / Remixed 5 (session-provided sources) / Slop rest.** True 2026-06-09 (rework-stephan): draw-in icon ANIMATION + cursive logo write-in (stephanewillems, faithful). Remixed: slap toggle (YarivFrd), ripple on buttons/social/tags (phojanecki), pinned ribbon (Rod snippet), HANA header spawn (hana.b-rave), cursor-lantern spotlight (yannesidibe + brittanychiang — Rod-authorized), atelierbram search caret, nfranciosi outline/pill button, hana nav-link ignite, MauriciAbad inline link, hana card panel/frame-draw. HANA also confirmed-from-source (2026-06-09): filter pills (phojanecki), empty state (PatternFly), post header + callouts + code block + bio cards (codepen, tuned w/Rod), status line (adevade) — see rework-hana-ledger. Remaining Slop shrinking.
- **Idea = `claude` ratio: TARGET < 25%.** Above ~25% Claude-originated => human-intentionality problem persists no matter what (Rod's guardrail). Recount whenever Idea values are confirmed.

---

## Foundations (cross-cutting)
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| Glow technique (all elements) | Slop | theirs? | — | lead: HANA stacked drop-shadow recipe (alpha-only, IGNITE on hover/scroll not constant) — see reference-site-breakdowns. De-glow principle: lantern needs night. |
| Palette | Slop | theirs? | — | lead: twilight festival — midnight-blue night + single amber/gold accent + rare pink pop (norikura + harumaki). Final swatch lock pending. |
| Typography | Slop | mine | — | FORK RESOLVED (2026-06-09) -> **HYBRID**: mono workhorse (UI/meta/code) + display face for big headers + hand-drawn logo + readable non-mono body for posts. Detailed FONT SELECTION deferred until the theme is basically complete (Rod's call). Current lab fonts (Shippori Mincho + IBM Plex Mono) fine meanwhile. CANDIDATE (2026-06-12, from potg.art scrape — `sources/potg-typography.md`): elegant high-contrast serif display + clean geometric sans = the Hybrid structure. Real faces Dahlia+Hellix are COMMERCIAL; free near-twins = Fraunces/Newsreader/Cormorant (serif) + Hanken/Schibsted Grotesk (sans). |
| Cursor-lantern spotlight | Remixed | theirs | yannesidibe.com/about + brittanychiang.com | ✅ Rod-authorized not-slop (2026-06-09). TWO techniques: (a) glow-reveal border = yannesidibe dual-layer radial gradients masked `mask-composite:exclude` at per-elem `--mx/--my`; (b) cursor vignette = brittanychiang fixed radial at `--cx/--cy`. Rod's synthesis = "cursor-as-lantern." DEDUP: `cursor-lantern.js` uses `.spot-host`, but `rework-merodev-yanne.html` reimplements inline as `.glow`/`.cursorglow` -> consolidate to one shared module. Verbatim source-diff still owed. BENCH 2026-06-14: technique (b) added as `extracted/components/cursor-glow/` (`?c=cursor-glow`) — merodev `.cursorglow` vignette recolored purple->gold (color-mix on --color-gold), driven via cursor-coords `onCursor` (singleton-bound). Trialing per Rod. **MATERIAL REWORK 2026-08-13:** Rod — "the reveal looks more like a metal band than a glow". Diagnosis: yannesidibe's construction (constant-width 1.5px strip, hard-cut by the mask, bright spot sliding along it, no spill) is literally how a specular highlight on polished trim is built. Fixed with three changes, on front AND back: `mix-blend-mode:plus-lighter` (emit, not paint), `filter:blur(2.5px)` (falloff instead of hard cuts), and an unmasked cursor-tracked radial spill. **IDEA = `claude` for these three treatments** (Rod selected them from an 8-option bench, `?c=glow-edge-tests`, but I originated them) — counts against the <25% guardrail. Rejected there: long tail, dim core, rounding the glow's corners. Shape untouched. The yannesidibe mask geometry underneath is unchanged, so the element stays Remixed/theirs. |
| Ambient / breathing bloom | Slop | ? | — | lead: foundations.css breathing bloom used across lab. Provenance unproven. |
| Drift + magnet BASE | Remixed | theirs + mine | breakdance4fun (drift) + dennissnellenberg/tdesero (magnet) + chriskalafatis (lerp) | BUILT 2026-06-14 `extracted/components/drift-magnet/`. 2-axis ambient drift (vanilla rAF, two desynced sines) x cursor magnetism (normalized-offset, data-strength/-text, inner parallax) in ONE engine. ORCHESTRATION = Rod's (Idea mine): magnet fully overrides drift (blend lerp); motion-off is GRACEFUL — ramps drift to origin, never stops mid-drift (body.motion-off + MutationObserver); prefers-reduced-motion gates by default. Hooks: `.js-magnetic` + `data-drift` + `data-magnet-clamp` (favicon box) + `.js-magnetic-inner`. Supersedes `magnetic` once buttons/socials/favicon/nav migrate. Sources: dennissnellenberg-buttons.md (verbatim), refs-floating-and-palette.md (breakdance). Motion NOT yet verified live (needs cursor). |

## Global / chrome
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| Favicon (+ spin) | **Rod original** | mine | Rod's own Figma artwork; spin behaviour also Rod's | **TIER QUESTION RESOLVED 2026-08-16 by Rod: the mark AND the spin are his own work, made in Figma.** It was sitting at `Slop` because the ledger's letter says "fabricated, no source" — but the provenance law exists to stop CLAUDE free-handing, not to stop Rod designing. Rod-originated assets are legitimate by definition; `Slop` was the wrong bucket and CLAUDE should have asked rather than leaving it flagged for three passes. **FULL-BLEED 2026-08-16 (Rod, asked four times):** the mark fills the bar's ENTIRE vertical height and is anchored hard to the left edge — 96x96 at 0,0 in a 96px bar, square, zero inset. It does NOT grow the bar: it escapes the bar's padding AND its 1px border with negative margins, so it spans edge to edge inside the height the bar already had. Earlier passes capped it at the wordmark's line box to avoid growing the bar, which was solving a problem Rod never raised. Wordmark decoupled at a 38px gap. |
| Avatar / logo (lantern-ring wordmark) | Slop | theirs? | — | lead: norikura / harumaki hand-drawn brush logos. CANDIDATE TREATMENT (True, theirs): stephanewillems cursive logo write-in via `clip-path:inset(0 100% 0 0)` wipe — proven in rework-stephan-ledger. |
| Site title + subtitle | Slop | theirs? | — | lead: 109ichiki mono, restrained. |
| Top nav links | Remixed | theirs | sources/hana-nav.md (hana.b-rave live) | hana stacked drop-shadow ignite recipe, recolored red->orange (in rework-hana). Per-letter GSAP stagger optional. |
| Active-nav indicator | Slop | ? | — | no ref yet. |
| Toggle switches (breathe/sparkler/fireworks) | Remixed | theirs | codepen.io/YarivFrd/pen/PEOJLj | source provided by Rod (session 2026-06-09). CHANGED: recolored to --glow, scaled, full-pill radius:40px, flip via JS `.on` class (`:checked~.flap` sibling unreliable on display:none radios). Only ONE built; 3 won't fit topbar -> placement TBD. Bar flex-wrap + toggle flex-shrink:0 -> drops to 2nd row intact on squish (verified @700px). |
| Theme toggle (dark/light) | Slop | ? | — | no ref yet. |
| Social buttons | Remixed | theirs | codepen.io/phojanecki/pen/vwyZpY | ripple hover = phojanecki CSS (provided session, recolored). Bordered-chip box + glyphs still free-handed (Slop part). Glow on CHIPS not button. |
| Back-to-top button | Slop | ? | — | no ref yet. |
| Cursor (sparkler trail) | Slop | mine? | — | existing site feature; pairs with cursor-lantern spotlight (Foundations). |
| Page transition | Slop | mine? | — | existing sparkler burst; keep? |
| Scrollbar | Slop | mine? | — | has custom amber; retune to palette. |
| Buttons (primary / outline / pill) | Remixed | theirs | phojanecki (primary) + codepen.io/nfranciosi/pen/eGRKON (outline/pill) | PRIMARY glow ripple = phojanecki (recolored). OUTLINE/PILL fill-sweep = nfranciosi two-layer translateX (recolored + `<span>`) -> sources/nfranciosi-button-fill.md. De-glow per principle. |
| Links (inline, animated underline) | Remixed | theirs | sources/mauriciabad-underline-link.md | MauriciAbad scaleX underline (origin BR->BL), currentcolor=gold (in rework-hana). |
| Tooltips | Slop | ? | — | no ref yet. |
| Footer (centered mono letter-spaced) | Slop | theirs? | — | lead: HANA footer line / norikura-junni minimal. |

## Landing / section
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| **Hero V6 (LOCKED 2026-08-16, ROD)** | Remixed | mine (synthesis) | ndt CSS (`.logo` slot: top:0/right:0/width:20%/min-width:250px) + harumaki main (social rail, scroll cue) + 109ichiki (frame-gutter clock) | Rod's synthesis of V5's demoted signature and V2's vertical column: three upright all-caps columns at the right edge (`rodney fan` / `technical` / `artist`) plus a large left-anchored live clock. Full per-move provenance is in `hero-tests.html`'s PROV panel (v6 entry) — this row exists so the LOCKED state is in the ledger, which it was not until now. Honest caveats carried from PROV: ndt's own lettering is ARTWORK (image), this is a webfont in the same PLACEMENT; six gallery sites set real CSS `writing-mode`, so the vertical type itself needs no art asset. SIZED IN vh NEVER vw (Rod: "wrong when scaled") — the block's height must clear the social rail, which is height-derived. **DONE 2026-08-23 - the Caveat placeholder IS NOW THE LINE BOIL** (see the `H0 line boil` row). The clock is NOT, and cannot be: the faces carry no digits (measured). **DEVIATION FROM THE CITED SOURCE, recorded rather than buried:** ndt's `.logo` slot is cited above as `width:20%/min-width:250px` and that is no longer what ships. Rod 2026-08-23 marked a target left edge that needed the block to double, and at a fixed 20% it overflowed its own container by 98px, so the width is now `auto` (shrink-to-fit). `top:0/right:0` are untouched, so the PLACEMENT still comes from ndt; only the sizing rule is ours now. |
| Hero social rail | ~~True~~ Remixed (2026-08-16) | theirs + mine | harumaki main (placement) | WAS a True transcription: pinned right, `top:50%`, exactly where harumaki main puts it — and it still is in V1-V5. **In V6 only** it was moved to hang 48px below the vertical name (Rod: "give it the same space we give the vertical text from the top bar", and that gap is 48px). That departure is what demotes it to Remixed, and only for V6. Positioned in JS, not CSS: the name block's height is `clamp()`ed in vh, so no CSS constant can express where it ends — a ResizeObserver measures it. The centring `transform` is cancelled in V6 or the rail rides half its height back up. |
| Section title + tagline (typewriter) | **Remixed** | **rod** | harumakigohan.com section headers, read from the live DOM 2026-08-22, assets saved to `references/harumaki-heads/` | lead: 109ichiki mono / harumaki. HANA per-letter L-to-R spawn (each letter glow->no-glow) + hero staggered scale/opacity spawn = candidate treatment; SOURCE IN HAND: hana.b-rave.tokyo (Rod's `hana-borntobloom-animations.html`, session 2026-06-09), built Remixed in `ref-hana.html` (adapted to text vs the real layered-image logo). | **REAL SOURCE READ 2026-08-22:** harumakigohan.com section headers are DRAWN PNGs (`wp-content/themes/gohantheme/img/m_*.png`, saved to `references/harumaki-heads/`). Their page has 124 images and ZERO html headings. Structure: drawn squiggle | white label | drawn squiggle. Label `#f0f0f0`, ornament `#f078f0` pink, fixed 30px tall (22 on minor sections). The "harumaki ~tilde~" citation on our aggregate variant is an APPROXIMATION of this, not a fabrication - typed `~` standing in for a drawn wave, gold for pink. Matching it properly is an ASSET, not CSS.
| Tool-icon strip + hover taglines | Remixed | theirs | **stephanewillems.be/skills - READ FROM THE LIVE DOM 2026-08-21, `sources/stephanewillems-skills.md`** | **CITATION FIXED 2026-08-21.** The old source read "via ref-stephanewillems.html", an in-repo file, so it was CIRCULAR and the real page had never been read. It has now, and the animation claim held up: 45 of 46 paths carry `pathLength` and a stroke-dasharray. The trick is that `pathLength="1"` renormalises every path to length 1, so ONE dashoffset animation drives 15 different icons with no per-icon measurement. Hover/focus captured live = `drop-shadow(<the icon's own brand colour> 2px 2px 10px)` + `scale(1.2)`, and every icon carries `tabindex="0"` so hover is also a FOCUS state. Verified too: focus does NOT change the tagline, so the words follow a SELECTED skill, not a hovered one. Original note follows: pathLength draw-in + staggered --d + label fade + hover scale/glow/recolor. CAVEAT: icon geometry = hand-approx, and the skill SET is stephanewillems's WEB-DEV list — REPLACE with Rod's real tools (HLSL/GLSL/Unity/Three.js/compute/RenderDoc/C#/Blender) via official SVGs. Real functional Technology<->Personal toggle also lives in ref. **CURSOR RECOLOUR CIRCLE REMOVED 2026-08-13 (Rod):** "I never wanted a hover to reveal effect". It cloned each icon and revealed a gold copy through a circle at the cursor, but the clone was built at init AFTER the base began its draw-in, so their `stroke-dashoffset` ran permanently out of phase and the icon read as doubled - Rod called it a clearly-AI artifact, correctly. Deleted from both the JS and the CSS; the draw-in and the magnet are untouched. The cursor-reveal move now belongs to the project cards alone, which also fixes the "every element does the same trick" problem. | **SET AND FORMAT LOCKED 2026-08-22 (ROD):** outline that draws in, deep gold, seven marks - Unity, Unreal, Godot, Blender, Maya, Python, Git. Three.js dropped, then OpenGL and Vulkan ("lets remove open GL and vulcan and go with row 3"). Marks are simpleicons.org CC0, saved verbatim to `references/tool-icons/`, inlined and STROKED not filled. **Why stroked, measured not argued:** devicon brand colours on the night ground are Unity 2.29:1, Blender 2.55:1, Vulkan 2.68:1 and Unreal 1.04:1 - invisible. Component: `extracted/components/skills-row/`. **C#, HLSL and Compute are ABSENT rather than faked** - Simple Icons has no C# (verified against their 3,453-icon index) and the other two are not products.
| Demo reel embed | **OURS** | **rod** | Rod's own capture + the already-approved reel head above it — `final-landing.html` slot `demo reel` | **RESOLVED BY ROD 2026-08-23:** *"for demo reel we already have the thing that plays videos near the bottom this is resolved?"* He is right and the open row was stale. Measured: a real **599x337** `<video>` is already playing at y=4405, under a `reel head` that was ALREADY approved. The "no source" objection was tracking a placeholder his own footage had since replaced. Origin ROD, same as the project cards - his content in a player he approved, so there is no external citation to chase. Slot marked approved. |
| "Featured Projects" header + icon-dot | Slop | theirs? | — | lead: norikura slash labels (/ FEATURED WORK /). |
| Project cards | Remixed/True | mixed | hana panel/frame + /assets/media (real videos) + john_r_muir ignite | rework-hana: panel+frame-draw from hana; IMAGE = real project `<video>` from main site (Rod's own = True); hover ignite = john_r_muir mouse-tracked glow (Remixed); key-learning reveal = Rod's concept (alt cross-fade). Rod's INVERSE of merodev (hover reveals learning, not GIF). |
| Key-learning hover quote (on card) | Slop | mine | — | Rod's existing hover-quote concept; rides hana alt-image cross-fade. CONCEPT is Rod's — re-source the code only, don't replace the idea. |
| Pinned-project badge | Remixed | theirs | Rod corner-ribbon snippet (session 2026-06-09) | classic CSS corner-ribbon Rod provided (no URL). CHANGED: recolored amber band / dark-orange tails, scaled ~0.7, on .card-img (overflow:visible -> overhangs+folds). |
| "View All" button | Slop | ? | — | de-glow. |
| Seam band (section transition) | True (mechanism) / art owed | theirs | harumakigohan.com/ndt/ `.borderpink`/`.borderblue`/`.bordernavy` + harumakigohan.com `img.top_hr_cloud` | BUILT 2026-08-13 `extracted/components/seam-band/`. TWO mechanisms kept apart because the sources differ: `--tiled` = 160px band, `background:url() repeat-x top / 70%` (mobile twin 40px/100%); `--sheet` = one full-width painted image used as an `<hr>` (their retina `hr_cloud.png`/`2x`), 96px here. CSS is verbatim-faithful; their pink/blue/navy colourways deliberately NOT copied. ARTWORK OWED: takes `--seam-art:url()`, degrades to a hairline until Rod draws a strip. |
| Edge rails (fixed page furniture) | True (mechanism) / art owed | theirs | harumakigohan.com/10/ `.left`/`.right` (their CSS comment labels the block カーテン, curtain) | BUILT 2026-08-13 `extracted/components/edge-rails/`. `position:fixed; top:0; height:100%; pointer-events:none`, art sized by HEIGHT (`img{height:100%;width:auto}`) so it never stretches, hidden `<=450px`. Their `.ue` top valance from the same block is NOT built (the layout reserves no space for it) but is available. ARTWORK OWED: `--rail-art`. |
| Dated timeline / entry list | True | theirs | harumakigohan.com/10/ `.handmade_*` block | BUILT 2026-08-13 `extracted/components/dated-timeline/`. Verbatim geometry: 20% date wrap / 4% tiled rule (+2% margin) / 70% body, `justify-content:space-between`, entry `margin-bottom:100px`, date font 36px (26px `<=1024`), weekday (`.youbi`) 16px under it. THE move: the date is `position:sticky; top:100px`, so it hangs beside its entry the whole read. Their `#fef694` NOT copied (palette owns colour). Rule strip `--tl-rule` degrades to a hairline. |
| Picture frame (fixed outer frame) | Remixed | theirs | 109ichiki.com `._frame` + `--padding-outside` | BUILT 2026-08-13 `extracted/components/picture-frame/`. Same numbers: inset 20px (15 `<=768`), `rx` 8px (6), 1px stroke. CHANGED: their SVG `<rect>` + `<mask>` (which exists only so JS can resize the rect to the viewport) is rebuilt as `inset` + one outset `box-shadow` — same painted band, no resize listener. OPEN CALL: opaque band is faithful, but it crops our lantern scene, so `--open` (stroke only) is currently used in the assembly. Also carries their gutter type (`._rightTop`). |
| JFA distance-field glow (lantern halo) | **REMOVED 2026-08-13** (was True machinery / Remixed shading) | theirs | [bzztbomb/three_js_outline](https://github.com/bzztbomb/three_js_outline) (Unlicense/public domain), implementing Ben Golus, [The Quest for Very Wide Outlines](https://bgolus.medium.com/the-quest-for-very-wide-outlines-ba82ed442cd9) | **CUT by ROD after seeing it: "interesting but not what I'm looking for". Files deleted.** Row kept so the sourcing is not redone from scratch: the port is trivially recoverable from the public-domain repo above, and the only bespoke part was one shading pass. Built and verified working first (58 meshes tagged via layers; glare scaled monotonically with halo width 4/12/32/64px, proving it was distance-driven). Original notes: BUILT 2026-08-13, lab only, `scene/shader/jfa/`. Direct port, TS->JS only; all three shaders (Sobel sub-pixel seed, jump flood, distance read) are theirs byte for byte. Two upstream bugs fixed in the port and noted in the file header: `renderOutline` passed a WebGLRenderTarget where a Texture was needed, and a stray per-frame `console.log`. OURS is a single final shading pass, one line different from their `outlinePass`: fade by distance instead of a flat band, plus `ceil(t*bands)/bands` for cel steps. Supplied through their own documented `outlinePass` extension point, so their file is unmodified. WHY: a gaussian spreads isotropically and cannot trace a silhouette at any resolution (see DECISIONS D10 rejects); a distance field can. Mask is the lantern silhouette via three.js layers, NOT a luminance threshold. Rod intends to rewrite this himself before shipping - proof of concept only. |
| Dilate + subtract glow (lantern halo) | **REMOVED 2026-08-13** (was Remixed, expression ours) | theirs (technique) | [lettier, 3D Game Shaders For Beginners - Dilation](https://lettier.github.io/3d-game-shaders-for-beginners/dilation.html) as REFERENCE ONLY | **CUT by ROD along with the JFA. Files deleted.** Row kept mainly for the LICENSING finding, which is the expensive part to rediscover. Original notes: BUILT 2026-08-13, lab only, `scene/shader/jfa/dilateGlow.js`. **LICENSING: deliberately NOT a copy.** lettier's `dilation.frag` is headed "(C) 2020 David Lettier" and the repo has NO license file (GitHub reports `license: null`); gizmo98/common-shaders is also unlicensed. So the operator is written from the textbook definition of a morphological dilation (max over a structuring element - image morphology, not anyone's IP) with lettier cited as the reference that informed the parameterisation. Contrast the JFA row above, which IS a verbatim port because its source is public domain. Re-tier to True if a permissive implementation is found. Reuses the JFA silhouette mask and targets so the two methods A/B fairly. |
| **H0 line boil** | True | mine | Rod's own three hand-drawn faces (`redesign-lab/assets/fonts/Lineboil{1,2,3}-Regular.ttf`) + traditional 2D animation line-boil practice | **BUILT 2026-08-23** `extracted/components/line-boil/` (css + js). The faces are Rod's OWN artwork, which is what makes this True rather than Remixed - there is no reference site to cite because the asset is his. The PATTERN is the tradition, not an invention: three drawings cycled in sequence on 4s (6/sec), which is why adding more fonts was never the lever. Rod's pick: *"Sequential per glyph 6sec on 4's looks best for me."* Record: **D37**. SHIPS ON THREE SURFACES: landing hero (vertical, 24 cells), top-bar wordmark on landing + post, portal centre mark. The random STARTING phase is load-bearing - without it per-glyph renders identically to swapping the whole line. **NO DIGITS in any face** (measured), so the hero clock cannot use it. The advance is pinned in px and RE-pinned on resize, because callers size this with `vh` clamps. |
| **Reference links** | Remixed | theirs | sources/ronja-tutorials-prose.md (`.nested-links a` + `:hover,:focus`), read off ronja-tutorials.com/dist/css/app.d876a0cc99d23e9edadbac7fc6abde13.css | **PICKED BY ROD 2026-08-24** from `reference-block-tests.html`: *"A Ronja Tutorials, colour-only links"*. Built at `extracted/components/reference-links/`, merged into final-post. **Replaces `.co-ref`** - callout-family V1 (flamedfury's box) with D30's "reference = no tape" - which arrived with the callout merge and was never judged on its own. THEIRS: colour-only scoped by one container class, no decoration rule of its own, **hover goes LIGHTER not darker** so the link recedes on approach, `:focus` grouped with `:hover`, and `word-wrap:break-word` so a bare URL cannot blow out the measure. OURS: their #357edd->#96ccff becomes gold->glow, which keeps the lighter-on-hover direction on a DARK page; plus the mono label. **DEVIATION, recorded:** Ronja keeps the browser underline; here the ladder strips it and substitutes a gold border, so the component sets colour only. **Both options Rod was shown dropped the box, and that was not a coincidence - the two gallery sites whose reference lists were transcribed are tutorial sites and NEITHER boxes its links.** |
| Scroll indicator (chevron) | Remixed | theirs | sources/chriskalafatis-chevron-divider.md | BUILT 2026-06-15 INTO the hero component (`extracted/components/hero/`): chriskalafatis True down-arrow SVG, recolored white->gold (currentColor); magnetic via the drift-magnet base (hero.js inits it); anchored to the hero bottom (hero given min-height 86vh). Replaced a free-handed page-level bob (slop) in home.html. |

## Portal
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| ~~Two-door cards (Tech Art / Game Design)~~ | — | — | — | SUPERSEDED 2026-08-23 by `portal-window` below. |
| ~~Time-of-day greeting~~ | — | — | — | REMOVED 2026-08-23, Rod: "remove the greeting and subtitle". |
| Ramblings link (coffee + steam) | Slop | mine? | — | SVG conveyor sine-wave steam; provenance unproven. Superseded on the portal itself by `portal-window`; the coffee/steam art is unused. |
| **portal-window** (frame, bar, close, body) | Remixed | mine | `merged-card.css` + `project-cards-expensive.css` (internal reuse, NOT an external citation) | BUILT 2026-08-23, `extracted/components/portal-window/`. Surface is our own APPROVED project card, taken as-is: `1px rgba(245,158,11,.14)` hairline, `0 10px 26px -10px` shadow, and the glass plane's asymmetric bright-left/faint-bottom edge. Ground is the card's own `--color-cover-hi`/`-lo` gradient (Rod: "the cards themselves should be the deep blues of the site"), OPAQUE — so the lantern scene no longer reads through. **TWO EARLIER PASSES RETIRED, both recorded because the trail matters:** Win98 chrome via 98.css, then a faithful zutomayo copy in their real `#E8D2E9`/`#6F2ADD` (possible only under the palette exception Rod granted, then spent). Both were properly sourced and neither landed — the answer was a component we already had. Bar type at 43%-of-bar-height is zutomayo's measured ratio and is the one thing that survived from them. |
| Portal window DRAG / THROW / BOUNCE / LEAN | Remixed | mine | zutomayo `common.js` `.drag-and-drop` (drag only) | `effects/portal-windows.js`. **Their drag is CONFIRMED** (read from their served JS 2026-08-23 — mousedown/touchstart, mousemove writes `style.top/left` from the press-relative offset, whole panel is the handle). **They have NO inertia and NO clamping of any kind**, so the THROW, the page-edge BOUNCE and the proximity LEAN are all ours and provably so. Lean carries Rod's constraint: STRENGTH 0.24, 150px radius, 10px cap — measured peak 9.0px at 75px, zero beyond the radius. |
| Portal layout (constellation) | Remixed | mine | spacejam.com/1996 (sizes only) | Space Jam supplies the SIZE relationships and the no-two-alike rule. **Positions are Rod's own, hand-placed** — he dragged them, screenshotted it, and they were converted from that image (P138). Held as a fraction of each window's TRAVEL RANGE, which is what makes the constellation and the drag bounds grow with the page together. Three tiers enforced at runtime: leads floored at 1.6x the tallest social, socials squared, every window grown to fit its own header. |
| Social icons (5) | True | mine | Simple Icons v13 — `references/social-icons/SOURCES.md` | github / x / linkedin / artstation / itchdotio, fetched verbatim. Same basis and pattern as `references/tool-icons/`: inlined not `<img>` (an `<img>` cannot take colour from outside), geometry byte-identical, only the brand fill dropped. No icon drawn or approximated. |
| Enter affordance | Remixed | mine | `button-kit` (phojanecki + dennissnellenberg via the kit) | **NOT a button** — a `<span>` marked `aria-hidden` inside the link that already covers the card, so it adds no second tab stop (verified: 2 tab stops per window on all eight). Uses the kit's OUTLINE variant and markup contract; the kit owns how it looks. **THREE overrides recorded:** squared (a correction TOWARD the locked square rule, since the kit ships `border-radius: 8px`), label black at header size, and the fill REVERSED — parked at `0` and draining to `76%` instead of the kit's rise. Rod first flagged an earlier zutomayo `.btn-round` version as "unsourced": it WAS read from their CSS, but that is not the same as being one of the site's own approved buttons. |
| Repeated-text fill | Slop | mine | — | **PLACEHOLDER, and flagged as one.** Rod: "do a spam of repeated text of what the header says inside the box for now." Generated from each window's own title so it cannot drift; `aria-hidden` with a `.pwin__sr` label carrying the real name, so a screen reader hears it once rather than forty times. Takes the h2 token `--color-gold-deep`. Deleting one script and two spans removes it. |
| Identity mark (centre) | True (inherited — CONFIRM) | mine | Rod's own Lineboil faces, via the `H0 line boil` row | **BUILT 2026-08-23 — the portal is 9/9.** Was Slop while it was a free-handed greybox at 272x165; it now renders the line-boil mark, so its tier follows that row rather than standing alone. **Tier flagged for Rod to confirm rather than self-assigned.** Rod 2026-08-23: "that will be replaced by the line boil h0 font later", which is BLOCKED on his three scratch fonts (P100 — identical metrics across all three or the text reflows every frame). Currently doubles as the RESET control: clicking it restores every closed window and returns the constellation to its authored positions. |

## Project / blog list
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| Search bar | Slop | mine | — | Rod's calls: square, always-open, `//` prefix, blinking caret, real typeable input, NO expand/animation (he rejected animated examples). Concept Rod's; re-source code. CARET now Remixed (atelierbram border-caret -> sources/atelierbram-border-caret.md) in rework-hana; box/prefix still Slop. |
| Filter pills / tag filters | Slop | theirs? | — | lead: active state = glow-ignite (not solid fill). |
| Empty state ("no results") | Slop | theirs? | — | lead: PatternFly small empty-state (heading + next-step + action link, NO dashed border). Voice in microcopy. |
| Blog/post preview cards | Slop | theirs? | — | follows Project-cards direction. |
| Category tree / collapse | Slop | ? | — | no ref yet. |
| Archives timeline | Slop | ? | — | no ref yet. |

## Post page (core — shader/tech-art write-ups)
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| **Post page LAYOUT / spine (ROD PICKED 2026-08-16)** | Remixed | theirs | stripe.dev — https://stripe.dev/blog/what-it-feels-like-building-with-stripe-projects, measured @1440 with getComputedStyle 2026-08-16 | The winner out of `post-blockout.html` ("stripe without a doubt"). MEASURED, not chosen: 24-col grid at **58.375px**/col, container **1425px**; sticky metadata rail **exactly 6 cols (350px)**; prose **exactly 12 cols (701px)** starting x=421; ~5 cols of bare right margin, so the layout is NOT centred — on our site that margin is where the scene shows. h1 101.085/92.998/**weight 300**/-6.065px. Built at `post-aggregate.html`. REMIXED not True because: their face is `sohne-var` (Klim Söhne, COMMERCIAL — substituted with Shippori Mincho), sticky offset raised 60→84px for our taller bar, and their ~78ch measure narrowed to 68ch (see next row). |
| Sticky metadata rail | Remixed | theirs | stripe.dev (same measurement) | **The device Rod named** — "two columns with the left data following when you scroll". Carries / METADATA (date, author, reading time, categories), / CONTENTS, / SHARE. Currently HAND-BUILT inside `post-aggregate.html`; **owed a real bench component** before this can ship. Verified pinning at 84px through a 1148px scroll. |
| Reading measure / reading well | Slop | theirs | Red Blob Games, Ciechanowski, lisyarus, Josh Comeau (analysis/component-sources/source-reading-well.md) | **CLAUDE'S EARLIER CLAIM HERE WAS WRONG AND IS WITHDRAWN.** It said every technical reference runs wider than our 65-75ch rule. Seven sites read from source say otherwise: **six give code the SAME measure as prose or narrower** (lisyarus makes code narrower - a 640px code card inside a 760px column). Nobody bleeds code out to fit long lines; where a bleed class exists it is reserved for FIGURES and interactive canvases, never `pre`. The wide references I cited are wide because they NEVER SET A MEASURE, not because they chose one: Cyanilux has no `max-width` in its entire stylesheet (fluid 60-90%, ~180 chars on a 2560px monitor) and IQ's 120ch is a two-column figure-grid width. Sorting these sites by CRAFT sorts them by measure, and the careful ones land on Rod's spec: Ciechanowski ~32em (60-65 chars), Red Blob 66 by explicit declaration, Josh Comeau ~70-76. **65-75ch is corroborated, not contradicted.** The four devices they use instead of widening: (1) SHRINK THE MONO - universal, code 0.81-0.89x prose size, which buys columns inside the same measure; (2) `overflow-x:auto` and accept the scrollbar - which is exactly Rod's D13; (3) author discipline, writing to the column (lisyarus sizes to 80 mono columns and his longest line is 78); (4) shrink code on mobile rather than scroll. NOT YET BUILT. | **MEASURE LOCKED 2026-08-22 (ROD): 94 characters = 767px**, chosen over 72 after an A/B. It is Catlike Coding's 768px re-expressed in our face; Catlike is the closest analogue to this site's content. Held as a character count, NOT a column span, because a column count drifts on a fluid spine. The unit that matters is CHARACTERS, not pixels: our body is M PLUS Rounded 1c 300/18px/-0.18px, averaging 8.16px per character (canvas-measured over a real sentence), which is narrower than any source face. The held sources render 640-768px, but those same widths are 78-94 chars in ours. Red Blob is the only one stating a target - `--body-linelen: 66` - and 66 chars here is 538px, not its 660. **Never port a measure as a pixel value.**
| Post header (TITLE ONLY as of 2026-08-21) | Slop | theirs? | — | **SCOPE CUT BY ROD 2026-08-21:** *"i like the post header being in a box but we dont need the descriptions, tags, etc its on the side column already"*. The box stays, the contents go: tags/date/read-time moved to the rail, description dropped entirely and now lives nowhere (flagged to him). Still unsourced. lead: norikura serif + slash. Watch `.ph`/`.phead` class collision with foundations placeholder. |
| Hero media | Slop | theirs | restaurant.nelu.osaka (base) + heralbony.jp (mechanism) — analysis/component-sources/source-hero-media.md | **SOURCE FOUND 2026-08-16.** NELU is recommended as the base on PROVENANCE grounds, not looks: it is ALREADY in Rod's gallery, tagged by him as the glowing-doorway/lantern principle. HERALBONY supplies the masking MECHANISM. UGOKKO is an optional alive-layer. KOSE was found but is explicitly NOT recommended. Replaces the old unsourced "norikura arch/circle masks" lead. Check per candidate whether the mask is CSS or a baked image asset before building — that decides whether we can have it. NOT YET BUILT. | lead: norikura arch/circle masks — masked not boxed. |
| TL;DR block | Slop | theirs | sources/cyanilux-notice-callout.md | SOURCE FOUND 2026-08-16: Cyanilux `.notice`, read live from https://www.cyanilux.com/css/style.css — verbatim in `sources/cyanilux-notice-callout.md`. Flex row, 3px border at radius 10, HATCHED left gutter (45deg repeating stripe, 12/24px period, radius 6 0 0 6), 28px round chip, 5px paragraph rhythm. ONE component in three roles, so this is a Remix with one named parent, not three transcriptions. Their chip accent #00aabb is CYAN and does NOT transfer (palette law: no cool UI accent) — use the gold ramp. Supersedes the old "left-accent + tint + faint glow" lead, which no source ever backed. NOT YET BUILT. Role variant: chip glyph `i`, `--color-gold`. |
| Content reading well / sections | Slop | mine? | — | readability TOP priority (clear for everyone, not just hiring managers). 109ichiki readability. |
| Reading progress bar | Slop | mine? | — | has amber bar; retune. |
| TOC (desktop sidebar + mobile popup) | Slop | theirs | docs.astro.build / Starlight (structure) + MDN + brittanychiang — analysis/component-sources/source-toc-scroll-spy.md | **SOURCE FOUND + VERIFIED 2026-08-16.** Starlight is the only candidate that covers the WHOLE brief in one place: a `<starlight-toc>` desktop rail AND a `<mobile-starlight-toc>` `<details>` popup, with nesting depth carried as a real inline `--depth` custom property per item (`style="--depth: 0"`, 1 on nesting) so the indent formula is genuinely how it works. Tokens verified live: `--sl-mobile-toc-height:3rem`, `--sl-z-index-toc:4`, `--sl-sidebar-width:18.75rem`, `--sl-content-width:45rem`. Their accent is BLUE (`#3369ff` dark / `#3d50f5` light) and does not transfer — palette law. Every selector carries an Astro `:where(.astro-XXXX)` scope hash that must be stripped. **Supersedes the old brittanychiang-only lead**, which stays as the active-state feel. NOT YET BUILT. Build it INTO the stripe rail — stripe itself has no TOC (verified). | leads: brittanychiang sticky sidebar; ALSO stripe.dev's rail `/ CONTENTS` block, now that the stripe spine is the picked layout — same rail, so the TOC should be built into it rather than as a separate device. |
| Meta chips / tags | **OURS** | **rod** | live site `.post-tag`, `_sass/base/_base.scss:247` | **ROD OVERRODE THE EXTERNAL SOURCE 2026-08-21:** *"Tags in general shouldnt be with doted lines like in the reference we can use the tag badges we already have on the main site"*. So this is NOT ameye/chriskirknielsen any more, it is our own live badge carried forward, squared per the 2026-08-11 shape pass (he chose that over the live `.5rem` radius). Filed OURS, not sourced - the bench makes a thing ours, not sourced. Bench: `extracted/components/tag-badge/`. The ameye + chriskirknielsen research at `analysis/component-sources/source-meta-chips.md` is now UNUSED, kept in case the hover behaviour is wanted later. |
| Takeaway block | Slop | theirs | sources/cyanilux-notice-callout.md | Same parent as TL;DR (see that row). Role variant: chip glyph star/check, `--color-gold-deep`. NOT YET BUILT. |
| Blockquote | Slop | theirs | sources/cyanilux-notice-callout.md | Same parent as TL;DR (see that row). Role variant: chip glyph `"`, or drop the chip and keep only the hatched gutter. NOT YET BUILT. |
| Code block + copy button | Slop | theirs | blog.maximeheckel.com (primary) + starlight.astro.build (alt) — analysis/component-sources/source-code-block.md | **SOURCE FOUND + ADVERSARIALLY VERIFIED 2026-08-16.** Maxime Heckel's snippet: full component, not a fragment — card, header strip, real title, copy button, line-number gutter, and already NO traffic-light chrome so nothing needs removing. A hand-made personal dev site (React Three Fiber / shader posts), which is the taste evidence the pillars want. Verify caught two errors in the sourcing claim: the library is **Stitches, not styled-components**, and the glassmorphism caveat is PHANTOM (`--card-blur` is undefined so `backdrop-filter` resolves to none — it does NOT conflict with paper-morphism). Real caveat that stands: `--code-snippet-background: var(--gray-200)` is a COOL grey slot, fill it with the night ground. ALT if Rod wants a protruding folder-TAB silhouette instead of a full-width header strip: Starlight/Expressive Code (also CONFIRMED; its terminal dots are a CSS `mask-image`, not artwork; note its copy button is HIDDEN until frame hover on desktop at 2rem, not a persistent 2.5rem). **THE OLD SHADCN PICK IS DEMOTED** — its registry returns HTTP 401, so it cannot be extracted and re-proven. NOT YET BUILT. | PICK (2026-06-09, norikura): shadcn code-snippet block (shadcn.io/blocks/cta-card-inset-code-snippet-copy). Still terminal-frame family — filename tab + copy, NO traffic lights (Rod). Re-prove (extract+match) before True. |
| Code block SYNTAX COLOURS | **Remixed** | **rod** | VS Code Dark+ palette (values) + ROD's role mapping — `decisions.css:659-668` | **LOCKED 2026-08-23: *"Keep dark blue for constants but 100% a."*** Separate element from the code block CHROME row above, and separately sourced. **Every HUE is Dark+'s own**, so nothing was invented; what is OURS is the MAPPING - orange to types/storage keywords (Dark+ spends it on strings), pink to functions (Dark+ spends it on control flow), dark blue to named constants, light blue to plain variables. Rod split const from var himself after seeing both drawn. **Explicit carve-out from the palette law** (D26), scoped to `pre code` only; inline code stays warm. **Contrast measured against the real composited ground `rgb(10,14,34)`:** type 7.23 · const 6.48 · var 12.82 · class 9.38 · func 6.87 · number 11.25, all pass 4.5:1 at 12.48px. **BINDING RULE, set by Rod after he caught the demo teaching it wrong:** `.tok-const` is **SCREAMING_SNAKE_CASE only**; everything that varies, **`_Prefixed` Unity uniforms included**, is `.tok-var`. Binds by NAMING, not position. **Three things still open:** strings share the type orange (Dark+ has no second string hue), `--syn-comment #6a6a6a` measures **3.53:1, below AA** (pre-existing placeholder, pending the green-vs-comments conflict), and HLSL semantics (`POSITION`, `SV_Target`) sit on the constant blue as fixed reserved names though `SV_Target` is not screaming. |
| Image lightbox | Slop | ? | — | no ref yet. |
| **Washi tape (SITE CONVENTION)** | Remixed | **rod** | gneiss.place `.taped::before` in `/nonhtml/css/deco.css` - `sources/gneiss-washi-tape.md` (re-verified 2026-08-22) | **FINALIZED AS A CONVENTION BY ROD 2026-08-21**, not as one callout's trick: *"since we are doing washi tape on the top of some cards lets follow the convention"*. One definition, three placements: top edge (theirs), left edge and two corners (ours). Their own `.taped` is a mixin "so tape can be stuck onto anything" - that factoring is the part adopted. Greybox draws it as hatching; the real thing is a photographic PNG, so there is an ASSET owed. Bench: `extracted/components/washi-tape/`. |
| Callout: left-tape (7th candidate) | Remixed | **rod** | gneiss.place tape + our own `.post-takeaway` (`_sass/pages/_post.scss:50`) | **NEW 2026-08-21 on Rod's call** - the classic left-bar admonition with the 3px bar swapped for a tape strip down the edge. Half ours, half theirs, idea Rod's; marked REMIX rather than SOURCED because calling it transcribed would be a false citation. Awaiting his eye on `text-decisions.html`. |
| Tape EDGE: serrated (roller cut) | Remixed | **rod** | css-tricks.com/using-css-masks-to-create-jagged-edges (Stuart Langridge) - `sources/serrated-edge-css.md` | Two-gradient mask: the tooth alone CANNOT be the mask, a second gradient must cover the rest of the element. Transposed to the two ENDS (ours) and the core gradient's stops are ours - his article gives that half in prose only. His CodePens return 403 to this session, so only the article body is cited. Drawn on `washi-tape-tests.html` as case C. |
| Tape EDGE: torn, 2-primitive | True | theirs | danieldarrenjones.com/articles/how-to-make-rough-edges-with-css-and-svgs - `sources/torn-edge-svg-filters.md` | `feTurbulence fractalNoise .05 / 4` + `feDisplacementMap scale 4`, applied as `filter:url(#squiggle)`. `scale` is in USER UNITS so it does not scale with the strip: 4 x (26/116) = 0.90, used as 1. Roughens all four edges, not just the ends. Case A. |
| Tape EDGE: torn, full chain | Remixed | theirs | TornPaper.js v0.0.3, happy358, MIT - `sources/torn-edge-svg-filters.md` | Blur 0.5 -> erode -> offset -> displace, with x/y taken from DIFFERENT noise channels so the edge wanders in two axes. REMIX because its `feDiffuseLighting` paper-grain half is DROPPED - D27 removed paper texture and keeping it would reverse a decision. Erode/offset/scale all scaled x(26/116). Case B. |
| Tape EDGE: irregular clip-path **(PICKED + BUILT)** | Remixed | **rod** | winterwind.com/tutorials/css/29 - `sources/serrated-edge-css.md` | 21 points at IRREGULAR spacing (1,4,9,13,19,21,26,30,36,43,50,52,61,69,70,76,81,84,91,97,99%) - the irregular pitch is what separates torn from zigzag. Theirs runs along a horizontal strip painted in the background colour OVER the sheet, which cannot work on a live scene, so it becomes a clip on the tape itself: their numbers, our axis. Cheapest route - no filter, no asset, no rasterisation (cases A/B would have cost an SVG filter). **ROD PICKED IT 2026-08-22: "washi tape d remix"** - he named the tier himself. BUILT into `extracted/components/washi-tape/washi-tape.css` and the greybox twin in `text-decisions.html`; the border is deleted with it. TWO THINGS ARE OURS: the AXIS (theirs is a long-edge tear on a 2px strip, ours is on the two ENDS) and the AMPLITUDE IN PIXELS - a percentage would resolve against `.tape--left`'s 300px+ height and bite 15px out of a 26px strip. 5.5px is not new, it is his approved `.tape--top` 5%-of-110px. |
| Tape FILL: three-stop stripe | True | theirs | codingartistweb.com/2021/10/how-to-create-washi-tape-notes-with-pure-css - `sources/serrated-edge-css.md` | **The citation our stripe never had.** Theirs is three stops over 15px WITH A TRANSPARENT BAND; ours is a two-tone opaque repeat over 10px, which is why ours reads as a painted bar. Case E tests the fill alone with square ends. |
| Heading AS A CARD | True | theirs | cyanilux.com/css/style.css?v=12 lines 817-832 - `sources/section-and-heading-blocks.md` | Rod P54 literally: every h2/h3/h4 gets `margin-top:30px; padding:7px 20px; background:#282828; border-left:10px solid #00aabb; border-radius:7px`. 20 h2 on one real page. Nesting carried by FILL (#282828 -> #303030 inside `.foldout`/`.tabpanel`). Cost: their page nests three opaque fills deep before prose, and ours has a live scene behind it. `section-break-tests.html` case A. |
| Section AS A TRANSLUCENT PANEL | True | theirs | dimden.dev/css/main.css?9 lines 168-180 - `sources/section-and-heading-blocks.md` | `background:#000000a8` - **translucent**, 66% black, with a Firefox override to 83% rather than to opaque. 17 `.box` on the homepage. The only boxed candidate that would not bury the scene, and a real precedent for D27's transparent grey cards. Corrects `about-blockout.html`, which has the border as `#666` (fine, greybox) and the margin as `10px top` (wrong - real is `5px` all round). Case B. |
| Section break: numbering, no box | True | theirs | catlikecoding.com tutorials.css - `sources/catlikecoding-prose.md` | Nested counters (`body{counter-reset:h2}` / `h2{counter-reset:h3}`) give 1, 1.1, 1.2 with the number DE-emphasised (14px #888 against a 20px heading on 16px prose). "h2 forces a page break in print, making the section the unit of the document." The strongest counter-proposal to a card, and the only one whose section boundary survives printing. Case D. |
| Section break: numbered ruled divider **(PICKED + BUILT)** | Remixed | **rod** | catlikecoding tutorials.css + acegikmo styles.css - `sources/section-and-heading-blocks.md` | **ROD PICKED IT 2026-08-22: "Yes G is good".** Catlike's nested COUNTERS on acegikmo's ruled chapter DIVIDER. Both halves verbatim; **THE COMBINATION IS OURS** - no source does both. They merge because neither uses SIZE to carry hierarchy. P73 applied: the counter is `0.68em` so it SCALES with its heading (0.68 is Catlike's own 14/20 ratio, not a pick), heads deep gold / subheads bright gold, and the rule recolours itself via acegikmo's borderless `border-bottom`. Built into `final-post.html`; the h3-as-chapter mapping is stated there because the article has no h2. |
| Achievement toast **(PICKED + BUILT)** | Remixed | **rod** | dimden.dev `.box` - `sources/section-and-heading-blocks.md` | **ROD 2026-08-22: "200x76 square and the dim den B style".** `#000000a8` translucent, `2px solid #383838`, zero radius, verbatim, at exactly 200x76. Two deviations stated not smuggled: 200x76 is NOT square (2.63:1) so "square" is read as the shape call; and B's one-line layout does not fit at 200px, so the text wraps to two lines beside the icon. Translucency is why B won - the only boxed option that does not bury the scene. |
| Floating window panel | True | theirs | zutomayo.net `.ztmy-pcmove-*` - `sources/zutomayo-pcmove-window.md` | 46px title bar, collapse to exactly the bar height via one class, `transition: height .3s`, `box-shadow: 3px 3px 0` (ZERO blur - a second independent site reaching for the device D15 locked for MinionsArt), 28x28 close at top:11 right:11, rotate-90-and-dock on mobile. **NOT verified: whether they drag** - the handle is `cursor:pointer` not `move`, and the JS was not read. Sourced for the PORTAL rebuild; not built. |
| About section rhythm | True | theirs | potg.art/about measured at 1440 - `sources/about-page-spacing.md` | **213 / 200 / 100 / 199px between sections**, column narrows to 800px for text and goes full bleed for imagery, 3 headings on a 6,404px page. thatskygame independently at 150-360px per zone. **Ours was 10px.** Built into `final-about.html?v=spacious`. The 100px pairs are potg's own tighter gap between two halves of one idea. |
| Draggable dialog window | True | theirs | 109ichiki.com `_astro/style.COBHKi4A.css` - `sources/109ichiki-dialog-window.md` | **The one that actually drags** - `cursor:grab` on the header with `:active{cursor:grabbing}`, which is exactly what zutomayo lacks. Title bar is a GRID ROW (`grid-template-rows:2rem 1fr`) so bar and body cannot drift, vs zutomayo's absolute bar + matching body padding. Close button carries a **2x invisible hit area** (`::before` at `scale(2)`) and its X is drawn in CSS as two rotated 1px lines - no asset, recolours with the frame. **NOT taken:** `[data-blink=true]` runs a 10Hz blink, a flash hazard against STYLE.md's 3/sec cap. Drag JS not read. Sourced for the PORTAL; not built. |
| Card / panel GREY **(REVERSED 2026-08-24 - see note below)** | Remixed | **rod** | none - his own pick from `card-greys-tests.html` | **WARM GREY, Rod 2026-08-23: "Warm grey is the winner".** `--color-panel: rgba(28,26,24,.55)`, solid `#1c1a18`. **NO EXTERNAL SOURCE and that is not hidden** - the swatch was drawn as "a grey pulled 2 points warm" and labelled ours on the page, and he chose it knowing that. Origin ROD, so it is a taste call rather than Slop. Alpha held at .55 so only hue moved. Removes the last blue from every surface but the sky; finishes what D27 started. **REVERSED ON ABOUT, 2026-08-24:** Rod, seeing the two treatments side by side - "the about me cards are all a weird grey that doesnt match any other backing element color on the site ... abandon the grey for this blue used here in ramblings". About's three cards now run the post/ramblings `rgba(8,15,27,.92)`. The TOKEN is unchanged, so the projects controls panel and the portrait window still carry this grey and are now the only ones that do. |
| About page ARRANGEMENT | Remixed | **rod** | final-landing.html, measured at 1440x900 | **Rod's own model, 2026-08-23: "about page is the landing page except the about me is the hero, the achievements are the project cards."** Numbers taken from HIS landing rather than a reference site: hero full first screen with a `min-height:520px` floor, `.sec` at `padding-block:30px 60px` (90px between), `.col` gutter 18.5%. Sections cut apart, each with its own backing on `--color-panel`. Supersedes the potg.art 200px rhythm used one pass earlier. |
| Section heading MARGINS | True | theirs | catlikecoding tutorials.css `h2 { margin: 2em 0 1em }` | Corrects a level mix-up of mine: the `.3em` bottom that shipped is **acegikmo's h3/h4/h5 value**, not their chapter h1, which sets no bottom at all. Catlike is the only one of the two that specifies both ends at chapter level, so its `2em 0 1em` is taken; in `em` it is their ratio and scales at our larger heading. acegikmo's `margin-top:60px` corroborates the generous top. |
| Tape placement: DIAGONAL corners | Remixed | **rod** | placement is ROD's; the strip is the sourced washi-tape component | **NEW placement, Rod 2026-08-23: "for the green one the tape should have 4 diagonal strips on each corner."** One strip laid across each corner at 45deg - how tape actually holds a photo down - rather than the two-strip L. **The angle is geometry, not taste:** a strip across the TOP-LEFT joins the top edge to the left edge, which is a "/" and therefore -45deg; tr/bl join the other way and take +45. Length 2.6x the roll width because a 45deg rotation needs ~1.41x reach to clear the corner. The STRIP is the existing sourced component (gneiss tape + winterwind torn ends); only the placement is new, and the placement is Rod's idea, so ORIGIN: ROD. |
| Callout: bracket -> taped corners | Remixed | **rod** | miroirs (corner positions) + gneiss.place (the tape) | miroirs' offset L-brackets kept their POSITION; only the marker changed, per Rod 2026-08-21: *"once again for the L brackets lets make it washi tape as well"*. |
| Image mat / padded backing | Slop | theirs | THREE options all sourced verbatim 2026-08-21: `sources/gneiss-paper-borderimage.md` (A, edge), `sources/gneiss-paper-ground.md` (B, ground), `sources/ibelick-feturbulence-grain.md` (C, surface) | Rod: *"for the padded backing of images lets also try to get a paper feel maybe"* + *"give me a few options to choose lets do three"*. The three differ by WHERE the paper is. A and B need a drawn/photographed asset; C needs none and is the only one legal in a greybox without a D8 exception, but it is film grain, so it gives tooth and not fibre. **AWAITING HIS PICK.** Base mat geometry is still stripe's padded backing. |
| Prev/next nav cards | Slop | theirs | therealmjp.github.io (geometry) + thebookofshaders.com (middle slot) — analysis/component-sources/source-prev-next.md | **SOURCED FROM SCRATCH 2026-08-16** (was "no ref yet"). TheRealMJP is a graphics/shader programmer's own blog rather than a theme demo, so the provenance is real, and its rule set is five declarations containing NO colour at all — it transfers whole. The Book of Shaders contributes the MIDDLE slot (a third control between prev and next). NOT YET BUILT. |
| Related posts cards | Slop | theirs? | — | follows Project-cards direction. |

## About
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| Bio cards (intro / more) | Slop | theirs? | — | Rod (2026-06-09): bio cards should MATCH the post callout formatting (TL;DR/takeaway/blockquote family), NOT a yannesidibe hover-reveal. |
| Stats — numbers | Slop | mine | — | Rod: SOLID amber serif (rejected hollow yannesidibe text-stroke as unreferenced/odd); matches headings. |
| "Currently studying…" status line | Slop | theirs? | — | lead: adevade live expanding-ring pulse, ember-colored. |
| Achievements trophy grid + unlock toast | Slop (grid) / Remixed (toast) | mine (grid) / theirs (toast) | toast: sources/robooneus-achievement-toast.md | Rod: keep bordered-tile grid (ok for now). UNLOCK TOAST = robooneus Xbox-style relocated bottom-right (Steam) + trigger button, in rework-hana. |

## Other
| Element | Tier | Idea | Source | Notes |
|---|---|---|---|---|
| 404 page | Slop | ? | — | low-stakes sandbox surface. |
| Ramblings index | Slop | theirs? | — | lead: midnightsolarium voice. |
| Text-card (news/member box) | Slop | theirs? | — | lead: HANA — NO chevron, "become a member" CTA row + dark panel, red low-contrast. |
| 3D scene / planar reflection | Slop | mine | — | DEFERRED — Rod evolves Three.js separately. Norikura new-form uses real `three-background-scene.js`. Vibe study only: samsy.ninja/works. |

---

## Source wells (where True Code can come from)
- **Lab `ref-*.html` captures + `*-skin.html` recreations:** hana, norikura, stephanewillems, merodev, yannesidibe, harumaki. (Verify each recreation is faithful before trusting as True.)
- **External references (Rod's finds):** yannesidibe.com/about (hover-reveal glow), stephanewillems.be/skills (SVG draw-in), merodev.net (card layout), 109ichiki.com (fonts), brittanychiang.com/#about (cursor spotlight), samsy.ninja/works (3D vibe, deferred), YarivFrd slap-toggle, phojanecki (ripple/glow), thejamespower (fill-sweep), PatternFly (empty state), adevade (pulse ring), midnightsolarium (ramblings voice).
- **Rod-provided exact snippets (session 2026-06-09) — True wells in hand:** YarivFrd slap-toggle (codepen.io/YarivFrd/pen/PEOJLj), phojanecki glow/ripple (codepen.io/phojanecki/pen/vwyZpY), classic corner-ribbon CSS (no URL — pasted), `hana-borntobloom-animations.html` (real HANA header spawn from hana.b-rave.tokyo).
- **Claude-found leads (session 2026-06-09, rework-harumaki — NOT proven, candidate sources):** buttons fill-sweep seadox `zYqxVjy` / davekilljoy `AqLQRL`; pill shine Jansbury `ooggpO` (rejected); search prefix/segtag designcouch `vYYByYY` / chamsi `LavooJ`; card dim-on-hover Bstolle `XWJGRdr`; toggle ice->flame jkantner `XYNONw` (rejected — Rod chose segmented option A).
- **Claude-found leads (session 2026-06-09, rework-norikura — candidate sources):** code block = shadcn code-snippet block (shadcn.io/blocks/cta-card-inset-code-snippet-copy — Rod's PICK); stats layout = Aceternity stats sections (ui.aceternity.com/blocks/stats-sections); alt code-block = ai-sdk.dev/elements/components/code-block; badge-grid structure = ui.trophy.so (styling rejected — too busy for norikura's restraint).
- **True code wired this session:** Rod's `/assets/js/dist/three-background-scene.min.js` added as a togglable bg in rework-harumaki + rework-hana (bundle = True/`mine`; the show/hide toggle wrappers = Slop/`claude`).
- **ROD'S FINAL PICKS — SCRAPED TRUE SOURCES (2026-06-12).** Rod's 5 chosen sites, web-agent-scraped verbatim into `sources/`:
  - **Magnetic / sticky-to-cursor** (Rod's SIGNATURE interaction, in BOTH Chris K + Dennis): `sources/dennissnellenberg-magnetic-cursor.md` (True — offset-from-center x data-strength, GSAP Power4/Elastic) + `sources/chriskalafatis-magnetic-cursor.md` (True — custom lerp 0.1, target 0.3*dist, layered parallax wrapper 1.0/inner 1.5/chevron 0.8 — the "alive" chevron).
  - **Wave button:** `sources/dennissnellenberg-wave-button.md` (True — 150%x200% ellipse slid up, overflow:hidden clips to pill; GSAP Power2; pure-CSS port included).
  - **Corner brackets + framing + 12-col layout:** `sources/filipporuffini-corner-brackets.md` + `-layout-grid.md` (True — Webflow clean CSS, var-driven L-brackets, fluid grid).
  - **Line page-transitions:** `sources/kaitonote-line-transition.md` (HTML/CSS Remixed; JS = rebuild recipe — GSAP vertical-line sweep + DrawSVG arc, native stroke-dasharray fallback).
  - **Elegant typography:** `sources/potg-typography.md` (True — Dahlia serif display + Hellix sans + recipe; free subs Fraunces/Newsreader + Hanken/Schibsted Grotesk). FEEDS the Hybrid font pick.
  - UNBLOCKED 2026-06-12 (Rod's Chrome agent): chriskalafatis grain = procedural `rand(uv+sin(time))` GLSL (`sources/chriskalafatis-grain-shader.md`, True) + organic line = WebGL vertex-deformed quad (`sources/chriskalafatis-organic-line.md`, True). noise.jpeg was a red herring (smoke displacement, not grain).
- **Live site:** extract verbatim via agent, cite `file:line`.

## ASSEMBLY (2026-06-12) — `redesign-lab/assembly.html` v1 BUILT + verified
First real composition of the redesign from Rod's 5 final picks, all on the locked night/amber palette. Verified live @ localhost:4000/redesign-lab/assembly.html (WebGL OK, 0 console errors). Each effect commented with its source:
- grain/flow bg (chriskalafatis, raw WebGL quad — grain mechanic verbatim, tuned mostly-night per de-glow principle) · magnetic pull + layered-parallax chevron (chris+dennis vanilla port) · wave button (dennis pure-CSS port) · corner brackets (filippo, ember recolor) · 12-col edge-frame (filippo) · elegant serif type (potg -> Fraunces+Hanken free subs) · line intro sweep + organic SVG divider (kaitonote+chris, Remixed).
- Placeholder: copy (Rod's), card media (-> real `<video>`/black), font licensing (Fraunces/Hanken are free stand-ins for Dahlia/Hellix).

## NEXT
1. Iterate assembly.html on Rod's feedback (grain intensity, type, spacing, which effects stay).
2. Wire real content/media; extend to post + about surfaces; then port into the Jekyll site.
